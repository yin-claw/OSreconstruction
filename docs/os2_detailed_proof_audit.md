Supervision correction (`2026-04-17`, ambient shell-zero supplier audit after the local-annihilation negative result):
this head note supersedes the repeated same-day audit blocks immediately
below.

Exact audited source set:

- `OSToWightmanPositivity.lean:5284-5565`
- `SCV/LocalBoundaryRecovery.lean:179-257`
- `SCV/DistributionalUniqueness.lean:1507-1517,1954-1969`
- `SCV/LaplaceSchwartz.lean:74-186`
- `SCV/TubeDistributions.lean:83-116`
- `Wightman/Reconstruction/ForwardTubeDistributions.lean:1352-1436`
- `Wightman/Reconstruction/WickRotation/Section43Codomain.lean:88-117`

Exact wrapper verdict:

- `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  is currently only a dominated-convergence wrapper over
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- hence the first honest supplier seam strictly beneath the ambient shell-zero
  theorem remains the explicit coefficient-side trace-data package rather than
  a distinct ambient-shell interface.

Exact audited source set for the live coefficient seam:

- `OSToWightmanPositivity.lean:5284-5469`
- `SCV/LocalBoundaryRecovery.lean:179-257`
- `SCV/DistributionalUniqueness.lean:1507-1517,1954-1969`
- `SCV/LaplaceSchwartz.lean:74-186`

Exact source-backed contract:

- the live consumer is
  `section43_fixedTimeShellRaw_flat_coefficient_tendsto_zero_of_localTraceData`
  at `OSToWightmanPositivity.lean:5401-5461`;
- it consumes exactly
  `DifferentiableOn`,
  `HasFourierLaplaceReprTempered`,
  `ContinuousOn B Uflat`,
  local ray limits to `B`,
  and local annihilation
  `∀ f, tsupport f ⊆ Uflat → hTempered.dist f = 0`;
- `fourierLaplace_schwartz_integral_convergence_local_of_trace`
  and
  `eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn`
  are the exact SCV consumers of that package.

Exact currently available upstream suppliers:

- `section43_fixedTimeShellRaw_differentiableOn_fixedTime` already supplies
  the holomorphy input;
- `section43_fixedTimeShellRaw_temperedRepr_fixedTime` already supplies the
  tempered FL package;
- `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  still supplies only
  `∃ Uflat, IsOpen Uflat ∧ ... ∧ ∀ xflat ∈ Uflat, ContinuousWithinAt G_t ...`;
- `HasFourierLaplaceReprTempered` still has no continuity field, while
  `boundary_continuous` and `tube_continuousWithinAt` remain only on
  `HasFourierLaplaceReprRegular`, whose upgrade theorem is still explicitly
  unformalized in `SCV/LaplaceSchwartz.lean:180-186`.

Exact obstruction statement:

- choose candidate **(A)** as the live theorem-contract gap:
  the missing supplier on `Uflat` is the explicit local trace-object package
  `B`, `ContinuousOn B Uflat`, the local ray-limit relation to `B`, and
  separate local annihilation
  `∀ f, tsupport f ⊆ Uflat → hTempered.dist f = 0`;
- reject candidate **(B)** as the current first seam:
  a bridge from explicit local trace data to `ContinuousWithinAt` is now only
  the older continuity-lane formulation and not the first missing live
  interface;
- local annihilation is necessary but not sufficient by itself;
- the missing supplier is not a theorem from `HasFourierLaplaceReprTempered`
  alone, because that structure has no continuity field, and it is not a
  theorem from the current local `ContinuousWithinAt` package alone, because
  the checked zero-extraction step still needs an explicit continuous
  real-edge trace object on `Uflat`;
- exact boundary-vs-tube obstruction:
  the apparent generic upgrade from tube-side
  `ContinuousWithinAt G_t ... (SCV.realEmbed xflat)` to boundary-side
  `ContinuousOn (fun xflat => G_t (SCV.realEmbed xflat)) Uflat` fails at the
  source level, because `SCV.realEmbed xflat` lies on the real boundary rather
  than inside `SCV.TubeDomain (ForwardConeFlat ...)`, so the within-set is not
  a neighborhood of the point.
- exact packaging decision from the consumer/topology reread:
  the live consumer still asks for `ContinuousOn B Uflat`, but since the same
  supplier theorem already yields `IsOpen Uflat`, Mathlib
  `IsOpen.continuousOn_iff` identifies that with the pointwise statement
  `∀ xflat ∈ Uflat, ContinuousAt (fun xflat => G_t (SCV.realEmbed xflat)) xflat`;
  the pointwise theorem is the sharper implementation target, and
  `ContinuousOn B₀ Uflat` is only the packaging needed at the consumer.

Judgment:

- no smaller honest local Lean helper should be inserted here yet;
- the ambient shell-zero theorem adds no new supplier surface beyond the live
  coefficient seam already audited;
- the remaining blocker is still purely missing source mathematics at the `B₀`
  local continuity seam, best designated primarily as
  `∀ xflat ∈ Uflat, ContinuousAt B₀ xflat` and secondarily as the packaged
  `ContinuousOn B₀ Uflat` form consumed downstream.

Supervision correction (`2026-04-17`, bounded audit of the exact coefficient-consumer proof surface: no split survives with only the current local flat continuity package):
this pass re-audited only the exact source around
`section43_fixedTimeShellRaw_temperedRepr_fixedTime`,
`section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`,
`section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`,
and the exact SCV theorem surfaces
`fourierLaplace_boundary_recovery_on_open_of_tempered`,
`boundary_trace_zero_of_tempered_of_trace`,
and `distributional_uniqueness_tube_of_tempered_of_trace`.

Exact audit result:

- no Lean theorem was landed on this pass;
- the proposed consumer split with hypothesis exactly equal to the current local
  flat continuity package is not source-supported.

Exact obstruction statement:

- the continuity package provides an open set `Uflat` and
  `ContinuousWithinAt G_t ... (SCV.realEmbed xflat)` on `Uflat`;
- the coefficient-limit conclusion is a zero boundary-trace statement, and the
  available SCV consumers do not derive zero from continuity alone;
- to conclude zero one also needs a boundary-distribution annihilation input on
  the same open set, e.g.
  `∀ f, tsupport f ⊆ Uflat -> hTempered.dist f = 0`,
  or the stronger global `∀ f, hTempered.dist f = 0`;
- `section43_fixedTimeShellRaw_temperedRepr_fixedTime` does not package that
  vanishing input, and
  `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  does not mention it.

Plain verdict:

- no honest theorem can currently be inserted at this surface with exactly the
  advertised local continuity package and the same coefficient-limit
  conclusion;
- if a future split is desired, its consumer theorem must state a larger local
  hypothesis package that includes both continuity and local boundary-
  distribution zero on the same open set.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, bounded audit immediately above the shifted real `consecutiveDiff` obstruction: the first non-wrapper consumer is the shell coefficient-limit theorem, but no Jost-only theorem split is checked):
this pass re-audited only the exact source around
`consecutiveDiff_xiShift_zero_ofReal_eq_add`,
`consecutiveDiff_xiShift_zero_ofReal_eq_of_ne`,
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
its transport wrappers
`section43_fixedTimeShellRaw_pointwiseContinuity_on_section43PositiveEnergyRegion_compl`,
`section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`,
`section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`,
the first substantive consumer
`section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`,
and the exact SCV theorem surfaces
`fourierLaplace_schwartz_integral_convergence_local_of_trace` and
`fourierLaplace_boundary_recovery_on_open_of_tempered`.

Exact audit result:

- no Lean theorem was landed on this pass;
- the first honest consumer surface above the shifted-`ForwardJostSet` gap is
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- the intervening continuity theorems are wrappers only and should not be
  counted as new consumer surfaces.

Exact obstruction statement:

- the first source-supported consumer-side decomposition is not
  “`hy` implies shifted `ForwardJostSet` implies the coefficient limit”;
  checked source does not support that implication chain;
- the source-supported split is instead:
  one theorem consuming the local flat continuity package
  `∃ Uflat, IsOpen Uflat ∧ ... ∧
    ∀ xflat ∈ Uflat, ContinuousWithinAt G_t (SCV.TubeDomain ...) (SCV.realEmbed xflat)`
  to prove the coefficient limit,
  together with a separate missing supplier theorem for that package;
- the missing supplier beneath that split remains
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- the exact shifted-`ForwardJostSet` package identified by the checked
  `consecutiveDiff` theorems is still only a possible geometric ingredient for
  that supplier, not an input already consumed by any checked theorem on this
  route;
- no honest Lean-facing restatement to a mere shifted-`ForwardJostSet`
  hypothesis is source-supported yet, because the ET route still requires the
  independent boundary-continuity input
  `ContinuousWithinAt ... (xiShift ... (fun k μ => (y k μ : ℂ)) (t : ℂ))`.

Plain verdict:

- the first theorem above the shifted-Jost gap that should be treated as a
  real consumer surface is
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- if it is rewritten, it should be split off from its local continuity package,
  not rewritten to depend only on `hy` or only on shifted-`ForwardJostSet`
  membership.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, bounded audit immediately above the repaired real consecutive-difference block: the true lower landing at the continuity/Jost seam is now the checked `consecutiveDiff` family):
this pass re-audited only the exact source around
`toDiffFlat_ofReal_apply_eq_consecutiveDiff`,
`consecutiveDiff_xiShift_zero_ofReal_eq_update`,
`consecutiveDiff_xiShift_zero_ofReal_eq_add`,
`consecutiveDiff_xiShift_zero_ofReal_eq_of_ne`,
the exact seam
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
and the exact Jost/ET consumers
`ForwardJostSet`, `forwardJostSet_subset_extendedTube`,
`extendF_eq_boundary_value_ET_of_continuousWithinAt`.

Exact audit result:

- no Lean theorem was landed on this pass;
- the lowest checked source surface below the continuity seam is no longer
  merely `toDiffFlat_xiShift_eq_update`;
  it is now the checked real consecutive-difference family listed above;
- no smaller already-checked theorem/interface below that family was found on
  this bounded audit.

Exact obstruction statement:

- checked source now gives the full one-slot real bookkeeping:
  at `j := ⟨n, Nat.lt_add_of_pos_right hm⟩`,
  `BHW.consecutiveDiff` at `(j,0)` becomes
  `BHW.consecutiveDiff y j 0 + t`,
  and every slot `(i, μ) ≠ (j, 0)` is unchanged;
- `ForwardJostSet` still requires the strict inequalities
  `∀ k, |BHW.consecutiveDiff x k 0| <
    BHW.consecutiveDiff x k ⟨1, by omega⟩`;
  therefore the exact missing theorem/interface/hypothesis is the upgrade from
  the checked shifted real consecutive-difference formulas to that full
  inequality family for the shifted configuration;
- the immediate consumer of such a theorem is
  `forwardJostSet_subset_extendedTube`;
  if one instead postulated ET membership directly, the next consumer would be
  `extendF_eq_boundary_value_ET_of_continuousWithinAt`, which still assumes
  the present `ContinuousWithinAt` seam rather than deriving it.

Plain verdict:

- the docs must no longer record `toDiffFlat_xiShift_eq_update` as the lowest
  checked landing here;
- the current lower landing is the real consecutive-difference theorem family,
  and the remaining missing object is the full shifted-point `ForwardJostSet`
  inequality package.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, bounded audit immediately below Entry #1006: the exact lower landing is only `toDiffFlat_xiShift_eq_update`, so the blocker is now the missing complement-to-consecutive-difference comparison itself):
this pass reread only the exact source surfaces cited in Entry #1006: `section43PositiveEnergyRegion`, `xiShift`, `toDiffFlat_xiShift_eq_update`, `fromDiffFlat_update_eq_xiShift`, `diffCoordEquiv_apply`, `consecutiveDiff`, `ForwardJostSet`, `forwardJostSet_subset_extendedTube`, and the exact continuity seam around `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`.

Exact bounded result:

- no Lean theorem was landed on this pass;
- the lowest checked source surface below the current seam is only the exact
  coordinate update theorem `toDiffFlat_xiShift_eq_update`;
- therefore the missing lower object is an explicit theorem/interface/hypothesis
  comparing the weak Section 4.3 complement data to the shifted-point
  consecutive-difference inequalities, not a hidden ET/Jost landing.

Exact source-first verdict:

- the actual hypothesis remains only `∃ i, y i 0 < 0` in cumulative
  coordinates;
- the checked `xiShift`/difference-coordinate theorems say only which single
  consecutive difference is altered by the shift;
- `ForwardJostSet` still requires a family of spacelike inequalities for every
  consecutive difference;
- no checked theorem on the reread surfaces upgrades the complement-side data
  to that stronger family.

Verification status:

- docs-only pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, bounded audit immediately below Entry `#1006`: the missing comparison is exactly the one-slot consecutive-difference Jost inequality package):
this pass re-audited only the exact seam
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
the exact transport wrapper
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
the exact coordinate surfaces
`section43PositiveEnergyRegion`, `xiShift`,
`toDiffFlat_xiShift_eq_update`, `fromDiffFlat_update_eq_xiShift`,
`diffCoordEquiv_apply`, `consecutiveDiff`,
and the exact Jost/ET surfaces
`ForwardJostSet`, `forwardJostSet_subset_extendedTube`,
`extendF_eq_boundary_value_ET_of_continuousWithinAt`,
`extendF_eq_boundary_value_ET`.

Exact audit result:

- no Lean theorem was landed on this pass;
- the audit frontier is sharper:
  no checked comparison theorem was missed, but the missing interface is now
  pinned down exactly.

Exact obstruction statement:

- the actual hypothesis `hy` says only that some cumulative time coordinate is
  negative;
- checked `xiShift` bookkeeping says only one difference-coordinate slot is
  updated, namely `(j, 0)` with `j := ⟨n, Nat.lt_add_of_pos_right hm⟩`;
- by direct source-near inference from `toDiffFlat_xiShift_eq_update` together
  with `diffCoordEquiv_apply` and `consecutiveDiff`, the shifted real
  configuration has:
  all `consecutiveDiff` entries unchanged except
  `consecutiveDiff ... j 0`, which becomes `consecutiveDiff y j 0 + t`;
- `ForwardJostSet` requires the full family
  `∀ k, |consecutiveDiff x k 0| < consecutiveDiff x k ⟨1, by omega⟩`,
  so the exact missing theorem/interface/hypothesis is the Jost inequality
  package with that single updated slot:
  unchanged inequalities for `k ≠ j`, and
  `|consecutiveDiff y j 0 + t| < consecutiveDiff y j ⟨1, by omega⟩`
  at `k = j`;
- ET membership alone would still not solve the current theorem, because the
  ET boundary-value theorems do not produce the missing
  `ContinuousWithinAt` for `bvt_F`.

Plain verdict:

- no checked lower landing exists beneath Entry `#1006`;
- the exact minimal missing object is the one-slot updated Jost-inequality
  comparison theorem/interface, not a generic complement-to-Jost slogan.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, bounded audit on the exact extended-tube / Jost fallback beneath the raw shifted-point continuity seam: the blocker is missing ET/Jost geometry for the shifted point):
this pass re-audited only the exact theorem/proof surfaces of
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
the exact `xiShift` / `section43PositiveEnergyRegion` declarations they use,
the exact JostPoints surfaces
`forwardJostSet_subset_extendedTube`,
`extendF_holomorphicOn`,
`extendF_eq_boundary_value`,
`extendF_eq_boundary_value_ET_of_continuousWithinAt`,
and `extendF_eq_boundary_value_ET`,
plus the exact nearby `bvt_F` invariance/extension source objects.

Exact audit result:

- no Lean theorem was landed on this pass;
- the audit frontier is sharper:
  the extended-tube / Jost route does not fail on `bvt_F` holomorphic or
  Lorentz-invariance inputs;
  it fails because current source does not supply ET/Jost membership for the
  exact shifted real point under the actual complement-side hypothesis.

Exact obstruction statement:

- the ET boundary-value theorems still assume the missing
  `ContinuousWithinAt`, so they do not themselves lower the seam;
- the checked `bvt_F` source already gives the analytic continuation inputs
  beneath that geometry seam;
- `section43PositiveEnergyRegion` complement is only negativity of some time
  coordinate, and no checked theorem upgrades that to `ForwardJostSet` or
  `ExtendedTube` membership for the shifted real point;
- `xiShift_mem_forwardTube_of_real` only preserves forward-tube interior
  membership for already-interior points and is not an ET/Jost theorem.

Plain verdict:

- no exact checked theorem on the reread surfaces feeds
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  through the extended-tube route;
- the minimal missing interface on that route is a checked ET/Jost membership
  theorem or hypothesis for the shifted real point.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, bounded audit at the raw shifted-point continuity seam: the checked source still stops exactly there):
this pass re-audited only the fresh docs/chat entries from the last pass,
the exact theorem surfaces of
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
`section43_fixedTimeShellRaw_pointwiseContinuity_on_section43PositiveEnergyRegion_compl`,
`section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`,
`section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`,
the exact fixed-time package surface
`section43_fixedTimeShellRaw_temperedRepr_fixedTime`,
the exact SCV package definitions around
`HasFourierLaplaceReprRegular.tube_continuousWithinAt` and
`HasFourierLaplaceReprTempered`,
and only the exact local tempered recovery / uniqueness theorem surfaces and
`xiShift` geometry needed to test whether a checked lower landing exists.

Exact audit result:

- no Lean theorem was landed on this pass;
- the audit frontier is sharper:
  there is no already-checked theorem-sized supplier beneath
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
  the honest seam still stops at that theorem.

Exact obstruction statement:

- the checked theorem immediately adjacent to the seam,
  `section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
  is a pure transport lemma whose hypothesis `hcont_shifted` is exactly the
  target continuity statement;
- every theorem downstream from it in the fixed-time file simply repackages
  that same missing continuity through unflattened, flat, or local-open
  transport;
- the checked fixed-time source below it supplies only
  `SCV.HasFourierLaplaceReprTempered ...`, not the stronger regular package;
- the exact generic boundary-continuity supplier lives only in
  `HasFourierLaplaceReprRegular.tube_continuousWithinAt`;
  the tempered structure intentionally omits any such field, so no checked SCV
  theorem closes the gap from the current source package;
- the local tempered recovery theorem and tempered uniqueness theorem are
  downstream because they explicitly assume the missing
  `ContinuousWithinAt` / full boundary-limit input;
- `xiShift` contributes only real affine transport and forward-tube interior
  preservation, not boundary continuity.

Plain verdict:

- no checked lower landing exists beneath the raw shifted-point continuity
  theorem;
- the minimal honest missing object is the boundary-regularity theorem itself
  (or a strictly stronger checked regular-package upgrade that would imply it),
  not another wrapper.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, bounded audit immediately beneath the unflattened real-boundary zero seam: the nearest generic SCV zero-trace theorem still does not land it):
this pass re-audited only the fresh docs/chat entries from the last pass,
the exact theorem surfaces of
`section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`,
`section43_fixedTimeShellRaw_temperedRepr_fixedTime`,
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
`section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`,
the exact nearby checked vanishing surface
`section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
and only the exact SCV theorem surfaces
`boundary_trace_zero_of_tempered_of_trace` and
`fourierLaplace_boundary_recovery_on_open_of_tempered`
needed to judge whether a smaller checked landing sits beneath the unflattened
equality.

Exact audit result:

- no Lean theorem was landed on this pass;
- the audit frontier is sharper:
  no smaller source-supported theorem/interface sits beneath the unflattened
  real-boundary zero statement;
  the nearest generic SCV theorem needs extra zero-trace inputs that the
  fixed-time source does not already provide.

Exact obstruction statement:

- the fixed-time source layer still provides only a weak boundary functional
  `T_t` and a tempered FL package for `G_t`;
- `SCV.boundary_trace_zero_of_tempered_of_trace` would require, in addition,
  a separate continuous trace `G`,
  raywise convergence to `G`,
  and a zero theorem for `hTempered.dist`;
- no checked theorem in the reread source supplies those extra inputs for the
  fixed-time continuation;
- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` also does not
  close the gap, because it still assumes local `ContinuousWithinAt` and only
  identifies the boundary distribution with an integral against
  `F (realEmbed x)`;
- the nearby checked positivity-side zero theorem still concerns ambient tensor
  weights on `section43PositiveEnergyRegion`, not the fixed-time `bvt_F`
  boundary value at a complement-side real point;
- the remaining matching fixed-time continuity and raywise-zero theorem
  surfaces are still `sorry`.

Plain verdict:

- no exact checked lower landing exists beneath the unflattened equality;
- the honest seam still stops there, with only definitional transport below
  and non-feedable generic SCV zero-trace theorems below that.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, bounded audit immediately beneath the real-boundary zero seam for `G_t`: no smaller checked theorem exists below it):
this pass re-audited only the fresh docs/chat entries from the last pass,
the exact theorem surfaces of
`section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`,
`section43_fixedTimeShellRaw_temperedRepr_fixedTime`,
`section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`,
`section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`,
`section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`,
the exact nearby checked vanishing surface
`section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
the exact definition of `section43PositiveEnergyRegion`,
and only the exact flatten/unflatten plus `realEmbed` source objects needed to
compare `G_t (SCV.realEmbed xflat)` with its unflattened boundary-value form.

Exact audit result:

- no Lean theorem was landed on this pass;
- the audit frontier is sharper:
  the proposed complement-side theorem
  `G_t (SCV.realEmbed xflat) = 0`
  has no smaller checked lower landing beneath it, apart from definitional
  rewriting.

Exact obstruction statement:

- exact definition of the target value:
  `G_t zflat := bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))`;
- exact transport below the seam:
  using `SCV.realEmbed`,
  `flattenCLEquiv_symm_apply`,
  and `flattenCLEquivReal_symm_apply`,
  if `y := (flattenCLEquivReal (n + m) (d + 1)).symm xflat`, then
  `G_t (SCV.realEmbed xflat) =
    bvt_F OS lgc (n + m)
      (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
        (fun k μ => (y k μ : ℂ)) (t : ℂ))`;
- no checked theorem in the reread source sets that unflattened real-boundary
  value to `0`;
- the flat continuity theorems only provide
  `ContinuousWithinAt G_t ... (SCV.realEmbed xflat)`;
- the nearby checked zero theorem
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`
  concerns tensor weights on the positive-energy region and is not a boundary
  theorem for `bvt_F`;
- the only directly matching zero-trace theorem surface,
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`,
  is still a bare `sorry`.

Plain verdict:

- no already-checked theorem-sized supplier exists strictly below
  `G_t (SCV.realEmbed xflat) = 0`;
- the honest bottom seam is the real-boundary zero theorem itself, with only
  definitional unflattening beneath it.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, bounded audit immediately beneath the flat pointwise zero-trace supplier: the exact smaller missing object is `G_t (realEmbed xflat) = 0`):
this pass re-audited only the fresh docs/chat entries from the last pass,
the exact theorem surface of
`section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`,
the exact complement-side flat continuity package
`section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`
and
`section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`,
the exact definition of `section43PositiveEnergyRegion`,
the exact canonical-direction/tube-membership lemmas
`canonicalForwardConeDirection_mem`,
`canonicalXiShift_mem_forwardTube`,
and the local flatten-transport use of `flattenCLEquiv_mem_tubeDomain_image`,
and only the exact nearby checked vanishing theorem surface in the positivity
file.

Exact audit result:

- no Lean theorem was landed on this pass;
- the proof-audit frontier is sharper:
  the flat pointwise trace-to-zero theorem is not the bottom seam;
  the exact smaller missing theorem is the real-boundary zero statement for
  `G_t`.

Exact obstruction statement:

- checked source already gives complement-side
  `ContinuousWithinAt G_t ... (SCV.realEmbed xflat)`;
- checked source already gives canonical-direction membership and shell-path
  tube membership after flattening;
- hence the flat `Tendsto ... (nhds 0)` theorem would be immediate from the
  smaller statement `G_t (SCV.realEmbed xflat) = 0`;
- no reread nearby zero theorem supplies that:
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`
  concerns tensor weights on the positive-energy region, not the boundary value
  of the fixed-time continuation on the complement side;
- the exact smaller missing interface is therefore:
  for
  `xflat ∈ (flattenCLEquivReal (n + m) (d + 1)) ''
    ((section43PositiveEnergyRegion d (n + m))ᶜ)`,
  prove
  `G_t (SCV.realEmbed xflat) = 0`.

Plain verdict:

- no already-checked theorem-sized supplier for that real-boundary zero value
  was found;
- the honest next seam below the flat zero-trace supplier is the boundary-point
  vanishing theorem, not another trace wrapper.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, bounded audit at the local-trace supplier seam: the honest missing precursor is now pointwise `htraceUflat`, not `B_t` itself):
this pass re-audited only the fresh docs/chat entries from the last pass,
the exact theorem surface of
`SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace`,
the exact fixed-time positivity-side package theorem surfaces
`section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`,
`section43_fixedTimeShellRaw_temperedRepr_fixedTime`,
`section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`,
and the adjacent pointwise coefficient-limit theorem surface
`section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`,
and only the exact flattening/canonical-direction geometry needed to state the
local trace supplier honestly.

Exact audit result:

- no Lean theorem was landed on this pass;
- the proof-audit frontier is sharper:
  precursor `(a)` can be taken to be the constant zero trace function and
  precursor `(b)` is its trivial continuity, so the first honest missing local
  supplier is precursor `(c)`, the pointwise trace-to-zero theorem.

Exact obstruction statement:

- `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace` still
  requires exactly
  `{G : (Fin m → ℝ) → ℂ}`,
  `_hG_contOn : ContinuousOn G U`,
  one `η ∈ C`,
  and pointwise
  `htraceU : ∀ x ∈ U, Tendsto ... (nhds (G x))`;
- `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  already supplies a suitable open `Uflat` inside the flattened complement
  image;
- with that `Uflat`, one may honestly set
  `B_t := fun _ => 0`, so no theorem constructing a nontrivial local trace
  function is actually needed at this seam;
- the exact missing supplier interface is:
  for the flattened fixed-time continuation `G_t` and the flattened canonical
  direction `ηflat`,
  prove
  `∀ xflat ∈ Uflat, Tendsto
    (fun ε => G_t (fun i => ↑(xflat i) + ↑ε * ↑(ηflat i) * Complex.I))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0)`;
- the exact missing source theorem underneath that interface is still
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`,
  which remains a bare `sorry`.

Plain verdict:

- no already-checked theorem-sized supplier producing `htraceUflat` was found;
- the previous audit claim that source stops before precursor `(a)` was too
  coarse;
- the first honest obstruction is now exactly the missing complement-side
  pointwise zero-trace theorem.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, bounded audit immediately beneath the unavailable flat local-trace bridge: source still stops before precursor `(a)`):
this pass re-audited only the fresh docs/chat entries from the last four
passes, the exact declaration/proof surfaces of
`section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`,
`section43_fixedTimeShellRaw_temperedRepr_fixedTime`,
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
and
`section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`,
the exact local-trace theorem surface in
`SCV/LocalBoundaryRecovery.lean`,
and only the exact flattening / shift / positivity geometry needed to judge
whether one precursor among `(a) B_t`, `(b) ContinuousOn B_t Uflat`, or
`(c) htraceUflat` is source-ready.

Exact audit result:

- no Lean theorem was landed on this pass;
- the current checked source still stops before even precursor `(a)`, namely a
  genuine local trace function `B_t` on a complement-side open set.

Exact obstruction statement:

- `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace` still
  requires a separate trace object
  `{G : (Fin m → ℝ) → ℂ}`,
  `_hG_contOn : ContinuousOn G U`,
  and
  `htraceU : ∀ x ∈ U, Tendsto ... (nhds (G x))`;
- `section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`
  still supplies only
  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ, ...`;
- `section43_fixedTimeShellRaw_temperedRepr_fixedTime` still supplies only
  `SCV.HasFourierLaplaceReprTempered (ForwardConeFlat d (n + m)) G_t`;
- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` still asks for
  the stronger local continuity input
  `hcontU : ∀ x ∈ U, ContinuousWithinAt F (TubeDomain C) (realEmbed x)`;
- `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  therefore remains the first live theorem-sized seam above a source layer
  that has no local pointwise trace function yet;
- `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  confirms that complement-side openness itself is already available.

Plain verdict:

- no precursor among `(a)`, `(b)`, or `(c)` is source-ready now;
- the precise first obstruction is the absence of any theorem turning the
  landed fixed-time distributional boundary-value object `T_t` or the tempered
  package for `G_t` into a local pointwise boundary function `B_t`;
- because `B_t` is absent, `ContinuousOn B_t Uflat` and pointwise
  `htraceUflat` are not yet meaningful source-level follow-ons.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, shifted-point continuity seam audited against the last four passes: no checked local-trace-to-continuity theorem exists yet):
this pass re-audited only the fresh docs/chat entries from the last four
passes, the exact declaration/proof surfaces of
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
`section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`,
and
`section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`,
the exact local-recovery theorem surfaces in
`SCV/LocalBoundaryRecovery.lean`,
the exact package boundary in `SCV/LaplaceSchwartz.lean`,
the exact tempered supplier in `SCV/VladimirovTillmann.lean`,
and only the exact positivity / flattening geometry used immediately around the
live seam.

Exact audit result:

- no Lean theorem was landed on this pass;
- the exact smaller theorem-sized lower object directly beneath
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  is the still-missing local-trace-to-`ContinuousWithinAt` theorem for the
  flattened fixed-time continuation `G_t`.

Exact obstruction statement:

- `section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`
  still only transports an already-given shifted-point continuity hypothesis;
- `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  is still the first live `sorry` at this seam;
- `section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`
  and
  `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  remain downstream transport/package theorems, not new source suppliers;
- the direct lower contract is still:
  `private theorem
      section43_fixedTimeShellRaw_flatContinuousWithinAt_on_compl_of_trace_fixedTime
      ...
      (hB_t_contOn : ContinuousOn B_t Uflat)
      (η : Fin ((n + m) * (d + 1)) → ℝ)
      (hη : η ∈ ForwardConeFlat d (n + m))
      (htraceUflat : ∀ xflat ∈ Uflat, Tendsto ... (nhds (B_t xflat))) :
      ∀ xflat ∈ Uflat,
        ContinuousWithinAt G_t
          (SCV.TubeDomain (ForwardConeFlat d (n + m)))
          (SCV.realEmbed xflat)`;
- `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace` still
  consumes exactly that local trace data but concludes only integral
  convergence on Schwartz tests;
- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` still takes
  local `ContinuousWithinAt` as an input and does not derive it;
- `SCV.HasFourierLaplaceReprRegular` still stores
  `boundary_continuous` and `tube_continuousWithinAt` as fields;
- `section43_fixedTimeShellRaw_temperedRepr_fixedTime` still gives only
  `SCV.HasFourierLaplaceReprTempered (ForwardConeFlat d (n + m)) G_t`.

Plain verdict:

- the shifted theorem itself is still not source-ready;
- the audited source layer has enough geometry to open the complement-side flat
  neighborhood, but it still lacks the theorem upgrading honest local trace
  data to the pointwise `ContinuousWithinAt` conclusion needed at the seam.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, complement-side supplier seam re-audited after the tempered package: no source-ready local trace object yet):
this pass re-audited only the fresh docs/chat entries from the last three
passes, the exact declaration/proof surfaces of
`section43_fixedTimeShellRaw_temperedRepr_fixedTime`,
`section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`,
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
and
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
the exact local-trace theorem surface in
`SCV/LocalBoundaryRecovery.lean`,
and only the exact flattening / shift transports immediately adjacent to the
live seam.

Exact audit result:

- no Lean theorem was landed on this pass;
- the exact smaller theorem-sized lower object beneath the supplier seam
  remains
  `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace`.

Exact obstruction statement:

- `section43_fixedTimeShellRaw_temperedRepr_fixedTime` still gives only
  `SCV.HasFourierLaplaceReprTempered (ForwardConeFlat d (n + m)) G_t`;
- `section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`
  still gives only distributional convergence of
  `∫ x, F_analytic (x + i ε η) * h x`;
- `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace` still
  requires the stronger pointwise local trace package
  `G`, `ContinuousOn G U`, and
  `htraceU : ∀ x ∈ U, Tendsto ... (nhds (G x))`;
- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` still requires
  the even higher local continuity input
  `hcontU : ∀ x ∈ U, ContinuousWithinAt F (TubeDomain C) (realEmbed x)`;
- `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  therefore remains the first live theorem-sized seam above an as-yet-unbuilt
  supplier layer.

Plain verdict:

- the supplier theorem itself is still not source-ready;
- the checked source layer still lacks the separate complement-side local trace
  object `B_t`, its `ContinuousOn` proof on an open set `Uflat`, and the
  pointwise trace identification `htraceUflat`, so distributional BV data
  cannot honestly be promoted to the needed pointwise continuity input.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, one smaller lower supplier is now landed, while the local-trace continuity bridge remains unavailable):
this pass re-audited only the fresh docs/chat entries from the last three
passes, the exact declaration/proof surfaces of
`section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`,
`section43_fixedTimeShellRawCLM_fl_representation_fixedTime`,
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
and
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
the exact interfaces in
`SCV/LocalBoundaryRecovery.lean`,
`SCV/LaplaceSchwartz.lean`,
`SCV/VladimirovTillmann.lean`,
and only the exact flattening / transport surfaces needed to formulate the
supplier honestly.

Exact audit result:

- one smaller theorem-sized lower object is now landed:
  `section43_fixedTimeShellRaw_temperedRepr_fixedTime`;
- exact landed theorem surface:
  `private theorem section43_fixedTimeShellRaw_temperedRepr_fixedTime
      ...
      : let G_t : (Fin ((n + m) * (d + 1)) → ℂ) → ℂ := ...
        SCV.HasFourierLaplaceReprTempered (ForwardConeFlat d (n + m)) G_t`;
- the theorem is source-first honest because it uses exactly:
  `section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  and
  `SCV.vladimirov_tillmann_hasFourierLaplaceReprTempered`.

Exact obstruction statement:

- the direct supplier for
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  is still not source-ready;
- the still-missing theorem surface remains the local-open pointwise bridge
  from a local trace object `B_t` and pointwise trace data `htraceUflat` to
  `ContinuousWithinAt G_t ... (SCV.realEmbed xflat)`;
- no checked theorem in the audited layer upgrades the newly landed tempered
  package to that trace object, and no checked theorem upgrades local trace
  data to the needed pointwise continuity conclusion.

Verification status:

- `lake env lean OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`
  was attempted first and failed before checking the edited file because
  `OSToWightmanBoundaryValueLimits.lean:7` contains `<<<` conflict markers;
- fallback
  `lake build OSReconstruction.Wightman.Reconstruction.WickRotation.OSToWightmanPositivity`
  also failed with exit code `1` on that same upstream file.

Supervision correction (`2026-04-17`, audit now pins the exact missing local-trace-to-`ContinuousWithinAt` theorem rather than a generic regularity upgrade):
this pass re-audited only the fresh docs/chat entries from the last two passes,
the exact declaration/proof surfaces of
`section43_fixedTimeShellRawCLM_fl_representation_fixedTime`,
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
and
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
the exact local-trace and package interfaces in
`SCV/LocalBoundaryRecovery.lean`,
`SCV/LaplaceSchwartz.lean`,
`SCV/VladimirovTillmann.lean`,
and only the immediate positivity / forward-tube consumers above the seam.

Exact audit result:

- the first live theorem-sized seam above the landed fixed-time FL
  specialization is still
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- the strongest honest missing source object is the direct local-open
  complement-side continuity bridge for the flattened fixed-time continuation
  `G_t`.

Exact obstruction statement:

- exact missing theorem surface:
  `private theorem
      section43_fixedTimeShellRaw_flatContinuousWithinAt_on_compl_of_trace_fixedTime
      ...
      (Uflat : Set (Fin ((n + m) * (d + 1)) → ℝ))
      (hUflat_open : IsOpen Uflat)
      (hUflat_sub :
        Uflat ⊆ (flattenCLEquivReal (n + m) (d + 1)) ''
          ((section43PositiveEnergyRegion d (n + m))ᶜ))
      {B_t : (Fin ((n + m) * (d + 1)) → ℝ) → ℂ}
      (hB_t_contOn : ContinuousOn B_t Uflat)
      (η : Fin ((n + m) * (d + 1)) → ℝ)
      (hη : η ∈ ForwardConeFlat d (n + m))
      (htraceUflat : ∀ xflat ∈ Uflat,
        Filter.Tendsto
          (fun ε : ℝ =>
            G_t (fun i => ↑(xflat i) + ↑ε * ↑(η i) * Complex.I))
          (nhdsWithin 0 (Set.Ioi 0))
          (nhds (B_t xflat))) :
      ∀ xflat ∈ Uflat,
        ContinuousWithinAt G_t
          (SCV.TubeDomain (ForwardConeFlat d (n + m)))
          (SCV.realEmbed xflat)`;
- this is exactly the missing input which, after the already-proved
  `xiShift` and flattening transport lemmas, feeds
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`.

Exact additional input required:

- the minimal honest extra hypothesis is a local boundary trace object `B_t`,
  continuous on an open complement-side set `Uflat`;
- plus the trace identification `htraceUflat` along one interior cone
  direction `η ∈ ForwardConeFlat d (n + m)`;
- this is source-faithful because
  `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace` already
  takes exactly that data.

Minimal consumer chain above the contract:

- direct local-open flat `ContinuousWithinAt`
  →
  `section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`
  →
  `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  →
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  →
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  →
  `hlimit_os`.

Exact source reason the contract is unavailable:

- `SCV.vladimirov_tillmann_hasFourierLaplaceReprTempered` returns only
  `HasFourierLaplaceReprTempered`;
- `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace` uses the
  local trace data only to conclude convergence of boundary-ray integrals on
  Schwartz tests supported in `Uflat`;
- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` still requires
  local
  `ContinuousWithinAt F (TubeDomain C) (realEmbed x)`
  as a separate hypothesis and concludes only support-restricted distributional
  recovery;
- `SCV.HasFourierLaplaceReprRegular` still stores
  `boundary_continuous` and `tube_continuousWithinAt` as fields, not as a
  theorem derived from local trace data;
- no checked theorem in the audited layer upgrades `htraceUflat` to that
  `hcontU` or to the displayed pointwise continuity conclusion for `G_t`.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, audit sharpened to the exact local-trace-to-`ContinuousWithinAt` contract now missing on the live branch):
this pass re-audited only the fresh docs/chat entries from the last two passes,
the exact declaration/proof surfaces of
`section43_fixedTimeShellRawCLM_fl_representation_fixedTime`,
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
and
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
the exact local-trace interfaces in `SCV/LocalBoundaryRecovery.lean`,
the exact package boundary in `SCV/LaplaceSchwartz.lean`,
the exact tempered supplier in `SCV/VladimirovTillmann.lean`,
and only the immediate fixed-time consumers above the seam needed to record the
minimal chain honestly.

Exact audit result:

- the first live theorem-sized seam above the landed fixed-time FL
  representation is still
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- the strongest honest missing source object on the live branch is the direct
  local-open `ContinuousWithinAt` bridge for the flattened fixed-time
  continuation, not a fake global regular-package shortcut.

Exact obstruction statement:

- direct bridge candidate:
  `private theorem
      section43_fixedTimeShellRaw_flatContinuousWithinAt_on_compl_of_trace_fixedTime
      (OS : OsterwalderSchraderAxioms d) (lgc : OSLinearGrowthCondition d OS)
      {n m : ℕ} (hm : 0 < m) (t : ℝ)
      (Uflat : Set (Fin ((n + m) * (d + 1)) → ℝ))
      (hUflat_open : IsOpen Uflat)
      (hUflat_sub :
        Uflat ⊆ (flattenCLEquivReal (n + m) (d + 1)) ''
          ((section43PositiveEnergyRegion d (n + m))ᶜ))
      {B_t : (Fin ((n + m) * (d + 1)) → ℝ) → ℂ}
      (hB_t_contOn : ContinuousOn B_t Uflat)
      (η : Fin ((n + m) * (d + 1)) → ℝ)
      (hη : η ∈ ForwardConeFlat d (n + m))
      (htraceUflat : ∀ xflat ∈ Uflat,
        Filter.Tendsto
          (fun ε : ℝ =>
            G_t (fun i => ↑(xflat i) + ↑ε * ↑(η i) * Complex.I))
          (nhdsWithin 0 (Set.Ioi 0))
          (nhds (B_t xflat))) :
      ∀ xflat ∈ Uflat,
        ContinuousWithinAt G_t
          (SCV.TubeDomain (ForwardConeFlat d (n + m)))
          (SCV.realEmbed xflat)`;
- raw shifted-point seam recovered from that bridge:
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  via the already-proved `xiShift` transport
  `section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`
  plus the existing flattening transport theorem
  `section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`.

Exact additional input required:

- the minimal honest extra hypothesis is not a global
  `HasFourierLaplaceReprRegular` package;
- it is a local boundary trace object
  `B_t : (Fin ((n + m) * (d + 1)) → ℝ) → ℂ`
  on an open complement-side set `Uflat`, continuous on `Uflat`, together with
  the trace-identification hypothesis `htraceUflat` above along one interior
  cone direction `η ∈ ForwardConeFlat d (n + m)`;
- this is exactly the source vocabulary already present in
  `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace`.

Minimal consumer chain above the contract:

- direct local-open flat `ContinuousWithinAt`
  →
  `section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`
  →
  `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  →
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  →
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  →
  `hlimit_os`.

Exact source reason the contract is unavailable:

- `SCV.vladimirov_tillmann_hasFourierLaplaceReprTempered` gives only
  `HasFourierLaplaceReprTempered`;
- `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace` uses the
  explicit trace contract only to prove convergence of boundary-ray integrals on
  Schwartz tests supported in `Uflat`;
- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` still assumes
  local `ContinuousWithinAt` as input and only concludes support-restricted
  distributional recovery;
- `SCV/LaplaceSchwartz.lean` exposes pointwise boundary continuity and
  `tube_continuousWithinAt` only as fields of
  `HasFourierLaplaceReprRegular`;
- no checked theorem in the audited layer derives those pointwise continuity
  fields from the weaker local trace contract.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, audit sharpened from a missing regular package to the exact unavailable local-trace continuity bridge):
this pass re-audited only the fresh docs/chat entries from the last two passes,
the exact declaration/proof surfaces of
`section43_fixedTimeShellRawCLM_fl_representation_fixedTime`,
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
and
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
the exact local-trace interfaces in `SCV/LocalBoundaryRecovery.lean`,
the exact package boundary in `SCV/LaplaceSchwartz.lean`,
and only the immediate fixed-time consumers above the seam needed to record the
minimal chain honestly.

Exact audit result:

- the first live theorem-sized seam above the landed fixed-time FL
  representation is still
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- the strongest honest missing source object on the live branch is no longer a
  vaguely named “regularity upgrade”; it is the direct local-open
  `ContinuousWithinAt` bridge for the flattened fixed-time continuation, with
  an explicit local boundary-trace input.

Exact obstruction statement:

- direct bridge candidate:
  `private theorem
      section43_fixedTimeShellRaw_flatContinuousWithinAt_on_compl_of_trace_fixedTime
      (OS : OsterwalderSchraderAxioms d) (lgc : OSLinearGrowthCondition d OS)
      {n m : ℕ} (hm : 0 < m) (t : ℝ)
      (Uflat : Set (Fin ((n + m) * (d + 1)) → ℝ))
      (hUflat_open : IsOpen Uflat)
      (hUflat_sub :
        Uflat ⊆ (flattenCLEquivReal (n + m) (d + 1)) ''
          ((section43PositiveEnergyRegion d (n + m))ᶜ))
      {B_t : (Fin ((n + m) * (d + 1)) → ℝ) → ℂ}
      (hB_t_contOn : ContinuousOn B_t Uflat)
      (η : Fin ((n + m) * (d + 1)) → ℝ)
      (hη : η ∈ ForwardConeFlat d (n + m))
      (htraceUflat : ∀ xflat ∈ Uflat,
        Filter.Tendsto
          (fun ε : ℝ =>
            G_t (fun i => ↑(xflat i) + ↑ε * ↑(η i) * Complex.I))
          (nhdsWithin 0 (Set.Ioi 0))
          (nhds (B_t xflat))) :
      ∀ xflat ∈ Uflat,
        ContinuousWithinAt G_t
          (SCV.TubeDomain (ForwardConeFlat d (n + m)))
          (SCV.realEmbed xflat)`;
- raw shifted-point seam recovered from that bridge:
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  via the already-proved `xiShift` and flattening transport lemmas.

Exact additional input required:

- the minimal honest extra hypothesis is not a global
  `HasFourierLaplaceReprRegular` package;
- it is a local boundary trace object
  `B_t : (Fin ((n + m) * (d + 1)) → ℝ) → ℂ`
  on an open complement-side set `Uflat`, continuous on `Uflat`, together with
  the trace-identification hypothesis `htraceUflat` above along one interior
  cone direction `η ∈ ForwardConeFlat d (n + m)`;
- this matches the exact source vocabulary already present in
  `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace`.

Minimal consumer chain above the contract:

- direct local-open flat `ContinuousWithinAt`
  →
  `section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`
  →
  `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  →
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  →
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  →
  `hlimit_os`.

Exact source reason the contract is unavailable:

- `SCV.vladimirov_tillmann_hasFourierLaplaceReprTempered` gives only
  `HasFourierLaplaceReprTempered`;
- `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace` and
  `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` stop at
  integral/distributional recovery for tests supported in the open set, even
  after supplying the explicit local trace;
- `SCV/LaplaceSchwartz.lean` exposes boundary continuity and
  `tube_continuousWithinAt` only as fields of
  `HasFourierLaplaceReprRegular`;
- no checked theorem in the audited layer derives those pointwise continuity
  fields from the weaker local trace contract.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, audit sharpened from a generic continuity gap to the exact missing regularity upgrade interface):
this pass re-audited only the fresh docs/chat entries from the last two passes,
the exact declaration surfaces of
`section43_fixedTimeShellRawCLM_fl_representation_fixedTime`,
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
and
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
the exact package/interfaces in
`SCV/LaplaceSchwartz.lean`,
`SCV/LocalBoundaryRecovery.lean`,
and
`SCV/VladimirovTillmann.lean`,
and only the exact forward-tube flat-regular consumers needed to record where
the missing upgrade would feed next.

Exact audit result:

- the first live theorem-sized seam above the landed fixed-time FL
  representation is still
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- the strongest honest missing theorem/interface on this seam is now pinned
  exactly, and it is still docs-only.

Exact reason:

- the landed fixed-time FL theorem plus the fixed-time spectral-support
  supplier and global growth package determine the flattened continuation
  `G_t`;
- the strongest honest upgrade theorem would be:
  from those landed inputs, produce
  `SCV.HasFourierLaplaceReprRegular (ForwardConeFlat d (n + m)) G_t`;
- the weaker direct branch-local theorem would be:
  for
  `y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`,
  prove
  `ContinuousWithinAt
      (bvt_F OS lgc (n + m))
      (ForwardTube d (n + m))
      (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
        (fun k μ => (y k μ : ℂ)) (t : ℂ))`;
- the consumer chain above the strong theorem is explicit in checked source:
  `HasFourierLaplaceReprRegular ... G_t`
  gives
  `tube_continuousWithinAt`,
  which feeds the flat real-boundary continuity consumers in
  `ForwardTubeDistributions.lean` such as
  `boundary_function_continuous_forwardTube_of_flatRegular` and
  `boundary_value_recovery_forwardTube_of_flatRegular`;
- the consumer chain above the weaker theorem is the live positivity-file seam:
  direct shifted-point continuity
  →
  `section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`
  →
  `section43_fixedTimeShellRaw_pointwiseContinuity_on_section43PositiveEnergyRegion_compl`
  →
  `section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`
  →
  `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`;
- exact source reason the strong theorem is unavailable:
  `SCV.vladimirov_tillmann_hasFourierLaplaceReprTempered` only returns
  `HasFourierLaplaceReprTempered`,
  and `LaplaceSchwartz.lean` explicitly notes that the theorem upgrading
  genuine FL input to `HasFourierLaplaceReprRegular` is not formalized;
- exact source reason the weaker theorem is unavailable:
  the only checked tempered local recovery theorem,
  `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`,
  still takes the local continuity hypothesis `hcontU` as an input and does
  not derive it from tempered FL data.

Exact obstruction statement:

- strong candidate:
  `private theorem
      section43_fixedTimeShellRaw_regularFourierLaplaceRepr_fixedTime
      (OS : OsterwalderSchraderAxioms d) (lgc : OSLinearGrowthCondition d OS)
      {n m : ℕ} (hm : 0 < m) (t : ℝ) :
      let G_t : (Fin ((n + m) * (d + 1)) → ℂ) → ℂ := fun zflat =>
        bvt_F OS lgc (n + m)
          (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
            ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))
      SCV.HasFourierLaplaceReprRegular (ForwardConeFlat d (n + m)) G_t`;
- weaker branch-local candidate:
  `private theorem
      section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl
      ... :
      ContinuousWithinAt
        (bvt_F OS lgc (n + m))
        (ForwardTube d (n + m))
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ => (y k μ : ℂ)) (t : ℂ))`.

Exact consequence:

- the next honest work item is no longer a vague “continuity gap” note;
- it is specifically the missing weak-to-regular FL upgrade, with the weaker
  direct `ContinuousWithinAt` theorem recorded as the minimal branch-local
  fallback.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, audit correction after landing the fixed-time FL representation specialization):
this pass re-audited only the fresh docs/chat entries from the last two passes,
the exact fixed-time theorem surface around
`section43_fixedTimeShellRawCLM_fl_representation_fixedTime`, the exact proof
cluster around
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point` and
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
and only the exact SCV continuity/boundary-recovery interfaces in
`LaplaceSchwartz.lean` and `LocalBoundaryRecovery.lean` needed to decide
whether the next seam was source-ready.

Exact audit result:

- the first live theorem-sized seam above the landed fixed-time FL
  representation is
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- no Lean theorem landing is source-ready on this seam.

Exact reason:

- the transport theorem
  `section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`
  is already proved and only repackages continuity of raw `bvt_F` at the
  shifted real boundary point;
- the blocker theorem itself asks for
  `ContinuousWithinAt
      (bvt_F OS lgc (n + m))
      (ForwardTube d (n + m))
      (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
        (fun k μ => (y k μ : ℂ)) (t : ℂ))`
  for `y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`;
- the SCV source audited above the landed FL representation does **not**
  provide that continuity from the current hypotheses:
  `LaplaceSchwartz.lean` gives boundary continuity and
  `tube_continuousWithinAt` only under
  `HasFourierLaplaceReprRegular`,
  while `LocalBoundaryRecovery.lean` proves only support-restricted
  distributional recovery from `HasFourierLaplaceReprTempered` plus an
  externally supplied local continuity hypothesis;
- the strongest theorem currently constructible from the landed fixed-time FL
  seam is the tempered package
  `SCV.HasFourierLaplaceReprTempered (ForwardConeFlat d (n + m)) G_t`
  via `SCV.vladimirov_tillmann_hasFourierLaplaceReprTempered`;
- no checked theorem in the audited source layer upgrades that tempered package
  to `HasFourierLaplaceReprRegular`, and no direct continuity theorem bypasses
  that missing regular upgrade.

Exact obstruction statement:

- the immediate missing theorem above the landed FL specialization is:
  `private theorem
      section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl
      (OS : OsterwalderSchraderAxioms d) (lgc : OSLinearGrowthCondition d OS)
      {n m : ℕ} (hm : 0 < m)
      (t : ℝ)
      (y : NPointDomain d (n + m))
      (hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ) :
      ContinuousWithinAt
        (bvt_F OS lgc (n + m))
        (ForwardTube d (n + m))
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ => (y k μ : ℂ)) (t : ℂ))`;
- source reason this is not theorem-sized ready:
  it would require either a new theorem producing
  `SCV.HasFourierLaplaceReprRegular (ForwardConeFlat d (n + m)) G_t`
  from the landed FL representation/support/growth data, or a direct SCV/flat
  continuity theorem implying the above `ContinuousWithinAt`; neither exists in
  the audited source layer.

Exact consequence:

- the live frontier has moved above the fixed-time FL representation theorem,
  but not to a source-ready Lean step;
- the next honest work item is a docs-level mathematical gap statement for the
  missing regularity/continuity upgrade, not a fake continuation of the landed
  supplier/comparison chain.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-17`, audit correction after landing the fixed-time spectral-support supplier):
this pass re-audited only the fresh docs/chat entries from the last two passes,
the exact fixed-time theorem surface around
`section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`,
the exact SCV consumer surfaces `SCV.bv_implies_fourier_support` and
`SCV.fl_representation_from_bv`, the exact declaration of
`schwartz_bv_to_flat_repr`, and only the nearby cone/flattening declarations
needed to identify the first still-live consumer seam honestly.

Exact audit result:

- the fixed-time supplier theorem
  `section43_fixedTimeShellRawCLM_bv_implies_fourier_support_fixedTime`
  is now landed in source;
- no new axiom or wrapper was introduced;
- the theorem consumes the new all-directions shell BV package and returns the
  flattened frequency-side witness `Tflat` with dual-cone support together with
  the exact `physicsFourierFlatCLM` pairing identity.

Exact reason:

- after the previous pass, the fixed-time seam already matched the full
  `hF_bv` hypothesis surface required by `SCV.bv_implies_fourier_support`;
- source also already contained the remaining supplier inputs on this exact
  seam:
  `F_analytic z = bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 z (t : ℂ))`,
  its holomorphy on `TubeDomainSetPi (ForwardConeAbs d (n + m))`,
  and the global tube-growth package from
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`;
- therefore the first live consumer seam above the new all-directions theorem
  really was the supplier specialization itself, and it was source-ready.

Exact consequence:

- the live gap has moved one step upward again;
- the first still-live consumer seam is now the fixed-time specialization of
  `SCV.fl_representation_from_bv` for the flattened continuation
  `G_t zflat = bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))`;
- `schwartz_bv_to_flat_repr` is not the first consumer on this route segment:
  it yields a `HasFourierLaplaceRepr` package from explicit boundary-value
  data, whereas the current theorem-3 seam has already advanced to the
  SCV-frequency-side supplier/comparison layer.

Exact obstruction statement now above the landed supplier:

- the next theorem-sized fixed-time seam is:
  from the landed `T_t`, `Tflat`, support witness, and pairing identity,
  specialize `SCV.fl_representation_from_bv` to prove
  `∀ zflat ∈ SCV.TubeDomain (ForwardConeFlat d (n + m)),
      G_t zflat =
        fourierLaplaceExtMultiDim
          (ForwardConeFlat d (n + m))
          (forwardConeFlat_isOpen d (n + m))
          (forwardConeFlat_convex d (n + m))
          (forwardConeFlat_isCone d (n + m))
          (forwardConeFlat_salient d (n + m))
          Tflat zflat`;
- that is now the first unlanded fixed-time consumer theorem immediately above
  the new supplier theorem.

Verification status:

- attempted `timeout 60s lake env lean` on the edited module; it failed on an
  existing import/environment duplication before local theorem checking;
- attempted `lake build
  OSReconstruction.Wightman.Reconstruction.WickRotation.OSToWightmanPositivity`;
  the build failed before the target module could be rebuilt, in the dependency
  `OSToWightmanBoundaryValueLimits.lean`, because of unresolved merge markers
  at line `7`, so no clean end-to-end target build is recorded.

Supervision correction (`2026-04-17`, audit correction after landing the fixed-time `fl_representation_from_bv` specialization):
this pass re-audited only the fresh docs/chat entries from the last two passes,
the exact fixed-time theorem surface around
`section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`,
the adjacent supplier theorem
`section43_fixedTimeShellRawCLM_bv_implies_fourier_support_fixedTime`, the
exact SCV axiom surfaces `SCV.bv_implies_fourier_support` and
`SCV.fl_representation_from_bv`, and only the nearby flattening/cone
declarations needed to decide whether the first consumer specialization above
the new BV theorem was already source-ready.

Exact audit result:

- the fixed-time specialization
  `section43_fixedTimeShellRawCLM_fl_representation_fixedTime`
  is now landed in source;
- no new axiom or wrapper theorem was introduced beyond specializing the first
  live consumer seam itself.

Exact reason:

- after the all-directions BV theorem landed, the supplier step was already
  present in source as
  `section43_fixedTimeShellRawCLM_bv_implies_fourier_support_fixedTime`;
- the first remaining theorem-sized consumer was therefore the fixed-time
  specialization of `SCV.fl_representation_from_bv`;
- the exact missing ingredients were already available in checked source:
  `hall` from the new all-directions theorem,
  the fixed-time `Tflat` supplier from
  `SCV.bv_implies_fourier_support`,
  and the flattened-cone salience argument pattern already used in
  `SCV.VladimirovTillmann`;
- this made the specialization source-ready rather than docs-only.

Exact consequence:

- the live theorem-3 frontier has moved upward again above the fixed-time FL
  representation specialization;
- the first live seam is no longer the all-directions BV contract and no longer
  the fixed-time supplier/comparison handoff itself.

Verification status:

- attempted narrow verification with
  `timeout 60s lake env lean OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`;
- the run failed because
  `OSReconstruction.Wightman.Reconstruction.WickRotation.OSToWightmanBoundaryValueLimits.olean`
  does not exist in the current tree, so no completed Lean build is claimed.

Supervision correction (`2026-04-17`, audit correction after landing the fixed-time all-directions boundary-value theorem):
this pass re-audited only the fresh docs/chat entries from the last two passes,
the exact fixed-time theorem surface around
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`, the exact SCV
axiom surfaces `SCV.bv_implies_fourier_support` and
`SCV.fl_representation_from_bv`, and only the nearby cone-direction /
flattening declarations needed to confirm whether the missing strengthening was
already source-ready.

Exact audit result:

- the all-directions strengthening is now landed in source as
  `section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`;
- no new axiom or wrapper was introduced;
- the old canonical-ray theorem remains as a corollary.

Exact reason:

- `SCV.tube_boundaryValueData_of_polyGrowth` already supplied
  `hT_t : ∀ h η, η ∈ ForwardConeAbs d (n + m) → Tendsto ... (nhds (T_t h))`;
- the former fixed-time theorem only consumed `hT_t` at
  `canonicalForwardConeDirection (d := d) (n + m)`;
- therefore the missing theorem statement was not blocked by absent source
  mathematics on this seam; it was blocked only by the local theorem surface
  throwing away the stronger contract already present in the proof.

Exact consequence:

- the earlier audit sentence “no checked theorem in the exact source layer
  upgrades the canonical-ray limit to the all-`η` package” is no longer true;
- the live gap on this exact layer has moved upward from the fixed-time
  boundary-value specialization itself to the later SCV/flattened consumers.

Verification status:

- attempted narrow verification with
  `timeout 20s lake env lean OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`;
- the command timed out with exit code `124` and no diagnostics, so this pass
  records theorem landing plus source reread, not a completed Lean build.

Supervision correction (`2026-04-16`, audit of the exact missing all-directions boundary-value theorem statement on the fixed-time seam):
this pass re-audited only the fresh docs/chat entries from the last two passes,
the exact fixed-time theorem surface around
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`, the exact SCV
axiom surfaces `SCV.bv_implies_fourier_support` and
`SCV.fl_representation_from_bv`, and only the nearby cone-direction /
flattening declarations needed to state the missing strengthening.

Exact audit result:

- the honest verdict is still docs-only;
- no theorem-sized Lean landing is source-ready on this exact layer.

Exact reason:

- `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` constructs
  `T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ` and proves convergence only for the
  fixed-time raw shell family driven by the built-in
  `canonicalForwardConeDirection (d := d) (n + m)`;
- the exact continuation used there is
  `F_analytic z = bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 z (t : ℂ))`;
- both `SCV.bv_implies_fourier_support` and `SCV.fl_representation_from_bv`
  require instead the all-directions hypothesis surface
  `∀ η ∈ ForwardConeAbs d (n + m), ∀ φ, Tendsto ... (nhds (T_t φ))`;
- no checked theorem in the audited source layer upgrades the canonical-ray
  limit to that all-`η` boundary-value package;
- nearby flattening theorems only transport an already available Pi-type
  all-directions statement to `ForwardConeFlat`; they do not create it.

Exact obstruction statement:

- the missing theorem is the fixed-time all-directions strengthening:
  for the same `T_t` and
  `F_analytic z := bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 z (t : ℂ))`,
  prove
  `∀ (η : Fin (n + m) → Fin (d + 1) → ℝ), η ∈ ForwardConeAbs d (n + m) →
      ∀ φ : SchwartzNPoint d (n + m),
        Filter.Tendsto
          (fun ε : ℝ => ∫ x : Fin (n + m) → Fin (d + 1) → ℝ,
            F_analytic (fun k μ =>
              (x k μ : ℂ) + (ε : ℂ) * (η k μ : ℂ) * Complex.I) * φ x)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          (nhds (T_t φ))`;
- this is the exact theorem-sized contract missing between the current
  fixed-time source theorem and the SCV supplier/comparison axioms.

Exact consequence:

- the earlier Fourier-normalization mismatch is not the first live obstruction
  on this pass;
- the route remains docs-only because the fixed-time source still fails to meet
  the all-directions `hF_bv` interface required before the SCV axioms can even
  be specialized honestly.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the physics-side supplier specialization on the exact fixed-time flattened seam):
this pass re-audited only the fresh docs/chat state, the exact fixed-time
source layer in `OSToWightmanPositivity.lean`, the exact physics-side supplier
and comparison axioms in `SCV/VladimirovTillmann.lean`, the exact primitive
Fourier-Laplace/Fourier declarations in `SCV/PaleyWienerSchwartz.lean`, and
the exact flattening transport declarations in
`ForwardTubeDistributions.lean`.

Exact audit result:

- the honest verdict is still docs-only;
- no theorem-sized Lean landing is source-ready on this exact supplier-to-
  comparison specialization seam.

Exact reason:

- the direct physics-side supplier route would have to instantiate
  `SCV.bv_implies_fourier_support` with
  `C := ForwardConeAbs d (n + m)`,
  `F := F_analytic`,
  `W := T_t`,
  where
  `F_analytic z = bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 z (t : ℂ))`;
- that axiom requires the full boundary-value hypothesis
  `∀ η ∈ C, ∀ φ, Tendsto ... (nhds (T_t φ))`;
- the exact fixed-time source theorem
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`
  supplies only
  `∀ φ, Tendsto ... canonicalForwardConeDirection ... (nhds (T_t φ))`;
- no checked theorem in the exact source layer upgrades this single-direction
  convergence to the all-directions hypothesis needed by
  `SCV.bv_implies_fourier_support`;
- consequently there is also no source-ready input package for the downstream
  comparison axiom `SCV.fl_representation_from_bv`, even before addressing
  any Fourier-convention transport.

Exact obstruction statement:

- the missing theorem-sized statement is an all-directions boundary-value
  specialization for the shifted fixed-time continuation:
  from the exact `T_t` already constructed, prove boundary convergence for
  every `η ∈ ForwardConeAbs d (n + m)`, not only for
  `canonicalForwardConeDirection`;
- flattening then transports that Pi-type statement to the flat seam, but
  source presently has no theorem that performs the needed strengthening.

Exact consequence:

- the exact obstruction is now earlier and sharper than the prior
  inverse-vs-physics normalization mismatch;
- the route remains docs-only because the fixed-time supplier specialization
  does not yet meet the hypothesis surface of the physics-side axioms.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact fixed-time comparison contract after collapsing the duplicate inverse-Fourier normalization):
this pass re-audited only the fresh docs/chat state, the exact fixed-time
source layer in `OSToWightmanPositivity.lean`, the exact inverse/physics
Fourier declarations in `SCV/FourierSupportCone.lean` and
`SCV/PaleyWienerSchwartz.lean`, and the exact comparison axiom in
`SCV/VladimirovTillmann.lean`.

Exact audit result:

- the honest verdict is still docs-only;
- no theorem-sized Lean landing is source-ready on the exact comparison layer.

Exact reason:

- the weakest honest comparison theorem is still the fixed-time specialization
  of `SCV.fl_representation_from_bv`, with hypotheses
  `HasFourierSupportInDualCone (ForwardConeFlat d (n + m)) Tflat_t` and
  `Wflat_t φ = Tflat_t (physicsFourierFlatCLM φ)`;
- the exact supplier theorem below,
  `SCV.fourierSupportInDualCone_of_tube_boundaryValue`,
  still lands in the inverse-Fourier convention
  `Wflat_t φ = Tflat_t (inverseFourierFlatSupportCLM φ)`;
- source check sharpens one normalization point:
  `inverseFourierFlatSupportCLM` and `inverseFourierFlatCLM` are literally the
  same transported Fourier map, so there is no additional checked mismatch
  between those two names;
- the real unchecked normalization gap is exactly the negative scaling in
  `SCV.physicsFourierFlatCLM_apply`,
  `physicsFourierFlatCLM f ξ =
    inverseFourierFlatCLM f ((-(1 / (2 * Real.pi) : ℝ)) • ξ)`;
- checked source contains no theorem transporting the frequency-side witness
  across that map and simultaneously preserving the same dual-cone support
  hypothesis;
- the exact comparison theorem itself is still only the axiom
  `SCV.fl_representation_from_bv`.

Exact obstruction statement:

- a source-ready bridge would need a theorem of the shape:
  from
  `HasFourierSupportInDualCone (ForwardConeFlat d (n + m)) Tflat_t`
  and
  `∀ φ, Wflat_t φ = Tflat_t (inverseFourierFlatCLM φ)`,
  produce some frequency-side witness in the physics convention, equivalently a
  precise rescaled witness with
  `∀ φ, Wflat_t φ = Tphys_t (physicsFourierFlatCLM φ)`;
- no such rescaling/support theorem exists in checked source, and because the
  scaling factor is negative this is not an administrative rewrite.

Exact consequence:

- there is still no honest Lean landing on the theorem-3 comparison seam;
- the route remains docs-only until both:
  1. the comparison theorem is de-axiomatized, and
  2. the inverse-to-physics normalization transport for the frequency-side
     witness is formalized.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact Fourier-normalization bridge layer between `inverseFourierFlatSupportCLM` and `physicsFourierFlatCLM` on the fixed-time seam):
this pass re-audited only the fresh docs/chat state, the exact normalization
definitions in `SCV/PaleyWienerSchwartz.lean` and
`SCV/FourierSupportCone.lean`, the exact supplier/comparison declarations in
`SCV/FourierSupportCone.lean` and `SCV/VladimirovTillmann.lean`, and the exact
fixed-time declarations needed to state the specialization in
`OSToWightmanPositivity.lean`.

Exact audit result:

- the honest verdict is still docs-only;
- no theorem-sized Lean landing is source-ready on the Fourier-normalization
  bridge layer.

Exact reason:

- the exact supplier theorem present in checked source is
  `SCV.fourierSupportInDualCone_of_tube_boundaryValue`, concluding
  `∀ φ, W φ = T (inverseFourierFlatSupportCLM φ)`;
- the exact comparison theorem present in source is only the axiom
  `SCV.fl_representation_from_bv`, requiring
  `∀ φ, Wflat φ = Tflat (physicsFourierFlatCLM φ)`;
- the exact normalization theorem actually present is only
  `SCV.physicsFourierFlatCLM_apply`, namely
  `physicsFourierFlatCLM f ξ =
    inverseFourierFlatCLM f ((-(1 / (2 * Real.pi) : ℝ)) • ξ)`;
- `SCV.inverseFourierFlatSupportCLM` is separately defined and no theorem in
  source links it to `SCV.physicsFourierFlatCLM` at the CLM level strongly
  enough to rewrite the supplier identity into the comparison identity.

Exact obstruction statement:

- a source-ready bridge would have to transport a frequency-side distribution
  across the negative scaling
  `ξ ↦ (-(1 / (2 * Real.pi) : ℝ)) • ξ`
  while preserving the same
  `HasFourierSupportInDualCone C ...` conclusion;
- checked source contains no such theorem;
- because that scaling is negative, same-cone preservation is not automatic,
  so this is not an administrative wrapper gap.

Exact source objects actually present:

- `SCV.inverseFourierFlatSupportCLM`
- `SCV.physicsFourierFlatCLM`
- `SCV.physicsFourierFlatCLM_apply`
- `SCV.fourierSupportInDualCone_of_tube_boundaryValue`
- `SCV.bv_implies_fourier_support`
- `SCV.fl_representation_from_bv`
- `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`

Exact consequence:

- there is no honest checked bridge from the supplier-side inverse-Fourier
  identity to the comparison-side physics-Fourier identity;
- the only physics-side supplier statement in source is the separate axiom
  `SCV.bv_implies_fourier_support`, not a checked normalization bridge from
  `SCV.fourierSupportInDualCone_of_tube_boundaryValue`;
- therefore this pass stays docs-only.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact post-supplier function-level FL comparison seam for the fixed-time continuation `G_t`):
this pass re-audited only the fresh docs/chat state, the exact fixed-time
cluster in `OSToWightmanPositivity.lean`, the exact primitive FL declarations
in `SCV/PaleyWienerSchwartz.lean`, the exact supplier surface in
`SCV/FourierSupportCone.lean`, the exact flattening/representation layer in
`SCV/VladimirovTillmann.lean`, and the weak/regular package note in
`SCV/LaplaceSchwartz.lean`.

Exact audit result:

- the honest verdict is still docs-only;
- no one-theorem Lean landing is source-ready on the checked comparison layer.

Exact reason:

- the exact supplier layer below is still the fixed-time boundary functional
  `T_t` from
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`;
- the exact next theorem above it is not yet continuity and not yet the regular
  package: it is the function-level identification of
  `G_t zflat = bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))`
  with
  `fourierLaplaceExtMultiDim (ForwardConeFlat d (n + m)) ... Tflat_t`;
- checked source contains exactly that theorem shape only as the axiom
  `SCV.fl_representation_from_bv`;
- its exact boundary-matching hypothesis is
  `Wflat_t φ = Tflat_t (physicsFourierFlatCLM φ)`;
- the nearest checked supplier theorem below,
  `SCV.fourierSupportInDualCone_of_tube_boundaryValue`,
  instead yields the boundary identity
  `Wflat_t φ = Tflat_t (inverseFourierFlatSupportCLM φ)`;
- so the route is blocked for two source-first reasons on this exact seam:
  the comparison theorem is itself axiom-backed, and its input convention does
  not match the nearest checked supplier theorem.

Exact candidate theorem statement:

- with
  `Wflat_t := T_t.comp (SchwartzMap.compCLMOfContinuousLinearEquiv ℂ
    (flattenCLEquivReal (n + m) (d + 1)))`,
  the weakest honest function-level comparison theorem is:
  `HasFourierSupportInDualCone (ForwardConeFlat d (n + m)) Tflat_t` plus
  `∀ φ, Wflat_t φ = Tflat_t (physicsFourierFlatCLM φ)`
  imply
  `∀ zflat ∈ SCV.TubeDomain (ForwardConeFlat d (n + m)),
      G_t zflat =
        fourierLaplaceExtMultiDim
          (ForwardConeFlat d (n + m)) ... Tflat_t zflat`;
- this is the fixed-time specialization of `SCV.fl_representation_from_bv`.

Exact consequence:

- no theorem-sized Lean landing is honest now on this comparison layer;
- the route stays docs-only even before the later strong-to-regular upgrade,
  because checked source still lacks a non-axiomatic comparison theorem and a
  checked Fourier-convention bridge from the supplier theorem into that
  comparison theorem.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact source-ready status of the dual-cone FL supplier theorem for the fixed-time continuation `G_t`):
this pass re-audited only the fresh docs/chat entries, the exact fixed-time
blocker cluster in `OSToWightmanPositivity.lean`, the exact dual-cone supplier
surface in `SCV/FourierSupportCone.lean`, the exact weak/regular package note
in `SCV/LaplaceSchwartz.lean`, the primitive FL declarations in
`SCV/PaleyWienerSchwartz.lean`, the flattened tempered supplier in
`SCV/VladimirovTillmann.lean`, and the local continuity consumer in
`SCV/LocalBoundaryRecovery.lean`.

Exact audit result:

- the honest verdict is still docs-only;
- no one-theorem Lean landing is source-ready on the checked layer.

Exact reason:

- the exact live blocker remains
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- the weak fixed-time source layer is still:
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  and
  `section43_fixedTimeShellRaw_uniformPolyGrowth_fixedTime`;
- the exact next honest theorem below the weak-to-regular contract is the
  supplier statement
  `∃ Tflat_t,
      HasFourierSupportInDualCone (ForwardConeFlat d (n + m)) Tflat_t ∧
      ∀ φ, T_t φ = Tflat_t (inverseFourierFlatSupportCLM φ)`,
  for the fixed-time functional `T_t` attached to
  `G_t zflat = bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))`;
- the nearest checked theorem on that exact layer is
  `SCV.fourierSupportInDualCone_of_tube_boundaryValue`;
- that theorem still depends on the axiom
  `SCV.tube_boundaryValue_realizes_dualCone_distribution`;
- even granting that supplier, checked source still lacks both:
  `G_t = fourierLaplaceExtMultiDim ... Tflat_t` at the function level, and the
  subsequent upgrade from genuine FL input to
  `SCV.HasFourierLaplaceReprRegular ... G_t`.

Exact consequence:

- the route remains docs-only for two distinct source reasons:
  the immediate supplier theorem is still axiom-backed, and the next
  strong-to-regular bridge is still unformalized/mismatched.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact dual-cone supplier theorem beneath the weak-to-regular contract for `G_t`):
this pass re-audited only the fresh docs/chat state, the exact theorem cluster
at the blocker, the exact fixed-time weak feeder/growth theorems, the exact
flattening declarations for `G_t`, and the exact SCV/Fourier-support source
surface around dual-cone realization.

Exact audit result:

- the honest verdict is still docs-only;
- no one-theorem Lean landing is source-ready on the checked layer.

Exact reason:

- the exact blocker remains
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- the exact theorem immediately below the weak/regular gap is a genuine
  supplier theorem identifying the fixed-time boundary functional with a
  dual-cone-supported Fourier-Laplace representation, not yet a continuity
  theorem;
- concretely, with
  `G_t zflat = bvt_F OS lgc (n + m)
      (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
        ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))`
  and `T_t` from
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
  the exact candidate supplier theorem is:
  `∃ Tflat_t : SchwartzMap (Fin ((n + m) * (d + 1)) → ℝ) ℂ →L[ℂ] ℂ,
      HasFourierSupportInDualCone (ForwardConeFlat d (n + m)) Tflat_t ∧
      ∀ φ, T_t φ = Tflat_t (inverseFourierFlatSupportCLM φ)`;
- the nearest checked source theorem is
  `SCV.fourierSupportInDualCone_of_tube_boundaryValue`, but it depends on the
  axiom `SCV.tube_boundaryValue_realizes_dualCone_distribution`;
- the checked strong source also still stops short of the next needed step:
  no theorem upgrades this genuine FL realization to
  `SCV.HasFourierLaplaceReprRegular ... G_t` or directly to the required
  real-locus `ContinuousWithinAt`;
- therefore the route is still docs-only both because the supplier theorem is
  not honestly formalized and because the strong-to-regular bridge remains
  missing in checked source.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact weak-to-regular contract needed for the shifted-point boundary continuity theorem):
this pass re-audited only the fresh docs/chat state, the exact theorem cluster
at
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
the fixed-time weak feeder/growth suppliers, the flattened geometry
declarations, and the exact SCV weak/regular package and continuity-consumer
surfaces.

Exact audit result:

- the honest verdict is still docs-only;
- no one-theorem Lean landing is source-ready on the checked layer.

Exact reason:

- the minimal live blocker remains
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- the theorem just above it is still only the `xiShift` transport lemma;
- the exact consumer surface below is still local `ContinuousWithinAt`, as
  shown by
  `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`;
- the weak fixed-time package still provides only distributional boundary-value
  convergence plus polynomial/ray growth, not pointwise boundary continuity;
- `HasFourierLaplaceReprTempered` still has no continuity field, while
  `HasFourierLaplaceReprRegular` is exactly the first source package carrying
  `tube_continuousWithinAt`;
- the note in `SCV/LaplaceSchwartz.lean` still says the needed upgrade theorem
  from genuine strong FL input to `HasFourierLaplaceReprRegular` is not
  formalized.

Exact weakest honest bridge theorem:

- for
  `G_t zflat = bvt_F OS lgc (n + m)
      (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
        ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))`,
  the first honest theorem is not
  `HasFourierLaplaceReprTempered ... G_t → ContinuousWithinAt ...`,
  which is false in this generality;
- it is a genuine weak-to-regular upgrade theorem for `G_t`: from holomorphy on
  `SCV.TubeDomain (ForwardConeFlat d (n + m))`, the weak fixed-time
  boundary-value/growth package, and an additional genuine FL representation by
  a tempered distribution with Fourier support in
  `ForwardConeFlat d (n + m)`'s dual cone, conclude either
  `SCV.HasFourierLaplaceReprRegular (ForwardConeFlat d (n + m)) G_t`
  or directly the needed real-locus continuity theorem for all complement-side
  `xflat`.

Exact obstruction:

- that dual-cone-supported FL upgrade is still not a checked theorem on the
  present source layer;
- so the route remains docs-only.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact theorem-sized supplier immediately below the shifted-point blocker):
this pass re-audited only the fresh docs/chat note that the regular-package
constructor seam was one layer too high, the exact theorem/proof cluster at
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
the fixed-time weak boundary-value and growth suppliers, the exact shifted-point
geometry, and the SCV continuity consumers that could honestly discharge the
missing hypothesis.

Exact audit result:

- the honest verdict is still docs-only;
- no one-theorem Lean landing is source-ready on the checked layer.

Exact reason:

- the exact minimal live blocker is again
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- the theorem immediately above it,
  `section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
  does not construct continuity; it only transports an already-supplied
  `hcont_shifted`;
- the smallest honest theorem to target immediately below the blocker is the
  direct fixed-time shifted-point continuity statement for the flattened
  continuation
  `G_t zflat = bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))`,
  namely
  `∀ xflat ∈ (flattenCLEquivReal (n + m) (d + 1)) ''
      ((section43PositiveEnergyRegion d (n + m))ᶜ),
      ContinuousWithinAt G_t
        (SCV.TubeDomain (ForwardConeFlat d (n + m)))
        (SCV.realEmbed xflat)`;
- equivalently, in raw unflattened form, prove continuity of `bvt_F` at the
  shifted real point
  `xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 (fun k μ => (y k μ : ℂ)) (t : ℂ)`
  for every complement-side `y`;
- source already supplies only the non-continuity data for this candidate:
  weak fixed-time boundary-value data from
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
  global shifted-tube growth from
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  and shell ray bounds from
  `section43_fixedTimeShellRaw_uniformPolyGrowth_fixedTime`;
- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` confirms the next
  local consumer needs exactly real-locus `ContinuousWithinAt`;
- `HasFourierLaplaceReprTempered` still has no continuity field, and the note
  in `SCV/LaplaceSchwartz.lean` still explicitly marks the real weak-to-regular
  upgrade theorem as unformalized;
- therefore the missing mathematics is still the actual boundary-regularity
  statement itself, not a wrapper theorem and not a source-ready constructor.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact fixed-time regular-package constructor seam):
this pass re-audited only the sharpened missing-source-object docs/chat notes,
the fixed-time feeder
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
the fixed-time shifted/flattened continuation declarations around `G_t`,
the exact SCV package fields,
and the immediate theorems that would consume a regular package or
`tube_continuousWithinAt`.

Exact audit result:

- the honest verdict is still docs-only;
- no one-theorem Lean landing is source-ready on the checked layer.

Exact reason:

- the exact current blocker remains
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- the exact candidate object beneath it is the fixed-time flattened shifted
  continuation
  `G_t zflat = bvt_F ... (xiShift ... ((flattenCLEquiv ...).symm zflat) (t : ℂ))`;
- the checked source already supplies for that candidate:
  tube holomorphy through the `F_analytic` proof in
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
  global growth through
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  and weak boundary-value data plus ray bounds through
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` and
  `section43_fixedTimeShellRaw_uniformPolyGrowth_fixedTime`;
- however `HasFourierLaplaceReprTempered` still stops at
  `dist`, `boundary_value`, `poly_growth`, and `uniform_bound`,
  while `HasFourierLaplaceReprRegular` is the first checked package carrying
  `tube_continuousWithinAt`;
- the checked local consumer
  `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`
  confirms the needed extra datum is exactly real-locus
  `ContinuousWithinAt`, and the forward-tube theorems confirm that regular
  flattened packages are only consumed, not constructed, on this layer;
- so the exact smallest honest theorem now missing is:
  a fixed-time constructor theorem for `G_t` producing
  `SCV.HasFourierLaplaceReprRegular (ForwardConeFlat d (n + m)) G_t`,
  or exactly equivalently a theorem with the same fixed-time data proving
  `ContinuousWithinAt G_t (SCV.TubeDomain (ForwardConeFlat d (n + m))) (SCV.realEmbed xflat)`
  for every complement-side real `xflat`;
- the missing mathematics is not a wrapper or transport lemma:
  it is the actual boundary-regularity upgrade from weak distributional
  boundary-value data plus polynomial/ray growth to pointwise boundary
  continuity on the real locus.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the first honest boundary-regularity supplier beneath the shifted-point theorem):
this pass re-audited only
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
its immediate transport/consumer theorems,
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
the exact SCV package declarations,
and the already-landed forward-tube theorems that consume regular flattened FL
input.

Exact audit result:

- the honest verdict is still docs-only;
- no one-theorem Lean landing is source-ready on this checked layer.

Exact reason:

- the shifted-point theorem remains the exact live blocker;
- the source below it still does not force any explicit trace-wrapper theorem;
- the exact first continuity-bearing interface already present in source is
  `SCV.HasFourierLaplaceReprRegular.tube_continuousWithinAt`;
- `HasFourierLaplaceReprTempered` still carries no continuity field, so the
  weak fixed-time boundary package cannot directly feed the needed local
  continuity hypothesis;
- the forward-tube consumers
  `boundary_function_continuous_forwardTube_of_flatRegular`,
  `boundary_value_recovery_forwardTube_of_flatRegular`,
  and
  `distributional_uniqueness_forwardTube_of_flatRegular`
  confirm the same ownership boundary: they only consume an already-built
  regular flattened package and do not construct one;
- `SCV/LaplaceSchwartz.lean` still records that the weak-to-regular upgrade
  theorem is not formalized.

Precise blocker now:

- exact missing source object:
  a theorem constructing a regular flattened Fourier-Laplace package for the
  fixed-time shifted continuation `G_t`, i.e.
  `SCV.HasFourierLaplaceReprRegular (ForwardConeFlat d (n + m)) G_t`,
  or an exactly equivalent theorem directly supplying its
  `tube_continuousWithinAt` field on the complement-side real locus;
- exact anti-drift conclusion:
  do not descend to explicit trace / `Uflat` wrappers; the honest missing seam
  is regular boundary continuity itself.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact shifted-point theorem after rechecking its immediate consumers and feeder):
this pass re-audited only
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
its two immediate consumers,
`section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`,
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
and the exact SCV local-recovery / package declarations that could supply the
missing boundary continuity.

Exact audit result:

- the honest verdict is still docs-only;
- no one-theorem Lean landing is source-ready on the checked layer.

Exact reason:

- the exact minimal next theorem is still
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- its first immediate consumer
  `section43_fixedTimeShellRaw_pointwiseContinuity_on_section43PositiveEnergyRegion_compl`
  is only the transport corollary through
  `section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`;
- the later coefficient theorem remains strictly downstream of that continuity
  seam;
- `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` still supplies
  only distributional boundary-value data;
- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` already shows the
  local consumer contract, and its extra input is exactly local
  `ContinuousWithinAt`, not a separate trace wrapper;
- `HasFourierLaplaceReprTempered` still has no continuity field;
- `HasFourierLaplaceReprRegular` would supply
  `tube_continuousWithinAt`, but the checked note in
  `SCV/LaplaceSchwartz.lean` still says the upgrade theorem to that regular
  package is not formalized;
- hence no checked theorem in the reread source constructs the required
  shifted-point continuity for raw `bvt_F`.

Precise blocker now:

- exact missing source object:
  a boundary-regularity theorem for raw `bvt_F` at the shifted real point on
  the complement side, equivalently a theorem supplying
  `ContinuousWithinAt` / `tube_continuousWithinAt` there, or a regular
  Fourier-Laplace package for the shifted continuation from which that
  continuity follows;
- exact anti-drift conclusion:
  do not descend again to an explicit trace theorem unless later source
  actually forces it.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact shifted-point continuity seam after rereading the actual SCV consumers):
this pass re-audited only the theorem
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
its two immediate consumers, the transport theorem immediately above it, the
fixed-time boundary-functional feeder below it, and the exact SCV
local-recovery / uniqueness theorems that could honestly produce the needed
boundary continuity.

Exact audit result:

- the honest verdict is still docs-only;
- no one-theorem Lean landing is source-ready on the checked layer.

Exact reason:

- the exact minimal next theorem is indeed
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  itself;
- the prior docs/chat claim that an explicit trace-supplier theorem must come
  first was too low relative to the checked source;
- `LocalBoundaryRecovery.lean` already contains the local tempered recovery
  theorem
  `fourierLaplace_boundary_recovery_on_open_of_tempered`,
  and its honest extra input is only local
  `ContinuousWithinAt`, not a separate boundary trace wrapper;
- however `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` still
  supplies only a distributional boundary functional, and
  `HasFourierLaplaceReprTempered` still does not include
  `tube_continuousWithinAt`;
- the corresponding regular package
  `HasFourierLaplaceReprRegular`, which would provide the needed boundary
  continuity, is explicitly marked in `SCV/LaplaceSchwartz.lean` as missing a
  proved upgrade theorem from stronger Fourier-Laplace input;
- no checked theorem in the reread source constructs that regular package for
  `bvt_F` or directly proves the required
  `ContinuousWithinAt` at the shifted real boundary point.

Precise blocker now:

- exact missing source object:
  a boundary-regularity theorem for raw `bvt_F` at the shifted real edge,
  namely the current theorem itself or an equivalent theorem supplying
  `tube_continuousWithinAt` on the relevant complement-side real set;
- exact anti-drift conclusion:
  do not descend to a new explicit trace wrapper beneath the current theorem
  unless source later forces it; the source gap presently sits exactly at
  boundary continuity.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact theorem surface directly under `hlimit_os` versus the lower proof blocker beneath it):
this pass re-audited the outside-region branch inside
`section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`
against the already-present outside-region shell lemmas in
`OSToWightmanPositivity.lean`.

Exact audit result:

- the honest verdict is still docs-only;
- the theorem immediately beneath `hlimit_os` is already source-designed and
  should remain generic, not pair-specific.

Exact reason:

- the fixed-pair branch comment currently advertises a new limit-level shell
  comparison theorem for
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`;
- but the checked source already contains the sharper general theorem surface
  that `hlimit_os` should consume:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- that theorem belongs exactly where it already lives, in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  between the coefficient theorem and the fixed-pair branch;
- its minimal input surface is only:
  `OS`, `lgc`, `hm`, `t`,
  `h : SchwartzNPoint d (n + m)`,
  and
  `hq : section43PositiveEnergyQuotientMap ... h = 0`;
- on the live branch, those data are fed without wrapper drift by taking
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)` and deriving `hq`
  from
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`
  plus linearity;
- so the exact direct-below-`hlimit_os` theorem category is:
  **descended shell functional theorem**,
  not a local shell-comparison wrapper and not a new pair-specialized theorem;
- the reason no Lean landing followed is lower:
  this theorem's proof still depends on
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`,
  which in turn still depends on the unresolved complement-side continuity
  theorem
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of exact file ownership and minimal hypotheses for the missing trace theorem):
this pass re-audited only the source objects needed to decide where the next
theorem belongs and what its honest input/output surface must be.

Exact audit result:

- the honest verdict is still docs-only;
- the minimal next theorem is still not Lean-landable from current source.

Exact reason:

- `TubeBoundaryValues.lean` stops at the checked theorem
  `SCV.tube_boundaryValueData_of_polyGrowth`, which returns only the
  distributional functional;
- `LaplaceSchwartz.lean` packages that data into
  `HasFourierLaplaceReprTempered`, but its checked structure fields are still
  only `dist`, `boundary_value`, `poly_growth`, and `uniform_bound`;
- `LocalBoundaryRecovery.lean` already has the right generic consumer
  interface:
  the first exact local theorem there,
  `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace`,
  consumes an explicit trace function on an open real set, not merely the
  distributional package;
- therefore the next theorem should be owned by the positivity file as the
  branch-specific supplier of that missing explicit trace object.

Precise blocker now:

- exact owner file/layer:
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  immediately above the present `sorry`
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- exact minimal statement:
  for fixed `t : ℝ`, define the branch-specific flattened continuation `G_t`
  and complement open set `Uflat`, then produce
  `∃ B_t, ContinuousOn B_t Uflat ∧ raywise-trace-limit-to-B_t-on-Uflat`;
- exact minimal hypotheses/data that should feed it:
  only the live fixed-time shell branch data already checked in that file:
  `OS`, `lgc`, `hm`, `t`, the induced shifted/flattened analytic continuation
  `G_t`, and the distributional/growth package already available there from
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` plus the same
  fixed-time growth estimates;
- exact anti-drift conclusion:
  do not ask callers to pass a new wrapper package or restate the theorem as a
  raw `ContinuousWithinAt` lemma first; the explicit pointwise trace package is
  the first missing object, and `ContinuousWithinAt` should be downstream.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact minimal theorem surface below the complement-side trace seam):
this pass re-audited only the exact supplier theorem
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
the immediate outside-region continuity chain in
`OSToWightmanPositivity.lean`,
and the direct SCV contracts in
`LocalBoundaryRecovery.lean`,
`LaplaceSchwartz.lean`,
and
`TubeBoundaryValues.lean`.

Exact audit result:

- the honest verdict is still docs-only;
- no one-theorem Lean landing exists in the current checked source layer.

Exact reason:

- `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` is sourced from
  `SCV.tube_boundaryValueData_of_polyGrowth`;
- that SCV theorem returns only a continuous Schwartz functional `W` with
  raywise convergence of pairings, not a pointwise trace on an open real set;
- `HasFourierLaplaceReprTempered` also stores only
  `dist : SchwartzMap ... → ℂ`
  plus `boundary_value`, `poly_growth`, and `uniform_bound`;
- the direct consumer theorem
  `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace`
  still requires:
  an explicit `G`,
  `ContinuousOn G U`,
  and
  `htraceU : ∀ x ∈ U, Tendsto (...) (nhds (G x))`;
- therefore no checked theorem in this source layer converts the available
  distributional object `T_t` into the explicit complement-side pointwise
  trace needed below
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`.

Precise blocker now:

- exact minimal new theorem/interface statement:
  for fixed `t > 0`, for
  `Uflat := (flattenCLEquivReal (n + m) (d + 1)) ''
    ((section43PositiveEnergyRegion d (n + m))ᶜ)`,
  and the flattened shifted shell continuation `G_t`,
  produce
  `∃ B_t : (Fin ((n + m) * (d + 1)) → ℝ) → ℂ,
      ContinuousOn B_t Uflat ∧
      ∀ xflat ∈ Uflat, Filter.Tendsto
        (fun ε : ℝ => G_t (SCV.realEmbed xflat + ε • ηflat * Complex.I))
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds (B_t xflat))`
  with
  `ηflat i := (canonicalForwardConeDirectionFlat (d := d) (n + m) i : ℂ)`;
- why present source still stops short:
  current source reaches only the distributional boundary-value functional,
  while the local SCV recovery layer still consumes an already-given pointwise
  trace.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the exact complement-side source-interface gap beneath the outside-region fixed-time branch):
this pass re-audited only the exact theorem cluster around
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
and
`section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`,
then checked the direct separate-trace consumers in
`OSReconstruction/SCV/LocalBoundaryRecovery.lean`.

Exact audit result:

- the honest verdict is still docs-only;
- no one-theorem Lean landing is source-ready on the current complement-side
  branch.

Exact reason:

- the first exact missing object is a pointwise complement-side local trace for
  the flattened shifted shell continuation on
  `Uflat = (flattenCLEquivReal ...) '' ((section43PositiveEnergyRegion ...)ᶜ)`,
  not a later shell-difference wrapper and not merely a restatement of
  `ContinuousWithinAt`;
- the available SCV theorem
  `fourierLaplace_schwartz_integral_convergence_local_of_trace`
  still requires as inputs:
  an explicit `G`,
  `ContinuousOn G U`,
  and
  `htraceU : ∀ x ∈ U, Tendsto (...) (nhds (G x))`;
- current positivity-side source only supplies
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
  i.e. a boundary-value functional
  `T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ`;
- there is still no theorem in the checked import layer extracting from that
  `T_t` an explicit complement-side trace function on `Uflat` with the needed
  continuity and raywise boundary-limit contract;
- therefore the present theorem
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  is still blocked by an earlier source-interface mismatch, and the later
  coefficient-limit theorem remains downstream of that gap.

Precise blocker now:

- first missing source object:
  the explicit complement-side pointwise trace `B_t` on `Uflat` with
  `ContinuousOn B_t Uflat` and the exact `htraceU` raywise limit contract for
  the fixed-time shifted shell;
- why current source still fails:
  `T_t` is only distributional, while the available SCV local-recovery layer
  still expects pointwise trace data as an input.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the live `hHlimit` consumer against the fixed-time outside-region shell branch):
this pass re-audited the actual consumer theorem surfaces in
`OSToWightmanBoundaryValueLimits.lean`
against the unresolved outside-region proof body in
`OSToWightmanPositivity.lean`.

Exact audit result:

- the honest verdict is still docs-only;
- no one-theorem Lean landing is source-ready on the current shell-comparison
  comment alone.

Exact reason:

- the theorem-3 consumer side is already correct:
  `bvt_wightmanInner_eq_osInner_of_componentwise_tendsto_singleSplit_xiShiftHolomorphicValue_nhdsWithin_zero`
  consumes only
  `hHlimit`,
  and
  `tendsto_bvt_singleSplit_xiShiftHolomorphicValue_nhdsWithin_zero_of_ofReal_eq_bvt_W_ambient_conjTensorProduct_timeShift`
  shows that it is enough upstream to prove the positive-real fixed-time
  equality `hreal`;
- the live fixed-time supplier theorem
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`
  is exactly that `hreal`-producer;
- but inside its outside-region branch, the apparently higher shell-comparison
  target still depends on the older unresolved complement-side package:
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  needs
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
  and that theorem remains a `sorry`;
- source beneath it still supplies only
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
  i.e. a distributional boundary functional, not the pointwise local trace or
  raw `ContinuousWithinAt` theorem needed on the outside-region real edge;
- therefore the first exact missing source object / contract mismatch beneath
  the current `hHlimit` consumer seam is still the complement-side
  boundary-trace / continuity object for shifted `bvt_F`, not the later
  ambient-vs-source shell-comparison wrapper mentioned in the fixed-time proof
  comment.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the separate-trace local recovery contract against the live outside-region shell seam):
this pass re-audited the theorem cluster around
`section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
against the exact assumptions of
`pointwise_convergence_local_of_trace`,
`fourierLaplace_schwartz_integral_convergence_local_of_trace`,
and
`fourierLaplace_boundary_recovery_on_open_of_tempered`.

Exact audit result:

- the honest verdict is still docs-only;
- the seam is not yet ready for a one-theorem Lean refactor to a separate
  local boundary-trace contract.

Exact reason:

- the theorem in `LocalBoundaryRecovery.lean` that really exposes the repaired
  contract is
  `fourierLaplace_schwartz_integral_convergence_local_of_trace`;
- but it still requires as input an explicit trace function `G` on an open
  real set `U`, plus:
  `ContinuousOn G U`
  and
  `htraceU : ∀ x ∈ U, Tendsto (...) (nhds (G x))`;
- on the live outside-region seam, the corresponding exact missing object is a
  complement-side trace for the flattened fixed-time shell continuation
  `G_t`,
  on
  `Uflat = (flattenCLEquivReal ...) '' ((section43PositiveEnergyRegion ...)ᶜ)`,
  with those two properties;
- current source only provides
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
  i.e. a boundary-value functional
  `T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ`,
  not a pointwise trace object on `Uflat`;
- therefore the separate-trace theorems do not yet close the outside-region
  branch: they consume the missing trace object rather than producing it from
  `T_t`.

Precise blocker now:

- first missing source object:
  an explicit local boundary trace
  `B_t` for the flattened shifted shell on `Uflat`,
  with `ContinuousOn B_t Uflat` and the exact raywise boundary-limit theorem
  `htraceU`;
- without that object, replacing the current raw-`ContinuousWithinAt` theorem
  by a trace-contract theorem would still leave the first seam unproved.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of why the first complement-side continuity theorem is still the exact blocker):
this pass re-audited the outside-region chain beneath `hlimit_os` against the
currently available SCV boundary-recovery infrastructure.

Exact audit result:

- the first exact blocker is still
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- it is not honestly landable on current source.

Exact reason:

- `bvt_F_holomorphic` gives only interior differentiability / continuity at
  points already in `ForwardTube d (n + m)`;
- `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` gives only the
  existence of a boundary-value functional for the shifted shell family;
- the local SCV recovery theorem
  `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`
  still assumes the missing ingredient
  `ContinuousWithinAt F (TubeDomain C) (realEmbed x)` on the open real set, so
  it cannot produce the needed continuity for raw `bvt_F`;
- no theorem was found in the live import layer that upgrades holomorphy plus
  tempered boundary-value data to complement-side real-edge
  `ContinuousWithinAt` for the shifted `bvt_F` point.

Precise blocker now:

- exact first failed theorem:
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- exact source obstruction:
  missing boundary-regularity theorem for raw `bvt_F` on the outside-region
  real edge; current source only supplies interior tube continuity and
  distributional boundary-value existence.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

Supervision correction (`2026-04-16`, audit of the live outside-region shell branch beneath `hlimit_os`):
this pass re-audited the actual proof body of
`section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`
against the complement-side shell lemmas declared immediately above it in
`OSToWightmanPositivity.lean`.

Exact audit result:

- the smallest honest theorem-sized next target is not a fresh shell-difference
  wrapper and not yet
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- the first real source blocker is the earlier theorem already stubbed in the
  file:
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`.

Exact reason:

- the local `hlimit_os` proof already has enough source-backed infrastructure
  to clear the source-support branch and the positive-energy/outside-source
  branch;
- the remaining outside-region route is visibly funneled through the
  complement-side continuity chain ending in
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- but the first theorem in that chain is itself an unresolved `sorry`, with
  exact conclusion
  `ContinuousWithinAt (bvt_F OS lgc (n + m)) (ForwardTube d (n + m))
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      (fun k μ => (y k μ : ℂ)) (t : ℂ))`
  for
  `y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`;
- source grep found no already-proved complement-side boundary regularity
  theorem for `bvt_F` at that shifted boundary point, so the earliest actual
  implementation blocker is this continuity theorem, not the later dominated-
  convergence packaging.

Precise blocker now:

- first lower blocker beneath `hlimit_os`:
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`;
- first downstream consumer waiting on it:
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`,
  then
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`.

Verification status:

- docs-only audit pass;
- no Lean verification command was needed because no Lean edit was attempted.

Supervision correction (`2026-04-16`, theorem-3 audit after the primitive-FL contract repair):
this pass re-audited the live theorem-3 files
`OSToWightmanBoundaryValueLimits.lean`
and
`OSToWightmanPositivity.lean`
against the contract-repair note in
`SCV/PaleyWienerSchwartz.lean`.

Exact audit result:

- the theorem-3 frontier is no longer any scalar real-edge continuity theorem
  for `fourierLaplaceExtMultiDim`;
- the live boundary-trace object is
  `bvt_singleSplit_xiShiftHolomorphicValue`;
- the exposed theorem-3 consumer hypothesis is `hHlimit`, i.e. the
  `t → 0+` Wightman-side boundary-value limit for that chosen scalar trace;
- `section43_fixedPair_upperHalfPlaneScalarization` is already proved, so the
  first remaining theorem-sized gap is lower: the fixed-time shell comparison
  inside `hlimit_os`.

Exact reason:

- `PaleyWienerSchwartz.lean` explicitly records that
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` was removed because
  it targeted the zero extension off the tube rather than the actual boundary
  value;
- `OSToWightmanBoundaryValueLimits.lean` explicitly states that the remaining
  theorem-3 input is the Wightman-side boundary-value identification
  `hHlimit` for `bvt_singleSplit_xiShiftHolomorphicValue`;
- inside
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  the live proof comment now gives the exact lower failure mode:
  the shell decomposition already closes on source support and on the positive-
  energy region outside source support, but fails for
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- source there also names the exact next honest theorem shape:
  a limit-level canonical-shell comparison showing the shell paired with
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`
  tends to `0`.

Precise blocker now:

- the first blocker is the outside-region contribution on the actual fixed-time
  canonical shell beneath `hlimit_os`;
- it is not another one-variable witness theorem, not a primitive FL boundary
  theorem, and not a same-shell shortcut.

Supervision correction (`2026-04-16`, audit of the post-relocation consumer-side seam probe):
this pass ran one honest direct probe in
`OSReconstruction/SCV/VladimirovTillmann.lean`
to test the exact VT supplier / forward consumer seam after moving
`forwardConeAbs_salient` upstream.

Exact audit result:

- once the temporary probe theorem was reduced to the real seam shape,
  `lake env lean OSReconstruction/SCV/VladimirovTillmann.lean`
  still failed on
  `unknown identifier 'forwardConeAbs_salient'`;
- source grep simultaneously shows
  `theorem forwardConeAbs_salient`
  in
  `OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:488`.

Exact reason:

- the old blocker "the theorem only lives downstream in
  `WickRotation/ForwardTubeLorentz.lean`" is no longer the right diagnosis;
- the new exact blocker is that the theorem is not resolving by name from the
  live consumer-side file despite now appearing in the upstream source file.

Verification status:

- exact probe run this pass:
  `lake env lean OSReconstruction/SCV/VladimirovTillmann.lean`;
- exit code: `1`;
- decisive remaining error after local probe cleanup:
  `unknown identifier 'forwardConeAbs_salient'`;
- the temporary probe theorem was reverted after the check.

Supervision correction (`2026-04-16`, 20:32 UTC, source-first clarification of the live forward-tube seam):
this pass confirmed that the direct VT -> forward-consumer theorem already exists in source as
`distributional_uniqueness_forwardTube_of_trace_from_vt_bvZero`
in
`OSReconstruction/SCV/VladimirovTillmann.lean`.
So the corrected seam is not blocked by a missing consumer theorem.
The exact remaining theorem-sized blocker is instead structural: the attempted re-derivation failed on
`unknown identifier 'forwardConeAbs_salient'`, because that salientness theorem currently lives in
`WickRotation/ForwardTubeLorentz.lean`
rather than a non-cyclic upstream import layer.
Hence the next honest bounded Lean step is to make
`IsSalientCone (ForwardConeAbs d n)`
available upstream enough for the VT supplier / forward-consumer seam, not to duplicate another wrapper theorem.

Supervision correction (`2026-04-16`, audit of the live forward-tube consumer seam after rereading the actual consumer and supplier files):
this pass re-audited
`distributional_uniqueness_forwardTube_of_flatTempered_of_trace`
in
`ForwardTubeDistributions.lean`
against
`vladimirov_tillmann_hasFourierLaplaceReprTempered`
and
`distributional_uniqueness_forwardTube_of_trace_from_vt_bvZero`
in
`SCV.VladimirovTillmann`.

Exact audit result:

- the direct forward-tube consumer theorem on the corrected seam is already
  present in source;
- it is
  `distributional_uniqueness_forwardTube_of_trace_from_vt_bvZero`;
- so the seam is not blocked by a missing theorem-sized VT-to-consumer proof.

Exact reason:

- `distributional_uniqueness_forwardTube_of_flatTempered_of_trace` is still the
  first real flattened-side consumer;
- `distributional_uniqueness_forwardTube_of_trace_from_vt_bvZero` already
  constructs the exact
  `SCV.HasFourierLaplaceReprTempered (ForwardConeFlat d n) ...`
  package from genuine forward-tube hypotheses and discharges that consumer;
- this theorem lives in `SCV.VladimirovTillmann.lean` because that file imports
  `ForwardTubeDistributions.lean`, so the consumer file cannot import back into
  VT without a cycle.

Precise first blocker now:

- none at the theorem-sized mathematical level on this seam;
- the first real blocker is module layering:
  a shared seam file / import refactor is needed before that already-proved
  theorem can be relocated nearer `ForwardTubeDistributions.lean`.

Supervision correction (`2026-04-16`, audit of the direct VT-to-forward-consumer attempt):
this pass re-audited the exact live seam
`SCV.vladimirov_tillmann_hasFourierLaplaceReprTempered`
→
`distributional_uniqueness_forwardTube_of_flatTempered_of_trace`.

Exact audit result:

- the first missing forward-tube ingredient is no longer on the old theorem-1
  route;
- the direct consumer theorem itself is blocked by one geometric prerequisite
  not available in the current non-cyclic import layer:
  `IsSalientCone (ForwardConeAbs d n)`.

Exact reason:

- `SCV.vladimirov_tillmann_hasFourierLaplaceReprTempered` requires
  `hC_salient`;
- for the forward tube, the needed witness is exactly
  `forwardConeAbs_salient d n`;
- that theorem exists only in
  `WickRotation/ForwardTubeLorentz.lean`;
- but that file imports `SCV/VladimirovTillmann.lean`, so reusing it from the
  live seam would create a cycle.

Why this is first:

- the attempted theorem already reduced the other issues to routine transport:
  converting `ForwardTube d n` to `TubeDomainSetPi (ForwardConeAbs d n)` and
  normalizing the ray-trace notation are not theorem-sized blockages;
- once salientness is available upstream, the VT supplier can be applied and
  the existing consumer
  `distributional_uniqueness_forwardTube_of_flatTempered_of_trace_from_bvZero`
  can be called directly;
- hence the exact first forward-tube blocker is theorem availability for the
  salient forward cone, not a new analytic estimate.

Verification status:

- exact probe run:
  `lake env lean OSReconstruction/SCV/VladimirovTillmann.lean`;
- decisive failure from that probe:
  `unknown identifier 'forwardConeAbs_salient'`;
- the attempted theorem was reverted after the check.

Supervision correction (`2026-04-16`, audit correction after rechecking theorem 1 against the actual VT upstream hypothesis):
this pass re-audited
`fourierLaplaceExtMultiDim_uniform_bound_near_boundary`
against
`HasFourierLaplaceReprTempered`,
`HasFourierLaplaceReprRegular`,
and the VT growth hypotheses in
`SCV.VladimirovTillmann`.

Exact audit result:

- theorem 1 is not honestly a theorem of bare support;
- the first correct stronger input is the VT/OS global tube-growth hypothesis,
  not a new support-adapted estimate from
  `HasFourierSupportInDualCone C T`;
- the exact theorem-sized supplier is now present as
  `SCV.uniform_bound_near_boundary_of_global_poly_growth`.

Exact reason:

- `LaplaceSchwartz.lean` already makes `uniform_bound` part of the
  tempered/regular package surface;
- the half-line example shows support alone cannot imply theorem 1;
- `VladimirovTillmann.lean` already assumes
  `∃ C_bd N, ∀ z ∈ TubeDomain C, ‖F z‖ ≤ C_bd * (1 + ‖z‖)^N`
  on the live OS route;
- that hypothesis directly implies the required ray estimate for
  `x + i ε η`, so the first honest supplier sits at the stronger VT growth
  layer, not at the support layer.

Precise seam change:

- the theorem-1-sized output needed by the OS route is the `uniform_bound`
  field for `HasFourierLaplaceReprTempered`;
- the honest way to obtain it is
  `global tube polynomial growth ⇒ uniform_bound`;
- only after that should source assemble or consume the tempered package.

Supervision correction (`2026-04-16`, audit correction after checking theorem 1 against a standard supported tempered counterexample):
this pass re-audited
`fourierLaplaceExtMultiDim_uniform_bound_near_boundary`
against the exact source hypotheses and the regular-package contract in
`SCV.LaplaceSchwartz`.

Exact audit result:

- the first missing ingredient is not merely a support-sensitive continuity
  theorem for bare dual-cone support;
- the current theorem-1 hypothesis surface itself is too weak;
- therefore no honest quantitative theorem extracted only from
  `HasFourierSupportInDualCone C T`
  can prove theorem 1 as currently stated.

Exact reason:

- theorem 1 demands an `ε`-uniform estimate along `x + i ε η`;
- in one dimension with `C = Ioi (0 : ℝ)`, the tempered distribution
  `T = 1_[0,∞)` is supported in `DualConeFlat C`;
- its Fourier-Laplace transform is
  `F(x + i ε) = ∫_0^∞ exp(i x ξ - ε ξ) dξ = 1 / (ε - i x)`,
  which satisfies `|F(i ε)| = ε⁻¹`;
- hence the target bound fails under bare support alone.

Precise first blocker statement:

- before theorem 1 can progress, source needs a theorem-sized statement that
  strengthens the input beyond `HasFourierSupportInDualCone C T`;
- the smallest honest statement is:
  identify the extra OS/Vladimirov regularity hypothesis on a dual-cone-
  supported tempered functional that is sufficient to produce a
  support-adapted continuity estimate for the boundary-ray kernels;
- without that stronger hypothesis, the sought support-adapted estimate is not
  merely absent from source but false in general.

Why this is first:

- the false seminorm supplier route is already ruled out in source;
- the existing `HasFourierSupportInDualCone` interface is only qualitative;
- the new audit point is sharper:
  even a hypothetical quantitative sharpening from bare support alone would
  contradict the half-line example, so the first honest repair is at the
  theorem-hypothesis layer, not inside the existing continuity proof shell.

Verification status:

- exact probe launched this pass:
  `lake env lean OSReconstruction/SCV/PaleyWienerSchwartz.lean`;
- this audit does not claim compile-clean output from that still-running probe.

Supervision correction (`2026-04-16`, audit correction after rereading the support interface used by theorem 1):
this pass re-audited the live caller
`fourierLaplaceExtMultiDim_uniform_bound_near_boundary`
against the exact source interfaces
`schwartz_clm_bound_by_finset_sup`
and
`HasFourierSupportInDualCone`.

Exact audit result:

- the first missing ingredient is **not** currently an estimate internal to
  `psiZRaw_iteratedFDeriv_decay` / `psiZRSchwartz`;
- the first missing ingredient is a new support-sensitive continuity theorem
  for `T`.

Exact reason:

- the live caller still reduces immediately via
  `schwartz_clm_bound_by_finset_sup T` to a finite family of full Schwartz
  seminorms;
- `HasFourierSupportInDualCone` only supplies qualitative support information
  (`vanish off C*`, `eqOn C*`), with no quantitative estimate on `|T f|`;
- therefore no amount of additional radius-explicit kernel bookkeeping can
  close theorem 1 from current source unless a sharper continuity interface for
  support-constrained `T` is added first.

Precise first blocker statement:

- source needs a theorem of the schematic form:
  if `HasFourierSupportInDualCone C T`, then `T` admits a bound on tests `f`
  by a finite family of seminorms / kernel norms that depend only on behavior
  relevant to `DualConeFlat C` and are compatible with the boundary-ray family
  `multiDimPsiZR ... (ε⁻¹) ... (x + i ε η)`;
- current source contains no such theorem in
  `FourierSupportCone.lean`,
  `ConeCutoffSchwartz.lean`,
  or
  `PaleyWienerSchwartz.lean`.

Why this is first:

- `multiDimPsiZR_eval_eq_of_support` already gives the radius-change transport;
- `cexp_bound_on_support` already gives the needed support-region exponential
  decay input;
- the proof blocks before any further kernel estimate can be consumed, because
  the only available continuity theorem for `T` is the support-blind full
  Schwartz finite-sup bound.

Verification status:

- exact probe rerun this pass:
  `lake env lean OSReconstruction/SCV/PaleyWienerSchwartz.lean`;
- output still reports `declaration uses sorry` at line `3440`.

Supervision correction (`2026-04-16`, audit correction: the candidate boundary-ray seminorm supplier is false):
this pass re-audited the source route through
`schwartz_clm_bound_by_finset_sup`
and the radius-changed kernel
`multiDimPsiZR ... (ε⁻¹) ... (x + i ε η)`.

Exact audit correction:

- the previously documented first missing theorem-sized ingredient
  “uniform seminorm control of `multiDimPsiZR` on the ray”
  is not a valid target theorem;
- for general `(k,n)`, that seminorm statement is false already at `n = 0`.

Exact reason:

- on the dual cone the cutoff disappears, so along any nonzero
  `ξ₀ ∈ DualConeFlat C` with `η·ξ₀ > 0`,
  the kernel equals
  `exp(i x·(t ξ₀) - ε t (η·ξ₀))`;
- hence the weighted `k`-seminorm contribution contains
  `t^k exp(-ε a t)` with `a > 0`;
- its supremum is of order `ε⁻k`, so no `ε`-uniform bound can hold for
  `SchwartzMap.seminorm ℝ k 0`.

Correct first blocker:

- the current route
  `|T ψ| ≤ C * sup finite_family_of_schwartz_seminorms(ψ)`
  is too coarse for boundary rays;
- the first honest missing ingredient is therefore a **support-adapted
  continuity/estimate for Fourier-supported functionals**, replacing the raw
  use of `schwartz_clm_bound_by_finset_sup` at this seam;
- no smaller scaled-cutoff derivative lemma can fix the current route, because
  the candidate supplier already fails before derivative bookkeeping enters.

Supervision correction (`2026-04-16`, sharpened audit of the first missing kernel supplier for the SCV boundary-ray theorem):
this pass reread the actual general-radius kernel estimates in
`OSReconstruction/SCV/ConeCutoffSchwartz.lean`
and the consumer seam in
`OSReconstruction/SCV/PaleyWienerSchwartz.lean:3440`.

Exact audit refinement:

- the first failing step is still on the kernel side;
- but it is more precise than “missing exponential/coercivity bound”:
  `cexp_bound_on_support` already exposes the radius parameter `R`;
- for the boundary ray
  `z = x + i ε η` with fixed `η ∈ C`, one may take coercivity
  `∑ η·ξ ≥ cη ‖ξ‖`, hence for `εη` use `c = ε * cη`;
- with the intended radius choice `R = ε⁻¹`, the exponential prefactor in
  `cexp_bound_on_support`
  `exp(((c + K * ‖Im z‖) * R))`
  becomes
  `exp(cη + K * ‖η‖)`,
  so the small-`ε` blowup is not located there.

Exact first missing theorem-sized ingredient:

- the missing ingredient is the next package up:
  a **general-radius raywise seminorm estimate** for `multiDimPsiZR`
  (or equivalently a radius-explicit quantitative version of
  `psiZRaw_iteratedFDeriv_decay`);
- what is not currently packaged in source is the full derivative bookkeeping
  for:
  1. the scaled cutoff derivatives `D^i[χ(·/R)] ~ R^{-i}`,
  2. the exponent derivatives `D^j[e^{iz·ξ}] ~ ‖L_z‖^j`,
  3. the specialization `R = ε⁻¹`, `Im z = εη`,
  in a form yielding an `ε`-uniform Schwartz seminorm bound.

Precise blocker statement:

- for fixed `η ∈ C` and each `(k,n)`, source needs a theorem asserting
  `∃ B N δ, 0 < δ ∧
    ∀ x ε, 0 < ε → ε < δ →
      SchwartzMap.seminorm ℝ k n
        (multiDimPsiZR ... (ε⁻¹) ... (fun i => (x i : ℂ) + (ε : ℂ) * (η i : ℂ) * I) ...)
      ≤ B * (1 + ‖x‖)^N`;
- current source has no theorem at that level in either
  `ConeCutoffSchwartz.lean` or `PaleyWienerSchwartz.lean`.

Why this is first:

- once this seminorm theorem exists, the current source already closes the
  caller:
  `multiDimPsiZR_eval_eq_of_support` transfers from radius `ε⁻¹` back to the
  primitive FL kernel, and `schwartz_clm_bound_by_finset_sup` converts the
  finite seminorm bounds into the desired pointwise estimate for
  `fourierLaplaceExtMultiDim`;
- there is therefore no earlier unresolved supplier than this radius-explicit
  seminorm theorem.

Supervision correction (`2026-04-16`, exact audit of the live SCV boundary-ray uniform bound seam):
this pass concerns
`SCV.fourierLaplaceExtMultiDim_uniform_bound_near_boundary`
in
`OSReconstruction/SCV/PaleyWienerSchwartz.lean:3440`.

Exact audit result:

- no honest compile-clean proof can be extracted from the current source stack;
- the first failing step is **not** a missing compact-slice argument;
- the first failing step is the absence of a quantitative kernel estimate for
  the general-radius family `multiDimPsiZR` that is uniform on the ray
  `x + i ε η` as `ε → 0+`.

Exact reason:

- `fourierLaplaceExtMultiDim_vladimirov_growth` and
  `fourierLaplaceExtMultiDim_poly_growth_on_compact` both route through the
  Vladimirov factor
  `(1 + infDist (Im z, Cᶜ)⁻¹)^M`;
- for `Im z = ε • η`, cone scaling forces
  `infDist (ε • η, Cᶜ) = ε * infDist (η, Cᶜ)`,
  so those theorems only give an `ε^{-M}`-type blowup, not the target uniform
  estimate;
- after extracting the proof structure, the remaining honest route is:
  1. use `schwartz_clm_bound_by_finset_sup` to reduce `|T ψ|` to finitely many
     Schwartz seminorms,
  2. use `multiDimPsiZR_eval_eq_of_support` to choose a radius `R = R(ε)`,
  3. apply an `ε`-uniform seminorm bound for
     `multiDimPsiZR ... R(ε) ... (x + i ε η)`,
  4. conclude the target pointwise bound.

First genuine missing supplier:

- a theorem proving exactly step `3` above;
- concretely: for fixed `η ∈ C` and each seminorm pair `(k,n)`, an
  `ε`-uniform bound
  `seminorm k n (multiDimPsiZR ... R(ε) ... (x + i ε η)) ≤ B * (1 + ‖x‖)^N`
  for all sufficiently small `ε > 0`,
  with an `ε`-dependent radius such as `R(ε) = ε⁻¹`;
- no current theorem in
  `PaleyWienerSchwartz.lean` or `ConeCutoffSchwartz.lean`
  packages this general-radius quantitative dependence.

Why this is first:

- all later steps are already present in source:
  support-based radius-independence and CLM finite-seminorm control are both
  available;
- without this kernel estimate, every proof attempt collapses back to the same
  non-uniform `infDist` factor.

Verification status:

- exact probe run on `2026-04-16`:
  `lake env lean OSReconstruction/SCV/PaleyWienerSchwartz.lean`;
- output still reports the live `sorry` warning at line `3440`.

Supervision correction (`2026-04-16`, source-first audit verification of the current SCV real-edge seam):
this pass concerns the now-removed surface
`SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`.

Exact audit result in current source:

- there is no production theorem with that name in
  `OSReconstruction/SCV/PaleyWienerSchwartz.lean`;
- `fourierLaplaceExtMultiDim` is still definitionally zero off the tube;
- the forward-tube caller at this seam now consumes an explicit trace-limit
  hypothesis instead of the stale zero-extension target.

Exact mathematical reason:

- at `realEmbed x`, the primitive function value of
  `fourierLaplaceExtMultiDim` is the `else 0` branch, so a pointwise
  `ContinuousWithinAt` theorem there would speak about the wrong object;
- the actual BV proof does not produce such a pointwise object;
- instead it proves convergence of the Fubini-rewritten Schwartz family
  `Φ ε` to the cutoff Fourier-side representative
  `hχf := χ · physicsFourierFlatCLM f`, and then uses dual-cone support to
  identify `T hχf` with `T (physicsFourierFlatCLM f)`.

Exact obstruction classification:

- this seam is no longer a missing proof;
- it is a repaired stale contract, so the correct classification is
  `4. wrong/stale theorem surface removed`;
- the first honest primitive interface remains
  `fourierLaplaceExtMultiDim_boundaryValue`, not a new pointwise continuity
  theorem for the zero extension.

Verification status:

- exact current probes succeeded on `2026-04-16`:
  `lake env lean OSReconstruction/SCV/PaleyWienerSchwartz.lean`
  and
  `lake env lean OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean`;
- the only remaining warning in the SCV file relevant to unfinished work is the
  pre-existing unrelated `sorry` at line `3440`.

Supervision correction (`2026-04-16`, audit of the primitive real-edge continuity theorem surface after source-first removal):
this pass concerns
`SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`.

Exact audit result:

- the theorem surface was removed from
  `OSReconstruction/SCV/PaleyWienerSchwartz.lean`;
- this is an honest contract repair, not a deferred proof;
- the primitive SCV file compiles after the removal.

Exact reason:

- `fourierLaplaceExtMultiDim` is definitionally `0` outside `TubeDomain C`, so
  at `SCV.realEmbed x` the stale target was the off-tube zero extension;
- the actual BV proof around `regularizedKernel_pointwise` and the damping step
  `hχf = χ · physicsFourierFlatCLM f` speaks only about the tempered boundary
  functional, not a primitive pointwise real-edge value of the zero extension;
- for generic tempered `T`, the fixed-cutoff oscillatory edge kernel is not a
  Schwartz test function, so no honest primitive pointwise boundary value is
  available on this surface.

Exact obstruction classification:

- this seam is `4. a wrong/stale theorem contract`;
- the first honest primitive outputs remain
  `fourierLaplaceExtMultiDim_boundaryValue`
  together with downstream theorems that take an explicit separate trace
  hypothesis when a pointwise trace object is available.

Verification status:

- exact probe succeeded on `2026-04-16`:
  `lake env lean OSReconstruction/SCV/PaleyWienerSchwartz.lean`;
- output reports only pre-existing warnings, including the existing live
  `sorry` warning at line `3440`.

Supervision correction (`2026-04-16`, exact contract audit and repair at the live forward-tube pointwise seam):
this pass concerns
`boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`.

Exact audit result:

- the theorem compiled cleanly after changing its boundary hypothesis;
- the old surface was mathematically wrong because it identified the chosen
  boundary value with
  `fourierLaplaceExtMultiDim ... (realEmbed (flatten x))`,
  while `fourierLaplaceExtMultiDim` is definitionally `0` off the tube;
- the corrected theorem now assumes the honest flattened-side boundary limit to
  `F(real x)` and proves forward-tube `ContinuousWithinAt` by transport.

Exact proof route now landed:

- transport `nhdsWithin` along `flattenCLEquiv`;
- compose the given flattened-side boundary trace limit with that transport;
- rewrite source values on `ForwardTube d n` using `hrepr_tube`.

Exact obstruction classification:

- the seam was `4. a wrong theorem contract at this surface`;
- it was not honestly blocked by a missing proof of
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`.

Verification status:

- exact probe succeeded on `2026-04-16`:
  `lake env lean OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean`;
- the file now reports only the pre-existing deprecation warning at line `177`.

Supervision correction (`2026-04-16`, source-first proof audit rerun on the live forward-tube pointwise seam):
this pass is docs-only and concerns
`boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`.

Exact audit result:

- no honest proof can be extracted for this theorem from the currently proved
  `LocalBoundaryRecovery` / `DistributionalUniqueness` interface;
- the first missing theorem is still upstream in
  `OSReconstruction/SCV/PaleyWienerSchwartz.lean`:
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`.

Exact reason:

- the forward-tube theorem already contains the exact interior representation
  `hrepr_tube` and the exact pointwise real-edge identification
  `hrepr_boundary`;
- after instantiating the flattened SCV function
  `fourierLaplaceExtMultiDim ... Tflat`,
  the remaining proof is only `ContinuousWithinAt` transport through
  `flattenCLEquiv` and rewriting;
- the currently available local SCV theorems only recover open-set boundary
  pairings or open-set equality once boundary continuity is already present,
  so they do not shrink this pointwise seam.

Exact obstruction classification:

- the blocker remains `FL-side boundary continuity`;
- not a missing shifted/forward-tube-side continuity statement,
  not an open-neighborhood boundary-agreement theorem,
  and not a wrong theorem contract at this forward-tube surface.

Verification status:

- exact probe succeeded on `2026-04-16`:
  `lake env lean OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean`;
- the file still reports the pre-existing deprecation warning at line `177`
  and exactly one live `sorry` warning at line `688`.

Supervision correction (`2026-04-16`, audit of the single remaining forward-tube production sorry):
this pass is docs-only and concerns
`boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`.

Exact audit result:

- no honest proof can be extracted for this theorem from the currently proved
  `LocalBoundaryRecovery` / `DistributionalUniqueness` interface;
- the first missing theorem is upstream in
  `OSReconstruction/SCV/PaleyWienerSchwartz.lean`:
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`.

Exact reason:

- the forward-tube theorem already has the full flattened FL witness,
  the exact interior representation `hrepr_tube`, and the exact pointwise
  boundary identification `hrepr_boundary`;
- if the FL extension were known continuous within the flat tube at the real
  boundary point, the remaining proof would be routine transport through
  `flattenCLEquiv` / `flattenCLEquivReal`;
- the current SCV theorems downstream of that supplier only recover boundary
  pairings or open-set equality once boundary continuity is already assumed on
  an open set, so they do not shrink this pointwise continuity seam.

Exact obstruction classification:

- the blocker is `FL-side boundary continuity`;
- the seam is not blocked by a missing forward-tube-side continuity theorem,
  not by open-neighborhood boundary agreement, and not by a wrong theorem
  contract at this surface.

Verification status:

- exact probe succeeded:
  `lake env lean OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean`;
- the file still reports exactly the pre-existing `sorry` warning at line `688`.

Supervision correction (`2026-04-16`, old regular public BV-zero caller switched to the trace-based uniqueness route):
this pass records one compile-clean in-place reroute and closes the previously isolated transport seam.

Exact landed theorem:

- `distributional_uniqueness_forwardTube_of_flatRegular_from_bvZero` in
  `OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean`;
- it still accepts the old regular flattened package and the same zero
  product-coordinate BV hypothesis;
- internally it now:
  builds the tempered package on the same flattened difference by field reuse,
  defines the separate boundary trace `B x := (F₁ - F₂) (real x)`,
  transports `hRegular_G.tube_continuousWithinAt` to the exact
  `htrace_boundary` surface,
  derives `htrace_ray` by composing with the forward-ray path,
  proves `hTempered_G.dist f = 0` by definitional reuse plus the existing
  `hRegular_G.dist = hRepr.dist` comparison,
  and finally calls
  `distributional_uniqueness_forwardTube_of_flatTempered_of_trace`.

Why this is honest:

- no regular-package redesign was used;
- no wrapper theorem was introduced;
- the only new proof work is the actual caller-side transport from the regular
  package's continuity field to the separate-trace hypotheses required by the
  trace theorem.

Verification status:

- `lake env lean OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean`
  succeeded after the reroute;
- only the already-known unrelated `sorry` warning at line `688` remains.

Supervision correction (`2026-04-16`, public forward-tube trace-based BV-zero caller landed):
this pass records one compile-clean forward-tube theorem and the exact remaining obstruction.

Exact landed theorem:

- `distributional_uniqueness_forwardTube_of_flatTempered_of_trace_from_bvZero` in
  `OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:1263`;
- it consumes:
  flattened tempered FL data for the forward-tube difference,
  a separate continuous boundary trace `B`,
  raywise convergence to `B`,
  full forward-tube boundary `nhdsWithin` convergence to `B`,
  and zero product-coordinate BV data `h_agree`;
- it concludes `F₁ z = F₂ z` for every `z ∈ ForwardTube d n`.

Why this is honest:

- the theorem does not rebuild a fake regular package;
- it derives `hTempered_G.dist = 0` by comparing the given tempered flattened package to the explicit zero BV witness built from `h_agree`;
- it then calls the already-landed trace-based forward-tube adapter
  `distributional_uniqueness_forwardTube_of_flatTempered_of_trace`.

Exact remaining obstruction:

- the trace route still cannot replace
  `distributional_uniqueness_forwardTube_of_flatRegular_from_bvZero`
  automatically;
- the remaining missing theorem is exactly a full forward-tube boundary trace supplier for a separate real-edge object `B`,
  not another regularity theorem and not a same-object boundary identification;
- implementation note:
  for the still-regular caller this `B` should come from the regular package's
  actual boundary trace, while the missing proof work is to expose the raywise
  and full `nhdsWithin` limits in the theorem surface required by the new
  trace-based uniqueness theorem.

Supervision correction (`2026-04-16`, first downstream trace-based uniqueness consumer landed):
this pass records one compile-clean consumer theorem and the exact remaining caller obstruction.

Exact landed theorem:

- `distributional_uniqueness_tube_of_tempered_of_trace` in
  `OSReconstruction/SCV/DistributionalUniqueness.lean:1954`;
- it consumes:
  tempered FL data for `F`,
  a separate continuous boundary trace `B`,
  raywise convergence to `B`,
  full `nhdsWithin` boundary convergence to `B`,
  and vanishing of the tempered boundary functional;
- it concludes `F z = 0` for every `z ∈ TubeDomain C`.

Why this is honest:

- the new theorem uses
  `boundary_trace_zero_of_tempered_of_trace` only for the real trace object `B`;
- it does not identify `B` with `F (realEmbed x)`;
- the global vanishing step is then delegated to the already existing
  trace-based uniqueness theorem `uniqueness_of_boundary_trace_zero`, which
  needs genuine full boundary convergence and therefore does not smuggle the old
  regular-package boundary fields back in.

Exact remaining obstruction:

- the first forward-tube callers still expose regular-package hypotheses, not a
  separate flattened trace with full `nhdsWithin` convergence;
- in particular,
  `distributional_uniqueness_forwardTube_of_flatRegular` at
  `ForwardTubeDistributions.lean:799` still calls
  `distributional_uniqueness_tube_of_regular`,
  and `distributional_uniqueness_forwardTube_of_flatRegular_from_bvZero` at
  `ForwardTubeDistributions.lean:1158` still derives zero by collapsing product
  BV data into that regular package.

Exact next bounded frontier:

- the next honest caller-side theorem must provide, on the flattened forward
  tube, a separate boundary-trace object together with full boundary
  `nhdsWithin` convergence to that trace;
- only then can the new trace-based uniqueness theorem replace the old regular
  uniqueness call.

Supervision correction (`2026-04-16`, audit after the first trace-zero consumer bridge landing):
this pass records one compile-clean theorem-sized Lean landing and the exact new frontier.

Exact landed theorem:

- `boundary_trace_zero_of_tempered_of_trace` in
  `OSReconstruction/SCV/TubeDistributions.lean`;
- hypotheses: `HasFourierLaplaceReprTempered C F`, continuous boundary trace
  `G`, one-direction trace convergence `F(x + i \varepsilon \eta) \to G(x)`, and
  zero boundary functional `hTempered.dist f = 0` for all Schwartz tests `f`;
- conclusion: `G x = 0` pointwise.

Why this is mathematically honest:

- it consumes the separate real-edge trace object `G` rather than the wrong
  zero-extension boundary value `F (realEmbed x)`;
- it uses the local trace recovery theorem only on compactly supported Schwartz
  tests and then closes the pointwise vanishing step through the existing
  compact-support distributional uniqueness machinery;
- local compile check succeeded on `OSReconstruction/SCV/TubeDistributions.lean`.

Exact new obstruction status:

- the regular-package interface `HasFourierLaplaceReprRegular` is still not an
  honest package for the primitive zero-extended witness;
- but the route has now crossed the first downstream consumer seam by proving a
  trace-zero theorem for the separate boundary object;
- the next bounded question is the first exact downstream theorem/caller that
  should consume this new trace-zero bridge.

Exact next bounded frontier:

- stay downstream of `boundary_trace_zero_of_tempered_of_trace`;
- prefer one compile-clean consumer theorem near the first regular/forward-tube
  caller surface;
- if that does not land honestly, isolate one exact theorem-level contract gap
  at that next caller seam and sync docs only.

Supervision correction (`2026-04-16`, audit of the first downstream boundary-interface consumer above the primitive FL seam):
this pass is docs-only.

Exact audit result:

- no honest compile-clean theorem statement/skeleton/helper lands at this seam yet;
- the first downstream surface still asking for the wrong boundary object is the regular package interface
  `HasFourierLaplaceReprRegular` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:118](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:118), not `LocalBoundaryRecovery`.

Exact source split:

- `HasFourierLaplaceReprRegular` requires the same function `F` to satisfy
  `boundary_continuous` and `tube_continuousWithinAt` on `realEmbed x`;
- for the primitive witness `F := fourierLaplaceExtMultiDim ... T`, the exact boundary value at `realEmbed x` is still the outside-tube zero-extension value `0`;
- the live interior-ray source object remains the nonzero cutoff oscillatory boundary trace coming from the kernel in
  `regularizedKernel_pointwise`;
- `HasFourierLaplaceReprTempered` and the local theorem
  `fourierLaplace_boundary_recovery_on_open_of_tempered` already avoid this mismatch by keeping pointwise boundary continuity as an explicit local hypothesis `hcontU`, separate from the tempered package.

Exact first consumers:

- the first regular consumers really using the wrong-object global package are
  `boundary_value_zero_of_regular` and
  `distributional_uniqueness_tube_of_regular` in
  [OSReconstruction/SCV/TubeDistributions.lean:78](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/TubeDistributions.lean:78)
  and
  [95](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/TubeDistributions.lean:95),
  via `hRegular.boundary_continuous` / `hRegular.tube_continuousWithinAt`.

Exact contract verdict:

- no theorem on the zero-extended primitive witness can honestly fill those regular fields now;
- the minimal honest replacement interface is a separate boundary-trace representative `G` together with interior agreement on `TubeDomain C`, with any regular package attached to `G`, not to the zero extension itself;
- until that interface is designed, the honest live route remains the local-tempered interface (`HasFourierLaplaceReprTempered` + explicit local `hcontU`) rather than a global regular package theorem.

Supervision correction (`2026-04-16`, audit of the exact pointwise ray-limit seam under theorem 1):
this pass is docs-only.

Exact audit result:

- no honest compile-clean helper/local lemma lands beneath
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`;
- the first blocking issue is now a theorem-contract mismatch at the exact
  extension definition, not a missing `Tendsto.comp` wrapper.

Exact source object already present:

- `multiDimPsiZExt` is defined at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:279](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:279)
  by `if hz : z ∈ TubeDomain C then multiDimPsiZ ... z hz else 0`;
- `fourierLaplaceExtMultiDim` is defined at
  [3052](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3052)
  by the same outside-tube-zero pattern;
- for `ε > 0`, `realPlusIEpsEta_mem_tubeDomain` at
  [3616](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3616)
  shows `x + i ε η ∈ TubeDomain C`, so the ray uses the interior kernel;
- the interior kernel is exactly the damping family already exposed in
  `regularizedKernel_pointwise` at
  [3737](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3737)
  through
  [3827](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3827):
  `χ(ξ) * exp(-(ε : ℂ) * ⟪η, ξ⟫) * exp(I * ⟪x, ξ⟫)`.

Exact first internal obligation:

- compare the `ε → 0+` limit of that interior kernel with the target value
  appearing in
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`;
- source computation gives limit kernel
  `χ(ξ) * exp(I * ⟪x, ξ⟫)`;
- the target value from the current exact extension at `realEmbed x` is the
  outside-tube branch, namely `0`.

Why this kills the hoped-for helper:

- a helper proving the current scalar ray limit would be proving convergence to
  the wrong target;
- a helper proving convergence to the cutoff boundary kernel would be honest
  mathematics, but it would no longer be a proof fragment for the current
  theorem surface;
- therefore the present docs were insufficient because they still treated the
  seam as a missing topology/composition lemma rather than a mismatch between
  the exact extension object and the boundary trace object.

Exact next theorem-contract gap:

- the route must first decide whether the boundary-continuity seam is about the
  exact outside-tube extension `fourierLaplaceExtMultiDim ... (realEmbed x)`,
  or about a separate boundary trace induced by the cutoff oscillatory kernel;
- until that target object is fixed, no honest theorem-sized Lean insertion
  exists below theorem 1 on this seam.

Supervision correction (`2026-04-16`, theorem-1 proof-body audit tightened to the first internal specialization step):
this pass is docs-only.

Exact audit result:

- no single honest compile-clean helper/local lemma inside
  `fourierLaplaceExtMultiDim_uniform_bound_near_boundary` lands now without
  becoming a wrapper around the first line of its proof;
- theorem 1 itself remains the first honest missing analytic package at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3444](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3444)
  through
  [3455](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3455).

Exact first internal proof obligation:

- obtain
  `⟨C_vlad, N_vlad, M_vlad, hC_vlad_pos, hgrowth⟩` from
  `fourierLaplaceExtMultiDim_vladimirov_growth`;
- for fixed `η ∈ C`, `x`, and `ε > 0`, apply `hgrowth` to
  `z_ε(x) = x + i ε η`, with tube membership supplied by
  `realPlusIEpsEta_mem_tubeDomain`;
- rewrite the resulting estimate to
  `‖F(z_ε(x))‖ ≤
    C_vlad * (1 + ‖z_ε(x)‖)^N_vlad *
      (1 + (Metric.infDist (ε • η) Cᶜ)⁻¹)^M_vlad`;
- bound the `‖z_ε(x)‖` term by a polynomial in `‖x‖`; the remaining coefficient
  still depends on `ε` through the inverse-distance term.

Why this does not yield an honest smaller lemma:

- the extracted statement would only prove a fixed-`ε` polynomial-growth bound,
  exactly the same phenomenon already used in
  `tubeSlice_temperedDistribution` at
  [OSReconstruction/SCV/TubeBoundaryValueExistence.lean:220](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/TubeBoundaryValueExistence.lean:220)
  through
  [304](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/TubeBoundaryValueExistence.lean:304);
- that is not theorem 1’s missing content, because the package contract asks for
  one bound uniform in `ε` on a punctured interval;
- using the already formalized cone fact
  `infDist_smul_cone`, one gets
  `Metric.infDist (ε • η) Cᶜ = ε * Metric.infDist η Cᶜ`, so the specialized
  Vladimirov coefficient behaves like `ε^{-M_vlad}`, not like a uniform
  constant.

Exact source/paper insufficiency:

- OS II pages `302`-`303`, equations `(6.21)`, `(6.28)`, `(6.30)`, `(6.31)`,
  still work on the renormalized family `S_{k,\varepsilon}`;
- the live Lean primitive seam is still phrased directly for
  `fourierLaplaceExtMultiDim ... T`;
- no already formalized renormalized-family contract exists at this seam that
  could be inserted as a smaller internal Lean theorem below theorem 1.

Supervision correction (`2026-04-16`, theorem-1 first-blocking-obligation audit on the primitive FL seam):
this pass is docs-only.

Exact audit result:

- no strictly smaller honest theorem/local lemma lands beneath theorem 1 on the
  currently formalized primitive FL route;
- the first missing obligation is still the proof of
  `fourierLaplaceExtMultiDim_uniform_bound_near_boundary` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3444](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3444)
  through
  [3455](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3455).

Exact first blocking obligation:

- from
  `fourierLaplaceExtMultiDim_vladimirov_growth` at
  [3304](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3304),
  the ray substitution `z = x + i ε η` leaves the factor
  `(1 + (Metric.infDist (ε • η) Cᶜ)⁻¹)^M`;
- removing that factor uniformly for all small `ε > 0` is exactly the missing
  theorem-1 content;
- any local cone-geometry lemma naturally suggested by the already proved seam
  only yields the scaling behavior of `Metric.infDist (ε • η) Cᶜ`, hence an
  `ε^{-M}` blowup, not the required `ε`-uniform polynomial bound.

Why the focused nearby theorems do not shrink it:

- `fourierLaplaceExtMultiDim_poly_growth_on_compact` at
  [3342](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3342)
  needs a compact `K ⊆ C`; the small ray segment approaching `0` does not stay
  in any fixed compact subset of `C`;
- `realPlusIEpsEta_mem_tubeDomain` at
  [3616](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3616)
  and
  `realPlusIEpsEta_tendsto_nhdsWithin_realEmbed` at
  [3631](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3631)
  control only path membership and approach, not growth;
- the boundary-value theorem at
  [4409](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4409)
  is distributional only, and its own comment still records possible blowup as
  `ε → 0+`.

Exact OS II citation verdict:

- PDF page `22` / printed page `302`: `(6.21)` and `(6.28)`;
- PDF page `23` / printed page `303`: maximum principle, `(6.30)`, `(6.31)`;
- these remain source-backed only for the renormalized `S_{k,\varepsilon}`
  family, so they do not become an honest smaller primitive Lean theorem at the
  present `fourierLaplaceExtMultiDim ... T` seam.

Exact insertion seam:

- keep the next honest work at the existing theorem-1 statement in
  `PaleyWienerSchwartz.lean`;
- no lower local lemma or higher package theorem now passes the no-wrapper test.

Supervision correction (`2026-04-16`, local OS II dependency audit one level beneath theorem 1):
this pass is docs-only.

Exact theorem-1 statement in the live `hunif` shape:

- for
  `F := fourierLaplaceExtMultiDim C hC_open hC_conv hC_cone hC_salient T`,
  the required theorem-1 surface is
  `∀ η ∈ C, ∃ C_bd N δ, C_bd > 0 ∧ δ > 0 ∧
      ∀ x ε, 0 < ε → ε < δ →
        ‖F (fun i => ↑(x i) + ↑ε * ↑(η i) * I)‖ ≤ C_bd * (1 + ‖x‖) ^ N`;
- this is exactly the shared `uniform_bound` field at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:126](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:126)
  through
  [129](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:129)
  and
  [151](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:151)
  through
  [154](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:154),
  and the explicit `hunif` input at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:921](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:921)
  through
  [924](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:924).

Exact local OS II citation/pages inspected:

- PDF page `22` / printed page `302`: `(6.21)`, `(6.22)`, `(6.23)`, and the
  inductive target `(6.28)` for the renormalized family `S_{k,ε}`;
- PDF page `23` / printed page `303`: the maximum-principle upgrade of
  `(6.28)` for `N = M + 1`, the pointwise membership choice `(6.30)`, and the
  final temperedness estimate `(6.31)`.

Exact first source-backed ingredient strictly beneath theorem 1, if any:

- source-visible in OS II only: the renormalized-family induction estimate
  `(6.28)` plus the pointwise `N = N(ζ)` selection step `(6.30)`;
- source-backed live-route verdict: this is **not** a genuinely smaller theorem
  at the current primitive Lean seam, because the live source in
  `PaleyWienerSchwartz.lean` is phrased directly for
  `fourierLaplaceExtMultiDim ... T`, while `(6.21)` and `(6.28)` belong to a
  different OS II family `S_{k,ε}` with extra renormalization data that is not
  yet formalized at that seam.

Exact Lean-seam test verdict:

- inspected first:
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241)
  through
  [3251](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3251),
  then
  [3304](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3304)
  through
  [3352](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3352),
  and the ray/path seam at
  [3612](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3612)
  through
  [3646](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3612);
- no honest compile-clean non-wrapper theorem-sized Lean landing follows from
  the OS II substructure there without introducing new renormalized-family
  machinery outside the live primitive FL route;
- therefore theorem 1 itself remains the first honest missing analytic package,
  and this audit stays docs-only.

Supervision correction (`2026-04-16`, bounded theorem-1 audit after the tempered-supplier blocker):
this pass is docs-only.

Exact theorem-1 statement in the required package shape:

- the proposed theorem name remains
  `fourierLaplaceExtMultiDim_uniform_bound_near_boundary`;
- the exact statement it must prove is the live `hunif` field shape already
  required by
  [OSReconstruction/SCV/LaplaceSchwartz.lean:921](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:921)
  through
  [924](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:924):
  for
  `F := fourierLaplaceExtMultiDim C hC_open hC_conv hC_cone hC_salient T`,
  `∀ η ∈ C, ∃ C_bd N δ, C_bd > 0 ∧ δ > 0 ∧
      ∀ x ε, 0 < ε → ε < δ →
        ‖F (fun i => ↑(x i) + ↑ε * ↑(η i) * I)‖ ≤ C_bd * (1 + ‖x‖) ^ N`;
- this is also the exact field shape of
  `HasFourierLaplaceReprTempered.uniform_bound` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:151](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:151)
  through
  [154](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:154).

Exact FL-side inputs still honestly available:

- `fourierLaplaceExtMultiDim_boundaryValue` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:4288](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4288);
- `fourierLaplaceExtMultiDim_poly_growth_on_compact` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3342](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3342);
- `realPlusIEpsEta_tendsto_nhdsWithin_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3510](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3510);
- `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` at
  [3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241)
  is not yet available because it is still `sorry`;
- `fourierLaplaceExtMultiDim_vladimirov_growth` at
  [3304](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3304)
  remains the strongest primitive pointwise estimate, and after substitution to
  `x + i ε η` it still carries the inverse boundary-distance factor;
- the nearby comment at
  [4303](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4303)
  still records that `F(x+iεη)` may blow up as `ε → 0+`.

Exact first missing ingredient and verdict:

- the first missing ingredient is still theorem 1 itself:
  an honest primitive derivation of the `ε`-uniform ray bound above from the
  dual-cone support hypothesis;
- no compile-clean theorem-sized Lean step at the checked
  `PaleyWienerSchwartz.lean` seam survives the no-wrapper rule, because any
  new theorem now would only repackage already-available growth or path facts;
- therefore this audit remains docs-only.

Exact insertion seam:

- first seam tested:
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241)
  through
  [3251](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3251),
  together with the adjacent proved growth theorem at
  [3304](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3304);
- downstream supplier seam remains immediately after
  `exists_fourierLaplaceReprTempered` in
  [OSReconstruction/SCV/LaplaceSchwartz.lean:907](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:907),
  but it stays blocked exactly by theorem 1.

Supervision correction (`2026-04-16`, compact-slice `hpoly` checked live against the tempered supplier seam):
this pass is docs-only.

Exact audit update:

- the compact-slice input is no longer hypothetical:
  `fourierLaplaceExtMultiDim_poly_growth_on_compact` is already present at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3342](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3342);
- the exact remaining supplier theorem, at the insertion seam immediately after
  `exists_fourierLaplaceReprTempered` in
  [OSReconstruction/SCV/LaplaceSchwartz.lean:907](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:907),
  is:
  for primitive witness
  `F := fourierLaplaceExtMultiDim C hC_open hC_conv hC_cone hC_salient T`,
  build
  `HasFourierLaplaceReprTempered C F`
  with distribution
  `fun f => T (physicsFourierFlatCLM f)`;
- the required inputs are exactly:
  `hT_cont` from continuity of the composite functional,
  `fourierLaplaceExtMultiDim_boundaryValue` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:4390](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4390),
  the landed compact-slice `hpoly`,
  and theorem 1 `hunif` in the field shape required at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:921](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:921)
  through
  [924](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:924).

Exact audit verdict:

- source-first, the first primitive supplier theorem is still blocked exactly by
  theorem 1;
- without theorem 1, the only theorem-sized Lean step available is the
  conditional constructor-packaging theorem that merely instantiates
  `exists_fourierLaplaceReprTempered` with the four inputs above;
- because that step is wrapper-only, it is not an honest landing under the
  stated constraints;
- therefore this pass remains docs-only.

Supervision correction (`2026-04-16`, theorem-1 exact source/probe audit on the primitive FL seam):
this pass is docs-only.

Supervision correction (`2026-04-16`, theorem-1 downstream dependency audit tightened immediately after the exact `uniform_bound` seam):
this pass is docs-only.

Supervision correction (`2026-04-16`, theorem-1 statement-or-skeleton package audit):
this pass is docs-only.

Exact audit result:

- the exact theorem-1 skeleton is already live at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3444](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3444)
  through
  [3455](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3455)
  as
  `fourierLaplaceExtMultiDim_uniform_bound_near_boundary`;
- this statement is exactly the `hunif` field shape demanded by
  `exists_fourierLaplaceReprTempered` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:921](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:921)
  through
  [924](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:924),
  so the theorem-sized insertion surface is already occupied honestly.

Exact landing verdict:

- there is no new bounded Lean landing to add now;
- re-inserting theorem 1 would be a duplicate wrapper/no-op;
- no strictly smaller helper inside theorem 1 became available from the source
  equations `(6.28)` and `(6.30)`, because those still belong to the
  renormalized `S_{k,\u03b5}` family rather than the current primitive Lean seam
  for `fourierLaplaceExtMultiDim ... T`.

Exact blocker after the existing skeleton:

- the first missing analytic obligation is now the proof of
  `fourierLaplaceExtMultiDim_uniform_bound_near_boundary` itself;
- if that proof is not supplied honestly, even the next primitive supplier
  theorem yielding `HasFourierLaplaceReprTempered` remains blocked exactly at
  `hunif`, with no honest smaller theorem beneath it on the live route.

Exact consumers showing theorem 1 comes first:

- the honest package theorem
  “primitive FL source yields `HasFourierLaplaceReprTempered`”
  cannot precede theorem 1, because the existing constructor
  `exists_fourierLaplaceReprTempered` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:907](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:907)
  still requires theorem-1’s raywise `hunif` input at
  [921](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:921)
  through
  [924](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:924);
- once `HasFourierLaplaceReprTempered` exists, the first actual DCT consumer is
  `fourierLaplace_schwartz_integral_convergence_local` at
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:105](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:105),
  which immediately uses `hTempered.uniform_bound` at
  [120](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:120);
- `fourierLaplace_boundary_recovery_on_open_of_tempered` at
  [156](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:156)
  then depends on that local convergence step at
  [172](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:172);
- the global regular route consumes the same field in
  `fourierLaplace_schwartz_integral_convergence` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:305](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:305)
  via
  [316](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:316),
  and in
  `fourierLaplace_boundary_recovery` at
  [386](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:386)
  via
  [395](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:395).

Exact theorem-choice verdict:

- sharp next theorem after theorem 1 is option `B(3)`, not yet `B(1)` or `B(2)`;
- the smaller genuine intermediate theorem is the compact-slice `poly_growth`
  corollary from
  `fourierLaplaceExtMultiDim_vladimirov_growth` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3304](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3304);
- source justification:
  on compact `K ⊆ C`, the boundary-distance factor in Vladimirov growth admits
  a uniform bound, so one obtains exactly the `poly_growth` field shape already
  required by
  `HasFourierLaplaceReprTempered` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:146](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:146)
  through
  [149](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:149).

Exact dependency chain now recorded:

- theorem 1:
  primitive near-boundary `uniform_bound`;
- smaller source-backed intermediate theorem:
  primitive compact-slice `poly_growth`;
- first honest primitive package theorem:
  produce `HasFourierLaplaceReprTempered` using
  `exists_fourierLaplaceReprTempered`;
- first honest boundary-continuity / regular theorem after that:
  a theorem yielding some boundary-valued representative `G` with
  `HasFourierLaplaceReprRegular C G`
  and tube-interior agreement with the primitive FL witness;
- exact future insertion seams:
  compact-slice theorem immediately after
  `fourierLaplaceExtMultiDim_vladimirov_growth` in
  `PaleyWienerSchwartz.lean`,
  tempered-package supplier immediately after
  `exists_fourierLaplaceReprTempered` in
  `LaplaceSchwartz.lean`,
  regular-representative theorem at the existing placeholder comment beginning
  [OSReconstruction/SCV/LaplaceSchwartz.lean:180](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:180).

Exact audit result:

- theorem 1 is already pinned in Lean by the two structure fields
  `HasFourierLaplaceReprRegular.uniform_bound` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:126](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:126)
  and
  `HasFourierLaplaceReprTempered.uniform_bound` at
  [151](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:151);
- the only existing theorem with that name-fragment,
  `fourierLaplace_uniform_bound_near_boundary` at
  [263](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:263),
  is merely the projection from `hRegular.uniform_bound`;
- the primitive FL layer currently proves only:
  holomorphicity,
  Vladimirov growth with inverse boundary-distance factor,
  the ray path theorem,
  and distributional boundary-value convergence;
- source comments at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:4303](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4303)
  explicitly warn that `F(x+iεη)` may blow up like `ε⁻ᴹ`;
- substituting `z = x + i ε η` into
  `fourierLaplaceExtMultiDim_vladimirov_growth`
  does not remove the factor
  `(1 + (Metric.infDist (ε • η) Cᶜ)⁻¹)^M`,
  so the proved primitive estimate still misses the theorem-1 field shape.

Reference audit, bounded to the two cited files:

- OS II, printed page `303`, equation `(6.31)`, remains consistent with a
  standard temperedness estimate carrying an inverse-power boundary factor in
  the real-part variables;
- along the fixed ray `x + i ε η`, that still behaves like an `ε^{-M}`-type
  factor;
- the local file `references/Zinoviev_1995.pdf` is an unrelated conifold paper
  and provides no relevant support for this seam.

Audit verdict:

- theorem 1 itself is the first honest missing analytic theorem on the current
  primitive source;
- there is no smaller source-backed primitive theorem already present that
  closes the same loop;
- there is no tiny non-wrapper Lean helper worth landing now;
- record explicitly:
  current source/reference gives distance-to-boundary growth and
  distributional boundary values, not theorem-1's `ε`-uniform bound in `x`.

Supervision correction (`2026-04-16`, exact audit of the candidate weaker FL package supplier beneath the primitive boundary-regularity seam):
this pass is docs-only.

Exact audit result:

- the existing extra-hypothesis interfaces are already present in source:
  `HasFourierLaplaceReprRegular` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:118](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:118)
  and
  `HasFourierLaplaceReprTempered` at
  [143](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:143);
- the transparent constructors are already present too:
  `exists_fourierLaplaceRepr` at
  [883](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:883)
  and
  `exists_fourierLaplaceReprTempered` at
  [907](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:907);
- so there is no honest new “interface theorem” to add here unless it proves
  real mathematics, not just repackaging;
- the candidate smaller theorem
  “`fourierLaplaceExtMultiDim ...` supplies `HasFourierLaplaceReprTempered`”
  fails source-first:
  the package field `uniform_bound` in
  [LaplaceSchwartz.lean:152](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:152)
  requires a bound uniform in small `ε > 0`,
  while the primitive FL comments at
  [PaleyWienerSchwartz.lean:4303](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4303)
  explicitly warn that `F(x+iεη)` may blow up like `ε⁻ᴹ`;
- therefore the weaker tempered package is not presently derivable from the
  proved primitive source, and the existing `HasFourierLaplaceReprRegular`
  interface remains exactly that: an extra-hypothesis package, not a theorem
  consequence.

Exact first smaller honest gap, after this audit:

- still not a new wrapper theorem;
- still not a primitive `HasFourierLaplaceReprTempered` supplier;
- yes: an upstream analytic theorem proving whichever near-boundary fields are
  actually true for the primitive FL witness from genuine dual-cone support,
  together with the correct real-boundary interpretation.

Audit landing:

- no Lean artifact;
- docs-only.

Supervision correction (`2026-04-16`, exact audit of the definition-level obstruction beneath `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`):
this pass is docs-only.

Exact audit result:

- `fourierLaplaceExtMultiDim` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3052](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3052)
  is defined by
  `if hz : z ∈ SCV.TubeDomain C then T (multiDimPsiZ ...) else 0`;
- `fourierLaplaceExtMultiDim_eq_ext` at
  [3070](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3070)
  rewrites this as
  `T (multiDimPsiZExt ... z)`;
- `multiDimPsiZExt` at
  [279](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:279)
  is itself the zero extension of the fixed-radius Schwartz kernel outside the
  tube.

Exact attempted proof route:

- try to bypass the missing theorem by proving continuity of
  `z ↦ multiDimPsiZExt ... z` at `SCV.realEmbed x`;
- compose with `T.continuous` to get continuity of
  `z ↦ fourierLaplaceExtMultiDim ... z`
  at `SCV.realEmbed x`;
- then restrict that ordinary continuity to
  `ContinuousWithinAt` on `SCV.TubeDomain C`.

Exact failed step:

- even if that smaller topology proof lands, it only proves convergence to
  the actual value of the zero extension at `SCV.realEmbed x`;
- this is not the boundary value needed by the SCV recovery theorems, which
  require convergence to the real trace representing the boundary
  distribution;
- the nearby source explicitly separates these notions:
  `fourierLaplaceExtMultiDim_boundaryValue` is distributional-only, and
  `HasFourierLaplaceReprRegular` in
  [OSReconstruction/SCV/LaplaceSchwartz.lean:118](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:118)
  records boundary continuity as extra data rather than deriving it from the
  weak FL package.

Exact first smaller honest gap:

- not a generic topology lemma;
- not merely the scalar ray-limit theorem for the same zero extension;
- yes: a missing **regular boundary extension theorem or extra hypothesis**
  upgrading genuine FL/dual-cone support data to an object with:
  1. the correct real boundary trace,
  2. `ContinuousWithinAt` up to that trace,
  3. compatibility with the distributional boundary-value theorem;
- this is the theorem-sized gap that would honestly justify a future
  `HasFourierLaplaceReprRegular` supplier from
  `fourierLaplaceExtMultiDim_boundaryValue`-type input.

Audit landing:

- no Lean artifact;
- docs-only.

Supervision correction (`2026-04-16`, exact audit of the primitive FL scalar boundary-ray limit seam):
this pass is docs-only.

Exact audit result:

- the direct scalar route is
  `realPlusIEpsEta_tendsto_nhdsWithin_realEmbed`
  plus composition with
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`;
- source still blocks that route at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241),
  because `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` remains
  `sorry`;
- the boundary-value theorem at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:4288](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4288)
  still proves only distributional convergence of pairings and explicitly
  states that the boundary value is not pointwise in general;
- the only nearby scalar convergence theorems at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:305](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:305)
  and
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:105](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:105)
  are circular for this seam, because they already assume
  `tube_continuousWithinAt` / local `hcontU`.

Exact failed proof attempt:

- fix `x` and `η ∈ C`;
- obtain the canonical path limit
  `ε ↦ x + i ε η → realEmbed x`
  within the tube;
- attempt
  `Filter.Tendsto.comp
      (SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed ...)
      (SCV.realPlusIEpsEta_tendsto_nhdsWithin_realEmbed ...)`;
- failure:
  the first theorem in that composition is not yet proved, and no nearby
  source theorem supplies the same scalar conclusion honestly.

Exact smallest honest missing analytic lemma:

- prove
  `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`
  for the exact FL extension from dual-cone support;
- equivalently, prove the fixed-`x` scalar raywise limit theorem for
  `fourierLaplaceExtMultiDim ... (x + i ε η)`,
  but only if that proof genuinely lands the missing continuity theorem rather
  than restating the same target under a new name;
- file placement:
  `PaleyWienerSchwartz.lean`;
  the shifted-side seam should treat this as an upstream analytic blocker, not
  duplicate it locally.

Audit landing:

- no Lean edit;
- docs-only.

Supervision correction (`2026-04-16`, exact Mathlib/repo audit of the local-open boundary-trace continuity bridge):
this pass is docs-only.

Exact audit result:

- source still provides only the ambient boundary package
  `∀ x ∈ U, ContinuousWithinAt F (TubeDomain C) (realEmbed x)`;
- the uniqueness consumer still requires `ContinuousOn` of the real traces on
  `U`;
- the generic Mathlib lemmas actually available are:
  `IsOpen.continuousOn_iff` at
  [Mathlib/Topology/ContinuousOn.lean:333](/home/claw/.openclaw/workspace/OSreconstruction/.lake/packages/mathlib/Mathlib/Topology/ContinuousOn.lean:333),
  plus
  `Topology.IsEmbedding.continuousOn_iff` and
  `Topology.IsInducing.continuousOn_image_iff` at
  [827](/home/claw/.openclaw/workspace/OSreconstruction/.lake/packages/mathlib/Mathlib/Topology/ContinuousOn.lean:827)
  and
  [823](/home/claw/.openclaw/workspace/OSreconstruction/.lake/packages/mathlib/Mathlib/Topology/ContinuousOn.lean:823);
- none of these upgrades boundary `ContinuousWithinAt` of the ambient tube
  function to continuity of the boundary trace
  `x ↦ F (realEmbed x)`;
- no repo-local lemma filling that exact gap was found in the required source
  files.

Exact smallest honest intermediate theorem now required:

- a local shifted-side theorem on the same `Uflat` proving
  `ContinuousOn (fun xflat => G_fl_t (SCV.realEmbed xflat)) Uflat`;
- and similarly
  `ContinuousOn (fun xflat => G_raw_t (SCV.realEmbed xflat)) Uflat`;
- after that, `eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn`
  becomes applicable with no widening of the FL supplier contract.

Exact failed alternatives:

- weaker:
  ambient `ContinuousWithinAt` alone does not match the uniqueness hypotheses;
- fake bridge:
  `hUflat.continuousOn_iff` only converts the problem into pointwise
  `ContinuousAt` for the trace;
- over-strong:
  demanding public FL-side `ContinuousOn` is not source-forced;
- wrong generic bridge:
  embedding/image continuity lemmas do not apply because `realEmbed x` lies on
  the boundary of the tube, not in an open subset where `F` is already known
  continuous in the usual sense.

Audit landing:

- no Lean artifact;
- docs-only.

Supervision correction (`2026-04-16`, audit of the local-open continuity bridge into the uniqueness consumer):
this pass is docs-only.

Exact bridge audit:

- `fourierLaplace_boundary_recovery_on_open_of_tempered` still only needs the
  local-open pointwise ambient package
  `∀ x ∈ U, ContinuousWithinAt F (TubeDomain C) (realEmbed x)`;
- `eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn` instead
  needs `ContinuousOn` of the real boundary traces on `U`;
- the only already-available generic conversion is
  `hU.continuousOn_iff`, which converts pointwise `ContinuousAt` of the real
  trace into `ContinuousOn` on the open set;
- no existing source lemma upgrades ambient boundary-point
  `ContinuousWithinAt` directly to real-trace `ContinuousOn`.

Exact smallest honest intermediate theorem now required:

- a local shifted-side trace-continuity theorem on the same open `Uflat`
  already produced by
  `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`,
  with output
  `ContinuousOn (fun xflat => G_fl_t (SCV.realEmbed xflat)) Uflat`;
- and similarly for the shifted raw trace on that same `Uflat`;
- only after that does the uniqueness theorem become applicable.

Exact failed alternatives:

- weaker:
  using only the ambient `ContinuousWithinAt` field skips the actual hypothesis
  surface of the uniqueness theorem;
- fake tiny bridge:
  applying `hUflat.continuousOn_iff` alone does not solve the problem, because
  the remaining pointwise `ContinuousAt` trace proof is the real missing work;
- over-strong:
  widening the FL supplier contract to external `ContinuousOn` is not forced by
  source and would just rename the same missing trace-continuity issue.

Audit landing:

- no Lean artifact;
- docs-only.

Supervision correction (`2026-04-16`, exact theorem-chain audit of the shifted local-open consumer):
this pass is docs-only.

Supervision correction (`2026-04-16`, exact primitive FL boundary-regularity theorem refinement beneath `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`):
this pass is docs-only.

Exact field extraction:

- `HasFourierLaplaceReprRegular` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:118](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:118)
  carries exactly
  `poly_growth`,
  `uniform_bound`,
  `boundary_continuous`,
  `tube_continuousWithinAt`;
- `HasFourierLaplaceReprTempered` at
  [143](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:143)
  carries exactly
  `poly_growth`
  and `uniform_bound`;
- the primitive source currently supplies only
  `fourierLaplaceExtMultiDim_holomorphic`,
  `fourierLaplaceExtMultiDim_vladimirov_growth`,
  and the distributional BV theorem
  `fourierLaplaceExtMultiDim_boundaryValue`;
- the primitive comments still explicitly warn that the BV is distributional,
  not pointwise, and that `F(x+iεη)` may blow up like `ε⁻ᴹ`.

Exact decision:

- the next honest gap is a two-step primitive chain;
- theorem 1 must prove the exact `uniform_bound` field for the primitive FL
  witness on fixed rays;
- theorem 2 must then produce the existing regularity package on a
  boundary-valued representative agreeing with the primitive FL function inside
  the tube;
- a continuity-only theorem is not enough, and a one-shot regularity theorem
  for the current zero extension is statement-drift.

Exact proposed statements:

- theorem 1:
  `fourierLaplaceExtMultiDim_uniform_bound_near_boundary`
  with conclusion
  `∀ (η : Fin m → ℝ), η ∈ C →
      ∃ (C_bd : ℝ) (N : ℕ) (δ : ℝ), C_bd > 0 ∧ δ > 0 ∧
        ∀ (x : Fin m → ℝ) (ε : ℝ), 0 < ε → ε < δ →
          ‖fourierLaplaceExtMultiDim C hC_open hC_conv hC_cone hC_salient T
              (fun i => ↑(x i) + ↑ε * ↑(η i) * I)‖ ≤
            C_bd * (1 + ‖x‖) ^ N`;
- theorem 2:
  `fourierLaplaceExtMultiDim_exists_regular_boundary_repr`
  with conclusion
  `∃ G : (Fin m → ℂ) → ℂ,
      (∀ z, z ∈ SCV.TubeDomain C →
        G z =
          fourierLaplaceExtMultiDim C hC_open hC_conv hC_cone hC_salient T z) ∧
      HasFourierLaplaceReprRegular C G`.

Audit verdict:

- theorem 1 is the first honest mathematical target because without it even
  `HasFourierLaplaceReprTempered` is not source-backed;
- theorem 2 is the minimal exact repair of the boundary-regularity seam because
  it uses the existing package surface and avoids asserting pointwise boundary
  continuity for the zero extension itself.

Exact local-open consumer theorem above `hrepr_boundary_shifted`:

- no existing theorem in the boundary-value files or the positivity seam yet
  upgrades the current open-set continuity packet to the pointwise boundary
  equality needed by theorem-(1);
- the exact missing consumer is a shifted local-open `Set.EqOn` theorem on the
  open `Uflat` already produced by
  `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  at
  [OSToWightmanPositivity.lean:4686](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4686);
- its output should then be specialized at the basepoint to obtain the future
  `hrepr_boundary_shifted` passed into
  `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`.

Exact theorem-chain verdict:

- `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  at
  [OSToWightmanPositivity.lean:4583](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4583)
  is still the raw shifted-point continuity placeholder;
- the next two theorems at
  [4601](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4601)
  and
  [4630](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4630)
  only transport that pointwise continuity through the shell and flattening;
- the local-open theorem at
  [4686](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4686)
  is therefore the exact lower end of the honest consumer seam.

Exact minimal FL-side continuity package:

- source still supports only the local-open field
  `∀ xflat ∈ Uflat, ContinuousWithinAt G_fl_t (SCV.TubeDomain ...) (SCV.realEmbed xflat)`;
- this is the precise input used by
  `fourierLaplace_boundary_recovery_on_open_of_tempered`;
- any stronger theorem surface demanding external full `ContinuousOn` is not
  justified by the current seam and does not reduce the blocker.

Exact failed weaker alternative:

- a single-point theorem for `hrepr_boundary_shifted` is too weak to match the
  open-set recovery/uniqueness route already encoded in the SCV files.

Audit landing:

- no Lean artifact;
- docs-only.

Supervision correction (`2026-04-16`, shifted local-open consumer audit on the theorem-(1) caller chain):
this pass is docs-only.

Exact theorem-chain verdict:

- theorem-(1)
  `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`
  at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688)
  still consumes only one pointwise boundary identity;
- the caller chain in
  [OSToWightmanPositivity.lean:4601](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4601),
  [4630](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4630),
  and
  [4686](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4686)
  shows the raw shifted side is already organized around an open `Uflat` with
  pointwise `ContinuousWithinAt`;
- therefore the immediate honest consumer above `hrepr_boundary_shifted` is an
  open-neighborhood boundary-agreement theorem on that same `Uflat`.

Exact local-open consumer theorem shape now forced by source:

- there should be a shifted-side theorem proving
  `Set.EqOn
     (fun xflat => G_raw_t (SCV.realEmbed xflat))
     (fun xflat => G_fl_t (SCV.realEmbed xflat))
     Uflat`
  for some open
  `Uflat ⊆ (flattenCLEquivReal ...) '' ((section43PositiveEnergyRegion ...)ᶜ)`
  containing the chosen basepoint;
- `hrepr_boundary_shifted` is then obtained by evaluation at that basepoint;
- no current theorem in
  `OSToWightmanBoundaryValueLimits.lean`
  or adjacent files already lands this exact contract.

Exact minimal FL-side continuity package:

- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` at
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:156](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:156)
  only asks for
  `∀ x ∈ U, ContinuousWithinAt F (TubeDomain C) (realEmbed x)`;
- so the honest FL-side supplier contract is exactly the local-open pointwise
  `ContinuousWithinAt` field on `Uflat`, not a stronger global or external
  `ContinuousOn` theorem surface;
- the later uniqueness step
  `eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn` at
  [OSReconstruction/SCV/DistributionalUniqueness.lean:1507](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/DistributionalUniqueness.lean:1507)
  is the consumer that turns local pairing equality into pointwise equality.

Exact failed weaker alternative:

- a theorem supplying only the single basepoint equality
  `hrepr_boundary_shifted`
  is not the honest local-open consumer, because it bypasses the actual SCV
  mechanism already present in source;
- a theorem that asks externally for full open-set `ContinuousOn` of the FL
  boundary trace widens the supplier contract beyond what the recovery theorem
  itself needs.

Audit landing:

- no Lean edit;
- docs-only.

Supervision correction (`2026-04-16`, source audit after rereading the local/global boundary-recovery proofs on the primitive FL seam):
this pass is docs-only.

Exact theorem-composition verdict:

- `SCV.realPlusIEpsEta_tendsto_nhdsWithin_realEmbed` is still the exact ray
  path theorem;
- if `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` were proved,
  the desired scalar raywise limit would still follow immediately by
  `Filter.Tendsto.comp`;
- however, the proved scalar boundary-limit theorems in
  [OSReconstruction/SCV/LaplaceSchwartz.lean:305](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:305)
  and
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:105](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:105)
  both build their pointwise convergence step by explicitly assuming
  `tube_continuousWithinAt` / `hcontU`.

Exact stronger obstruction from source:

- `HasFourierLaplaceReprRegular` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:118](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:118)
  exists precisely because the weaker boundary-value package does not imply
  `boundary_continuous` or `tube_continuousWithinAt`;
- `PaleyWienerSchwartz.lean` explicitly notes at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3865](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3865)
  and
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:4303](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4303)
  that the boundary value is distributional, not pointwise in general.

Refined verdict:

- the desired universal raywise pointwise-limit theorem for
  `fourierLaplaceExtMultiDim ... T`
  is not derivable from current proved source;
- after this reread, the current theorem surface is source-suspect at full
  generality, not merely unproved;
- the honest missing ingredient for the live theorem-3 seam is narrower and
  more local:
  an additional boundary-regularity theorem for the specific FL witness on the
  shifted open set, yielding
  `ContinuousWithinAt` there.

Audit landing:

- no Lean edit;
- docs-only.

Supervision correction (`2026-04-16`, source-first audit of the primitive FL raywise scalar-limit theorem):
this pass is docs-only.

Exact theorem statements rechecked from source:

- `SCV.realPlusIEpsEta_tendsto_nhdsWithin_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3510](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3510)
  is the exact canonical path theorem into
  `nhdsWithin (SCV.realEmbed x) (SCV.TubeDomain C)`;
- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241)
  is still the primitive missing SCV theorem;
- the target scalar conclusion is
  `fourierLaplaceExtMultiDim(x + i ε η) → fourierLaplaceExtMultiDim(realEmbed x)`
  as `ε → 0+`.

Exact failed composition:

- intended proof:
  `Filter.Tendsto.comp
    (SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed ...)
    (SCV.realPlusIEpsEta_tendsto_nhdsWithin_realEmbed ...)`;
- failure point:
  the first theorem is still `sorry`.

Exact substitute search verdict:

- `SCV.fourierLaplaceExtMultiDim_boundaryValue` is distributional only;
- `SCV.fourierLaplace_schwartz_integral_convergence` is circular because
  `HasFourierLaplaceReprRegular` already contains
  `tube_continuousWithinAt`;
- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` and
  `SCV.eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn`
  still require prior pointwise continuity on the open set;
- `SCV.boundary_value_zero_of_regular` and
  `SCV.distributional_uniqueness_tube_of_regular`
  remain downstream regular-package consumers, not a source of the missing
  primitive evaluator.

Refined missing-ingredient verdict:

- the raywise pointwise-limit theorem is not derivable from existing proved source;
- the exact smaller missing analytic ingredient remains a direct pointwise
  boundary-regularity theorem for
  `fourierLaplaceExtMultiDim ... T`,
  equivalently the scalar ray-limit theorem itself.

Audit verdict:

- no honest Lean landing on this pass;
- docs-only.

Supervision correction (`2026-04-16`, exact source theorem-composition audit for the primitive raywise FL scalar limit):
this pass is docs-only.

Exact theorem statements rechecked from source:

- `SCV.realPlusIEpsEta_tendsto_nhdsWithin_realEmbed` gives the canonical ray
  `x + i ε η` as a map into
  `nhdsWithin (SCV.realEmbed x) (SCV.TubeDomain C)`;
- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` is the exact
  missing SCV theorem:
  `ContinuousWithinAt
    (fourierLaplaceExtMultiDim ...)
    (SCV.TubeDomain C)
    (SCV.realEmbed x)`;
- the wanted scalar theorem is obtained from those two statements by direct
  `Filter.Tendsto.comp`, and no extra wrapper is needed.

Exact failed source composition:

- attempted proof:
  `Filter.Tendsto.comp
    (SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed ...)
    (SCV.realPlusIEpsEta_tendsto_nhdsWithin_realEmbed ...)`;
- failure point:
  the first theorem is still `sorry`;
- no already-proved theorem in the inspected SCV files replaces it on the exact
  FL witness surface.

Exact substitute search verdict:

- `SCV.fourierLaplaceExtMultiDim_boundaryValue` is distributional only;
- `SCV.fourierLaplace_schwartz_integral_convergence` is circular here because
  `HasFourierLaplaceReprRegular` already assumes
  `tube_continuousWithinAt`;
- `fourierLaplace_boundary_recovery_on_open_of_tempered`,
  `eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn`,
  `boundary_value_zero_of_regular`,
  and
  `distributional_uniqueness_tube_of_regular`
  all still need prior pointwise continuity or the stronger regular package.

Refined missing-ingredient verdict:

- not merely a missing evaluator on top of an already-identified boundary
  distribution;
- not a hidden topology lemma;
- yes, the exact smaller missing analytic ingredient is still the direct
  pointwise boundary-regularity theorem for
  `fourierLaplaceExtMultiDim ... T`,
  equivalently the raywise scalar-limit theorem along `x + i ε η`.

Supervision correction (`2026-04-16`, primitive FL-side continuity theorem audit):
this pass is docs-only.

Supervision correction (`2026-04-16`, primitive FL-side raywise pointwise-limit audit):
this pass is docs-only.

Exact theorem statements rechecked:

- `SCV.realPlusIEpsEta_tendsto_nhdsWithin_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3510](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3510)
  is the exact path theorem into
  `nhdsWithin (SCV.realEmbed x) (SCV.TubeDomain C)`;
- `SCV.fourierLaplaceExtMultiDim_boundaryValue` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:4288](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4288)
  gives only distributional convergence of
  `∫ fourierLaplaceExtMultiDim(x+iεη) f(x) dx`;
- `SCV.fourierLaplace_schwartz_integral_convergence` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:309](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:309)
  gives scalar integral convergence to the boundary integral, but only under
  `HasFourierLaplaceReprRegular`.

Exact circularity check:

- `HasFourierLaplaceReprRegular` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:118](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:118)
  already contains
  `tube_continuousWithinAt : ∀ x, ContinuousWithinAt F (TubeDomain C) (realEmbed x)`;
- therefore the only nearby theorem that yields scalar convergence along the ray
  already assumes the missing continuity theorem in packaged form.

Exact failed theorem composition:

- intended proof:
  `Filter.Tendsto.comp
    (SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed ...)
    (SCV.realPlusIEpsEta_tendsto_nhdsWithin_realEmbed ...)`;
- failure:
  `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` is still `sorry`,
  and no proved theorem supplies the same conclusion for the exact FL witness.

Refined missing-ingredient verdict:

- the raywise theorem is not currently derivable from existing source;
- the exact smaller missing analytic ingredient is a direct pointwise
  boundary-regularity theorem for
  `fourierLaplaceExtMultiDim ... T`
  at real boundary points;
- equivalently:
  for every `x` and every `η ∈ C`,
  `Filter.Tendsto
    (fun ε : ℝ =>
      fourierLaplaceExtMultiDim ... (fun i => (x i : ℂ) + (ε : ℂ) * (η i : ℂ) * I))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds (fourierLaplaceExtMultiDim ... (SCV.realEmbed x)))`;
- this is narrower than any weak-to-regular package upgrade and narrower than
  any boundary-distribution identification theorem.

Audit verdict for this pass:

- no honest Lean landing on the primitive seam;
- docs-only.

Exact theorem surface rechecked:

- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241)
  is the point-boundary theorem
  `ContinuousWithinAt (fourierLaplaceExtMultiDim ...) (SCV.TubeDomain C)
    (SCV.realEmbed x)`;
- the nearby proved theorem
  `SCV.fourierLaplaceExtMultiDim_continuousOn` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3204](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3204)
  only gives interior `ContinuousOn` on `SCV.TubeDomain C`.

Exact direct deduction failure:

- `SCV.TubeDomain C = { z | Im z ∈ C }` by
  [OSReconstruction/SCV/TubeDomainExtension.lean:64](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/TubeDomainExtension.lean:64);
- `SCV.realEmbed x` has imaginary part `0` by
  [OSReconstruction/SCV/TubeDomainExtension.lean:110](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/TubeDomainExtension.lean:110);
- hence `SCV.realEmbed x ∈ SCV.TubeDomain C` is equivalent to `0 ∈ C`,
  not available on this theorem surface;
- therefore the standard topological lemma
  `ContinuousOn f s -> ContinuousWithinAt f s x`
  cannot close the goal here.

Smallest missing-lemma classification:

- not pure topology;
- not real-embed membership;
- yes, a genuinely stronger SCV/analytic boundary theorem:
  dual-cone support for `T` must force boundary approach continuity of
  `fourierLaplaceExtMultiDim ... T` at real points.

Audit verdict for the theorem-(1) route:

- the first honest missing FL-side antecedent remains exactly
  `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`;
- it does not lower further to a trivial shape-only helper from nearby source;
- no Lean artifact was landed in this pass.

Supervision correction (`2026-04-16`, exact audit of the `continuousOn`-plus-topology route beneath the primitive FL theorem):
this pass is docs-only.

Exact audit result:

- `SCV.fourierLaplaceExtMultiDim_continuousOn` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3204](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3204)
  proves continuity only on the open interior tube;
- the target boundary point is `SCV.realEmbed x`, and that point is not in the
  open set `SCV.TubeDomain C`;
- the only nearby smaller continuity transport theorem is
  `continuous_multiDimPsiZExt_comp_of_mem_tube` at
  [2739](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:2739),
  whose hypothesis is exactly
  `∀ a, z a ∈ SCV.TubeDomain C`.

Exact attempted proof route:

- rewrite
  `fourierLaplaceExtMultiDim ... z = T (multiDimPsiZExt ... z)`
  by `fourierLaplaceExtMultiDim_eq_ext`;
- try first to obtain continuity of `multiDimPsiZExt` at the real boundary
  point, either from the interior `continuousOn` theorem or from
  `continuous_multiDimPsiZExt_comp_of_mem_tube`;
- then compose with `T.continuous`.

Exact failed step:

- `continuousOn` on the tube cannot be evaluated at a boundary point outside
  the tube;
- `continuous_multiDimPsiZExt_comp_of_mem_tube` only handles parameter maps
  whose images remain in the tube, so it does not cover the endpoint
  `ε = 0` of the ray
  `ε ↦ x + i ε η`;
- therefore no existing theorem upgrades the interior continuity of the
  Paley-Wiener kernel to the required boundary `ContinuousWithinAt`.

Exact first smaller honest gap after this check:

- a boundary-point continuity theorem for `multiDimPsiZExt` would be strictly
  smaller than the target;
- but proving only that would still establish continuity of the zero extension
  to the zero boundary value, not the intended boundary trace used elsewhere on
  the seam;
- since the file itself still states that the general boundary value is
  distributional rather than pointwise, the real blocker is better classified
  as a missing **boundary-valued regularity theorem or extra hypothesis**, not
  as a pure topology lemma.

Audit verdict:

- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` still does not
  collapse from current source;
- the first smaller honest theorem/hypothesis gap belongs upstream in
  `PaleyWienerSchwartz.lean`;
- no Lean artifact was landed.

Supervision correction (`2026-04-16`, exact adjacent-antecedent audit under theorem-(1)`):
this pass is docs-only.

Exact theorem-(1) hypothesis list from
[OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688):

- witness-side geometry and support:
  `Tflat`,
  `hCflat_open`,
  `hCflat_conv`,
  `hCflat_cone`,
  `hCflat_salient`,
  `hTflat_support`;
- point-local data:
  `x`,
  `hrepr_tube`,
  `hrepr_boundary`;
- output:
  `ContinuousWithinAt F (ForwardTube d n) (fun k μ => (x k μ : ℂ))`.

Exact supply status on the live shifted seam:

- supplied by the witness theorem at
  [OSToWightmanBoundaryValueLimits.lean:233](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233):
  every witness-side geometry/support field and `hrepr_tube`;
- supplied by the shifted-point specialization:
  `x := xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 y t`;
- intended output of the immediate caller:
  `hrepr_boundary`;
- therefore theorem-(1) has exactly one unsupplied input on this seam:
  the shifted real-point boundary equality.

Exact local-open SCV surfaces that manufacture that boundary equality:

- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` supplies only
  compact-support test pairing equality from a tempered FL package and
  open-set pointwise `ContinuousWithinAt`;
- `SCV.eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn`
  upgrades such pairing equality to `Set.EqOn`, but only after both traces are
  `ContinuousOn` on the same open set.

Audit verdict on what is already covered vs still missing:

- already covered by the fixed shifted-side supplier:
  the open neighborhood `Uflat`, basepoint membership, complement-image
  placement, and shifted-side
  `∀ xflat ∈ Uflat, ContinuousWithinAt g ... (SCV.realEmbed xflat)`;
- already covered witness-locally:
  the FL witness data `Tflat`, cone geometry, `hTflat_support`, and the
  distribution identity `hTflat_W`;
- still missing beneath the immediate caller:
  the matching FL-side open-neighborhood boundary regularity
  `∀ xflat ∈ Uflat, ContinuousWithinAt h ... (SCV.realEmbed xflat)`,
  where
  `h xflat :=
    fourierLaplaceExtMultiDim (ForwardConeFlat d q)
      hCflat_open hCflat_conv hCflat_cone hCflat_salient
      Tflat (SCV.realEmbed xflat)`;
- exact source anchor for that missing antecedent:
  [PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241),
  `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`,
  still `sorry`.

Classification verdict:

- the final theorem-(1) input is indeed the raw pointwise equality
  `hrepr_boundary_shifted`;
- but the first honest missing adjacent theorem surface is stronger than that:
  an open-set `ContinuousWithinAt` regularity statement for the FL trace of the
  same `Tflat`;
- it is not a same-input W=S shortcut, not a fake density route, and not a
  wrapper around `hrepr_boundary_shifted`;
- it is the exact regularity antecedent required by the two SCV local-open
  theorems that are supposed to produce `hrepr_boundary_shifted`.

Supervision correction (`2026-04-16`, binder-by-binder classification for the live shifted-side witness-local route):
this pass is docs-only.

Exact witness theorem extraction from
[OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233):

- inputs:
  only `OS`, `lgc`, `n`;
- returned data:
  `Tflat`,
  `hCflat_open`,
  `hCflat_conv`,
  `hCflat_cone`,
  `hCflat_salient`,
  `hTflat_support`,
  `hTflat_W`,
  `hrepr_tube`.

Exact shifted-side supplier verdict:

- recommended theorem:
  `exists_open_flat_neighborhood_shifted_bvt_F_continuousWithinAt`;
- must stay in the theorem statement:
  `OS`, `lgc`, `hm`, `t`, `y`, `hy`;
- must be returned as output data:
  `Uflat`,
  `IsOpen Uflat`,
  `flattenCLEquivReal ... y ∈ Uflat`,
  `Uflat ⊆ (flattenCLEquivReal ...) '' ((section43PositiveEnergyRegion ...)ᶜ)`,
  and
  `∀ xflat ∈ Uflat, ContinuousWithinAt Gshift ... (SCV.realEmbed xflat)`;
- must stay local proof data only:
  `q`,
  `j := ⟨n, Nat.lt_add_of_pos_right hm⟩`,
  and the derived shifted trace `Gshift`;
- must be omitted entirely as unused on this seam:
  `Tflat`,
  `hTflat_support`,
  `hTflat_W`,
  `hrepr_tube`,
  cone openness/convexity/cone/salience data,
  and any prepackaged `ContinuousOn`;
- reason:
  the exact local-open SCV input at
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:156](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:156)
  asks for an open real set plus pointwise `ContinuousWithinAt`, not witness
  data and not another wrapper theorem.

Exact caller above `hrepr_boundary_shifted` verdict:

- recommended theorem:
  `exists_open_flat_neighborhood_shifted_boundary_eqOn_fourierLaplace_repr`;
- must stay in the theorem statement:
  `OS`, `lgc`, `hm`, `t`, `y`, `hy`,
  `Tflat`,
  `hCflat_open`,
  `hCflat_conv`,
  `hCflat_cone`,
  `hCflat_salient`,
  `hTflat_support`,
  `hTflat_W`;
- must be returned as output data:
  `Uflat`,
  `IsOpen Uflat`,
  basepoint membership,
  complement-image inclusion,
  and `Set.EqOn g h Uflat`;
- must stay local proof data only:
  `q`,
  `j`,
  the trace functions `g` and `h`,
  `forwardConeFlat_nonempty d q`,
  the local `ContinuousOn` upgrades from open-set pointwise continuity,
  and the locally assembled `HasFourierLaplaceReprTempered` package;
- must be omitted entirely as unused at this caller:
  `hrepr_tube`;
- reason:
  the theorem stops at real-side boundary agreement on an open neighborhood;
  the interior FL equality is consumed only later by theorem-(1) at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688).

Exact SCV interface audit behind the keep/omit split:

- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` needs
  `U`, `IsOpen U`, and
  `∀ x ∈ U, ContinuousWithinAt F (TubeDomain C) (realEmbed x)`;
- `SCV.eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn`
  needs `ContinuousOn` of both real traces on `U` plus compact-support pairing
  equality on `U`;
- therefore the supplier should keep pointwise `ContinuousWithinAt` and the
  caller should derive `ContinuousOn` proof-locally rather than pushing either
  a wrapper theorem or a stronger theorem surface upward.

Exact non-circular flow remains:

- witness theorem output
  `Tflat`, cone geometry, `hTflat_support`, `hTflat_W`, `hrepr_tube`;
- witness-free shifted-side supplier;
- witness-local open-neighborhood `Set.EqOn` caller;
- basepoint evaluation to `hrepr_boundary_shifted`;
- unchanged theorem-(1) consumer.

Supervision correction (`2026-04-16`, exact omitted caller binders on the shifted-side local-open route):
this pass is docs-only.

Exact additional binder verdict from source:

- the insertion-site shifted-side supplier still quantifies only
  `OS`, `lgc`, `hm`, `t`, `y`, `hy`;
- the open-neighborhood boundary-agreement caller above
  `hrepr_boundary_shifted` should quantify only
  `Tflat`, `hCflat_open`, `hCflat_conv`, `hCflat_cone`, `hCflat_salient`,
  `hTflat_support`, `hTflat_W`;
- it should **not** quantify:
  `hrepr_tube`,
  a separate cone nonemptiness proof,
  or a separate `HasFourierLaplaceReprTempered` witness.

Reason for each omitted binder:

- omit `hrepr_tube` because the local-open boundary-agreement theorem uses only
  real-side continuity plus compact-support pairing equality on `Uflat`;
  the interior FL identity first reappears one step later, at the unchanged
  theorem-(1) consumer in
  [ForwardTubeDistributions.lean:688](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688);
- omit cone nonemptiness because the caller is specialized to
  `ForwardConeFlat d (n + m)`, so `forwardConeFlat_nonempty d (n + m)` is
  canonical proof-local data, not theorem interface;
- omit a tempered-package binder because the SCV local-open recovery theorem
  needs `HasFourierLaplaceReprTempered`, but that package can be built locally
  from existing source:
  `fourierLaplace_boundaryValue_recovery`,
  `fourierLaplaceExtMultiDim_vladimirov_growth`,
  the standard fixed-ray bound input,
  and
  `exists_fourierLaplaceReprTempered`.

Exact caller-local data flow:

- `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`
  supplies the fixed witness-side data;
- the new shifted-side supplier supplies only the open neighborhood and
  pointwise `ContinuousWithinAt` for the shifted raw trace;
- the FL-side pointwise continuity theorem supplies the matching continuity for
  `fourierLaplaceExtMultiDim ... Tflat`;
- those two continuity families plus internally derived compact-support pairing
  equality feed
  `SCV.eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn`;
- the resulting `Set.EqOn` at the basepoint yields `hrepr_boundary_shifted`
  without using theorem-(1) or the circular positivity wrappers.

Supervision correction (`2026-04-16`, exact binder split for the shifted-side insertion surface):
this pass is docs-only.

Exact theorem-shape verdict:

- insertion-site theorem immediately below
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`
  should be the shifted-side neighborhood continuity supplier only:
  it quantifies `OS`, `lgc`, `hm`, `t`, `y`, `hy`,
  returns `∃ Uflat`, keeps the complement-image inclusion, and concludes
  `∀ xflat ∈ Uflat, ContinuousWithinAt Gshift ... (SCV.realEmbed xflat)`;
- it should **not** quantify any witness field from the private FL witness
  package;
  those fields belong to the later boundary-agreement caller, not to the raw
  shifted-trace continuity supplier;
- the exact caller theorem immediately above `hrepr_boundary_shifted` should
  then quantify only the witness data genuinely used by boundary agreement:
  `Tflat`, cone geometry, `hTflat_support`, `hTflat_W`;
  it should leave out `hrepr_tube`, because tube-interior FL equality is not
  needed to obtain the open-neighborhood `Set.EqOn`;
- the boundary-agreement caller should return
  `∃ Uflat, IsOpen Uflat ∧ flattenCLEquivReal ... y ∈ Uflat ∧
    Uflat ⊆ ... ∧ Set.EqOn g h Uflat`,
  where `g` is the shifted flattened real trace of `bvt_F`
  and `h` is the real trace of
  `fourierLaplaceExtMultiDim ... Tflat`.

Reason for each binder kept vs omitted:

- keep `hm`, `t`, `y`, `hy` in the shifted-side supplier because they determine
  the live `xiShift` shell and the complement-side neighborhood;
- omit `Tflat`, `hTflat_support`, `hTflat_W`, `hrepr_tube` from that supplier
  because continuity of the shifted raw trace is a separate boundary-regularity
  theorem and should not be wrapped together with witness-local FL data;
- keep `Tflat` and cone geometry in the caller because they are required to
  state the comparison function
  `fourierLaplaceExtMultiDim ... Tflat`;
- keep `hTflat_support` because the exact FL-side continuity theorem surface
  [PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241)
  depends on it;
- keep `hTflat_W` because it is the witness-side boundary distribution identity
  that feeds the compact-support Schwartz pairing equality on `Uflat`;
- leave out `hrepr_tube` from the caller because it is only consumed later by
  theorem-(1), after `hrepr_boundary_shifted` has been extracted by evaluating
  the `EqOn` theorem at the basepoint.

Exact non-circular flow now fixed:

- witness theorem
  `233` gives
  `Tflat`, `hTflat_support`, `hTflat_W`, `hrepr_tube`;
- new insertion-site supplier gives shifted-side `ContinuousWithinAt` on an
  open `Uflat`;
- exact FL-side supplier gives FL-side `ContinuousWithinAt` on real points;
- open-neighborhood caller combines those continuity inputs with `hTflat_W`
  and
  `SCV.eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn` at
  [DistributionalUniqueness.lean:1506](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/DistributionalUniqueness.lean:1506)
  to get `Set.EqOn`;
- evaluating at `flattenCLEquivReal (n + m) (d + 1) y` gives
  `hrepr_boundary_shifted`;
- theorem-(1) then consumes `hrepr_boundary_shifted` together with the already
  separate `hrepr_tube`.

Supervision correction (`2026-04-16`, audit of the smallest honest local-open theorem surface above `hrepr_boundary_shifted`):
this pass is docs-only.

Exact local-open interfaces re-audited:

- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` at
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:156](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:156)
  requires an open `U` and
  `∀ x ∈ U, ContinuousWithinAt F (TubeDomain C) (realEmbed x)`;
- `SCV.eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn` at
  [OSReconstruction/SCV/DistributionalUniqueness.lean:1506](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/DistributionalUniqueness.lean:1506)
  is the exact local-open equality theorem shape: pairing equality on compactly
  supported Schwartz tests supported in `U`, plus `ContinuousOn` of both
  functions on `U`, gives `Set.EqOn`;
- `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4686](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4686)
  already states the shifted-complement open-neighborhood continuity package for
  the flattened fixed-time shell continuation `G_t`;
- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241)
  is still the exact FL-side boundary continuity theorem surface and is still
  `sorry`.

Audit verdict on the smallest honest local-open theorem statement:

- the right theorem immediately above the raw pointwise blocker is an
  open-neighborhood boundary-agreement statement, not merely another pointwise
  continuity lemma;
- concretely, after fixing
  `q := n + m`,
  `j := ⟨n, Nat.lt_add_of_pos_right hm⟩`,
  the witness package
  `Tflat`, cone geometry, `hTflat_support`, `hTflat_W`, `hrepr_tube`,
  and
  `xflat0 := flattenCLEquivReal q (d + 1) (xiShift j 0 y t)`,
  the minimal local-open target is:
  `∃ Uflat, IsOpen Uflat ∧ xflat0 ∈ Uflat ∧ Set.EqOn g h Uflat`,
  where
  `g` is the shifted flattened real trace of `bvt_F`
  and
  `h` is the exact FL trace of `Tflat`;
- evaluating this `EqOn` at `xflat0` yields
  `hrepr_boundary_shifted`.

Audit verdict on whether current source already almost supplies the needed hypotheses:

- only at theorem-surface level, not as proved source;
- the shifted complement side already has the right open-neighborhood
  continuity theorem surface at positivity line `4686`, but that theorem is
  discharged entirely by the pointwise theorem at line `4630`, which itself is
  downstream of the live `sorry`
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  at line `4583`;
- the exact FL side still lacks even the pointwise boundary continuity theorem
  as proved source because
  `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` is still
  `sorry`;
- therefore the open-neighborhood continuity theorem is not already available
  on the shifted complement branch in any trustworthy sense, and the SCV
  local-open route is still blocked first by boundary regularity mathematics,
  not by additional positivity transport.

Exact insertion site / caller remains:

- place the future open-neighborhood boundary-agreement theorem immediately
  below the private witness theorem in
  `OSToWightmanBoundaryValueLimits.lean`;
- use its value at the distinguished shifted point to supply
  `hrepr_boundary_shifted`;
- then feed the unchanged theorem-(1) consumer
  `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`.

Supervision correction (`2026-04-16`, source-pinned audit of the smallest honest boundary-agreement theorem surface):
this pass is docs-only.

Exact proved SCV interfaces inspected:

- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` at
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:156](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:156);
- `SCV.eq_zero_on_open_of_compactSupport_schwartz_integral_zero` at
  [OSReconstruction/SCV/DistributionalUniqueness.lean:1353](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/DistributionalUniqueness.lean:1353);
- `SCV.uniqueness_of_boundary_zero` at
  [OSReconstruction/SCV/DistributionalUniqueness.lean:1659](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/DistributionalUniqueness.lean:1659);
- `SCV.distributional_uniqueness_tube_of_zero_bv` at
  [OSReconstruction/SCV/DistributionalUniqueness.lean:1958](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/DistributionalUniqueness.lean:1958);
- `SCV.boundary_value_zero_of_regular` and
  `SCV.distributional_uniqueness_tube_of_regular` at
  [OSReconstruction/SCV/TubeDistributions.lean:78](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/TubeDistributions.lean:78)
  and
  [OSReconstruction/SCV/TubeDistributions.lean:95](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/TubeDistributions.lean:95).

Audit verdict:

- `LocalBoundaryRecovery` still packages only support-local distributional
  recovery:
  `dist f = ∫ boundaryTrace * f`;
- `DistributionalUniqueness` gives two relevant proved endpoints:
  an open-set real-side vanishing theorem from compactly supported Schwartz
  pairings, and an interior tube uniqueness theorem from pointwise boundary
  zero plus `ContinuousWithinAt`;
- neither theorem bridges from the fixed witness package
  `hTflat_W + hrepr_tube`
  to a raw boundary value of `bvt_F` at one real point;
- `TubeDistributions` only exposes pointwise real-boundary conclusions under
  the stronger `HasFourierLaplaceReprRegular` package, which is exactly the
  unavailable upgrade on this seam.

Exact smallest candidate theorem statement:

- after unpacking
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`,
  the first honest missing theorem is still
  `hrepr_boundary_shifted :
    bvt_F OS lgc q z0 =
      fourierLaplaceExtMultiDim
        (ForwardConeFlat d q)
        hCflat_open hCflat_conv hCflat_cone hCflat_salient
        Tflat ((flattenCLEquiv q (d + 1)) z0)`;
- this should keep the same explicit witness data
  `Tflat`, cone geometry, `hTflat_support`, `hTflat_W`, `hrepr_tube`,
  and nothing later from positivity.

Continuity strength verdict:

- existing proved machinery does **not** almost package this from
  witness-local continuity at one point;
- the nearest proved route would need an open real neighborhood `U` and
  continuity on all of `U` to use
  `fourierLaplace_boundary_recovery_on_open_of_tempered` and then
  `eq_zero_on_open_of_compactSupport_schwartz_integral_zero`;
- so, unless a new direct point theorem is proved, the available SCV route is
  strictly stronger than one-point continuity.

Exact insertion site / caller:

- insert the future theorem immediately below the private witness theorem in
  `OSToWightmanBoundaryValueLimits.lean`;
- pass it as the `hrepr_boundary` input to
  `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`;
- that theorem remains the direct input supplier for the live theorem-(1)
  seam
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`.

Supervision correction (`2026-04-16`, source-pinned audit of whether `hrepr_boundary_shifted` is already derivable):
this pass is docs-only.

Exact sources inspected:

- `bvt_boundary_values` at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValuesBase.lean:360](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValuesBase.lean:360);
- the private witness theorem
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233);
- `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point` at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688);
- `boundary_value_recovery_forwardTube_of_flatRegular` and
  `boundary_value_recovery_forwardTube_of_flatRegular_from_bv` at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:752](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:752)
  and
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:1105](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:1105);
- `SCV.fourierLaplace_boundary_recovery`,
  `SCV.fourierLaplace_repr_dist_unique`, and
  `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:386](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:386),
  [OSReconstruction/SCV/LaplaceSchwartz.lean:840](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:840),
  and
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:156](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:156);
- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241).

Audit verdict:

- yes, the witness theorem already supplies the exact same `Tflat` on both the
  `bvt_W` distributional side and the `bvt_F` interior FL side;
- no, none of the proved recovery/uniqueness theorems derive the shifted raw
  boundary identity from that package;
- the obstruction is categorical, not transport-related:
  all proved theorems recover equality after pairing with Schwartz tests, while
  `hrepr_boundary_shifted` asks for equality of raw values at one real point;
- `fourierLaplace_boundary_recovery_on_open_of_tempered` still assumes local
  `ContinuousWithinAt` and therefore cannot be used to manufacture the missing
  equality below theorem-(1);
- so the shifted pointwise identity remains the first honest missing theorem,
  and it is another face of the upstream boundary-trace/continuity machinery.

Exact smallest missing theorem/ingredient after this pass:

- a theorem that, for the witness supplied by
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`,
  upgrades weak BV recovery
  `hTflat_W`
  plus the interior identity
  `hrepr_tube`
  to
  `hrepr_boundary_shifted :
    bvt_F OS lgc q z0 =
      fourierLaplaceExtMultiDim
        (ForwardConeFlat d q)
        hCflat_open hCflat_conv hCflat_cone hCflat_salient
        Tflat ((flattenCLEquiv q (d + 1)) z0)`.

Supervision correction (`2026-04-16`, corrected theorem-(1) seam audit after the boundary-agreement probe):
this pass is docs-only.

Exact theorem-(1) binders inspected:

- `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point` at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688)
  requires:
  `Tflat`, cone geometry, `hTflat_support`, a real point `x`,
  `hrepr_tube` on `ForwardTube d n`,
  and one pointwise boundary equality `hrepr_boundary`;
- `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr` at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233)
  supplies the same witness package plus the interior identity
  on `TubeDomainSetPi (ForwardConeAbs d q)`;
- `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4583](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4583)
  remains the live theorem-(1) seam and still asks for full
  `ContinuousWithinAt` of raw `bvt_F` at the shifted real point.

Audit verdict:

- for
  `q := n + m`,
  `j := ⟨n, Nat.lt_add_of_pos_right hm⟩`,
  `x0 := xiShift j 0 y t`,
  `z0 := xiShift j 0 (fun k μ => (y k μ : ℂ)) (t : ℂ)`,
  the positivity-local proof of theorem-(1) now reduces exactly to the missing
  binder
  `hrepr_boundary_shifted`;
- after rewriting
  `ForwardTube d q = TubeDomainSetPi (ForwardConeAbs d q)`,
  no further positivity-local neighborhood theorem is missing at the use site;
- the exact-FL continuity theorem remains upstream inside
  `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`,
  not as an extra local hypothesis below theorem-(1);
- because theorem-(1) itself is the active consumer theorem surface on the
  current branch, this audit does **not** justify weakening that consumer below
  full `ContinuousWithinAt`.

Exact smallest missing theorem/ingredient after this pass:

- `hrepr_boundary_shifted :
    bvt_F OS lgc q z0 =
      fourierLaplaceExtMultiDim
        (ForwardConeFlat d q)
        hCflat_open hCflat_conv hCflat_cone hCflat_salient
        Tflat ((flattenCLEquiv q (d + 1)) z0)`.

Supervision correction (`2026-04-16`, exact theorem-(1) boundary-agreement audit):
this pass is docs-only.

Exact theorem-(1) binders inspected:

- `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point` at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688)
  requires:
  `Tflat`, cone geometry, `hTflat_support`, a real point `x`,
  `hrepr_tube` on `ForwardTube d n`,
  and one pointwise boundary equality `hrepr_boundary`;
- `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr` at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233)
  supplies the same witness package plus the interior identity
  on `TubeDomainSetPi (ForwardConeAbs d q)`;
- `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4583](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4583)
  asks for `ContinuousWithinAt` of raw `bvt_F` at the shifted real point.

Audit verdict:

- for
  `q := n + m`,
  `j := ⟨n, Nat.lt_add_of_pos_right hm⟩`,
  `x0 := xiShift j 0 y t`,
  `z0 := xiShift j 0 (fun k μ => (y k μ : ℂ)) (t : ℂ)`,
  the positivity-local theorem-(1) proof now reduces exactly to the missing
  binder
  `hrepr_boundary_shifted`;
- after rewriting
  `ForwardTube d q = TubeDomainSetPi (ForwardConeAbs d q)`,
  no further local neighborhood or flatten-transport theorem is missing at the
  positivity use site;
- the only upstream regularity theorem still involved is the already exposed
  exact-FL boundary continuity surface
  `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`,
  and it is expected to be consumed inside
  `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`,
  not as an extra theorem-(1)-local hypothesis.

Exact smallest missing theorem/ingredient after this pass:

- `hrepr_boundary_shifted :
    bvt_F OS lgc q z0 =
      fourierLaplaceExtMultiDim
        (ForwardConeFlat d q)
        hCflat_open hCflat_conv hCflat_cone hCflat_salient
        Tflat ((flattenCLEquiv q (d + 1)) z0)`.

Supervision correction (`2026-04-16`, canonical-ray consumer audit for blocker A):
this pass is docs-only.

Exact theorem chain audited:

- the unresolved seam theorem at
  [OSToWightmanPositivity.lean:4583](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4583)
  produces full `ContinuousWithinAt` of raw `bvt_F` at the shifted real point;
- that is transported through
  lines `4601`, `4630`, and `4686`;
- the first actual downstream consumer is the coefficient theorem at
  [OSToWightmanPositivity.lean:4736](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4736).

Exact limit shape consumed at blocker A:

- for fixed
  `q := n + m`,
  `j := ⟨n, Nat.lt_add_of_pos_right hm⟩`,
  `η0 := canonicalForwardConeDirection (d := d) q`,
  `y : NPointDomain d q`,
  `t : ℝ`,
  the only limit used is
  `Filter.Tendsto
      (fun ε : ℝ =>
        bvt_F OS lgc q
          (xiShift j 0
            (fun k μ => ↑(y k μ) + ε * ↑(η0 k μ) * Complex.I)
            (t : ℂ)))
      (nhdsWithin (0 : ℝ) (Set.Ioi 0))
      (nhds 0)`.

Audit verdict:

- blocker A does **not** require arbitrary within-tube continuity on the exact
  current branch;
- full `ContinuousWithinAt` was stronger-than-needed packaging;
- the new SCV geometric ray theorem is relevant because it already supplies the
  domain-side approach filter for the precise canonical net used at line
  `4736`.

What still remains missing:

- a boundary-regularity upgrade for the same witness `Tflat` along that one
  canonical ray;
- plus the already-tracked shifted real-point identification at
  `z0 = xiShift j 0 (fun k μ => (y k μ : ℂ)) (t : ℂ)`.

Exact smallest missing theorem/ingredient after this pass:

- a witness-local pathwise theorem, not full neighborhood continuity:
  `Filter.Tendsto
      (fun ε : ℝ =>
        bvt_F OS lgc q
          (xiShift j 0
            (fun k μ => ↑(y k μ) + ε * ↑(η0 k μ) * Complex.I)
            (t : ℂ)))
      (nhdsWithin (0 : ℝ) (Set.Ioi 0))
      (nhds (bvt_F OS lgc q z0))`;
- after the separate boundary-value computation of `bvt_F OS lgc q z0`, this
  is exactly what the coefficient theorem needs.

Why no Lean theorem was added:

- adding a local wrapper theorem for that same path with `sorry` would still
  just restate the current mathematical gap;
- no compile-clean theorem smaller than the existing ray geometry was available
  without proving new boundary regularity.

Supervision correction (`2026-04-16`, bounded SCV supplier decomposition audit for blocker A):
this pass lands one tiny compile-clean geometric ingredient, but blocker A
itself remains unchanged.

Exact source ingredients rechecked:

- `fourierLaplaceExtMultiDim_continuousOn` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3204](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3204);
- `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241);
- `fourierLaplaceExtMultiDim_holomorphic` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3256](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3256);
- `SCV.tubeDomain_isOpen` at
  [OSReconstruction/SCV/TubeDomainExtension.lean:71](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/TubeDomainExtension.lean:71);
- the repeated inline path proof in `LaplaceSchwartz.lean` / `LocalBoundaryRecovery.lean`
  showing `ε ↦ x + i ε η` tends to `nhdsWithin (realEmbed x) (TubeDomain C)`.

Compile-clean ingredient landed:

- `realPlusIEpsEta_tendsto_nhdsWithin_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3510](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3510);
- this isolates only the geometry of the canonical boundary ray and is strictly
  weaker than any `ContinuousWithinAt` statement for the Fourier-Laplace
  extension itself.

Audit verdict:

- yes, the SCV supplier theorem decomposes slightly at the geometry level;
- no, that decomposition does not lower the first genuinely new mathematical
  theorem beneath blocker A;
- the first honest new SCV theorem is still
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`, because no checked
  nearby theorem upgrades one-ray approach control to full within-tube boundary
  continuity at `SCV.realEmbed x`.

Exact smallest missing theorem after this pass:

- `ContinuousWithinAt
    (fourierLaplaceExtMultiDim C hC_open hC_conv hC_cone hC_salient T)
    (SCV.TubeDomain C)
    (SCV.realEmbed x)`.

Supervision correction (`2026-04-16`, exact SCV/consumer binder audit for blocker A):
this pass is docs-only.

Exact nearest SCV theorem surface:

- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241)
  takes only
  `C`, `hC_open`, `hC_conv`, `hC_cone`, `hC_salient`,
  `T`, `hT_support`, `x`
  and proves continuity of the exact Fourier-Laplace extension at
  `SCV.realEmbed x`;
- the adjacent surfaces are
  `fourierLaplaceExtMultiDim_continuousOn` at line `3204`
  and
  `fourierLaplaceExtMultiDim_holomorphic` at line `3256`.

Exact binder match with the live consumer:

- the consumer theorem
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688)
  already has the right shape:
  same `Tflat`, same cone/support data, one interior identity on
  `ForwardTube d q`, plus one boundary equality at the chosen real point;
- the private witness theorem
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233)
  already supplies everything except that final boundary equality;
- after rewriting `ForwardTube d q` as
  `TubeDomainSetPi (ForwardConeAbs d q)`, its `hrepr_tube` binder matches.

Exact single missing antecedent:

- for
  `q := n + m`,
  `j := ⟨n, Nat.lt_add_of_pos_right hm⟩`,
  `x0 := xiShift j 0 y t`,
  `z0 := xiShift j 0 (fun k μ => (y k μ : ℂ)) (t : ℂ)`,
  the only unsupplied binder is
  `hrepr_boundary_shifted :
    bvt_F OS lgc q z0 =
      fourierLaplaceExtMultiDim
        (ForwardConeFlat d q)
        hCflat_open hCflat_conv hCflat_cone hCflat_salient
        Tflat (SCV.realEmbed (flattenCLEquivReal q (d + 1) x0))`;
- equivalently, after the obvious point/flattening identifications,
  `bvt_F OS lgc q z0 =
    fourierLaplaceExtMultiDim ... Tflat ((flattenCLEquiv q (d + 1)) z0)`.

Why no compile-clean Lean step was added:

- any private theorem statement or local skeleton at the insertion surface
  would still just expose the same missing `hrepr_boundary_shifted`;
- that would be wrapper drift, not a smaller honest subproblem.

Supervision correction (`2026-04-16`, witness-local binder recheck for blocker A):
this pass is docs-only.

Exact insertion point rechecked:

- [OSToWightmanBoundaryValueLimits.lean:233](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233),
  immediately below
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`
  and before `sum_over_flat_timeSlots`;
- the surrounding file already carries `variable {d : ℕ} [NeZero d]`, so the
  future theorem should inherit that context.

Exact future theorem shape:

- keep the flattened witness explicit:
  `OS`, `lgc`, `hm`, `t`, `y`,
  `Tflat`, `hCflat_open`, `hCflat_conv`, `hCflat_cone`, `hCflat_salient`,
  `hTflat_support`, `hTflat_W`, `hrepr_tube`;
- conclude only the raw shifted equality at
  `z0 = xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      (fun k μ => (y k μ : ℂ)) (t : ℂ)`.

Why no Lean theorem statement was added:

- a theorem with exactly those binders would still just restate blocker A;
- a smaller witness-local theorem is not available: the only compile-clean
  items left at that surface are administrative aliases or witness repackaging,
  which are wrapper drift and do not shrink the real proof obligation;
- `hTflat_W` remains part of the signature sketch because any honest proof is
  still expected to pass through the boundary-distribution identification, not
  just the interior FL formula.

First missing proof obligation:

- upgrade `bvt_boundary_values` from test-function convergence to raw
  evaluation at the shifted real point for the same witness `Tflat`,
  using `hTflat_W` and `hrepr_tube`;
- this remains the first genuine mathematical subproblem beneath blocker A.

Supervision correction (`2026-04-16`, witness-local insertion-surface audit for blocker A):
this pass is docs-only.

Exact insertion point:

- the only honest source-local landing surface remains
  [OSToWightmanBoundaryValueLimits.lean:233](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233),
  immediately below
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`
  and above the next unrelated local theorem block.

Exact future theorem shape:

- do not reintroduce an existential witness wrapper;
- the future theorem should take the already-fixed witness package explicitly:
  `OS`, `lgc`, `hm`, `t`, `y`,
  `Tflat`, `hCflat_open`, `hCflat_conv`, `hCflat_cone`, `hCflat_salient`,
  `hTflat_support`, `hTflat_W`, `hrepr_tube`,
  and conclude the raw shifted equality at
  `z0 = xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      (fun k μ => (y k μ : ℂ)) (t : ℂ)`.

Why no Lean theorem statement was added:

- a bare statement with those binders would still be exactly blocker A;
- a theorem that bundles the witness again as `∃ Tflat, ... ∧ shiftedEquality`
  would not shrink the proof surface and would violate the no-wrapper rule.

First missing proof obligation:

- upgrade the available weak boundary-value theorem
  `bvt_boundary_values`
  from test-function convergence to raw evaluation at the shifted real point
  for the same witness `Tflat`;
- this categorical upgrade is the first genuine mathematical subproblem, and
  no smaller compile-clean theorem statement presently isolates less than that.

Supervision correction (`2026-04-16`, minimal missing-theorem sizing pass at blocker A):
this pass is docs-only.

Exact theorem statements checked:

- `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point` at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688)
  still requires the raw boundary equality
  `F (fun k μ => (x k μ : ℂ)) =
    fourierLaplaceExtMultiDim ... Tflat
      (SCV.realEmbed (flattenCLEquivReal n (d + 1) x))`
  as an explicit hypothesis;
- the immediately adjacent recovery/uniqueness layer,
  `boundary_value_recovery_forwardTube_of_flatRegular`,
  `distributional_uniqueness_forwardTube_of_flatRegular`, and
  `boundary_value_recovery_forwardTube_of_flatRegular_from_bv`
  at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:752),
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:799),
  and
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:1105),
  never leave the distribution/test-function level.

Minimal missing theorem shape now pinned:

- after unpacking the private witness theorem at `q := n + m`, the exact next
  theorem needed is
  `bvt_F OS lgc q z0 =
    fourierLaplaceExtMultiDim
      (ForwardConeFlat d q)
      hCflat_open hCflat_conv hCflat_cone hCflat_salient
      Tflat ((flattenCLEquiv q (d + 1)) z0)`
  for
  `z0 = xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      (fun k μ => (y k μ : ℂ)) (t : ℂ)`;
- this is the smallest honest theorem that bridges blocker A without wrappers,
  fake density, or widening back to theorem (2).

Minimal additional hypothesis package:

- no new general regularity wrapper is source-justified;
- the available package already contains the same witness `Tflat`, cone/support
  data, and the interior exact FL identity from
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`;
- the only genuinely new ingredient is a boundary-trace upgrade theorem for that
  same witness-local context at the shifted real point.

Best file location:

- witness-local in
  `OSToWightmanBoundaryValueLimits.lean`, not as a new general theorem in
  `ForwardTubeDistributions.lean`;
- reason:
  the general forward-tube file already has the exact consumer theorem, while
  the missing supplier depends on the private `Tflat` witness and the current
  OS-side shifted-point data.

Supervision correction (`2026-04-16`, bounded source audit just above blocker A):
this pass is docs-only.

Exact theorem surfaces rechecked:

- `bvt_boundary_values` at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValuesBase.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValuesBase.lean:360)
  gives only weak BV convergence after pairing with a Schwartz test;
- `bv_zero_point_is_evaluation` at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValuesComparison.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValuesComparison.lean:167)
  is a degree-`0` normalization theorem for `W_0`, not a boundary-trace theorem
  for `bvt_F`;
- `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr` at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233)
  supplies the fixed witness `Tflat` and the interior FL identity only;
- `boundary_value_recovery_forwardTube_of_flatRegular_from_bv` at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:1105)
  remains test-function level;
- `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point` at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688)
  still asks for the exact raw boundary equality as input.

Exact blocker-A obstruction:

- no current theorem specializes to the needed raw shifted-point equality for
  `bvt_F` and the same `Tflat`;
- the mismatch is categorical: weak boundary-value convergence and `W_0`
  evaluation do not upgrade to pointwise equality of `bvt_F` with the
  Fourier-Laplace trace at `z0`.

Supervision correction (`2026-04-16`, bounded source audit of upstream boundary-value theorems on blocker A):
this pass is docs-only.

Exact upstream theorem surfaces checked:

- `bvt_boundary_values`:
  `∀ f η, InForwardCone d n η →
    Tendsto
      (fun ε =>
        ∫ x, bvt_F OS lgc n (fun k μ => ↑(x k μ) + ε * ↑(η k μ) * Complex.I) * f x)
      (nhdsWithin 0 (Set.Ioi 0))
      (nhds (bvt_W OS lgc n f))`
  at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValuesBase.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValuesBase.lean:360);
- `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`:
  same-witness interior identity
  `∀ z ∈ TubeDomainSetPi C,
    bvt_F OS lgc n z =
      fourierLaplaceExtMultiDim Cflat ... Tflat (flattenCLEquiv n (d + 1) z)`
  at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233);
- `boundary_value_recovery_forwardTube_of_flatRegular_from_bv`:
  only
  `T f = ∫ x, F (fun k μ => (x k μ : ℂ)) * f x`
  at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:1105);
- `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`:
  still consumes the raw boundary equality at the chosen real point as an
  explicit hypothesis
  at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688).

Exact obstruction:

- no checked theorem specializes to
  `bvt_F OS lgc q z0 =
    fourierLaplaceExtMultiDim ... Tflat ((flattenCLEquiv q (d + 1)) z0)`;
- the mismatch is theorem-category, not indexing: available source gives
  distributional convergence / equality after pairing with Schwartz tests,
  while blocker A needs raw point evaluation at the shifted real point for that
  same witness `Tflat`.

Supervision correction (`2026-04-16`, exact source-binder audit for blocker A on the live complement seam):
this pass is docs-only.

Exact audited theorem shapes from source:

- `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`
  at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688)
  consumes one fixed witness `Tflat`, cone geometry, dual-cone support,
  `x : NPointDomain d n`, the interior identity on all `z ∈ ForwardTube d n`,
  and one boundary equality at the chosen real point; it concludes
  `ContinuousWithinAt F (ForwardTube d n) (fun k μ => (x k μ : ℂ))`;
- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`
  at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241)
  consumes only cone geometry, `T`, dual-cone support, and
  `x : Fin m → ℝ`; it concludes
  `ContinuousWithinAt
    (fourierLaplaceExtMultiDim C hC_open hC_conv hC_cone hC_salient T)
    (SCV.TubeDomain C)
    (SCV.realEmbed x)`;
- both remain theorem-level `sorry`s in current source.

Exact blocker-A composition:

- set
  `q := n + m`,
  `j := ⟨n, Nat.lt_add_of_pos_right hm⟩`,
  `x0 : NPointDomain d q := xiShift j 0 y t`,
  `z0 := xiShift j 0 (fun k μ => (y k μ : ℂ)) (t : ℂ)`;
- unpack the private witness theorem
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233)
  to obtain
  `⟨Tflat, hCflat_open, hCflat_conv, hCflat_cone, hCflat_salient,
    hTflat_support, _hTflat_W, hrepr_tube⟩`;
- the exact boundary-point consumer then needs only
  `(fun k μ => (x0 k μ : ℂ)) = z0`,
  `SCV.realEmbed (flattenCLEquivReal q (d + 1) x0) =
    (flattenCLEquiv q (d + 1)) z0`,
  and
  `hrepr_boundary_shifted :
    bvt_F OS lgc q z0 =
      fourierLaplaceExtMultiDim
        (ForwardConeFlat d q) hCflat_open hCflat_conv hCflat_cone hCflat_salient
        Tflat ((flattenCLEquiv q (d + 1)) z0)`;
- with those in hand, the forward-tube theorem yields
  `ContinuousWithinAt (bvt_F OS lgc q) (ForwardTube d q) z0`.

Exact blocker split after this audit:

- witness theorem already supplies:
  `Tflat`, support, cone geometry, and `hrepr_tube`;
- SCV theorem, if genuinely proved, supplies:
  continuity of that exact Fourier-Laplace extension at the flattened shifted
  real point;
- still genuinely missing:
  the raw shifted boundary equality `hrepr_boundary_shifted`.

Supervision correction (`2026-04-16`, exact blocker-A theorem-surface refinement from current source):
this pass is docs-only.

Exact audited source anchors:

- consumer theorem:
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688);
- SCV supplier theorem:
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241);
- private flattened witness theorem:
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:233);
- live complement-side consumer `sorry`:
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4583).

Exact audited theorem statements:

- `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`
  assumes one fixed flattened witness `Tflat`, dual-cone support, one real
  point `x`, the interior identity on all `z ∈ ForwardTube d n`, and one exact
  boundary equality at that chosen point; it concludes
  `ContinuousWithinAt F (ForwardTube d n) (fun k μ => (x k μ : ℂ))`.
- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`
  assumes only cone geometry, dual-cone support for `T`, and `x : Fin m → ℝ`;
  it concludes
  `ContinuousWithinAt
    (fourierLaplaceExtMultiDim C hC_open hC_conv hC_cone hC_salient T)
    (SCV.TubeDomain C)
    (SCV.realEmbed x)`.
- both remain theorem-level `sorry`s in current source.

Exact composition blueprint at blocker A:

- set
  `q := n + m`,
  `j := ⟨n, Nat.lt_add_of_pos_right hm⟩`,
  `x0 : NPointDomain d q := xiShift j 0 y t`,
  `z0 := xiShift j 0 (fun k μ => (y k μ : ℂ)) (t : ℂ)`;
- unpack the private witness theorem at `q` to obtain
  `⟨Tflat, hCflat_open, hCflat_conv, hCflat_cone, hCflat_salient,
    hTflat_support, _hTflat_W, hrepr_tube⟩`;
- the flattened real point matches exactly:
  `SCV.realEmbed (flattenCLEquivReal q (d + 1) x0) =
    (flattenCLEquiv q (d + 1)) z0`;
- therefore the forward-tube consumer would apply to `F := bvt_F OS lgc q`
  once the single extra hypothesis
  `bvt_F OS lgc q z0 =
    fourierLaplaceExtMultiDim
      (ForwardConeFlat d q) hCflat_open hCflat_conv hCflat_cone hCflat_salient
      Tflat ((flattenCLEquiv q (d + 1)) z0)`
  is available.

Exact blocker split after this audit:

- already supplied by the private witness theorem:
  `Tflat`, support, cone geometry, and `hrepr_tube`;
- supplied by the SCV theorem if genuinely proved:
  continuity of the exact Fourier-Laplace extension at
  `(flattenCLEquiv q (d + 1)) z0`;
- still genuinely missing:
  the raw shifted-point boundary equality for `bvt_F` at `z0`.

Supervision correction (`2026-04-16`, bounded source-pinned composition refinement for blocker A):
no Lean edits; this pass is docs-only.

Exact audited theorem statements:

- `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`
  requires one fixed flattened witness `Tflat`, its dual-cone support, the
  exact interior representation on `ForwardTube d n`, and one exact boundary
  equality at the chosen real point `x : NPointDomain d n`; it concludes
  `ContinuousWithinAt F (ForwardTube d n) (fun k μ => (x k μ : ℂ))`.
- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`
  gives boundary continuity of the exact Fourier-Laplace extension at
  `SCV.realEmbed x` from dual-cone support data alone.
- both are still theorem-level `sorry`s in current source.

Exact composition blueprint at the live shifted point:

- in blocker A set
  `q := n + m`,
  `j := ⟨n, Nat.lt_add_of_pos_right hm⟩`,
  `x0 : NPointDomain d q := xiShift j 0 y t`,
  `z0 := xiShift j 0 (fun k μ => (y k μ : ℂ)) (t : ℂ)`;
- unpack
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`
  at `q` to obtain
  `⟨Tflat, hCflat_open, hCflat_conv, hCflat_cone, hCflat_salient,
    hTflat_support, _hTflat_W, hrepr_tube⟩`;
- use the definitional identity
  `SCV.realEmbed (flattenCLEquivReal q (d + 1) x0) =
    (flattenCLEquiv q (d + 1)) z0`;
- then the forward-tube theorem would apply to `F := bvt_F OS lgc q` once the
  single extra hypothesis
  `bvt_F OS lgc q z0 =
    fourierLaplaceExtMultiDim
      (ForwardConeFlat d q) hCflat_open hCflat_conv hCflat_cone hCflat_salient
      Tflat ((flattenCLEquiv q (d + 1)) z0)`
  is available.

Exact blocker split after this audit:

- witness theorem already supplies:
  `Tflat`, cone geometry, support, and `hrepr_tube`;
- SCV continuity theorem, if genuinely proved, supplies:
  continuity of
  `fourierLaplaceExtMultiDim
    (ForwardConeFlat d q) hCflat_open hCflat_conv hCflat_cone hCflat_salient Tflat`
  within `SCV.TubeDomain (ForwardConeFlat d q)` at
  `(flattenCLEquiv q (d + 1)) z0`;
- still missing:
  the raw shifted-point boundary equality for `bvt_F` at `z0`.

Supervision correction (`2026-04-16`, bounded shifted-boundary theorem-sizing pass):
no honest Lean edit survived; this pass is docs-only.

Exact audit refinement:

- the mathematically correct file for the next theorem-sized step is
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean`,
  not `OSToWightmanPositivity.lean`, because the relevant
  `Tflat` witness is only available there from the private theorem
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`;
- the correct local proof context is immediately after unpacking that witness
  theorem for `n + m`;
- intended missing theorem name:
  `section43_shifted_boundary_agreement_of_flattened_bvt_F_witness`;
- intended statement:
  for
  `z0 = xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      (fun k μ => (y k μ : ℂ)) (t : ℂ)`,
  prove
  `bvt_F OS lgc (n + m) z0 =
    fourierLaplaceExtMultiDim
      (ForwardConeFlat d (n + m))
      hCflat_open hCflat_conv hCflat_cone hCflat_salient
      Tflat ((flattenCLEquiv (n + m) (d + 1)) z0)`.

Exact reason no safe skeleton was landed:

- a theorem in `OSToWightmanPositivity.lean` cannot even state the witness-level
  equality honestly, because the witness theorem is private upstream;
- a theorem in `OSToWightmanBoundaryValueLimits.lean` with the full witness data
  exposed but no proof would only repackage the already-known missing
  hypothesis from
  `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`;
- no current source theorem upgrades `bvt_boundary_values` to this raw
  pointwise identity, so there is no honest proof term to attach.

Supervision correction (`2026-04-16`, bounded source verification of the shifted raw boundary-point seam):
no honest Lean edit survived; this pass is docs-only.

Exact audit refinement:

- exact shifted point in the live theorem:
  `z0 = xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      (fun k μ => (y k μ : ℂ)) (t : ℂ)`;
- verified current source still has no theorem proving
  `bvt_F OS lgc (n + m) z0 =
    fourierLaplaceExtMultiDim ... Tflat
      ((flattenCLEquiv (n + m) (d + 1)) z0)`
  for the witness `Tflat` coming from
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`;
- the interior equality needed by the forward-tube transfer theorem is present
  in that private witness theorem;
- the boundary-point equality needed by the same transfer theorem is the exact
  missing input;
- both the SCV continuity supplier
  `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`
  and the forward-tube transfer theorem
  `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`
  remain theorem-level `sorry`s in current source and must not be consumed as
  landed results.

Exact missing theorem surface:

- target file:
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean`;
- intended theorem:
  specialize the private flattened witness to the shifted real point `z0` and
  prove the exact boundary equality above, or equivalently package the direct
  consequence
  `ContinuousWithinAt (bvt_F OS lgc (n + m)) (ForwardTube d (n + m)) z0`
  once the exact FL continuity supplier is genuinely available.

Exact reason blocker A still does not land:

- `bvt_boundary_values` is distributional only; it produces test-function
  convergence to `bvt_W`, not raw pointwise equality of `bvt_F` with the
  Fourier-Laplace boundary trace at `z0`.

Supervision correction (`2026-04-16`, bounded source-first check of the raw shifted-point consumer):
no honest Lean edit survived; this pass is docs-only.

Exact audit refinement:

- the exact transfer theorem already exists in source:
  `ForwardTubeDistributions.boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`;
- the private witness theorem
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`
  already supplies the flattened dual-cone witness and the exact interior
  `fourierLaplaceExtMultiDim` representation needed by that transfer theorem;
- the missing hypothesis is now pinned precisely as the boundary-point equality
  at the shifted raw real point:
  `bvt_F ... z0 =
    fourierLaplaceExtMultiDim ... Tflat (flattenCLEquiv ... z0)`;
- no currently found theorem in the Wick-rotation / SCV source graph proves
  that equality from the available weak boundary-value package.

Exact reason blocker A still does not land:

- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` is still one
  unresolved source theorem;
- but even after that theorem is landed, blocker A still needs the separate
  boundary-point agreement theorem above before the transfer theorem can be
  applied to raw `bvt_F`;
- `bvt_boundary_values` is distributional only and does not identify raw
  boundary point values.

Exact missing theorem surface now pinned:

- file:
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean`
  or a nearby forward-tube consumer file;
- statement shape:
  specialize the private flattened witness theorem to the shifted real point
  and prove the exact raw boundary equality with the same Fourier-Laplace trace,
  or directly package the consequence
  `ContinuousWithinAt (bvt_F ...) (ForwardTube d (n + m)) z0`.

Supervision correction (`2026-04-16`, bounded source-first check of the exact FL consumer layer):
no honest Lean edit survived; this pass is docs-only.

Exact audit correction:

- the exact missing consumer theorem is now source-pinned not just by intended
  file, but by an already-existing theorem surface:
  `ForwardTubeDistributions.boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`;
- exact audited statement shape:
  if `F` agrees on `ForwardTube d n` with the flattened exact
  `fourierLaplaceExtMultiDim ... Tflat` coming from a dual-cone witness, and if
  `F` also agrees with that same exact FL trace at the chosen real point, then
  `F` is `ContinuousWithinAt` the forward tube at that point.

Exact blocker refinement:

- this consumer theorem still does not land automatically, because its natural
  SCV input theorem
  `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`
  is itself still a theorem-level `sorry` in current source;
- therefore the live seam is now accurately classified as a two-node exact-FL
  boundary-continuity frontier:
  1. continuity of the exact Fourier-Laplace extension at `realEmbed x`;
  2. transfer of that continuity to a raw boundary function once interior and
     boundary-point agreement with the exact extension are supplied.

Exact route consequence:

- no theorem downstream of `HasFourierLaplaceReprRegular`,
  `HasFourierLaplaceReprTempered`, or local boundary recovery should be treated
  as a source theorem for this seam;
- the next honest Lean work must land one of the two exact theorem surfaces
  above, not widen to theorem (2) or repackage tempered data.

Supervision correction (`2026-04-16`, bounded exact-FL boundary-continuity skeleton landing):
one honest smaller theorem surface now survives in Lean:
`SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` in
`OSReconstruction/SCV/PaleyWienerSchwartz.lean`.

Exact audit consequence:

- the exact SCV subproblem beneath blocker A is now source-pinned as boundary
  continuity of the **exact** Fourier-Laplace extension built from dual-cone
  support data;
- this is the strongest honest consumer theorem available directly from the
  private witness package for flattened `bvt_F`.

Exact sharpened caveat:

- the private witness theorem
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`
  only identifies `bvt_F` with that Fourier-Laplace extension on the interior
  of the forward tube;
- therefore interior representation plus support still does not by itself imply
  `ContinuousWithinAt` for `bvt_F` at a real boundary point;
- one further boundary-agreement step remains necessary unless the eventual
  theorem upgrades all the way to `HasFourierLaplaceReprRegular`.

Supervision correction (`2026-04-16`, bounded raw-`bvt_F` shifted-point source refinement):
no honest Lean edit survived; this pass is docs-only.

Exact audit consequence:

- blocker A remains
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  in `OSToWightmanPositivity.lean`;
- the sharper source verdict is now:
  the missing theorem is not merely an unspecified SCV continuity statement,
  but the absent upgrade from an **already available flattened dual-cone
  Fourier-Laplace representation** of `bvt_F` to the regular package or its
  direct continuity consequence.

Exact source pin:

- `OSToWightmanBoundaryValueLimits.lean` private theorem
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`
  already provides the strong input:
  `Tflat`, `HasFourierSupportInDualCone Cflat Tflat`, and the exact
  `fourierLaplaceExtMultiDim` representation of flattened `bvt_F`;
- `SCV/LaplaceSchwartz.lean` explicitly still lacks the deep upgrade theorem
  from genuine Fourier-Laplace support data to
  `HasFourierLaplaceReprRegular`;
- `ForwardTubeDistributions.boundary_function_continuous_forwardTube_of_flatRegular`
  remains downstream of that absent upgrade.

Exact smaller theorem/skeleton now indicated:

- file:
  `OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean`
- statement shape:
  from a flattened dual-cone support witness `Tflat` and the exact flattened
  Fourier-Laplace representation of `F`, derive
  `SCV.HasFourierLaplaceReprRegular (ForwardConeFlat d n)
    (F ∘ (flattenCLEquiv n (d + 1)).symm)`,
  or at minimum derive the boundary consequence
  `∀ x, ContinuousWithinAt F (ForwardTube d n) (fun k μ => (x k μ : ℂ))`.

Exact reason blocker A still does not land:

- the local shell route in `OSToWightmanPositivity.lean` only reconstructs a
  tempered boundary-value package;
- the only exported forward-tube continuity theorem still needs regular input;
- the strong FL representation theorem for `bvt_F` is private and presently has
  no public consumer that outputs `ContinuousWithinAt`.

Supervision correction (`2026-04-16`, bounded raw-`bvt_F` shifted-point skeleton landing):
one honest Lean reduction step now survives beneath blocker A. The old
unflattened pointwise continuity theorem is proved from a new smaller raw
continuity theorem surface, which now carries the only live `sorry` on this
subseam.

Exact audit consequence:

- newly landed theorem-level blocker:
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  in `OSToWightmanPositivity.lean`;
- exact reduced statement:
  for `y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`, prove
  `ContinuousWithinAt (bvt_F OS lgc (n + m)) (ForwardTube d (n + m))
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      (fun k μ => (y k μ : ℂ)) (t : ℂ))`;
- proved reduction:
  `section43_fixedTimeShellRaw_pointwiseContinuity_on_section43PositiveEnergyRegion_compl`
  now follows by applying
  `section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`
  to that new raw theorem.

Exact blocker classification after this pass:

- blocker A is no longer the shell continuation theorem itself;
- blocker A is now exactly raw `bvt_F` boundary continuity at the shifted
  complement-side real point;
- the missing mathematics is still SCV boundary regularity, not local recovery,
  not flatten transport, and not affine shell transport.

Verification note:

- a bounded `timeout 30s lake env lean
  OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`
  run did not return diagnostics before the harness stopped yielding, so no
  completed file check is claimed on this pass.

Supervision correction (`2026-04-16`, bounded shifted-point reduction landing):
one honest Lean sub-step now survives beneath theorem (1): the unflattened
pointwise supplier itself now reduces to raw `bvt_F` continuity at the shifted
real boundary point.

Exact audit consequence:

- newly landed helper theorem:
  `section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`
  in `OSToWightmanPositivity.lean`;
- proved reduction:
  continuity of the shell continuation
  `z ↦ bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 z (t : ℂ))`
  at `y` now follows from `ContinuousWithinAt` of raw `bvt_F` at the shifted
  real point
  `xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 (fun k μ => (y k μ : ℂ)) (t : ℂ)`;
- therefore affine `xiShift` transport is no longer part of blocker A.

Exact blocker classification after this pass:

- blocker A is now raw pointwise boundary continuity of `bvt_F` on
  `ForwardTube d (n + m)` at the shifted complement-side real point;
- the missing mathematics is still boundary regularity, not local recovery,
  flattening transport, or affine shell transport;
- no regularity upgrade theorem was added.

Supervision correction (`2026-04-16`, bounded pointwise-supplier transport landing):
one honest Lean sub-step now survives beneath theorem (1): the flat pointwise
supplier is proved by transport from a smaller unflattened theorem surface.

Exact audit consequence:

- newly landed theorem-level blocker:
  `section43_fixedTimeShellRaw_pointwiseContinuity_on_section43PositiveEnergyRegion_compl`
  in `OSToWightmanPositivity.lean`;
- proved reduction:
  `section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`
  now follows from that theorem by composing with
  `(flattenCLEquiv (n + m) (d + 1)).symm` and using
  `flattenCLEquiv_symm_mem_tubeDomainSetPi_of_mem_tubeDomain_image`;
- therefore flattening transport is no longer part of blocker A.

Exact blocker classification after this pass:

- blocker A is now the unflattened pointwise boundary continuity theorem on
  `ForwardTube d (n + m)`;
- the missing mathematics is still boundary regularity, not local recovery and
  not neighborhood transport;
- no regularity upgrade theorem was added.

Supervision correction (`2026-04-16`, bounded theorem-(1) decomposition landing):
theorem (1) is now partially discharged in Lean. The complement-side
neighborhood package is proved by taking the whole flattened complement image as
`Uflat`; the first remaining theorem-level blocker is the smaller pointwise
continuity supplier
`section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`
immediately above theorem (1) in
`OSToWightmanPositivity.lean`.

Exact audit consequence:

- blocker A is no longer the existence/open/subset packaging of `Uflat`;
- blocker A is now exactly:
  `xflat ∈ (flattenCLEquivReal (n + m) (d + 1)) ''
    ((section43PositiveEnergyRegion d (n + m))ᶜ)
    ⟹ ContinuousWithinAt G_t
      (SCV.TubeDomain (ForwardConeFlat d (n + m)))
      (SCV.realEmbed xflat)`;
- theorem (1) now reduces honestly to this pointwise continuity statement plus
  openness of the flattened complement image.

Supervision correction (`2026-04-16`, bounded theorem-(1) skeleton landing):
the local flat complement-side continuity package is now explicit in Lean source
as
`section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
in
`OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`.
The statement matches the theorem-(1) contract audited below; the proof remains
the live theorem-level blocker. So theorem (1) is no longer merely docs-pinned,
but theorem (2) remains blocked on this exact `sorry`.

Supervision correction (`2026-04-16`, bounded complement-side boundary-regularity audit):
no honest Lean edit survived; this pass was docs-only.

Exact candidate source theorems inspected:

- `ForwardTubeDistributions.boundary_function_continuous_forwardTube_of_flatRegular`;
- `SCV.HasFourierLaplaceReprRegular.restrict`;
- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`;
- `SCV.LocalBoundaryRecovery.pointwise_convergence_local`;
- `SCV.exists_fourierLaplaceReprTempered`;
- `SCV.tube_boundaryValueData_of_polyGrowth`,
  `SCV.tube_boundaryValueData_of_polyGrowth'`,
  `SCV.tube_boundaryValue_of_vladimirov_growth`;
- `TubeBoundaryValueExistence.continuous_tubeSlice_ray_deriv`.

Exact audit verdict:

- `boundary_function_continuous_forwardTube_of_flatRegular` is the only
  inspected theorem that actually concludes boundary continuity, but it requires
  a flattened `HasFourierLaplaceReprRegular` package unavailable on this seam;
- `fourierLaplace_boundary_recovery_on_open_of_tempered` and
  `pointwise_convergence_local` are not theorem-(1) sources because they assume
  the local boundary continuity package `hcontU`;
- `exists_fourierLaplaceReprTempered` and the tube-boundary-value existence
  theorems build only distributional boundary-value data and growth control,
  not `ContinuousWithinAt` at real boundary points;
- `continuous_tubeSlice_ray_deriv` concerns continuity of the sliced integral in
  the positive ray parameter and does not speak about boundary regularity of the
  holomorphic function itself.

Exact blocker classification:

- theorem (1) is still absent in source;
- the first missing theorem is best classified as an SCV boundary-value
  continuity / local boundary-regularity theorem yielding
  `ContinuousWithinAt` on an open real set;
- it is not a missing local-boundary-recovery theorem and not a missing
  flattening transport theorem.

Supervision correction (`2026-04-16`, bounded theorem-(1) audit): no honest
Lean edit survived; this pass was docs-only.

Exact intended theorem-(1) statement:

- for the flattened fixed-time shell continuation
  `G_t zflat := bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))`
  and
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`,
  produce an open
  `Uflat` around `flattenCLEquivReal (n + m) (d + 1) y`,
  contained in the flattened complement image, together with
  `∀ xflat ∈ Uflat, ContinuousWithinAt G_t
    (SCV.TubeDomain (ForwardConeFlat d (n + m)))
    (SCV.realEmbed xflat)`.

Exact source theorems that almost supply it:

- `flattenCLEquivReal ... .toHomeomorph.isOpenMap`;
- `ForwardTubeDistributions.boundary_function_continuous_forwardTube_of_flatRegular`;
- `SCV.HasFourierLaplaceReprRegular.restrict`;
- `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`;
- `differentiableOn_flatten`;
- `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`;
- `section43_fixedTimeShellRaw_uniformPolyGrowth_fixedTime`;
- `SCV.exists_fourierLaplaceReprTempered`.

Exact reason it still fails:

- the complement-side neighborhood can be transported honestly by openness;
- the missing part is the continuity conclusion, because the available source
  only gives that conclusion from a regular flattened package, while the live
  seam currently constructs only a tempered package;
- the gap is therefore a regularity/continuity mismatch, sourced exactly at the
  absent upgrade from the fixed-direction shell bounds to
  `HasFourierLaplaceReprRegular`.

Blocker classification:

- not a missing local-neighborhood lemma;
- not a flattening transport lemma;
- yes: a continuity formulation mismatch driven by missing regularity.

Supervision correction (`2026-04-16`, bounded fixed-direction helper audit): no
honest Lean edit survived; this pass was docs-only.

Exact helper statement still needed below theorem (1):

- from `hUflat_open`, `hcontUflat`,
  `HasCompactSupport (fflat : _ → ℂ)`, and
  `tsupport (fflat : _ → ℂ) ⊆ Uflat`, derive
  `∃ M δ, 0 < δ ∧
    ∀ xflat ∈ tsupport (fflat : _ → ℂ), ∀ ε, 0 < ε → ε < δ →
      ‖G_t (fun i => ↑(xflat i) + ↑ε * ↑(η0flat i) * Complex.I)‖ ≤ M`.

Exact source theorems that almost supply it:

- `HasCompactSupport.isCompact`;
- `IsCompact.exists_bound_of_continuousOn`;
- `ForwardTubeDistributions.forwardConeFlat_posScale_closed`.

Exact reason the specialization still fails:

- the source currently provides only pointwise boundary continuity
  `ContinuousWithinAt G_t (SCV.TubeDomain ...) (SCV.realEmbed xflat)` at each
  real edge point `xflat ∈ Uflat`;
- there is still no public theorem upgrading that package to a `ContinuousOn`
  statement for the strip/path map on a compact subset of
  `ℝ × (Fin ((n + m) * (d + 1)) → ℝ)`;
- without that upgrade, `IsCompact.exists_bound_of_continuousOn` cannot be
  applied honestly to produce one common `M` and `δ`.

Blocker classification after this pass:

- blocker A: theorem (1) is still absent in Lean source;
- blocker B: once blocker A lands, the remaining obstruction is a missing
  continuity-to-compact-bound lemma;
- it is not a path-membership blocker, because
  `forwardConeFlat_posScale_closed` already supplies the fixed-direction tube
  membership once `η0flat ∈ ForwardConeFlat ...` is known;
- it is not an integrable-dominator packaging blocker, because
  `xflat ↦ M * ‖fflat xflat‖` is routine once `M` exists.

Supervision correction (`2026-04-16`, bounded theorem-(2) landing audit): no
honest Lean edit survived. This pass tightened the source status further:

- theorem (2) is still the correct next local theorem surface;
- but theorem (1), i.e. the explicit `Uflat`/`hcontUflat` package beneath the
  coefficient theorem, is still not landed in
  `OSToWightmanPositivity.lean`;
- therefore theorem (2) could not honestly be implemented this pass, even
  before addressing the DCT helper.

Exact source extraction that remains valid:

- `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` gives the weak
  product-coordinate boundary-value functional `T_t`;
- `ForwardTubeDistributions.schwartz_bv_to_flat_repr_dist_apply` gives the
  exact transport/rewrite of `Tflat`;
- the proof body of `SCV.LocalBoundaryRecovery.pointwise_convergence_local`
  remains the correct source template for local pointwise convergence on tests
  with `tsupport ⊆ Uflat`.

Exact insertion-site theorem-(2) statement remains:

- after defining `G_t`, `T_t`, `Tflat`, and `η0flat` exactly as above the live
  coefficient theorem, prove
  `∀ fflat, tsupport (fflat : _ → ℂ) ⊆ Uflat →
    HasCompactSupport (fflat : _ → ℂ) →
    Tflat fflat = ∫ xflat, G_t (SCV.realEmbed xflat) * fflat xflat`.

Exact smallest blocker, in the right order:

- blocker A:
  the theorem-(1) hypotheses `Uflat`, `hUflat_open`, `hyUflat`, `hUflat_sub`,
  `hcontUflat` are still absent from Lean source and only pinned in docs;
- blocker B:
  once blocker A is cleared, theorem (2) still needs one tiny local-bound
  helper converting theorem (1)'s open-set `ContinuousWithinAt` data and
  compact support into a fixed-direction uniform bound near the edge.

Supervision correction (`2026-04-16`, bounded theorem-(2) source audit): no
honest Lean edit survived. The source-backed verdict is now:

- theorem (2) is still the correct next local theorem surface;
- it cannot be proved by calling
  `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`, because that
  theorem requires `HasFourierLaplaceReprTempered`, whose `uniform_bound` field
  is unavailable on this seam outside the canonical direction;
- the surviving proof route is a direct weak-BV local recovery theorem on the
  flat side, specialized to the canonical flat approach direction and to
  compactly supported tests.

Exact extracted inputs already present in source:

- weak flat boundary-value functional:
  `Tflat fflat := T_t ((SchwartzMap.compCLMOfContinuousLinearEquiv ℂ
    (flattenCLEquivReal (n + m) (d + 1))) fflat)`,
  where `T_t` comes from
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`;
- exact rewrite back to product coordinates:
  `ForwardTubeDistributions.schwartz_bv_to_flat_repr_dist_apply`;
- local pointwise convergence pattern:
  the proof body of `SCV.LocalBoundaryRecovery.pointwise_convergence_local`.

Exact theorem-(2) consumer chain:

- theorem (1): `Uflat`, `IsOpen Uflat`, `hyUflat`, `hUflat_sub`, `hcontUflat`;
- theorem (2): supported-test identity for `Tflat`;
- rewrite via `schwartz_bv_to_flat_repr_dist_apply`;
- feed localizers back into
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`.

Exact smallest blocker exposed by source:

- one tiny helper theorem is still missing:
  for compact `K ⊆ Uflat`, the open-set hypotheses
  `hUflat_open` and `hcontUflat` should imply a fixed-direction uniform bound
  `∃ M δ, 0 < δ ∧ ∀ xflat ∈ K, ∀ ε, 0 < ε → ε < δ →
    ‖G_t (xflat + i ε η0flat)‖ ≤ M`;
- that helper is enough to finish theorem (2) by DCT together with the weak BV
  limit and local pointwise convergence;
- without it, no honest Lean DCT proof of theorem (2) is currently available.

Supervision correction (`2026-04-16`, bounded source-first local package audit):
the repeated audit claim "single local-trace theorem" is stale/incomplete. No
honest Lean theorem landed. The exact obstruction is now source-pinned:

- the flat consumer remains
  `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`;
- the live flattened continuation is
  `G_t zflat := bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))`;
- the weak flat BV witness is already available from
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`
  through `ForwardTubeDistributions.schwartz_bv_to_flat_repr`;
- but the all-directions `uniform_bound` field required by
  `HasFourierLaplaceReprTempered` is **not** available: the current fixed-time
  theorem `section43_fixedTimeShellRaw_uniformPolyGrowth_fixedTime` only bounds
  the canonical approach
  `ε • canonicalForwardConeDirection (d := d) (n + m)`.

Therefore the honest minimal frontier is a **two-theorem cluster** in
`OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
inserted immediately above the coefficient theorem:

1. a flat local boundary-trace theorem for `G_t` producing
   `Uflat`, `IsOpen Uflat`, membership of
   `flattenCLEquivReal (n + m) (d + 1) y`, containment in the flattened
   complement image, and
   `∀ xflat ∈ Uflat, ContinuousWithinAt G_t
      (SCV.TubeDomain (ForwardConeFlat d (n + m)))
      (SCV.realEmbed xflat)`;
2. a flat fixed-time local boundary-recovery theorem for the transported flat
   functional
   `Tflat fflat := T_t ((SchwartzMap.compCLMOfContinuousLinearEquiv ℂ
      (flattenCLEquivReal (n + m) (d + 1))) fflat)`,
   using that `hcontUflat` together with the already available weak BV data and
   the canonical flat direction
   `η0flat := flattenCLEquivReal (n + m) (d + 1)
      (canonicalForwardConeDirection (d := d) (n + m))`,
   and concluding the supported-test identity
   `Tflat fflat = ∫ xflat, G_t (SCV.realEmbed xflat) * fflat xflat`.

Audit consequence:

- verdict `(A)` fails because the previously advertised inline construction of
  `HasFourierLaplaceReprTempered` does not type-check against the real
  `uniform_bound` field in `SCV/LaplaceSchwartz.lean`;
- verdict `(C)` fails because no existing source theorem already gives the
  recovered local integral identity on supported tests;
- the surviving answer is `(B)` in substance, but the first theorem is not a
  generic wrapper transport lemma: it is a fixed-time flattened local recovery
  theorem specialized to the transported boundary functional and canonical
  direction.

Supervision correction (`2026-04-16`, bounded insertion-site fidelity audit):
no honest Lean theorem landed. The exact first missing theorem-sized bridge
belongs in
`OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`
as a `private theorem` inserted immediately above
`section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`,
not in `ForwardTubeDistributions.lean` and not in the SCV layer. Source reason:
the generic transport pieces are already present:

- `differentiableOn_flatten` gives the flattened holomorphy input;
- `schwartz_bv_to_flat_repr` and
  `schwartz_bv_to_flat_repr_dist_apply` already convert the product-coordinate
  CLM witness from
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` into the flat
  weak boundary-value functional and back;
- `SCV.exists_fourierLaplaceReprTempered` already packages the flat tempered
  representation once the existing growth inputs are supplied.

Therefore the first mathematically absent source object is still only the local
flat boundary continuity package for
`G_t zflat := bvt_F OS lgc (n + m)
  (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
    ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))`
near `flattenCLEquivReal (n + m) (d + 1) y`, with conclusion
`∀ xflat ∈ Uflat, ContinuousWithinAt G_t
  (SCV.TubeDomain (ForwardConeFlat d (n + m)))
  (SCV.realEmbed xflat)`.

Exact audit verdict: a two-theorem cluster is still not forced. Any extra
"transport theorem" would only rename the already available flatten steps, and
every weaker candidate still fails because the actual consumer,
`SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`, still needs this
open-set `hcontU` input.

Supervision correction (`2026-04-16`, bounded flatten-fidelity audit): audited
the exact consumer types beneath the live coefficient theorem. No honest Lean
theorem landed. The previous docs understated one source constraint: the local
recovery theorem actually available in repo/source,
`SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`, consumes a flat SCV
function `Fin k → ℂ`, not the raw product-coordinate shell continuation.

Exact audited correction:

- the live function entering the consumer is the flattened fixed-time shell
  continuation
  `G_t zflat := bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))`;
- the weak flat boundary-value package is already available from existing
  source:
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`
  `→` `ForwardTubeDistributions.schwartz_bv_to_flat_repr`;
- the tempered flat package is also already available inline:
  combine `differentiableOn_flatten`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `section43_fixedTimeShellRaw_uniformPolyGrowth_fixedTime`,
  and `SCV.exists_fourierLaplaceReprTempered`.

So the exact first missing theorem-sized package remains one theorem, but with
the corrected flat target:

- for fixed
  `y : NPointDomain d (n + m)` with
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`,
  produce an open neighborhood
  `Uflat ⊆ Fin ((n + m) * (d + 1)) → ℝ`
  of `flattenCLEquivReal (n + m) (d + 1) y`,
  contained in the flattened complement image, together with
  `∀ xflat ∈ Uflat, ContinuousWithinAt G_t
    (SCV.TubeDomain (ForwardConeFlat d (n + m)))
    (SCV.realEmbed xflat)`.

Exact immediate consumer chain after that one theorem:

- build `hTemperedFlat` inline;
- apply `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`;
- transport the recovered equality back to product coordinates via the existing
  flatten change-of-variables / `schwartz_bv_to_flat_repr_dist_apply` pattern;
- finish by the shrinking-localizer step at the fixed complement witness.

Exact reason a two-theorem cluster is **not** forced:

- the flat weak/tempered package does not require a new theorem; the repo
  already contains the needed transport and constructor surfaces;
- a separate forward-tube local-open transport theorem would be only a
  convenience wrapper around existing flatten equivalences, not the first
  mathematically missing bridge.

Supervision correction (`2026-04-16 03:22 UTC`, bounded audit pass): audited
the constructor-level frontier immediately below the live coefficient theorem.
No honest Lean theorem landed. The new exact audit result is that the missing
surface is still one local-trace theorem and not a cluster, because the
tempered package itself is already mechanically constructible from existing
source data:

- `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` gives the
  boundary functional `T_t`;
- the same theorem already contains the needed holomorphy proof for
  `F_t z := bvt_F OS lgc (n + m) (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 z (t : ℂ))`;
- `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth` and
  `section43_fixedTimeShellRaw_uniformPolyGrowth_fixedTime` provide exactly the
  `poly_growth` and `uniform_bound` inputs expected by
  `SCV.exists_fourierLaplaceReprTempered`.

So the exact first absent theorem-sized package is only:

- for fixed
  `y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`,
  produce
  `∃ U, IsOpen U ∧ y ∈ U ∧
    U ⊆ (section43PositiveEnergyRegion d (n + m))ᶜ ∧
    ∀ x ∈ U, ContinuousWithinAt F_t
      (TubeDomainSetPi (ForwardConeAbs d (n + m))) (SCV.realEmbed x)`.

Exact consumer chain after that one theorem:

- assemble
  `hTempered : HasFourierLaplaceReprTempered (ForwardConeAbs d (n + m)) F_t`
  using `SCV.exists_fourierLaplaceReprTempered`;
- feed `hTempered` and the new local trace theorem into
  `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`;
- recover the local representation of `T_t` on Schwartz tests supported in `U`;
- then the shrinking-localizer argument is an ordinary local evaluation step at
  `y`, giving
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`.

Exact reason one weaker theorem still does not move the seam:

- continuity or a limit statement only at the single point `y` is not enough,
  because the existing recovery theorem consumes continuity on every point in
  an open support neighborhood of the test function;
- a theorem only about `T_t (φ_N)` for one localizer family is not smaller in
  source terms, because it already presupposes the missing representation of
  `T_t` by a local boundary trace.

No production Lean edit was made.

Supervision correction (`2026-04-16 04:05 UTC`, bounded audit pass): audited
the exact first theorem-sized gap beneath the live coefficient theorem against
the actual SCV constructors. No honest Lean theorem landed. The sharpened audit
result is that the missing package can be expressed as a single theorem, not a
two-theorem cluster, because the tempered Fourier-Laplace side is already
packagable from existing source by `SCV.exists_fourierLaplaceReprTempered`
once one combines the fixed-time CLM from
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` with the existing
holomorphicity and growth estimates for the same fixed-time shell continuation.
So the exact remaining theorem surface is only the local boundary trace:

- for
  `F_t z := bvt_F OS lgc (n + m) (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 z (t : ℂ))`,
  given
  `y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`,
  produce
  `∃ U, IsOpen U ∧ y ∈ U ∧ U ⊆ (section43PositiveEnergyRegion d (n + m))ᶜ ∧
    ∀ x ∈ U, ContinuousWithinAt F_t
      (TubeDomainSetPi (ForwardConeAbs d (n + m))) (SCV.realEmbed x)`;
- exact immediate consumer:
  `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`;
- exact downstream chain:
  local trace theorem
  `→` local recovery of the fixed-time tempered functional on tests supported in
  `U`
  `→` honest shrinking-localizer evaluation at `y`
  `→`
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`.

Exact reason this is minimal:
- weaker pointwise-limit data at `y` does not connect to `T_t`;
- weaker uniqueness/linearity data stays on fixed Schwartz tests and does not
  control shrinking localizers;
- a stronger theorem yielding full `HasFourierLaplaceReprRegular` would also
  suffice, but it is strictly stronger than the first missing theorem because
  the current local recovery theorem only requires the local `hcontU` input.

No production Lean edit was made.

Supervision correction (`2026-04-16 03:02 UTC`, bounded audit pass): audited
the exact first theorem-sized package that would be needed beneath the live
coefficient theorem if the shrinking-localizer route is to be honest. No honest
Lean theorem landed. The first genuinely new theorem surface exposed by the
source is a **local boundary-trace supplier** for the fixed-time shell
continuation, not a new uniqueness lemma and not a bare approximate-identity
statement. The reason is structural: the existing theorem
`fourierLaplace_boundary_recovery_on_open_of_tempered` already converts a
tempered boundary functional into integration against `F(realEmbed x)` on tests
supported in `U`, but only after one supplies
`hcontU : ∀ x ∈ U, ContinuousWithinAt F (TubeDomain C) (realEmbed x)`. For the
live function
`F_t z := bvt_F OS lgc (n + m) (xiShift ... z (t : ℂ))`, no such `hcontU` is
available near the complement shell point attached to `y`. So any attempt to
state a theorem directly about `T_t (φ_N)` for shrinking localizers still
silently assumes the missing trace representation. No production Lean edit was
made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T03:02Z`):

- exact audited first missing package:
  a theorem producing local real-boundary continuity for the fixed-time shell
  continuation on some open neighborhood of the chosen complement shell point;
- exact audited consumer:
  `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`;
- exact audited downstream chain:
  local trace
  `→` local recovery of `T_t` on supported tests
  `→` honest shrinking-localizer evaluation
  `→` the coefficient limit theorem;
- exact audited reason smaller candidates fail:
  `boundaryPointwise`-style existence is not linked to `T_t`;
  fixed-test linearity/uniqueness does not control varying localizers;
  approximate-identity evaluation without a recovered local trace has nothing to
  apply to.

Supervision correction (`2026-04-16 03:20 UTC`, bounded audit pass): audited
the exact regular/pointwise theorem surfaces against the live fixed-time shell
supplier. No honest Lean theorem landed. The new exact mismatch is that
`WightmanAnalyticity.boundaryPointwise` in `WightmanAxioms.lean:746-756` is
strictly weaker than what the complement coefficient theorem needs: it gives
only existence of some pointwise boundary limit of the analytic continuation at
a real configuration and direction, but does not identify that limit with the
stored boundary-value functional, does not relate it to
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`, and does not
force vanishing off `section43PositiveEnergyRegion`. The SCV theorems that do
identify a boundary functional with a concrete boundary trace,
`fourierLaplace_boundary_recovery` and
`fourierLaplace_boundary_recovery_on_open_of_tempered`, require either the full
regular package `HasFourierLaplaceReprRegular` or the local continuity
hypothesis `hcontU`; `boundary_value_zero_of_regular` then also depends on that
same regular package. The fixed-time shell theorem supplies only the tempered
CLM `T_t`, so the missing ingredient is still continuous boundary trace near
the complement shell point, not another uniqueness argument. No production Lean
edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T03:20Z`):

- exact audited theorem boundary:
  `WightmanAxioms.lean:738-756` separates distributional `boundaryValue` from
  existential `boundaryPointwise`;
  `SCV/LaplaceSchwartz.lean:118-134` packages the stronger trace data as
  `boundary_continuous` and `tube_continuousWithinAt` inside
  `HasFourierLaplaceReprRegular`;
- exact audited non-bridge:
  `boundaryPointwise` is not a theorem identifying the distributional boundary
  functional with evaluation at a fixed real point, so by itself it cannot
  discharge the fixed-`y` coefficient limit;
- exact audited missing hypothesis:
  a continuous boundary trace at least on some open real neighborhood of the
  shell point attached to `y` (`hcontU` / regular boundary continuity), which
  the fixed-time theorem does not provide;
- exact audited frontier verdict:
  the coefficient-limit theorem remains the first missing mathematics, now with
  the sharper diagnosis that the gap is specifically absence of regular/local
  boundary trace data rather than absence of another fixed-test uniqueness
  theorem.

Supervision correction (`2026-04-16 02:41 UTC`, bounded audit pass): audited the
remaining tempered-side theorem surfaces that might have turned the fixed-time
boundary functional into something usable on shrinking localizers near the
complement shell point. No honest Lean theorem landed. The exact result is that
theorems such as `fourierLaplace_dist_map_add_tempered`,
`fourierLaplace_dist_map_smul_tempered`,
`fourierLaplace_dist_isLinearMap_tempered`, and
`fourierLaplace_repr_dist_unique` in `SCV/LaplaceSchwartz.lean` only establish
fixed-test linearity/uniqueness of the boundary distribution. They do not
provide any representation theorem for `dist`, any control of `dist (φ_N)` for
varying Schwartz localizers, or any limit-interchange statement. The OS-side
packaging layer `boundary_values_tempered` / `bvt_boundary_values` in
`OSToWightmanBoundaryValuesBase.lean` stays at the same level. OS II printed
page `291`, formula `(5.4)`, also remains explicitly distributional in the
remaining variables. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T02:41Z`):

- exact audited theorem boundary:
  `SCV/LaplaceSchwartz.lean`
  (`fourierLaplace_dist_map_add_tempered`,
  `fourierLaplace_dist_map_smul_tempered`,
  `fourierLaplace_dist_isLinearMap_tempered`,
  `fourierLaplace_repr_dist_unique`) proves only properties of the fixed
  Schwartz functional `dist`;
  `OSToWightmanBoundaryValuesBase.lean`
  (`boundary_values_tempered`, `bvt_boundary_values`) packages only the same
  fixed-test convergence on `SchwartzNPoint`;
- exact audited failed varying-test route:
  a shrinking Schwartz bump / approximate identity centered at the complement
  shell point `y`;
  failure reason: current source has no theorem realizing the fixed-time CLM as
  a local function/measure near `y`, no theorem on `T_t (φ_N)` for such
  varying localizers, and no theorem exchanging `N → ∞` with `ε → 0+`;
- exact audited frontier verdict:
  the coefficient-limit theorem remains the first missing mathematics, now with
  the sharper source-backed obstruction that even the full tempered-side API is
  extensional only on fixed tests and contains no localization bridge.

Supervision correction (`2026-04-16 02:38 UTC`, bounded audit pass): audited the
remaining theorem surfaces that could conceivably convert the fixed-time CLM
boundary functional into something usable on a shrinking localizer family. No
honest Lean theorem landed. The sharper exact obstruction is now source-backed
inside `SCV/LaplaceSchwartz.lean` itself: the file defines
`HasFourierLaplaceReprTempered` and the stronger
`HasFourierLaplaceReprRegular` as genuinely different packages, where only the
regular package contains `boundary_continuous` and
`tube_continuousWithinAt`. The same file then states explicitly that the
missing theorem producing `HasFourierLaplaceReprRegular` from strong
Fourier-Laplace input is not yet formalized, and downstream theorems therefore
take regularity explicitly rather than claiming a weak-to-regular upgrade. So
the fixed-time supplier
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`, which is built
from `SCV.tube_boundaryValueData_of_polyGrowth`, still yields only the weak
tempered/distributional side. Theorems that could have localized that
functional, namely
`SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` and the regular
transport theorems in `SCV/TubeDistributions.lean`, all require exactly the
missing boundary continuity / regular package. OS II `(5.4)` still remains on
the distributional side. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T02:38Z`):

- exact audited theorem boundary:
  `OSToWightmanPositivity.lean:4131-4144` gives only the fixed-time CLM
  `T_t`;
  `SCV/LaplaceSchwartz.lean:97-190` separates
  `HasFourierLaplaceReprTempered` from `HasFourierLaplaceReprRegular` and
  explicitly records the absent weak-to-regular upgrade;
- exact audited failed varying-test route:
  a shrinking Schwartz bump / approximate identity centered at the complement
  shell point `y`;
  failure reason: current source has no theorem realizing the fixed-time
  tempered package as a regular boundary trace near `y`, hence no theorem on
  `T_t (φ_N)`, no representation by a local function/measure, and no justified
  interchange with the shell limit `ε → 0+`;
- exact audited frontier verdict:
  the coefficient-limit theorem remains the first missing mathematics, now with
  the sharper obstruction that current repo/source lacks not just `hcontU` for
  this example, but any existing theorem upgrading the weak boundary-value
  package to the regular boundary-trace package that such localization would
  require.

Supervision correction (`2026-04-16 02:27 UTC`, bounded audit pass): audited
the fixed-time localization attempt against the SCV recovery theorems. No
honest Lean theorem landed. The sharper obstruction is that the repo's only
general theorem converting a distributional boundary-value package into local
pointwise boundary evaluation is
`SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` in
`SCV/LocalBoundaryRecovery.lean`, and that theorem explicitly requires
`hcontU`, a continuous boundary trace on an open real neighborhood `U`. The
fixed-time shell supplier
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` provides no such
open-set boundary continuity for
`z ↦ bvt_F OS lgc (n + m) (xiShift ... z (t : ℂ))` near the complement shell
point indexed by `y`; it provides only the CLM boundary functional `T_t`. The
candidate shrinking Schwartz bump / approximate-identity family at `y`
therefore still cannot be localized by current API. No production Lean edit was
made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T02:27Z`):

- exact audited theorem boundary:
  weak boundary-value data comes from
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`;
  local boundary recovery exists only under the extra hypothesis
  `hcontU` in `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered`;
- exact audited failed local family:
  shrinking Schwartz localization at the complement shell point `y`;
  failure reason: there is no source-backed open neighborhood on which the
  fixed-time shell continuation is known to extend continuously to the real
  boundary, so the recovery theorem cannot identify `T_t` with local shell
  evaluation there;
- exact audited frontier verdict:
  the coefficient-limit theorem remains the first missing mathematics because
  the absent ingredient is now exact: a local boundary-trace / `hcontU` input
  for the fixed-time shell continuation, or an equivalent `boundaryPointwise`
  theorem near `y`.

Supervision correction (`2026-04-16 03:07 UTC`, bounded audit pass): audited the
localization attempt against the repo's explicit theorem interfaces, not just
the fixed-time implementation. No honest Lean theorem landed. The exact
obstruction is now even sharper: `WightmanAxioms.lean:726-778` already treats
distributional boundary values and pointwise boundary limits as genuinely
different theorem surfaces. The current fixed-time shell supplier
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` lands only on the
distributional side, because `SCV.tube_boundaryValueData_of_polyGrowth` returns
only `T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ` with convergence for each fixed
Schwartz test. The candidate localizer family, a shrinking Schwartz bump /
approximate identity centered at the real shell point `y`, still cannot be used
to recover point evaluation from the present API: there is no theorem on
`T_t (φ_N)` for such varying tests, no interchange between `N → ∞` and the
boundary limit `ε → 0+`, and no representation theorem identifying `T_t` with
an actual boundary function or measure near `y`. No production Lean edit was
made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T03:07Z`):

- exact audited theorem/definition boundary:
  `WightmanAnalyticity.boundaryValue` versus
  `WightmanAnalyticity.boundaryPointwise` in
  `WightmanAxioms.lean:726-778`;
  the fixed-time shell theorem only supplies the former kind of data;
- exact audited failed local family:
  shrinking Schwartz localization at the complement shell point `y`;
  failure reason: current CLM continuity handles fixed tests in the Schwartz
  topology, but gives no source-backed control for a varying approximate-
  identity family whose seminorms blow up under localization;
- exact audited frontier verdict:
  the coefficient-limit theorem remains the first missing mathematics because
  the needed shell evaluation is exactly a missing `boundaryPointwise`-type
  input, not a consequence of the existing distributional boundary-value API.

Supervision correction (`2026-04-16 02:42 UTC`, bounded audit pass): audited the
localization attempt inside
`section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`
against the actual fixed-time boundary-value implementation and the underlying
SCV theorem. No honest Lean theorem landed. The exact obstruction is now:
`section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` only provides
`T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ` together with convergence for each
fixed Schwartz test, and `SCV.tube_boundaryValueData_of_polyGrowth` only
packages the same CLM-level object with seminorm control. The natural candidate
localization family, a shrinking Schwartz bump / approximate identity centered
at the real shell point `y`, cannot be used from the current API because there
is no theorem about `T_t (φ_N)` as the family localizes, no interchange between
that localization limit and the boundary limit `ε → 0+`, and no representation
theorem identifying `T_t` with a pointwise boundary function or measure. The
only nearby point-evaluation theorem,
`bv_zero_point_is_evaluation`, is a separate `n = 0` normalization argument
using a special Euclidean restriction identity and does not upgrade the generic
fixed-time boundary functional. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T02:42Z`):

- exact audited theorem boundary:
  `OSToWightmanPositivity.lean:4131-4144` quantifies only over fixed `h` and
  returns only `T_t h`;
  `SCV/TubeBoundaryValues.lean` constructs only a CLM with growth/seminorm
  bounds, not a pointwise boundary representative;
- exact audited failed local family:
  shrinking Schwartz localization around the complement shell point `y`;
  failure reason: no theorem controls `T_t` on such a varying family, and the
  coefficient theorem needs exactly that localization to recover pointwise shell
  evaluation;
- exact audited non-analogy:
  `OSToWightmanBoundaryValuesComparison.lean:172-186`
  (`bv_zero_point_is_evaluation`) is a special zero-point theorem using a
  separate Euclidean restriction input, not a generic consequence of the tube
  boundary-value package;
- exact audited frontier verdict:
  the coefficient-limit theorem remains the first missing mathematics, now with
  the precise obstruction that current code lacks any source-backed mechanism
  turning the weak boundary-value functional `T_t` into evaluation at the
  complement-side shell point.

Supervision correction (`2026-04-16 02:13 UTC`, bounded audit pass): audited the exact interior of
`section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`
rather than only its caller. No honest compile-clean Lean theorem landed. The
precise blocker is now sharper: the proof needs a pointwise boundary statement
for the scalar continuation
`ε ↦ bvt_F OS lgc (n + m) (xiShift ... (fun k μ => ↑(y k μ) + ε * ... * I) (t : ℂ))`
at the real-shell boundary point `ε = 0`, while the currently available fixed-
time boundary-value theorem supplies only weak convergence of ambient linear
functionals to a boundary-value functional `T_t`. The candidate smaller lemma
“the shell map tends to its `ε = 0` boundary point” was rejected because it is
only an affine-input fact and does not bridge the missing boundary continuity /
boundary localization of `bvt_F`. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T02:13Z`):

- exact audited blocker inside the coefficient theorem:
  the missing source object is a theorem identifying the weak fixed-time
  boundary-value functional with pointwise shell evaluation, or otherwise
  forcing that pointwise boundary value to be `0`, at the chosen complement
  witness `y`;
- exact audited source boundary:
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` at
  `OSToWightmanPositivity.lean:4131-4144` still yields only
  `section43_fixedTimeShellRawCLM ... h → T_t h`;
  OS II printed page `291`, formula `(5.4)`, still gives only one-variable
  analytic continuation with distributional dependence on the remaining
  variables;
- exact audited rejected sub-lemma:
  the affine shell-map limit in `ε`;
  rejected because the real blocker is not convergence of the inputs but lack
  of any pointwise boundary regularity theorem for `bvt_F` on that boundary
  shell;
- exact audited minimality verdict:
  after the previous pass removed the inherited factor `h y`, this pointwise
  boundary-value problem is still the first missing mathematics on the live
  complement seam.

Supervision correction (`2026-04-16 01:57 UTC`, bounded audit pass): the first missing theorem under the live complement branch has now been isolated more sharply and exposed as a private theorem stub in `OSToWightmanPositivity.lean`. The honest lower statement is no longer the full scalar-times-`h y` branch goal but the coefficient-only canonical-shell limit at fixed `y` outside `section43PositiveEnergyRegion`. This is a real lowering of the seam: `h`, the indicator, and the quotient hypothesis `hq` are inherited packaging, not part of the first missing shell mathematics. No smaller source-backed supplier was found in the code or paper, because every nearby theorem still stops either at on-region quotient equality or at the fixed-time existence/analytic-continuation level.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T01:57Z`):

- exact first missing theorem now isolated in code:
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  with target
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`
  under
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`;
- essential hypotheses:
  `OS`, `lgc`, `n`, `m`, `hm`, `t`, `y`, `hy`;
- inherited noise removed by the refactor:
  `h`, the constant scalar `h y`, the indicator wrapper, and the unused quotient
  hypothesis `hq`;
- audit verdict on minimality:
  this is the smallest visible theorem surface that still expresses the actual
  missing outside-region shell mathematics;
  any smaller candidate would need an already-landed complement-localization,
  quotient-zero-on-complement, or shell-to-slice comparison theorem, and the
  audit still finds none;
- audit verdict on placement:
  a nearby private lemma is the natural home, because the theorem is used
  immediately by the pointwise branch of
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  and remains the same first obstruction seen later in `hlimit_os`.

Supervision correction (`2026-04-16 01:51 UTC`, bounded audit pass): re-audited the exact live seam from the theorem body together with the later `hlimit_os` case-`(3)` decomposition, explicitly checking whether that higher-level outside-region failure exposes any stricter source-backed insertion point below the coefficient-limit restatement. Again no honest retained theorem-body step exists inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`. The post-`simp only [F, indicator_of_mem hy]` obligation is still the same fixed-`y` complement-side scalar shell limit; pulling out `h y` still only renames the blocker as the canonical-shell coefficient limit; the first meaningful coefficient-limit theorem surface is still only that same blocker restated; the nearby dominated-convergence theorem still assumes exactly that pointwise limit as `hlim`; the earlier boundary-value existence theorem only yields existence of an ambient limit functional; the on-region shell / quotient transport API still stops on `section43PositiveEnergyRegion`; the later `hlimit_os` case `(3)` obstruction is the same outside-region mathematics one level higher, not a new lower theorem surface; and OS II `(5.4)` still stops at the fixed-time semigroup / one-variable analytic-continuation identity. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T01:51Z`):

- exact audited branch goal:
  in
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514),
  the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  still becomes, after
  `simp only [F, indicator_of_mem hy]`,
  exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited blocker verdict:
  no honest local `have` / `calc` / factorization lowers that goal;
  the only plausible rewrite still separates the constant `h y`, but that leaves
  the same canonical-shell coefficient limit and therefore does not shrink the
  mathematics;
  the first meaningful coefficient-limit theorem surface is still only the same
  blocker restated, not a new lower supplier;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4566)
  is not a missed local step, because it assumes this same pointwise limit as
  hypothesis `hlim`;
- exact audited source boundary:
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4131)
  supplies existence of an ambient limit functional only;
  the shell realization and weight-comparison lemmas at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:8916), [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9095), and [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9126)
  still stop on `section43PositiveEnergyRegion`;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  still stop on-region;
  the later `hlimit_os` decomposition at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9870)
  first fails in case `(3)` at line `9888`, but this is only the same
  outside-region obstruction at the integral-comparison level;
  OS II printed page `291`, formula `(5.4)`, still reaches only the fixed-time
  analytic-continuation identity in one time variable;
- exact first missing mathematics:
  the first missing theorem remains the complement-side coefficient limit for the
  canonical shell family;
  no audited source object yet supplies complement-localization or quotient-zero-
  on-complement below this branch.

Supervision correction (`2026-04-16 01:41 UTC`, bounded audit pass): re-audited the exact live seam from the theorem body, explicitly checking whether any stricter source-backed insertion point exists below the coefficient-limit restatement. Again no honest retained theorem-body step exists inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`. The post-`simp only [F, indicator_of_mem hy]` obligation is still the same fixed-`y` complement-side scalar shell limit; pulling out `h y` still only renames the blocker as the canonical-shell coefficient limit; the first meaningful coefficient-limit theorem surface is still only that same blocker restated; the nearby dominated-convergence theorem still assumes exactly that pointwise limit as `hlim`; the earlier boundary-value existence theorem only yields existence of an ambient limit functional; the on-region shell / quotient transport API still stops on `section43PositiveEnergyRegion`; and OS II `(5.4)` still stops at the fixed-time semigroup / one-variable analytic-continuation identity. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T01:41Z`):

- exact audited branch goal:
  in
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514),
  the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  still becomes, after
  `simp only [F, indicator_of_mem hy]`,
  exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited blocker verdict:
  no honest local `have` / `calc` / factorization lowers that goal;
  the only plausible rewrite still separates the constant `h y`, but that leaves
  the same canonical-shell coefficient limit and therefore does not shrink the
  mathematics;
  the first meaningful coefficient-limit theorem surface is still only the same
  blocker restated, not a new lower supplier;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4566)
  is not a missed local step, because it assumes this same pointwise limit as
  hypothesis `hlim`;
- exact audited source boundary:
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4131)
  supplies existence of an ambient limit functional only;
  the shell realization and weight-comparison lemmas at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:8916), [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9095), and [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9126)
  still stop on `section43PositiveEnergyRegion`;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  still stop on-region;
  OS II printed page `291`, formula `(5.4)`, still reaches only the fixed-time
  analytic-continuation identity in one time variable;
- exact first missing mathematics:
  the first missing theorem remains the complement-side coefficient limit for the
  canonical shell family;
  no audited source object yet supplies complement-localization or quotient-zero-
  on-complement below this branch.

Supervision correction (`2026-04-16 01:32 UTC`, bounded audit pass): re-audited the exact live seam from the theorem body, the earlier fixed-time shell existence theorem at `OSToWightmanPositivity.lean:4131-4144`, the immediately following dominated-convergence closure at `OSToWightmanPositivity.lean:4566-4672`, the on-region shell realization / weight-comparison cluster at `OSToWightmanPositivity.lean:8916-9148`, the quotient API, and OS II page `291`, formula `(5.4)`. Again no honest retained theorem-body step exists inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`. The post-`simp only [F, indicator_of_mem hy]` obligation is still the same fixed-`y` complement-side scalar shell limit; pulling out `h y` still only renames the blocker as the canonical-shell coefficient limit; the earliest meaningful coefficient-limit theorem surface is still just that same blocker restated; the nearby dominated-convergence theorem still assumes exactly that pointwise limit as `hlim`; the earlier boundary-value existence theorem only yields existence of an ambient limit functional; the on-region shell / quotient transport API still stops on `section43PositiveEnergyRegion`; and OS II `(5.4)` still stops at the fixed-time semigroup / one-variable analytic-continuation identity. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T01:32Z`):

- exact audited branch goal:
  in
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514),
  the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  still becomes, after
  `simp only [F, indicator_of_mem hy]`,
  exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited blocker verdict:
  no honest local `have` / `calc` / factorization lowers that goal;
  the only plausible rewrite still separates the constant `h y`, but that leaves
  the same canonical-shell coefficient limit and therefore does not shrink the
  mathematics;
  the first meaningful coefficient-limit theorem surface is still only the same
  blocker restated, not a new lower supplier;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4566)
  is not a missed local step, because it assumes this same pointwise limit as
  hypothesis `hlim`;
- exact audited source boundary:
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4131)
  supplies existence of an ambient limit functional only;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4430)
  still supplies domination only;
  the shell realization and weight-comparison lemmas at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:8916), [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9095), and [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9126)
  still stop on `section43PositiveEnergyRegion`;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  still stop on-region;
  OS II printed page `291`, formula `(5.4)`, still reaches only the fixed-time
  analytic-continuation identity in one time variable;
- exact first missing mathematics:
  the first missing theorem remains the complement-side coefficient limit for the
  canonical shell family;
  no audited source object yet supplies complement-localization or quotient-zero-
  on-complement below this branch.

Supervision correction (`2026-04-16 01:16 UTC`, bounded audit pass): re-audited the exact live seam from the theorem body, the immediately following generic dominated-convergence closure at `OSToWightmanPositivity.lean:4584-4672`, the quotient API, the downstream `hlimit_os` note, and OS II page `291`, formula `(5.4)`. Again no honest retained theorem-body step exists inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`. The post-`simp only [F, indicator_of_mem hy]` obligation is still the same fixed-`y` complement-side scalar shell limit; pulling out `h y` still only renames the blocker as the canonical-shell coefficient limit; the nearby dominated-convergence theorem still assumes exactly that pointwise limit as `hlim`; the quotient API still stops on-region; and OS II `(5.4)` still stops at the fixed-time semigroup / one-variable analytic-continuation identity. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T01:16Z`):

- exact audited branch goal:
  in
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514),
  the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  still becomes, after
  `simp only [F, indicator_of_mem hy]`,
  exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited blocker verdict:
  no honest local `have` / `calc` / factorization lowers that goal;
  the only plausible rewrite still separates the constant `h y`, but that leaves
  the same canonical-shell coefficient limit and therefore does not shrink the
  mathematics;
  the nearby theorem
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4566)
  is not a missed local step, because it assumes this same pointwise limit as
  hypothesis `hlim`;
- exact audited source boundary:
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4430)
  still supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  still stop on `section43PositiveEnergyRegion`;
  OS II printed page `291`, formula `(5.4)`, still reaches only the fixed-time
  analytic-continuation identity in one time variable;
- exact first missing mathematics:
  the first missing theorem remains the complement-side coefficient limit for the
  canonical shell family;
  no audited source object yet supplies complement-localization or quotient-zero-
  on-complement below this branch.

Supervision correction (`2026-04-16 01:06 UTC`, bounded audit pass): re-audited the exact live seam from the theorem body, quotient API, a targeted `OSToWightmanPositivity.lean` search for any already-landed coefficient-limit theorem on the same canonical shell, and OS II page `291`, formula `(5.4)`. Again no honest retained theorem-body step exists inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`. The post-`simp only [F, indicator_of_mem hy]` obligation is still the same fixed-`y` complement-side scalar shell limit; pulling out `h y` still only renames the blocker as the canonical-shell coefficient limit; the quotient API still stops on-region; the targeted search found no local theorem that supplies the missing coefficient limit; and OS II `(5.4)` still stops at the fixed-time semigroup / one-variable analytic-continuation identity. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T01:06Z`):

- exact audited branch goal:
  in
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514),
  the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  still becomes, after
  `simp only [F, indicator_of_mem hy]`,
  exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited blocker verdict:
  no honest local `have` / `calc` / factorization lowers that goal;
  the only plausible rewrite still separates the constant `h y`, but that leaves
  the same canonical-shell coefficient limit and therefore does not shrink the
  mathematics;
- exact audited source boundary:
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4430)
  still supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  still stop on `section43PositiveEnergyRegion`;
  the targeted search found no existing theorem whose conclusion is the missing
  complement-side coefficient limit on this exact shell;
  OS II printed page `291`, formula `(5.4)`, still reaches only the fixed-time
  analytic-continuation identity in one time variable;
- exact first missing mathematics:
  the first missing theorem remains the complement-side coefficient limit for the
  canonical shell family;
  no audited source object yet supplies complement-localization or quotient-zero-
  on-complement below this branch.

Supervision correction (`2026-04-16 01:01 UTC`, bounded audit pass): re-audited the exact live seam directly from the theorem body, quotient API, and OS II page `291` extract, and again found no honest retained theorem-body step inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`. The post-`simp only [F, indicator_of_mem hy]` obligation is still the same fixed-`y` complement-side scalar shell limit; pulling out `h y` still only renames the blocker as the canonical-shell coefficient limit; the quotient API still stops on-region; and OS II `(5.4)` still stops at the fixed-time semigroup / one-variable analytic-continuation identity. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T01:01Z`):

- exact audited branch goal:
  in
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514),
  the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  still becomes, after
  `simp only [F, indicator_of_mem hy]`,
  exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited blocker verdict:
  no honest local `have` / `calc` / factorization lowers that goal;
  the only plausible rewrite is to separate the constant `h y`, but that leaves the coefficient limit for the same canonical shell family and therefore does not shrink the mathematics;
- exact audited source boundary:
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4430)
  still supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  still stop on `section43PositiveEnergyRegion`;
  OS II printed page `291`, formula `(5.4)`, still reaches only the fixed-time analytic-continuation identity in one time variable;
- exact first missing mathematics:
  the first missing theorem remains the complement-side coefficient limit for the canonical shell family;
  no audited source object yet supplies complement-localization or quotient-zero-on-complement below this branch.

Supervision correction (`2026-04-16 00:56 UTC`, bounded audit pass): re-audited the exact live seam and again found no honest retained theorem-body step inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`. The post-`simp only [F, indicator_of_mem hy]` obligation is still the same fixed-`y` complement-side scalar shell limit; pulling out `h y` still only renames the blocker as the canonical-shell coefficient limit; the quotient API still stops on-region; and OS II `(5.4)` still stops at the fixed-time semigroup / one-variable analytic-continuation identity. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T00:56Z`):

- exact audited branch goal:
  in
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514),
  the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  still becomes, after
  `simp only [F, indicator_of_mem hy]`,
  exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited blocker verdict:
  no honest local `have` / `calc` / factorization lowers that goal;
  the only plausible rewrite is to separate the constant `h y`, but that leaves the coefficient limit for the same canonical shell family and therefore does not shrink the mathematics;
- exact audited source boundary:
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4430)
  still supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  still stop on `section43PositiveEnergyRegion`;
  OS II printed page `291`, formula `(5.4)`, still reaches only the fixed-time analytic-continuation identity in one time variable;
- exact first missing mathematics:
  the first missing theorem remains the complement-side coefficient limit for the canonical shell family;
  no audited source object yet supplies complement-localization or quotient-zero-on-complement below this branch.

Supervision correction (`2026-04-16 00:51 UTC`, bounded audit pass): one more bounded audit recheck of the exact live seam again found no honest compile-clean local step to retain inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`. The post-`simp only [F, indicator_of_mem hy]` branch is still the same fixed-`y` complement-side scalar shell limit; factoring out `h y` still only renames the blocker as the canonical-shell coefficient limit; the quotient API still stops on `section43PositiveEnergyRegion`; and OS II `(5.4)` still stops at fixed-time semigroup / one-variable analyticity. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T00:51Z`):

- exact audited theorem and branch goal:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local factorization through the constant `h y` still demands the coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  so it does not lower the live mathematics;
- exact audited feeder/source boundary:
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4430)
  still supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  still stop on-region;
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity in one time variable;
- exact first missing mathematics:
  the first missing input remains the complement-side coefficient limit for the canonical shell family;
  no audited source object yet reaches complement-localization or quotient-zero-on-complement below this seam.

Supervision correction (`2026-04-16 00:31 UTC`, bounded audit pass): one bounded pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II printed page `291`, formula `(5.4)`, via local `python3`/`PyPDF2`. The live source still shows that no honest compile-clean local step can be kept inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`: after `simp only [F, indicator_of_mem hy]`, the first remaining content is still the fixed-`y` complement-side scalar shell limit; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made; the recorded worker `rapid-falcon` has completed and there is currently no live Codex worker.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T00:31Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4556),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the complement witness `hy`;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker;
  even an explicit local `have` for the quotient-side `Set.EqOn` fact is not retainable as progress, because it does not enter the audited `hy` branch or reduce the active pointwise goal;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9893)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  direct `PyPDF2` extraction still shows that printed page `291`, formula `(5.4)`, is PDF page `11` in the local file and still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-16`, bounded audit pass): one bounded pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II printed page `291`, formula `(5.4)`. The live source still shows that no honest compile-clean local step can be kept inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`: after `simp only [F, indicator_of_mem hy]`, the first remaining content is still the fixed-`y` complement-side scalar shell limit; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made; the recorded worker `young-harbor` has completed and there is currently no live Codex worker.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4556),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the complement witness `hy`;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker;
  an explicit local `have` for the quotient-side `Set.EqOn` fact is compile-clean but not retainable as progress because it does not enter the audited `hy` branch or reduce the active pointwise goal;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9893)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  direct `PyPDF2` extraction still shows that printed page `291`, formula `(5.4)`, is PDF page `11` in the local file and still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-16 00:44 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II printed page `291`, formula `(5.4)`, via local `python3`/`PyPDF2`. The live source still shows that no honest compile-clean local step can be kept inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`: after `simp only [F, indicator_of_mem hy]`, the first remaining content is still the fixed-`y` complement-side scalar shell limit; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made; the recorded worker `wild-cloud` has completed and there is currently no live Codex worker.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T00:44Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4556),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the complement witness `hy`;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker;
  an explicit local `have` for the quotient-side `Set.EqOn` fact is compile-clean but not retainable as progress because it does not enter the audited `hy` branch or reduce the active pointwise goal;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9893)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  direct `PyPDF2` extraction shows that printed page `291`, formula `(5.4)`, is PDF page `11` in the local file and still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-16 00:26 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II printed page `291`, formula `(5.4)`, via local `python3`/`PyPDF2`. The live source still shows that no honest compile-clean local step can be kept inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`: after `simp only [F, indicator_of_mem hy]`, the first remaining content is still the fixed-`y` complement-side scalar shell limit; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made; the recorded worker `wild-cloud` has completed and there is currently no live Codex worker.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T00:26Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4556),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the complement witness `hy`;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker;
  an explicit local `have` for the quotient-side `Set.EqOn` fact is compile-clean but not retainable as progress because it does not enter the audited `hy` branch or reduce the active pointwise goal;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9890)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-16 00:11 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II printed page `291`, formula `(5.4)`, via local `python3`/`PyPDF2`. The live source still shows that no honest compile-clean local step can be kept inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`: after `simp only [F, indicator_of_mem hy]`, the first remaining content is still the fixed-`y` complement-side scalar shell limit; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made; the recorded worker `nimble-crustacean` has completed and there is currently no live Codex worker.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T00:11Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4556),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the complement witness `hy`;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker;
  an explicit local `have` for the quotient-side `Set.EqOn` fact is compile-clean but not retainable as progress because it does not enter the audited `hy` branch or reduce the active pointwise goal;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9890)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-16 00:00 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II printed page `291`, formula `(5.4)`. The live source still shows that no honest compile-clean local step can be kept inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`: after `simp only [F, indicator_of_mem hy]`, the first remaining content is still the fixed-`y` complement-side scalar shell limit; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made; the earlier recorded `fast-seaslug` worker is not live and there is currently no live Codex worker.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4556),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the complement witness `hy`;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker;
  an explicit local `have` for the quotient-side `Set.EqOn` fact is compile-clean but not retainable as progress because it does not enter the audited `hy` branch or reduce the active pointwise goal;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9893)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 23:49 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II printed page `291`, formula `(5.4)`, via local `python3`/`PyPDF2`. The live source still shows that no honest compile-clean local step can be kept inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`: after `simp only [F, indicator_of_mem hy]`, the first remaining content is still the fixed-`y` complement-side scalar shell limit; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T23:49:00Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4556),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the complement witness `hy`;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9893)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 23:36 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II `(5.4)`. The live source still shows that no honest compile-clean local step can be kept inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`: after `simp only [F, indicator_of_mem hy]`, the first remaining content is still the fixed-`y` complement-side scalar shell limit; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. The earlier worker `crisp-mist` has exited; there is currently no live Codex worker. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T23:36:31Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4556),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9890)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 23:31 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II `(5.4)`, and reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. The route is idle; there is currently no live productive worker. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T23:31:42Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4556),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9890)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 23:21 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II `(5.4)`, and reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. There is currently no live Codex worker. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T23:21:04Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4556),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9893)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 23:11 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II `(5.4)`, and reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. There is currently no live Codex worker. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T23:11:38Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4556),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9893)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 23:02 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II `(5.4)`, and reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T23:02:58Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9893)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 22:56 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and a direct extract of OS II `(5.4)`, and reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T22:56:28Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9893)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 22:46 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and OS II `(5.4)`, and reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. The just-finished worker `mild-claw` was docs-only after a source-first reread, and there is currently no live Codex worker. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T22:46Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9890)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 22:41 UTC`): one bounded audit pass on the exact live seam rechecked the theorem body itself, the direct quotient API, the exact `hlimit_os` consumer branch, and OS II `(5.4)`, and reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T22:41Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` / refactor survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9890)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 22:36 UTC`): one bounded audit pass on the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables; and the just-finished worker `young-pine` completed docs-only after a source-first reread, with no live Codex worker now. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T22:36Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9890)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 22:31 UTC`): one bounded audit pass on the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables; and the just-finished worker `nimble-claw` was docs-only, with no live Codex worker now. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T22:31Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9890)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 22:26 UTC`): one bounded audit pass on the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables; and by `2026-04-15 22:26 UTC` there is no live Codex worker. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T22:26:21Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9893)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 22:21 UTC`): one bounded audit pass on the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables; and by `2026-04-15 22:20 UTC` there is again no live Codex worker. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T22:21:36Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9890)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 22:16 UTC`): one bounded audit pass on the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; the direct `hlimit_os` consumer still fails first in outside-region case `(3)`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T22:16:59Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9890)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 22:06 UTC`): one bounded audit pass on the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T22:06:40Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9890)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 22:02 UTC`): one bounded audit pass on the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The exact first remaining content is still the fixed-`y` complement-side scalar shell limit after `simp only [F, indicator_of_mem hy]`; `hq` still unfolds only to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`; and OS II `(5.4)` still gives only one-variable analyticity with distributional dependence on the remaining variables. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T22:02:03Z`):

- exact audited theorem and pointwise branch:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514);
  in the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `simp only [F, indicator_of_mem hy]`,
  the audited goal is still exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` is the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  converts
  `hq` only into
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`,
  which does not touch the audited complement witness `hy`;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` remains only the later DCT consumer once this branch is solved;
- exact audited blocker verdict:
  no honest retained local `have` / `calc` survives here;
  the only plausible in-body rewrite is to separate the constant factor `h y`,
  but that leaves the same missing complement-side shell coefficient limit and therefore only renames the blocker rather than lowering the proof;
- exact audited insertion surface / consumer chain / source status:
  this unresolved theorem still feeds `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701),
  and the downstream comment at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9890)
  still correctly says the decomposition fails first in case `(3)`,
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  OS II printed page `291`, formula `(5.4)`, still supplies no fixed-`y` complement-shell vanishing statement below this seam.

Supervision correction (`2026-04-15 21:56 UTC`): one bounded audit reread of the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The only genuinely sharper point beyond the `21:42 UTC` pass is that `hq` can only be unfolded to `Set.EqOn h 0 (section43PositiveEnergyRegion d (n + m))`, which gives no information at the live complement witness `hy`; and OS II formula `(5.4)` is still only a one-variable analytic continuation statement while remaining distributional in the other variables, so it still cannot prove a fixed-`y` complement-shell limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T21:56:57Z`):

- exact audited hypothesis-consumer obstruction:
  the theorem hypothesis
  `hq : section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`
  only yields
  `Set.EqOn (h : NPointDomain d (n + m) → ℂ) 0 (section43PositiveEnergyRegion d (n + m))`
  via
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106);
  hence the quotient API can only eliminate `h y` on-region, never in the audited branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`;
- exact audited paper-status sharpening:
  direct extraction of OS II printed page `291`, formula `(5.4)`, shows the paper still gives only
  a semigroup / analytic-continuation identity whose right-hand side is analytic in the single variable
  `z = x⁰ + x⁰' + τ` for `Re z > 0`,
  while remaining a distribution in all remaining variables;
  therefore the paper source still does not offer a pointwise fixed-`y` theorem, a complement-localization principle, or a complement-shell coefficient vanishing statement that could feed the audited branch;
- exact audited blocker verdict:
  after
  `simp only [F, indicator_of_mem hy]`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4556),
  the branch still reduces only to the same unmasked scalar limit,
  and no honest retained local rewrite survives beyond repackaging that unresolved target.

Supervision correction (`2026-04-15 21:42 UTC`): one bounded audit reread of the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit; the quotient API still stops on the positive-energy region; and OS II formula `(5.4)` still does not supply a complement-localization or complement-shell vanishing theorem. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T21:42:00Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  the nearby theorem comment block at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9813)
  still correctly isolates the first live failure as complement case `(3)`;
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 21:31 UTC`): one bounded audit reread of the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body calculation, factoring out the constant `h y`, immediately requires the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T21:31:37Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  the nearby theorem comment block at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9813)
  correctly isolates the first live failure as complement case `(3)`;
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 21:26 UTC`): one bounded audit reread of the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body calculation, factoring out the constant `h y`, immediately requires the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T21:26:43Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  the nearby theorem comment block at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9813)
  correctly isolates the first live failure as complement case `(3)`;
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 21:21 UTC`): one bounded audit reread of the exact live seam confirmed again that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body calculation, factoring out the constant `h y`, immediately requires the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T21:21:25Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 21:16 UTC`): one bounded audit reread of the exact live seam confirmed again that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body reduction through factoring the constant `h y` immediately requires the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T21:16:27Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 21:06 UTC`): one bounded audit reread of the exact live seam confirmed again that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body reduction through factoring the constant `h y` immediately requires the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T21:06:37Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 20:46 UTC`): one bounded audit reread of the exact live seam confirmed again that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body reduction through factoring the constant `h y` immediately requires the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T20:46:42Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 20:42 UTC`): one bounded audit reread of the exact live seam confirmed again that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body reduction through factoring the constant `h y` would immediately require the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T20:41:57Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 20:36 UTC`): one bounded audit reread of the exact live seam confirmed again that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body reduction through factoring the constant `h y` would immediately require the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T20:36:52Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 20:32 UTC`): one bounded audit reread of the exact live seam confirmed again that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body reduction through factoring the constant `h y` would immediately require the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T20:32:23Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 20:26 UTC`): one bounded audit reread of the exact live seam confirmed again that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and no live quotient/codomain/shell/source theorem converts that branch into quotient-zero, equality-on-region, or canonical-shell coefficient vanishing. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T20:26:39Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the next retained line would already need either the same complement-side scalar limit itself, or a nonexistent theorem killing `h y` on the complement, or a nonexistent theorem forcing complement-side vanishing of the canonical-shell coefficient;
  none of those exist live, so any in-body `have` here would only rename the blocker;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 20:22 UTC`): one bounded audit reread of the exact live seam confirms again that no honest compile-clean local step can now be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and no current quotient or shell theorem converts that branch into a proved quotient-zero or complement-vanishing statement. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T20:22:06Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the next retained line would already need either the same complement-side scalar limit,
  or a theorem asserting that quotient-zero on `h` kills `h y` on the complement,
  or a theorem asserting complement-side vanishing of the canonical-shell coefficient;
  none of those exist live, so any in-body `have` at this point would only rename the blocker;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 20:12 UTC`): one bounded audit reread of the exact live `hy` branch after the new `hpointwise` insertion confirms that no honest compile-clean local step can now be kept inside the theorem body. The branch genuinely reduces to the unmasked fixed-`y` complement-side shell limit, but that reduced goal is still the blocker itself. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T20:12:33Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited post-`simp` complement-branch goal:
  for fixed
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`,
  the theorem body now asks exactly for
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  the theorem-local `F` plus `indicator_of_mem hy` produce that goal;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  closes only domination;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  remains only the unmasked DCT consumer once such pointwise limits are already available;
  the Section-4.3 quotient API still controls `h` only on the region, never on the complement;
- exact audited blocker verdict:
  no honest retained local rewrite or quotient-zero calculation remains after that `simp`;
  the next line would already need the missing complement-side scalar limit itself, so any retained helper at this point would only repackage the blocker;
- exact audited insertion surface / consumer chain / source citation:
  the theorem at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  remains the direct supplier for case (3) of `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.

Supervision correction (`2026-04-15 20:02 UTC`): one bounded audit reread of the exact live complement theorem surface confirms again that no compile-clean Lean contraction can be retained. The theorem still has the same dominated-convergence shape, but not as a direct application of the local generic theorem because the generic theorem is unmasked while the complement theorem integrates the indicator-masked family. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T20:02:32Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited domination input:
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  is live and closes the domination half for the masked integrand;
- exact audited generic supplier:
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  is live, but only for the unmasked shell family;
- exact audited theorem-body verdict:
  the complement theorem does admit the same dominated-convergence proof pattern,
  namely domination
  + pointwise complement limit
  + dominated convergence on the indicator-masked family,
  but not an honest compile-clean retained Lean contraction through the local generic theorem surface;
- exact audited first remaining proof obligation:
  for each fixed
  `y ∉ section43PositiveEnergyRegion d (n + m)`,
  prove the pointwise limit
  `indicator ((section43PositiveEnergyRegion d (n + m))ᶜ) (...) y → 0`
  as `ε → 0+`,
  equivalently the unmasked scalar
  `bvt_F OS lgc (n + m) (xiShift ...) * h y → 0`;
- exact audited no-smaller-theorem verdict:
  this is the first missing mathematics inside the theorem body, but theorem-wise it still does not shrink the seam honestly;
  any theorem or helper strong enough to continue would already prove exactly that same complement-side pointwise limit;
- exact audited insertion surface / consumer chain:
  the unresolved theorem at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  remains the direct supplier for case (3) of `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9678);
- exact audited quotient/API status:
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  and `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  still only control `h` on the positive-energy region itself;
- exact audited source citation status:
  OS II page index `10` / printed page `291`, formula `(5.4)`,
  still supports only the fixed-time analytic-continuation identity and no separate complement-localization theorem below it;
- production Lean verdict:
  no production Lean change was kept.

Supervision correction (`2026-04-15 20:02 UTC`): one bounded audit reread of the exact live complement theorem surface confirms again that the theorem now has a clean internal dominated-convergence decomposition, but no compile-clean Lean contraction can be retained because the first remaining input is still the complement-side pointwise shell limit and any theorem-sized extraction of it only restates the blocker. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T20:02:00Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited domination input:
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  is live and closes the domination half;
- exact audited generic supplier:
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  is live and would close the shell integral once the complement-side pointwise limit is supplied;
- exact audited theorem-body verdict:
  the theorem does admit the mathematical contraction
  domination
  + pointwise complement limit
  + dominated convergence,
  but not an honest compile-clean retained Lean contraction, because the pointwise complement limit is still the same live blocker and cannot be hidden inside the body;
- exact audited first remaining proof obligation:
  for each fixed
  `y ∉ section43PositiveEnergyRegion d (n + m)`,
  prove
  `bvt_F OS lgc (n + m) (xiShift ...) * h y → 0`
  as `ε → 0+`;
- exact audited no-smaller-theorem verdict:
  this is the first missing mathematics inside the theorem body, but theorem-wise it still does not shrink the seam honestly;
  any theorem or helper strong enough to continue would already prove exactly that same complement-side pointwise limit;
- exact audited insertion surface / consumer chain:
  the unresolved theorem at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  remains the direct supplier for case (3) of `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9678);
- exact audited quotient/API status:
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  and `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  still only control `h` on the positive-energy region itself;
- exact audited source citation status:
  OS II page index `10` / printed page `291`, formula `(5.4)`,
  still supports only the fixed-time analytic-continuation identity and no separate complement-localization theorem below it;
- production Lean verdict:
  no production Lean change was kept.

Supervision correction (`2026-04-15 19:40 UTC`): one bounded audit reread of the live complement theorem surface confirms that the theorem now has a clear internal dominated-convergence decomposition, but no compile-clean Lean contraction can be kept because the first remaining input is still the complement-side pointwise shell limit and any theorem-sized extraction of it just restates the blocker. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T19:40:00Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited domination input:
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  is live and closes the domination half;
- exact audited generic supplier:
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  is live and would close the shell integral once the complement-side pointwise limit is supplied;
- exact audited theorem-body verdict:
  the theorem does admit the mathematical contraction
  domination
  + pointwise complement limit
  + dominated convergence,
  but not an honest compile-clean retained Lean contraction, because the pointwise complement limit is still the same live blocker and cannot be hidden inside the body;
- exact audited first remaining proof obligation:
  for each fixed
  `y ∉ section43PositiveEnergyRegion d (n + m)`,
  prove
  `bvt_F OS lgc (n + m) (xiShift ...) * h y → 0`
  as `ε → 0+`;
- exact audited no-smaller-theorem verdict:
  this is the first missing mathematics inside the theorem body, but theorem-wise it still does not shrink the seam honestly;
  any theorem or helper strong enough to continue would already prove exactly that same complement-side pointwise limit;
- exact audited insertion surface / consumer chain:
  the unresolved theorem at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  remains the direct supplier for case (3) of `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9678);
- exact audited quotient/API status:
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  and `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  still only control `h` on the positive-energy region itself;
- exact audited source citation status:
  OS II page index `10` / printed page `291`, formula `(5.4)`,
  still supports only the fixed-time analytic-continuation identity and no separate complement-localization theorem below it;
- production Lean verdict:
  no production Lean change was kept.

Supervision correction (`2026-04-15 19:31 UTC`): one bounded audit reread of the live complement theorem surface confirms again that the only unresolved input left after the new domination lemma is the complement-side pointwise shell limit, and that no compile-clean contraction lowers the theorem-sized blocker beneath the full complement theorem. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T19:31:50Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited domination input:
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  is live and closes only the domination half;
- exact audited strongest honest package:
  full complement theorem
  =
  complement domination
  +
  complement-side pointwise limit
  +
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact audited first genuinely new mathematics:
  for each fixed
  `y ∉ section43PositiveEnergyRegion d (n + m)`,
  prove
  `bvt_F OS lgc (n + m) (xiShift ...) * h y → 0`
  as `ε → 0+`;
- exact audited no-smaller-theorem verdict:
  this pointwise complement limit is mathematically the first missing input,
  but theorem-wise it still does not shrink the blocker honestly;
  any theorem or proof contraction strong enough to continue the proof would
  already have to prove exactly that same complement-side pointwise limit;
- exact audited insertion surface / consumer chain:
  the unresolved theorem at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  remains the direct supplier for case (3) of `hlimit_os` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9678);
- exact audited quotient/API status:
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  still yields `h y = 0` only on the region, never on its complement;
- exact audited source citation status:
  OS II page index `10` / printed page `291`, formula `(5.4)`,
  still supports only the fixed-time analytic-continuation identity and no
  separate complement-localization theorem beneath it;
- production Lean verdict:
  no production Lean change was kept.

Supervision correction (`2026-04-15 19:27 UTC`): one bounded audit reread of the live complement theorem and its consumer branch confirms that the only unresolved input left after the new domination lemma is the complement-side pointwise shell limit, and that isolating that statement as a separate theorem would not honestly lower the blocker. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T19:27:37Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited domination input:
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  is live and closes only the domination half;
- exact audited first genuinely new mathematics:
  for each fixed
  `y ∉ section43PositiveEnergyRegion d (n + m)`,
  prove
  `bvt_F OS lgc (n + m) (xiShift ...) * h y → 0`
  as `ε → 0+`;
- exact audited minimality verdict:
  this pointwise complement limit is mathematically the first missing input,
  but theorem-wise it does not shrink the blocker honestly;
  the full complement theorem already consists of that pointwise limit plus the
  domination theorem plus the generic dominated-convergence supplier,
  so a separate pointwise theorem would only repackage the same obstruction;
- exact audited insertion surface / consumer chain:
  the unresolved theorem remains the direct supplier for case (3) of
  `hlimit_os` in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited quotient/API status:
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  still yields `h y = 0` only on the region, never on its complement;
- exact audited source citation status:
  OS II page index `10` / printed page `291`, formula `(5.4)`,
  still supports only the fixed-time analytic-continuation identity and no
  separate complement-localization theorem beneath it;
- production Lean verdict:
  no production Lean change was kept.

Supervision correction (`2026-04-15 19:24 UTC`): one bounded audit of a direct production proof attempt on `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl` confirms that the theorem still fails first on the complement-side pointwise limit after the new domination lemma is supplied. The speculative Lean edit was fully reverted.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T19:24:49Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited proof route attempted:
  reduce the theorem directly to
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  using the already-landed domination input
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`;
- exact audited first remaining obligation:
  for each fixed
  `y ∉ section43PositiveEnergyRegion d (n + m)`,
  prove
  `bvt_F OS lgc (n + m) (xiShift ...) * h y → 0`
  as `ε → 0+`;
- exact audited reason the obligation is still open:
  the Section-4.3 quotient API only yields `h y = 0` on the region itself via
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`,
  so there is still no theorem that annihilates or even rewrites the
  complement-side scalar required by dominated convergence;
- exact audited minimality verdict:
  after the domination lemma, the full complement theorem is already the
  minimal honest theorem surface;
  any pointwise/eventual-zero helper useful here would merely restate the same
  complement-side shell limit;
- exact audited new-mathematics surface:
  a theorem proving the complement-side pointwise limit
  `indicator ((section43PositiveEnergyRegion d (n + m))ᶜ) (...) y → 0`
  under quotient-zero on `h`,
  equivalently the full complement-indicator shell integral tends to `0`;
- exact audited source citation status:
  OS II page index `10` / printed page `291`, formula `(5.4)`,
  still supports only the fixed-time analytic-continuation target and no
  separate complement-localization theorem below it;
- production Lean verdict:
  no production Lean change was kept.

Supervision correction (`2026-04-15 19:12 UTC`): one bounded audit reread of the newly landed domination lemma, the full complement theorem statement, its `hlimit_os` consumer branch, the Section-4.3 quotient API, and OS II page index `10` / printed page `291`, formula `(5.4)`, confirms that after the domination half lands there is still no honest theorem/local lemma strictly below the complement theorem. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T19:12:25Z`):

- exact audited new local lemma:
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`;
- exact audited remaining theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited decomposition after the new lemma:
  the complement theorem now factors only as
  integrable domination
  + pointwise complement-indicator limit
  + application of
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact audited first still-missing obligation:
  the pointwise limit on the complement,
  namely for each fixed `y`,
  `indicator ((section43PositiveEnergyRegion d (n + m))ᶜ) (...) y → 0`
  under quotient-zero on `h`;
- exact audited reason this does not lower to a smaller honest theorem:
  quotient-zero only gives region equality / vanishing on
  `section43PositiveEnergyRegion d (n + m)`,
  not on its complement, so any pointwise/eventual-zero helper strong enough to
  discharge the theorem would already be the same outside-region actual-shell
  annihilation/control in a different wrapper;
- exact audited minimality verdict:
  the full complement theorem is now the minimal honest theorem surface on this
  seam;
- exact audited source citation status:
  OS II page index `10` / printed page `291`, formula `(5.4)`,
  still backs only the fixed-time analytic-continuation target and provides no
  separate theorem for complement-indicator shell vanishing or outside-region
  quotient descent below that package;
- production Lean verdict:
  no production Lean change landed.

Supervision correction (`2026-04-15 18:41 UTC`): one bounded audit reread of the exact live `hlimit_os` body, the outside-region complement branch, the real insertion surface, the Section-4.3 quotient API, and OS II page index `10` / printed page `291`, formula `(5.4)`, confirms again that no honest theorem sits strictly below the outside-region complement-localization package for the actual-shell integrand. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T18:41:33Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited unresolved branch:
  the outside-region case
  `y ∉ section43PositiveEnergyRegion d (n + m)`
  for the actual-shell integrand
  `bvt_F OS lgc (n + m) (xiShift ...) * h y`
  under quotient-zero input
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`,
  with
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`;
- exact audited best theorem shape:
  a limit-level outside-region complement-localization theorem for that true
  actual-shell integrand, equivalently the theorem that the complement-indicator
  shell integral tends to `0` under quotient-zero on `h`;
- exact audited reason smaller candidates collapse:
  the three-piece shell decomposition is already proof algebra in
  `hlimit_os`;
  region equality / vanishing is already supplied by the Section-4.3 quotient
  API plus the landed source-support and in-region-outside-source lemmas;
  the generic dominated-convergence supplier is already landed at
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  therefore any coefficient-only outside-region control, fixed-`ε`
  complement-vanishing theorem, or shell-functional quotient-descent theorem
  useful here would already need the same outside-region actual-shell
  annihilation/localization and is not genuinely smaller;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at lines `4424`-`4429`,
  then consumed in `hlimit_os` at lines `9564`-`9783`;
- exact audited source citation status:
  OS II page index `10` / printed page `291`, formula `(5.4)`, re-extracted
  directly via PyMuPDF, still backs only the fixed-time
  analytic-continuation target
  `(Ψ_n(x, ξ) | e^{-τH} Ψ_m(x', ξ'))`
  and provides no separate theorem for actual-shell complement localization,
  complement-indicator vanishing, coefficient annihilation, or quotient descent
  below that package;
- production Lean verdict:
  no production Lean change landed.

Supervision correction (`2026-04-15 18:37 UTC`): one bounded audit reread of the exact live `hlimit_os` body, the outside-region complement branch, the dominated-convergence supplier at the real insertion surface, the Section-4.3 quotient API, and OS II printed page `291`, formula `(5.4)`, confirms again that no honest theorem sits strictly below the outside-region complement-localization package for the actual-shell integrand. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T18:37:13Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited unresolved branch:
  the outside-region case
  `y ∉ section43PositiveEnergyRegion d (n + m)`
  for the actual-shell integrand
  `bvt_F OS lgc (n + m) (xiShift ...) * h y`
  under quotient-zero input
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`;
- exact audited best theorem shape:
  a limit-level outside-region complement-localization theorem for that true
  actual-shell integrand, equivalently the theorem that the complement-indicator
  shell integral tends to `0` under quotient-zero on `h`;
- exact audited reason smaller candidates collapse:
  the three-piece shell decomposition is already proof algebra in `hlimit_os`;
  region equality / vanishing is already supplied by the Section-4.3 quotient
  API plus the landed source-support and region-outside-source lemmas;
  the generic dominated-convergence supplier is already landed at
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  therefore any coefficient-only outside-region control, fixed-`ε`
  complement-vanishing theorem, or shell-functional quotient-descent theorem
  useful here would already need the same outside-region actual-shell
  annihilation/localization and is not genuinely smaller;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at lines `4424`-`4429`,
  then consumed in `hlimit_os` at lines `9564`-`9783`;
- exact audited source citation status:
  OS II printed page `291`, formula `(5.4)`, re-extracted directly via
  `python3`/PyMuPDF, still backs only the fixed-time analytic-continuation
  target and provides no separate theorem for actual-shell complement
  localization or quotient descent below that package;
- production Lean verdict:
  no production Lean change landed.

Supervision correction (`2026-04-15 18:32 UTC`): one bounded audit reread of the exact live `hlimit_os` body, the full outside-region complement-localization package candidate, the landed source-region lemmas, the generic dominated-convergence supplier, the Section-4.3 quotient API, and OS II printed pages `289`-`293` confirms again that no honest theorem sits strictly below that package. Its only nontrivial internal step is the same outside-region actual-shell complement limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T18:32:00Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited unresolved branch:
  the outside-region case
  `y ∉ section43PositiveEnergyRegion d (n + m)`
  for
  `bvt_F OS lgc (n + m) (xiShift ...) * h y`
  under
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`;
- exact audited full theorem statement shape:
  an outside-region complement-localization theorem at limit level for the
  true actual-shell integrand, equivalently a theorem that the complement
  indicator piece of the shell integral tends to `0` under quotient-zero on
  `h`;
- exact audited internal decomposition:
  shell localization by indicators is already present proof algebra in
  `hlimit_os`;
  region equality / vanishing is already supplied by the Section-4.3 quotient
  API plus the landed source-support and region-outside-source lemmas;
  the dominated-convergence supplier is already landed as
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  the only genuinely new step left is proving the outside-region complement
  limit itself, so the package does not further decompose honestly;
- exact audited reason smaller candidates collapse:
  coefficient-only outside-region control, fixed-`ε` complement vanishing, and
  shell-functional quotient descent would all already need the same
  outside-region actual-shell annihilation/localization and are therefore the
  same blocker restated;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  and then consumed in `hlimit_os`;
- exact audited source citation status:
  OS II printed pages `289`-`293`, especially page `291`, formula `(5.4)`,
  still back only the fixed-time analytic-continuation target and provide no
  separate theorem for actual-shell complement localization or quotient descent
  below that package;
- production Lean verdict:
  no production Lean change landed.

Supervision correction (`2026-04-15 18:27 UTC`): one bounded audit reread of the exact live `hlimit_os` body, its already-landed source-region lemmas, the shell-limit existence theorem, the Section-4.3 quotient API, and OS II printed pages `289`-`293` confirms again that no honest compile-clean theorem sits strictly below an actual-shell outside-region complement-localization package. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T18:27:00Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited live branch split inside `hlimit_os`:
  source-support branch,
  in-region outside-source branch,
  and outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact audited branch status:
  the first two branches are still discharged by the landed source-support and
  region-outside-source tensor-weight lemmas, while the third branch still has
  no theorem that annihilates, localizes, or rewrites the actual-shell
  integrand
  `bvt_F OS lgc (n + m) (xiShift ...) * h y`
  under quotient-zero on `h`;
- exact audited best theorem shape:
  an outside-region complement-localization theorem at limit level for the
  actual-shell integrand itself under
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`;
- exact audited reason smaller candidates collapse:
  the live quotient API only gives region equality / vanishing on
  `section43PositiveEnergyRegion d (n + m)`;
  any useful coefficient-only outside-region statement, fixed-`ε` shell
  vanishing theorem, or shell-functional quotient-descent theorem would
  already need the same outside-region shell control and therefore is the same
  blocker restated;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  and then consumed by the live `hlimit_os` branch;
- exact audited source citation status:
  OS II printed page `291`, formula `(5.4)`, re-extracted directly via
  `python3`/PyMuPDF, still supports only the fixed-time analytic-continuation
  target;
  pages `289`-`293` again showed no separate source theorem for actual-shell
  outside-region complement-localization or quotient descent;
- production Lean verdict:
  no production Lean change landed.

Supervision correction (`2026-04-15 18:21 UTC`): one bounded audit reread of the exact live `hlimit_os` body, its three-case split, the dominated-convergence supplier, and the Section-4.3 quotient lemmas confirms again that no honest compile-clean theorem sits strictly below an actual-shell outside-region complement-localization package. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T18:21:42Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited live split:
  source support,
  in-region outside-source,
  and outside-region
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact audited best theorem shape:
  an outside-region complement-localization theorem at limit level for the
  actual-shell integrand
  `bvt_F OS lgc (n + m) (xiShift ...) * h y`
  under quotient-zero on `h`,
  not a wrapper around quotient language and not a fixed-`ε` whole-shell
  vanishing claim;
- exact audited reason smaller candidates collapse:
  the only live quotient information is region equality / vanishing on
  `section43PositiveEnergyRegion d (n + m)`;
  any coefficient-only outside-region statement, fixed-`ε` shell vanishing
  theorem, or shell-functional quotient-descent theorem useful here would
  already need the same actual-shell outside-region control and therefore is
  the same blocker restated;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above the future theorem removing the outside-region shell
  contribution and then above the generic quotient-zero actual-shell limit
  theorem consumed in `hlimit_os`;
- exact audited source citation status:
  OS II printed page `291`, formula `(5.4)`, still supports only the fixed-time
  analytic-continuation target;
  `mutool draw -F txt` again returned blank page text in this harness;
  no separate paper theorem was found for actual-shell outside-region
  complement-localization or quotient descent;
- production Lean verdict:
  no production Lean change landed.

Supervision correction (`2026-04-15 18:24 UTC`): one more bounded audit reread of the exact live `hlimit_os` body, its three-case split, and the dominated-convergence supplier confirms again that no honest compile-clean theorem sits strictly below an actual-shell outside-region complement-localization package. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T18:24:00Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited live split:
  source-support,
  in-region outside-source,
  and outside-region
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact audited best theorem shape:
  an outside-region complement-localization theorem for the actual-shell
  integrand
  `bvt_F OS lgc (n + m) (xiShift ...) * h y`
  under quotient-zero on `h`,
  stated at limit level on the true shell integral rather than as a wrapper
  around quotient language;
- exact audited reason smaller candidates collapse:
  the only live quotient information is region equality / vanishing on
  `section43PositiveEnergyRegion d (n + m)`;
  any smaller coefficient-only statement, fixed-`ε` shell vanishing statement,
  or descended shell-functional theorem useful here would already need the same
  outside-region shell control and therefore is the same blocker restated;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above the future outside-region-removal theorem and then above
  the generic quotient-zero actual-shell limit theorem consumed in `hlimit_os`;
- exact audited source citation status:
  OS II printed page `291`, formula `(5.4)`, still supports only the fixed-time
  analytic-continuation target;
  `mutool draw -F txt` again returned blank page text in this harness;
  no separate paper theorem was found for actual-shell outside-region
  complement-localization or quotient descent;
- production Lean verdict:
  no production Lean change landed.

Supervision correction (`2026-04-15 18:24 UTC`): one bounded audit reread of the exact live `hlimit_os` body and its already-present three-case split confirmed that the outside-region branch is still the first unresolved actual-shell obligation. Among the two candidate theorem shapes, the complement-localization package is closer to the true next mathematics because it directly targets that branch; a descended shell-functional package remains too optimistic as a lower theorem surface because honest quotient descent for the actual-shell CLM would already need the same outside-region control.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T18:24:00Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited live branch split inside `hlimit_os`:
  source-support branch,
  in-region outside-source branch,
  and outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact audited branch status:
  the first two branches are already discharged by the existing pointwise
  tensor-weight lemmas, while the third branch still has no theorem that
  annihilates, localizes, or rewrites the actual-shell integrand
  `bvt_F OS lgc (n + m) (xiShift ...) * h y`
  under quotient-zero on `h`;
- exact audited candidate-package verdict:
  the closer package is an exact first outside-region complement-localization
  theorem for the actual-shell integrand itself;
  the descended shell-functional / quotient-dependence package is not genuinely
  smaller, because if it were strong enough to be useful here it would already
  subsume the missing outside-region control and therefore collapse back to the
  generic quotient-zero actual-shell limit blocker;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above the future first theorem that removes the outside-region
  contribution and then above the generic quotient-zero actual-shell limit
  theorem consumed in `hlimit_os`;
- exact audited source citation status:
  OS II printed page `291`, formula `(5.4)`, still backs only the
  analytic-continuation target;
  `mutool draw -F txt` again returned blank output for that page in this
  harness;
  no separate source theorem was found for actual-shell outside-region
  complement-localization or shell-functional quotient descent;
- production Lean verdict:
  no speculative production Lean edit was made in this pass.

Supervision correction (`2026-04-15 18:08 UTC`): one bounded audit reread of the exact live theorem body, the three-case decomposition inside `hlimit_os`, the raw-shell CLM definition, and the Section-4.3 quotient lemmas confirmed again that no genuinely smaller compile-clean production theorem exists below the generic quotient-zero actual-shell limit surface in one pass. The first honest new theorem contract is still the generic quotient-zero actual-shell limit theorem itself, and the exact first internal obligation is still actual-shell outside-region control.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T18:08:26Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited supplier chain:
  `hlimit_os`
  -> specialization to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`
  -> generic quotient-zero actual-shell limit
  -> `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact audited generic quotient-zero theorem statement:
  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      Filter.Tendsto
        (fun ε : ℝ =>
          if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0)
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;
- exact audited first internal proof obligation after rereading the live split:
  control the outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`
  for the actual-shell integrand
  `bvt_F OS lgc (n + m) (xiShift ...) * h y`
  under
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`;
  the source-support and in-region-outside-source cases are already handled by
  the existing pointwise weight lemmas, so no sharper lower theorem was found
  beneath this outside-region obligation;
- exact audited theorem-shape verdict:
  a shell-functional descent / annihilation package remains plausible only if
  it directly removes or controls that outside-region branch, but it is still
  too optimistic to record as the next production theorem because no
  source-backed lower statement was found in this pass;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above the future generic quotient-zero actual-shell limit theorem
  and then above
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited source citation status:
  OS II printed page `291`, formula `(5.4)`,
  still backs only the analytic-continuation target;
  `mutool draw -F txt` again returned blank output for that page and
  `pdftotext` is unavailable in this harness;
  no separate source theorem was found for actual-shell complement-localization
  or quotient descent;
- production Lean verdict:
  no speculative production Lean edit was made in this pass.

Supervision correction (`2026-04-15 17:57 UTC`): one bounded audit reread of the exact live theorem body, the raw-shell CLM definition, the dominated-convergence supplier, and the Section-4.3 quotient lemmas confirmed again that no genuinely smaller compile-clean production theorem exists below the generic quotient-zero actual-shell limit surface in one pass. The first honest new theorem contract is still the generic quotient-zero actual-shell limit theorem itself, and the exact first internal obligation is still outside-region control on the actual shell.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T17:57:02Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited supplier chain:
  `hlimit_os`
  -> specialization to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`
  -> generic quotient-zero actual-shell limit
  -> `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact audited generic quotient-zero theorem statement:
  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      Filter.Tendsto
        (fun ε : ℝ =>
          if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0)
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;
- exact audited rejected fixed-`ε` candidate:
  `∀ ε : ℝ, ∀ hε : 0 < ε, ∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h = 0`;
- exact audited reason it is not smaller:
  fixed-`ε` vanishing would already be whole-shell descent of the full actual
  CLM through `section43PositiveEnergyQuotientMap`, so it fails at the same
  missing complement branch as the generic limit theorem;
- exact audited first internal proof obligation:
  control
  `y ∉ section43PositiveEnergyRegion d (n + m)`
  for the actual-shell integrand
  `bvt_F OS lgc (n + m) (xiShift ...) * h y`
  under
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`;
  no sharper source-backed internal theorem was found beneath that obligation;
- exact audited theorem-shape verdict:
  a shell-functional descent / annihilation package remains plausible only if
  it directly handles that outside-region branch, but it is still too
  optimistic to record as the next production theorem because no source-backed
  lower statement was found in this pass;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above the future generic quotient-zero actual-shell limit theorem
  and then above
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited source citation status:
  OS II printed page `291`, formula `(5.4)`,
  still backs only the analytic-continuation target;
  `mutool draw -F txt` again returned blank output for that page in this
  harness and `pdftotext` is unavailable;
  no separate source theorem was found for actual-shell complement-localization
  or quotient descent;
- production Lean verdict:
  no speculative production Lean edit was made in this pass.

Supervision correction (`2026-04-15 17:52 UTC`): one bounded audit reread of the exact live theorem body, the supplier chain, the raw-shell CLM definition, and the Section-4.3 quotient lemmas confirmed again that the first honest new theorem contract is the generic quotient-zero actual-shell limit theorem itself. No compile-clean smaller production theorem lands in one pass: the rejected fixed-`ε` candidate is not genuinely smaller, and no separate source-backed actual-shell complement-localization theorem was found beneath the generic limit surface.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T17:52:01Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited supplier chain:
  `hlimit_os`
  -> specialization to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`
  -> generic quotient-zero actual-shell limit
  -> `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact audited rejected fixed-`ε` candidate:
  `∀ ε : ℝ, ∀ hε : 0 < ε, ∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h = 0`;
- exact audited reason it is not smaller:
  fixed-`ε` vanishing already asks for whole-shell descent of the full raw CLM
  through `section43PositiveEnergyQuotientMap`, so it fails at the same missing
  complement control as the limit theorem;
- exact audited next honest contract:
  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      Filter.Tendsto
        (fun ε : ℝ =>
          if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0)
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;
- exact audited decision on alternatives:
  the first honest new contract is already this generic quotient-zero
  actual-shell limit theorem;
  a weaker actual-shell complement-localization theorem would also be
  acceptable only if it directly controlled the live complement branch, but no
  such source-backed theorem surface was found in this pass;
- exact audited blocker:
  quotient-zero only controls `h` on
  `section43PositiveEnergyRegion d (n + m)`,
  while the CLM integrates over every shell point;
  there is still no theorem handling
  `y ∉ section43PositiveEnergyRegion d (n + m)`
  for the actual canonical-shell integrand;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above the future generic quotient-zero actual-shell limit theorem
  and then above
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited source citation status:
  OS II printed page `291`, formula `(5.4)`,
  still backs only the analytic-continuation target;
  `mutool draw -F txt` again returned blank page text and `pdftotext` is not
  available in this harness;
  no separate source theorem was found for actual-shell complement-localization
  or quotient descent;
- production Lean verdict:
  no speculative production Lean edit was made in this pass;
  only audit/docs/chat notes were tightened.

Supervision correction (`2026-04-15 17:41 UTC`): one bounded audit reread of the exact live theorem body, raw-shell CLM definition, and dominated-convergence supplier chain confirmed again that the direct quotient-zero actual-shell limit theorem is still the first honest lower theorem surface. No compile-clean smaller production theorem lands in one pass: the natural fixed-`ε` quotient-descent candidate is not genuinely smaller because it already asks for whole-shell kernel annihilation and still fails first on the uncontrolled complement branch outside the Section-4.3 region.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T17:41:44Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited supplier chain:
  `hlimit_os`
  -> specialization to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`
  -> generic quotient-zero shell limit
  -> `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact audited lower theorem statement confirmed again:
  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      Filter.Tendsto
        (fun ε : ℝ =>
          if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0)
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;
- exact audited candidate theorem statement:
  `∀ ε : ℝ, ∀ hε : 0 < ε, ∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h = 0`;
- exact audited reason it is not smaller:
  `section43_fixedTimeShellRawCLM` is already the actual full-shell integral,
  so a fixed-`ε` vanishing statement would already prove descent of the whole
  shell functional through `section43PositiveEnergyQuotientMap`;
- exact audited blocker:
  quotient-zero only controls the region branch
  `section43PositiveEnergyRegion d (n + m)`,
  while the CLM still integrates over every shell point;
  there is still no theorem handling
  `y ∉ section43PositiveEnergyRegion d (n + m)`
  for the actual canonical-shell integrand;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above the future generic quotient-zero shell limit theorem and
  then above
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited source citation:
  OS II printed page `291`, formula `(5.4)`,
  still backs only the analytic-continuation target;
  `pdftotext` is unavailable in this harness, and no separate source theorem
  was found for actual-shell complement-branch removal;
- exact audited stale-comment status:
  the current Lean comment is stale, but should still wait for a real theorem
  before being updated in production.

Supervision correction (`2026-04-15 17:37 UTC`): one bounded audit reread of the live theorem body, supplier chain, and raw-shell CLM definition confirmed again that the direct quotient-zero actual-shell limit theorem is still the first honest lower theorem surface. No compile-clean smaller production theorem lands in one pass: the natural fixed-`ε` quotient-descent candidate for `section43_fixedTimeShellRawCLM` is not genuinely smaller, because it already requires full-kernel annihilation on the actual canonical shell and still fails first on the uncontrolled complement branch.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T17:37:04Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited supplier chain:
  `hlimit_os`
  -> specialization to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`
  -> generic quotient-zero shell limit
  -> `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact audited lower theorem statement confirmed again:
  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      Filter.Tendsto
        (fun ε : ℝ =>
          if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0)
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;
- exact audited candidate theorem statement:
  `∀ ε : ℝ, ∀ hε : 0 < ε, ∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h = 0`;
- exact audited reason it is not smaller:
  `section43_fixedTimeShellRawCLM` is defined as a full-shell integral against
  the actual canonical-shell coefficient, so a fixed-`ε` kernel-annihilation
  theorem would already be a descended shell functional theorem through
  `section43PositiveEnergyQuotientMap`;
  that is stronger than, not lower than, the limit theorem currently exposed;
- exact audited blocker:
  quotient-zero only controls `h` on
  `section43PositiveEnergyRegion d (n + m)`,
  but the fixed-`ε` CLM still sees all shell points;
  there is still no theorem handling
  `y ∉ section43PositiveEnergyRegion d (n + m)`
  for the actual canonical-shell integrand;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above the future generic quotient-zero shell limit theorem and
  then above
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited source citation:
  OS II printed page `291`, formula `(5.4)`,
  still backs only the analytic-continuation target;
  no separate source theorem was found for actual-shell quotient descent on
  the complement branch;
- exact audited stale-comment status:
  the current Lean comment is stale, but should still wait for a real theorem
  before being updated in production.

Supervision correction (`2026-04-15 17:31 UTC`): one bounded audit reread of the exact live theorem body, its supplier chain, and the quotient transport lemmas confirmed again that the direct quotient-zero actual-shell limit theorem is still the first honest lower theorem surface. No compile-clean smaller production theorem lands in one pass, because the actual canonical-shell complement branch remains uncontrolled. The stale Lean comment should be updated later only after a real production theorem exists at that lower surface.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T17:31:55Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited supplier chain:
  `hlimit_os`
  -> specialization to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`
  -> generic quotient-zero shell limit
  -> `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact audited lower theorem statement confirmed again:
  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      Filter.Tendsto
        (fun ε : ℝ =>
          if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0)
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;
- exact audited reason it is first:
  the live shell-difference limit in `hlimit_os` is exactly its specialization
  to
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`,
  and the quotient-zero premise is already available from
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`
  plus
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`;
- exact audited blocker:
  the quotient-zero shell limit still first reduces through the dominated-
  convergence supplier, and there is still no theorem handling the actual-shell
  complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact audited one-pass verdict:
  no honest representative-independence, complement-vanishing, or direct
  quotient-zero shell rewrite theorem lands strictly below that lower surface
  in one pass;
- exact audited source citation:
  OS II printed page `291`, formula `(5.4)`,
  still backs only the analytic-continuation target;
  no separate source theorem was found for eliminating the actual-shell
  complement contribution;
- exact audited insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  for later use inside `hlimit_os` after
  `hshellToSlice`,
  `hsliceTransport`,
  and
  `hweightEqOnSource`;
- exact audited stale-comment status:
  the current Lean comment is stale, but should only be updated in production
  once a real theorem exists to point to.

Supervision correction (`2026-04-15 17:28 UTC`): one bounded audit reread of the exact live `hlimit_os` body against the still-live `sorry` comment corrected the lower-seam classification in the audit trail. The direct quotient-zero actual-shell limit theorem is still the first honest lower theorem surface; no smaller compile-clean production theorem lands in one pass; and the live comment in `OSToWightmanPositivity.lean` is now stale because it still points one step too high.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T17:28:33Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited supplier theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact audited lower theorem statement confirmed again:
  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      Filter.Tendsto
        (fun ε : ℝ =>
          if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0)
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;
- exact audited reason it is first:
  the live shell-difference limit in `hlimit_os` is exactly its specialization
  to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`,
  and the quotient-zero premise for that specialization is already available
  from
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`
  plus
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`;
- exact audited blocker:
  the quotient-zero shell limit still first reduces through the full-shell
  domination and pointwise-limit hypotheses of
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  and there is still no theorem handling the actual-shell complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact audited one-pass verdict:
  no honest compile-clean theorem lands strictly below that quotient-zero limit
  statement in one pass;
- exact audited stale-comment correction:
  the live comment directly above the `sorry` in `hlimit_os` still names the
  ambient-vs-source shell comparison limit as the first lower theorem, but that
  surface is already only the `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`
  specialization of the generic quotient-zero limit theorem above;
- exact insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  for later use inside `hlimit_os` after
  `hshellToSlice`,
  `hsliceTransport`,
  and
  `hweightEqOnSource`;
- source citation rechecked:
  OS II printed page `291`, formula `(5.4)`,
  still backs only the analytic-continuation target;
  no separate source theorem was found for eliminating the actual-shell
  complement contribution.

Supervision correction (`2026-04-15 18:02 UTC`): one bounded audit reread of the exact live `hlimit_os` body and the generic dominated-convergence supplier corrected the 17:12 package split. The direct quotient-zero actual-shell limit theorem is still the first honest lower theorem surface. The full-shell dominated-convergence input package is only an internal proof package for that theorem, not a separate production theorem-sized seam.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T18:02:00Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited supplier theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact audited lower theorem statement confirmed again:
  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      Filter.Tendsto
        (fun ε : ℝ =>
          if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0)
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;
- exact audited reason it is first:
  the live shell-difference limit in `hlimit_os` is exactly its specialization
  to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`,
  and the quotient-zero premise for that specialization is already available
  from
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`
  plus
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`;
- exact audited blocker:
  the quotient-zero shell limit still first reduces through the full-shell
  pointwise-limit / domination hypotheses of
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  and there is still no theorem handling the actual-shell complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact audited correction to the 17:12 split:
  the `hbound` / `hbound_int` / `hlim` package is not an honest smaller
  production theorem surface;
  it is only internal supplier data, and its only genuinely new mathematical
  content is still the unresolved outside-region control;
- exact insertion surface:
  `OSToWightmanPositivity.lean`,
  immediately above
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  for later use inside `hlimit_os` after
  `hshellToSlice`,
  `hsliceTransport`,
  and
  `hweightEqOnSource`;
- source citation rechecked:
  OS II printed page `291`, formula `(5.4)`,
  still backs only the analytic-continuation target;
  no separate source theorem was found for eliminating the actual-shell
  complement contribution.

Supervision correction (`2026-04-15 17:12 UTC`): one bounded audit reread of the exact dominated-convergence supplier sharpened the missing theorem package above the quotient-zero shell-limit surface. The next package is the full-shell dominated-convergence input package for quotient-zero `h`, and it does split one theorem-sized step smaller: the region branch is already discharged by quotient-zero-on-region, so the only genuinely new component is complement-branch shell control on `y ∉ section43PositiveEnergyRegion d (n + m)`.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T17:12:09Z`):

- exact audited supplier theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact audited package above the quotient-zero limit theorem:
  for quotient-zero
  `h`,
  provide the three supplier inputs on the full shell:
  eventual domination `hbound`,
  integrability `hbound_int`,
  and pointwise limit `hlim`;
- exact audited reason it sits immediately above:
  the quotient-zero actual-shell limit theorem is obtained by invoking that
  supplier with those data, so no smaller limit theorem below it can bypass
  this package on the current route;
- exact audited smaller decomposition:
  the package does split once;
  existing quotient-zero region equality already gives
  `h y = 0`
  on
  `section43PositiveEnergyRegion d (n + m)`,
  so the region part of `hbound` and `hlim` is already available;
  the only new theorem-sized component is the complement-side control needed on
  `((section43PositiveEnergyRegion d (n + m))ᶜ)`;
- exact audited missing component statement inventory:
  shell-side control of
  `bvt_F ... (xiShift ...) * h y`
  outside the positive-energy region, strong enough to furnish the missing
  pointwise-limit and domination pieces of the dominated-convergence package;
- exact source citation / lack:
  OS II printed page `291`, formula `(5.4)`,
  still backs only the final analytic-continuation target;
  this pass found no separate source theorem supplying actual-shell complement
  control;
- exact insertion surface and consumer chain:
  `OSToWightmanPositivity.lean`,
  above the future quotient-zero shell-limit theorem and ultimately below
  `hlimit_os`;
  complement control
  + quotient-zero region vanishing
  -> full-shell dominated-convergence data
  -> quotient-zero shell limit
  -> live shell-difference specialization.

Supervision correction (`2026-04-15 17:07 UTC`): one bounded audit reread of the exact dominated-convergence supplier and the live actual-shell integrand confirmed that the direct quotient-zero actual-shell limit theorem is still the first honest lower theorem surface beneath `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`. The outside-region complement branch does not honestly decompose into a smaller fixed-`ε` or pointwise theorem in one pass.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T17:07:07Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited supplier theorem body:
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact audited actual-shell integrand:
  `fun y : NPointDomain d (n + m) =>
    bvt_F OS lgc (n + m)
      (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
        (fun k μ =>
          ↑(y k μ) +
            ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
              Complex.I)
        (t : ℂ)) *
      h y`;
- exact audited lower theorem statement remains:
  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      Filter.Tendsto
        (fun ε : ℝ =>
          if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0)
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;
- exact audited reason it is first:
  the live shell-difference limit in `hlimit_os` is exactly its specialization
  to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`,
  and the required quotient-zero premise is already source-backed by
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`
  plus
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`;
- exact first blocker from the audited proof body:
  the dominated-convergence supplier demands full-shell pointwise-limit and
  domination data, but current source only controls the branch on
  `section43PositiveEnergyRegion d (n + m)`;
  there is still no theorem handling
  `y ∉ section43PositiveEnergyRegion d (n + m)` on the actual canonical shell;
- exact audited theorem-sized decision:
  no honest compile-clean production theorem lands strictly below that
  quotient-zero limit surface in one pass;
  the uncontrolled complement branch is not itself a source-backed smaller
  theorem surface;
- exact insertion surface:
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  immediately above
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  for later use inside `hlimit_os` after
  `hshellToSlice`,
  `hsliceTransport`,
  and
  `hweightEqOnSource`;
- source citation rechecked:
  `reconstruction theorem II.pdf`, printed page `291`, formula `(5.4)`,
  still backs the analytic-continuation target only,
  not the missing actual-shell complement elimination;
  local `mutool draw -F txt` again returned blank page text in this harness.

Supervision correction (`2026-04-15 17:01 UTC`): one bounded audit reread of the exact `hlimit_os` body confirmed that the direct quotient-zero actual-shell limit theorem is still the first honest lower theorem surface beneath `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`. No compile-clean production Lean step lands there in one pass, because the dominated-convergence route still first fails on the complement branch `y ∉ section43PositiveEnergyRegion d (n + m)`.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T17:01:42Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited lower theorem statement confirmed from the live body:
  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      Filter.Tendsto
        (fun ε : ℝ =>
          if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0)
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;
- exact audited reason it is first:
  the live shell-difference limit in `hlimit_os` is exactly the specialization
  to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`,
  and the kernel hypothesis for that specialization is already source-backed by
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`
  plus
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`;
- exact first blocker:
  the only current supplier route still runs through
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  whose domination / pointwise-limit package still lacks any theorem handling
  the actual-shell complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact audited theorem-sized decision:
  no honest compile-clean production theorem lands at that surface in one pass;
- exact insertion surface:
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  immediately above
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  for later use inside `hlimit_os` after
  `hshellToSlice`,
  `hsliceTransport`,
  and
  `hweightEqOnSource`;
- source citation rechecked:
  `reconstruction theorem II.pdf`, printed page `291`, formula `(5.4)`,
  still backs the analytic-continuation target only,
  not the missing complement elimination on the actual shell;
  local `mutool draw -F txt` extraction again returned blank page text in this
  harness.

Supervision correction (`2026-04-15 16:52 UTC`): one bounded audit reread of the exact `hlimit_os` body confirmed that the direct quotient-zero actual-shell limit theorem is still the first honest lower theorem surface beneath `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`. No compile-clean production Lean step lands there in one pass, because the dominated-convergence proof still first fails on the complement branch `y ∉ section43PositiveEnergyRegion d (n + m)`.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T16:52:31Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited lower theorem statement confirmed from the live body:
  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      Filter.Tendsto
        (fun ε : ℝ =>
          if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0)
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;
- exact audited reason it is first:
  the live shell-difference limit in `hlimit_os` is exactly the specialization
  to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`,
  and the kernel hypothesis for that specialization is already source-backed by
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`
  plus
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`;
- exact first blocker:
  the only current route still runs through
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  whose domination / pointwise-limit package still lacks any theorem handling
  the actual-shell complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact audited theorem-sized decision:
  no honest compile-clean production theorem lands at that surface in one pass;
- exact insertion surface:
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  immediately above
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  for later use inside `hlimit_os` after
  `hshellToSlice`,
  `hsliceTransport`,
  and
  `hweightEqOnSource`;
- source citation rechecked:
  `reconstruction theorem II.pdf`, printed page `291`, formula `(5.4)`,
  still backs the analytic-continuation target only,
  not the missing complement elimination on the actual shell.

Supervision correction (`2026-04-15 16:47 UTC`): one bounded audit reread of the exact `hlimit_os` body sharpened the theorem-sized outcome. The first honest lower theorem is the direct quotient-zero shell-limit theorem on the actual canonical shell, not the full ambient-vs-source specialization itself; but that smaller theorem still does not land compile-clean in one pass because the complement branch `y ∉ section43PositiveEnergyRegion d (n + m)` remains uncontrolled.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T16:47:00Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited full contract inside `hlimit_os`:
  the live local goal is still the shell-difference limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      ∫ y : NPointDomain d (n + m),
        bvt_F OS lgc (n + m)
          (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
            (fun k μ =>
              ↑(y k μ) +
                ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                  Complex.I)
            (t : ℂ)) *
          (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0)`;
- exact audited lower theorem statement:
  the first honest lower target is the direct quotient-zero shell-limit theorem
  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
      Filter.Tendsto
        (fun ε : ℝ =>
          if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0)
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;
- exact audited reason it is first:
  the live shell-difference limit is exactly the specialization to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`,
  and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  becomes applicable after
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`;
- exact first blocker:
  even this smaller theorem still reduces through
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  to shell-side domination and pointwise limit control, and no existing theorem
  handles the complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact insertion surface:
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  immediately above
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  then consumed in `hlimit_os` after
  `hshellToSlice`,
  `hsliceTransport`,
  and
  `hweightEqOnSource`;
- source citation rechecked:
  `reconstruction theorem II.pdf`, printed page `291`, formula `(5.4)`,
  still backs the analytic-continuation target only,
  not the missing actual-shell complement elimination;
- production Lean verdict:
  no production code edit was made;
  only audit notes were tightened.

Supervision correction (`2026-04-15 17:45 UTC`): one bounded audit reread of the exact `hlimit_os` body confirmed that no production Lean step honestly lands in one pass below the current theorem. The first real lower theorem is still the full ambient-vs-source shell limit, while the branch `y ∉ section43PositiveEnergyRegion d (n + m)` remains only the first uncontrolled case, not a separate compile-clean theorem surface.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T17:45:00Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited lower theorem statement:
  the first honest lower target remains
  `Filter.Tendsto
    (fun ε : ℝ =>
      ∫ y : NPointDomain d (n + m),
        bvt_F OS lgc (n + m)
          (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
            (fun k μ =>
              ↑(y k μ) +
                ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                  Complex.I)
            (t : ℂ)) *
          (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0)`;
- exact audited branch blocker:
  the first uncontrolled case remains
  `y ∉ section43PositiveEnergyRegion d (n + m)`
  for the live canonical-shell integrand;
- exact theorem-sized decision:
  there is still no honest compile-clean theorem in one pass strictly below that full limit statement,
  because the branch-only route would still require a new source-backed theorem annihilating or controlling the outside-region contribution itself;
- exact audited reason it is first:
  the source-support branch is already handled by
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
  and the positive-energy-outside-source branch is already handled by
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;
- exact insertion surface:
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  inside `hlimit_os`,
  after
  `hshellToSlice`,
  `hsliceTransport`,
  and
  `hweightEqOnSource`;
- source citation rechecked:
  `reconstruction theorem II.pdf`, printed page `291`, formula `(5.4)`,
  still backs the analytic-continuation target only,
  not a separate ambient-shell outside-region elimination;
- production Lean verdict:
  no production code edit was made;
  only audit notes were tightened.

Supervision correction (`2026-04-15 16:32 UTC`): one bounded audit pass on the live theorem body confirmed the same proof-theoretic outcome as the route notes: no production Lean step honestly lands in one pass. The next real theorem surface is still the full ambient-vs-source shell limit inside `hlimit_os`, and the branch `y ∉ section43PositiveEnergyRegion d (n + m)` does not yet isolate to a separate compile-clean theorem below it.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T16:32:08Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited lower theorem statement:
  the first honest lower target remains
  `Filter.Tendsto
    (fun ε : ℝ =>
      ∫ y : NPointDomain d (n + m),
        bvt_F OS lgc (n + m)
          (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
            (fun k μ =>
              ↑(y k μ) +
                ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                  Complex.I)
            (t : ℂ)) *
          (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0)`;
- exact audited branch blocker:
  the first uncontrolled case remains
  `y ∉ section43PositiveEnergyRegion d (n + m)` for the actual canonical-shell integrand;
- exact theorem-sized decision:
  there is still no honest compile-clean theorem in one pass strictly below that full limit statement,
  because the branch-only route would still require a new source-backed theorem annihilating or controlling the outside-region contribution itself;
- exact audited reason it is first:
  the source-support branch is already handled by
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
  and the positive-energy-outside-source branch is already handled by
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;
- exact insertion surface:
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  inside `hlimit_os`,
  after
  `hshellToSlice`,
  `hsliceTransport`,
  and
  `hweightEqOnSource`;
- source citation rechecked:
  `reconstruction theorem II.pdf`, printed page `291`, formula `(5.4)`,
  still backs the analytic-continuation target only,
  not a separate ambient-shell outside-region elimination;
- production Lean verdict:
  no production code edit was made;
  only audit notes were tightened.

Supervision correction (`2026-04-15 16:27 UTC`): a fresh bounded audit of the live theorem body found the same first blocker as the current route notes: the internal ambient-vs-source shell limit in `hlimit_os`. No smaller honest compile-clean theorem cut exists below that branch in one pass, so no production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T16:27:00Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact first audited blocker:
  the proof still first fails at the need for an ambient-domain shell comparison
  showing
  `ε ↦ ∫ y, bvt_F ... (xiShift ... (t : ℂ)) *
    (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y)`
  tends to `0` as `ε → 0+`;
- exact reason it is first:
  the source-support branch is already handled by
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
  and the positive-energy-outside-source branch is already handled by
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  but there is still no theorem for the remaining branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact insertion surface:
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  in `hlimit_os`,
  after
  `hshellToSlice`,
  `hsliceTransport`,
  and
  `hweightEqOnSource`;
- audited theorem-sized decision:
  this ambient-vs-source shell limit is the first honest lower theorem beneath the current theorem statement,
  and there is no smaller honest compile-clean repair below it in this pass;
- source citation rechecked:
  `reconstruction theorem II.pdf`, printed page `291`, formula `(5.4)`,
  still backs the analytic-continuation / semigroup target only,
  not the missing outside-region elimination on the ambient shell domain;
- production Lean verdict:
  no production code edit was made;
  only audit notes were tightened.

Supervision correction (`2026-04-15 17:25 UTC`): the first live theorem-level `sorry` is now `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`, not anything above `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`. A bounded source/doc/Lean audit found no honest compile-clean theorem cut strictly below the internal limit-comparison seam already described inside `hlimit_os`, so no production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T17:25:00Z`):

- exact audited theorem surface:
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact audited statement:
  the theorem identifies the fixed-time boundary value
  `bvt_W OS lgc (n + m)
    (φ.conjTensorProduct (timeShiftSchwartzNPoint (d := d) t ψ))`
  with the semigroup-side scalar
  `OSInnerProductTimeShiftHolomorphicValue ... (t : ℂ)`;
- exact audited insertion surface:
  the local proof `hlimit_os` in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  after the already-landed facts
  `hshellToSlice`,
  `hsliceTransport`,
  and
  `hweightEqOnSource`;
- exact first audited blocker:
  no existing theorem removes or controls the contribution from
  `y ∉ section43PositiveEnergyRegion d (n + m)`
  for the actual canonical-shell integrand
  `bvt_F OS lgc (n + m) (xiShift ... (t : ℂ)) * (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y)`;
- audited route consequence:
  the first missing lower theorem is still a limit-level ambient-vs-source shell comparison on the live shell integral, not another pointwise frozen-slice transport identity and not a direct `psiZ` scalar theorem;
- theorem-sized decision:
  this lower comparison theorem is genuinely smaller than
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  and there was no smaller honest compile-clean repair below it in this pass;
- source citation rechecked:
  `reconstruction theorem II.pdf`, printed page `291`, formula `(5.4)`,
  supports the analytic continuation / semigroup matrix-element target,
  but does not itself expose the ambient-shell outside-region elimination now missing in Lean;
- production Lean verdict:
  no production code edit was made;
  only audit notes were tightened.

Supervision correction (`2026-04-15 17:10 UTC`): the previous audit note that still placed a theorem-local `sorry` after `hMapsTo` is stale on direct source reread. The final strong-topology conversion is already present in `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`; the bounded pass did not expose a new internal proof obstruction. The honest limit of this pass was only bounded compile verification of the whole file, which timed out before completion.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T17:10:00Z`):

- exact audited source status:
  the theorem now contains the tail
  `rw [ContinuousLinearMap.hasBasis_nhds_zero.tendsto_right_iff]`,
  followed by the `hMapsTo` consumer and the final `const_add` step;
- exact correction to the old audit:
  there is no theorem-local `sorry` remaining in
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- exact file-level `sorry` search result:
  `rg -n "sorry" OSToWightmanPositivity.lean`
  returns only one remaining `sorry`, at line `9783`, outside the target theorem;
- exact source-backed basis API rechecked:
  `ContinuousLinearMap.hasBasis_nhds_zero :
    (𝓝 0).HasBasis ...`
  and
  `Filter.HasBasis.tendsto_right_iff :
    Tendsto f la lb ↔ ...`;
- exact bounded verification blocker:
  not a local theorem step;
  the attempted full file confirmation
  `timeout 120s lake env lean OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`
  exited `124`;
- audit consequence:
  the honest stale item is the earlier claim that the active seam was still the last strong-topology elimination;
  in source, that elimination is already written;
- production Lean verdict:
  no production code edit was made in this pass.

Supervision correction (`2026-04-15 16:35 UTC`): the current theorem body now audits cleanly through the existing `hMapsTo` insertion. The first remaining blocker is only the final strong-topology rewrite from that `MapsTo` statement to `Filter.Tendsto ... (nhds T_t)` on the complex-linear strong dual.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T16:35:00Z`):

- exact audited body status:
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  already contains
  `pR`, `hpointwiseR`, `hfamily_bound`, `hT0_bound`, `hsub_bound`, and `hMapsTo`;
- compile audit:
  the file-level check succeeds with the theorem still ending at the final `sorry`,
  so no earlier internal error remains below `hMapsTo`;
- exact attempted final conversion:
  rewrite the target `nhds 0` / `nhds T_t` using
  `ContinuousLinearMap.nhds_zero_eq`,
  feed the resulting principal-neighbourhood goals from the already-proved
  `hMapsTo`,
  then add back the constant limit map;
- exact first Lean-side blocker:
  after the `nhds_zero_eq` rewrite, the first `le_iInf` is ill-typed because Lean leaves the lattice target unresolved and reports
  `CompleteLattice ?m`;
- audit consequence:
  the live frontier is now strictly the strong-topology neighborhood-basis elimination on the complex-linear target space,
  not the shell-domain finite-net theorem and not the bounded-set consumer;
- production Lean verdict:
  no production code change was kept from the failed finish attempt.

Supervision correction (`2026-04-15 16:05 UTC`): the shell obstruction was a compiled export / import-surface seam, not the theorem statement shape and not the instance target. After a real `lake build` of `OSReconstruction.SCV.PartialFourierSpatial`, downstream `OSToWightmanPositivity` imports can synthesize the shell-domain `NuclearSpace` instance and apply the finite-net consumer.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T16:05:00Z`):

- pre-build downstream audit:
  a scratch import of
  `OSToWightmanPositivity`
  failed on
  `#synth NuclearSpace (SchwartzNPoint d (n + m))`,
  `#synth NuclearSpace (SchwartzMap (NPointDomain d (n + m)) ℂ)`,
  and
  `#check OSReconstruction.instNuclearSpaceComplexNPointDomain`;
- audited source status:
  the declaration is present in
  `OSReconstruction/SCV/PartialFourierSpatial.lean`,
  so the failure was not absence of the theorem/instance from source;
- compile check that actually repaired the import surface:
  `lake build OSReconstruction.SCV.PartialFourierSpatial`
  exits `0`;
- post-build downstream audit:
  the same scratch import now succeeds on the named instance check and on both `NuclearSpace` synthesis goals;
- exact blocked consumer now cleared:
  a scratch example importing
  `OSToWightmanPositivity`
  now compiles with
  `NuclearSpace.finite_net_of_complex_schwartz_seminorm_of_isVonNBounded
    (E := NPointDomain d (n + m)) s hB ε hε`;
- blocker classification update:
  not `(statement shape)` and not `(instance target)`;
  exact seam was compiled export / import visibility;
- production Lean verdict:
  no further source edit in this pass;
  the existing shell-domain instance is now audited as usable at the actual theorem-3 call site.

Supervision correction (`2026-04-15 15:05 UTC`): the new candidate `instNuclearSpaceComplexNPointDomain` was checked directly and is not yet an audited downstream fix. The defining file compiles, but downstream probes still cannot synthesize `NuclearSpace (SchwartzMap (NPointDomain d (n + m)) ℂ)`, so the finite-net consumer remains blocked at the same shell domain surface. No production Lean edit was kept.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T15:05:00Z`):

- compile check:
  `lake env lean OSReconstruction/SCV/PartialFourierSpatial.lean`
  exits `0`;
- audited downstream probe:
  after that rebuild, a direct scratch check still fails to synthesize
  `NuclearSpace (SchwartzMap (NPointDomain d (n + m)) ℂ)`;
- exact blocked consumer therefore remains:
  `NuclearSpace.finite_net_of_complex_schwartz_seminorm_of_isVonNBounded`;
- blocker classification:
  `(d)` another exact source-backed sub-obligation,
  now sharpened to:
  the candidate transport proof exists but is not exported in a form that downstream instance search can use on the raw shell test-space type;
- production Lean verdict:
  none;
  an attempted seam repair was reverted because the downstream audit result did not change.

Supervision correction (`2026-04-15 14:50 UTC`): `grand-crustacean` is still a live host process on the exact `NPointDomain` nuclearity / finite-net transport seam and has still emitted no PTY log text beyond startup, so there is not yet a bounded-pass outcome to audit. Because the worker remains live and route-faithful, it was not interrupted. The audit refinement from this check is that the current obstruction is best viewed as a local transport obligation, not new theorem-3 mathematics: make `NuclearSpace.finite_net_of_complex_schwartz_seminorm_of_isVonNBounded` callable on `SchwartzMap (NPointDomain d (n + m)) ℂ` using the existing `PartialFourierSpatial.lean` continuous-linear equivalences and the Euclidean complex nuclearity supplied in `ComplexSchwartz.lean`. Once this transport is available, the route returns directly to the bounded-set finite-net step inside `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and then to the strong-topology `MapsTo` assembly. No production Lean change was made in this supervision pass.

Supervision correction (`2026-04-15 14:05 UTC`): a direct proof attempt at `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` was made and then reverted after compile failure. No production Lean change was kept.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T14:05:00Z`):

- theorem surface tested:
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- exact proof packaging reached before failure:
  pointwise shell limit data
  + common seminorm control
  + manual `ℝ`-linear packaging of the shell family and the existential limit functional
  + planned use of `tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  + strong-topology basis on `ContinuousLinearMap`;
- exact first compile-blocking obligation:
  Lean cannot synthesize
  `NuclearSpace (SchwartzMap (NPointDomain d (n + m)) ℂ)`
  at the finite-net invocation site;
- exact blocked consumer:
  `NuclearSpace.finite_net_of_complex_schwartz_seminorm_of_isVonNBounded`;
- audit consequence:
  the route does not yet fail at the final strong-topology neighborhood packaging;
  it fails one step earlier, at the bounded-set finite-net transport from the Euclidean nuclear Schwartz instance to the actual `NPointDomain` shell test space;
- blocker classification:
  `(d)` another exact source-backed sub-obligation;
- production Lean verdict:
  none;
  the theorem remains `sorry`.

Supervision correction (`2026-04-15 14:08 UTC`): a fresh source-first audit plus a direct file compile check moved the live status of the shell-local common finite-seminorm theorem. The theorem is already present in source, but it is not compile-clean because of the scalar-restriction seam at `.restrictScalars ℝ`.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T14:08:00Z`):

- live theorem correction:
  `section43_fixedTimeShellRawCLM_uniformSeminormBound_fixedTime`
  is already written in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`
  at lines `4237-4272`;
- exact audit status:
  the theorem has not landed compile-clean;
  the file-level compile check fails first at line `4247` on
  `.restrictScalars ℝ`
  with missing
  `LinearMap.CompatibleSMul (SchwartzNPoint d (n + m)) ℂ ℝ ℂ`;
- exact audit consequence:
  the first remaining obstruction at this theorem surface is generic scalar packaging for the shell CLM family,
  not a new Banach-Steinhaus step
  and not a missing shell-side estimate;
- exact proof inputs already available remain:
  `section43_fixedTimeShellRaw_uniformPolyGrowth_fixedTime`,
  `section43_fixedTimeShellRawCLM_pointwiseBounded_fixedTime`,
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
  `finite_net_of_complex_schwartz_seminorm_of_isVonNBounded`,
  and
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`;
- downstream frontier once that packaging seam is resolved remains:
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  followed by
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- production Lean verdict:
  no theorem landed and no code change was kept in production.

Supervision correction (`2026-04-15 13:50 UTC`): a fresh source-first audit re-checked the live shell theorem surfaces, the Section V source formulas, the bounded-set finite-net consumer, and the strong-topology `MapsTo` basis. No smaller honest theorem exists below the common finite-seminorm theorem.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T13:50:00Z`):

- theorem-sized decision:
  no genuinely smaller honest theorem exists strictly between
  `section43_fixedTimeShellRawCLM_pointwiseBounded_fixedTime`
  and the common finite-seminorm theorem;
- exact source-backed reason:
  OS II page `291` formula `(5.4)` only supplies the shell family at the analytic / matrix-element level already reflected in the live shell family and its fixed-test-function boundedness;
  the next missing mathematical upgrade is still the Banach-Steinhaus passage to one common seminorm;
- exact audit consequence:
  the direct shell-facing use of
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  still cannot start until the shell family has one common finite-seminorm bound;
  therefore the apparent “eventual-MapsTo step” is not itself a smaller theorem surface;
- exact next honest theorem remains:
  `section43_fixedTimeShellRawCLM_uniformSeminormBound_fixedTime`;
- exact minimal hypotheses remain:
  `OS`, `lgc`, `{n m}`, `hm`, `t`;
- exact insertion surface remains:
  immediately below
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`
  and immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- exact downstream consumer chain remains:
  common finite-seminorm bound
  ->
  `finite_net_of_complex_schwartz_seminorm_of_isVonNBounded`
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- production Lean verdict:
  no theorem landed and no code change was kept in production.

Supervision correction (`2026-04-15 13:40 UTC`): a direct `lake env lean` scalar-probe against the live shell-family type confirms that the current obstruction above `section43_fixedTimeShellRawCLM_pointwiseBounded_fixedTime` is still purely packaging. No smaller honest theorem exists below the common finite-seminorm theorem.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T13:40:00Z`):

- theorem-sized decision:
  no genuinely smaller honest theorem exists strictly between
  `section43_fixedTimeShellRawCLM_pointwiseBounded_fixedTime`
  and the common finite-seminorm theorem;
- exact Lean-side obstruction now pinned by direct probe:
  importing `OSToWightmanPositivity` is not enough for Lean to synthesize
  `IsScalarTower ℝ ℂ (SchwartzMap (NPointDomain d (n + m)) ℂ)`,
  hence it also fails to synthesize
  `LinearMap.CompatibleSMul (SchwartzMap (NPointDomain d (n + m)) ℂ) ℂ ℝ ℂ`;
- exact audit consequence:
  the shell family still cannot be written as
  `fun ε => (section43_fixedTimeShellRawCLM ...).restrictScalars ℝ`
  at the insertion surface,
  so the blocked move is still only the generic scalar-packaging prerequisite
  to the already-known Banach-Steinhaus step;
- exact next honest theorem remains:
  the shell-family common finite-seminorm bound itself;
- exact minimal hypotheses remain:
  `OS`, `lgc`, `{n m}`, `hm`, `t`;
- exact insertion surface remains:
  immediately below
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`
  and immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- exact downstream consumer chain remains:
  common finite-seminorm bound
  ->
  `finite_net_of_complex_schwartz_seminorm_of_isVonNBounded`
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- production Lean verdict:
  no theorem landed and no code change was kept in production.

Supervision correction (`2026-04-15 13:30 UTC`): the bounded theorem-3 pass tested whether the shell-local strong-topology / eventual-`MapsTo` route admits one smaller theorem surface above `section43_fixedTimeShellRawCLM_pointwiseBounded_fixedTime`. It does not. The only new fact established is that the local obstruction is a generic `LinearMap.CompatibleSMul` / `IsScalarTower` packaging gap on `SchwartzMap`, not a new mathematical theorem below the common finite-seminorm bound.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T13:30:00Z`):

- theorem-sized decision:
  no genuinely smaller honest theorem exists strictly between
  `section43_fixedTimeShellRawCLM_pointwiseBounded_fixedTime`
  and the common finite-seminorm theorem;
- exact Lean-side obstruction now pinned:
  even after coercing
  `SchwartzNPoint d (n + m)`
  to
  `SchwartzMap (NPointDomain d (n + m)) ℂ`,
  the real restriction call
  `.restrictScalars ℝ`
  still requires
  `LinearMap.CompatibleSMul (SchwartzMap (NPointDomain d (n + m)) ℂ) ℂ ℝ ℂ`,
  which is not synthesized at this insertion surface;
- audit consequence:
  this is not a new theorem beneath the common finite-seminorm bound;
  it is only the generic scalar-packaging prerequisite needed to express the Banach-Steinhaus route already determined by the live source;
- exact next honest theorem remains:
  the shell-family common finite-seminorm bound itself;
- exact minimal hypotheses remain:
  `OS`, `lgc`, `{n m}`, `hm`, `t`;
- exact insertion surface remains:
  immediately below
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`
  and immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- exact downstream consumer chain remains:
  common finite-seminorm bound
  ->
  `finite_net_of_complex_schwartz_seminorm_of_isVonNBounded`
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- production Lean verdict:
  no theorem landed and no code change was kept in production.

Supervision correction (`2026-04-15 13:00 UTC`): the shell-local common finite-seminorm theorem was tested again by an actual insertion attempt in `OSToWightmanPositivity.lean`, then reverted after compile failure. The failure reproduces the same local scalar-packaging seam already suspected; it does not expose a smaller theorem below the common-seminorm surface.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T13:00:00Z`):

- theorem surface tested:
  the common finite-seminorm bound immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- proof route tested:
  reindex the shell family by
  `{ε : ℝ // 0 < ε ∧ ε < 1}`,
  use
  `section43_fixedTimeShellRawCLM_pointwiseBounded_fixedTime`,
  invoke
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  and identify the resulting real seminorm supremum with the complex seminorm supremum using the same formula already packaged in
  `finite_net_of_complex_schwartz_seminorm_of_isVonNBounded`;
- exact Lean-side blocker confirmed:
  the shell family still does not restrict scalars compile-clean at this surface:
  `fun ε => (section43_fixedTimeShellRawCLM ... ε.1 ε.2.1).restrictScalars ℝ`
  fails with
  `LinearMap.CompatibleSMul (SchwartzNPoint d (n + m)) ℂ ℝ ℂ`;
- audit consequence:
  there is no new smaller mathematical theorem between the pointwise-bounded theorem and the common-seminorm theorem;
  the only missing bridge is still the full Banach-Steinhaus upgrade itself, plus the local scalar-packaging prerequisite needed to state it here;
- exact next honest theorem remains:
  the shell-family common finite-seminorm bound itself;
- production Lean verdict:
  no theorem landed and no code change was kept in production.

Supervision correction (`2026-04-15 12:45 UTC`): the bounded pass that directly tested the shell-local common finite-seminorm theorem did not produce a compile-clean Lean landing. The mathematical gap is still the same Banach-Steinhaus upgrade already isolated by the audit; the new information is that the first local proof attempt is blocked by a real-vs-complex scalar-packaging seam at the exact insertion surface.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T12:45:00Z`):

- theorem surface tested:
  the common finite-seminorm bound immediately above
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`
  and immediately below
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- proof route tested:
  reindex the shell family by
  `{ε : ℝ // 0 < ε ∧ ε < 1}`,
  use
  `section43_fixedTimeShellRawCLM_pointwiseBounded_fixedTime`
  for pointwise boundedness,
  then invoke
  `SchwartzMap.tempered_uniform_schwartz_bound`;
- exact Lean-side blocker found:
  the shell family is exposed here as a `ℂ`-linear family on the alias
  `SchwartzNPoint d (n + m)`,
  while the available Banach-Steinhaus theorem is packaged for `ℝ`-linear families;
  the attempted local call to `.restrictScalars ℝ` hit the missing typeclass seam
  `IsScalarTower ℝ ℂ (SchwartzNPoint d (n + m))`
  / `LinearMap.CompatibleSMul (SchwartzNPoint d (n + m)) ℂ ℝ ℂ`;
- audit consequence:
  this does not reveal a new smaller mathematical theorem;
  it only sharpens the exact minimal non-mathematical prerequisite still missing at the insertion site;
- next honest theorem remains:
  the shell-family common finite-seminorm bound itself;
- production Lean verdict:
  no theorem landed and no code change was kept in production.

Supervision correction (`2026-04-15 12:40 UTC`): the live shell audit shows that the candidate smaller bridge is already real and already landed. `OSToWightmanPositivity.lean` contains the compile-clean theorem
`section43_fixedTimeShellRawCLM_pointwiseBounded_fixedTime`
immediately above `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`; it does not collapse back to the full shell-local common-seminorm problem.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T12:40:00Z`):

- live theorem correction:
  the route already has one honest theorem-sized step strictly between
  `section43_fixedTimeShellRaw_uniformPolyGrowth_fixedTime`
  and the full shell-local uniform finite-seminorm bound;
- exact landed bridge:
  `section43_fixedTimeShellRawCLM_pointwiseBounded_fixedTime`;
- exact mathematical content:
  for each fixed ambient test `φ`, the shell family
  `ε ↦ section43_fixedTimeShellRawCLM ... ε ... φ`
  is uniformly bounded on `0 < ε < 1`;
- why this is a genuine reduction:
  the proof uses the shell-local polynomial-growth estimate plus one fixed Schwartz seminorm of the chosen `φ`;
  the remaining upgrade to a common finite seminorm is then exactly the Banach-Steinhaus step supplied by
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  not another shell-integral estimate;
- exact next missing theorem surface:
  the common finite-seminorm bound for the whole shell family on `0 < ε < 1`;
- exact downstream chain after that theorem:
  common finite-seminorm bound
  ->
  `finite_net_of_complex_schwartz_seminorm_of_isVonNBounded`
  ->
  `tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  strong-topology neighbourhood control
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- production Lean edit verdict:
  none in this bounded pass;
- reason:
  the theorem-sized candidate being checked was already present compile-clean, so only the audit description required correction.

Supervision correction (`2026-04-15 12:25 UTC`): the last bounded Lean pass honestly landed the smaller raw-kernel bridge `section43_fixedTimeShellRaw_uniformPolyGrowth_fixedTime` in `OSToWightmanPositivity.lean`. So the missing lower shell theorem is no longer that polynomial-growth package itself, but the next shell-local upgrade above it: a fixed-time uniform finite-seminorm bound for `section43_fixedTimeShellRawCLM` on `0 < ε < 1`, unless there is one genuinely smaller immediately consumable theorem between the new polynomial-growth bridge and the full seminorm-bound theorem.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T23:59:59Z`):

- re-check confirmed again:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- direct bounded verdict:
  the next honest landing is not yet the shell-local `MapsTo` theorem itself;
- exact smaller bridge required beneath it:
  a theorem of the form
  `∃ s C_sem, 0 ≤ C_sem ∧
    ∀ ε, 0 < ε → ε < 1 → ∀ φ,
      ‖section43_fixedTimeShellRawCLM ... ε ... φ‖ ≤
        C_sem * (s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)) φ`;
- insertion surface:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- why this is the first honest missing shell theorem:
  the pointwise limit supplier is already live as
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
  and the bounded-set finite-net theorem is already live as
  `finite_net_of_complex_schwartz_seminorm_of_isVonNBounded`;
  the remaining missing consumer hypothesis is the common seminorm bound on the raw shell family;
- downstream chain once landed:
  shell uniform seminorm bound
  ->
  finite nets on bounded complex Schwartz sets
  ->
  `tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  on the real-restricted CLM family
  ->
  strong-topology neighbourhood control
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- why no smaller distinct bridge remains below that uniform-bound theorem:
  any weaker candidate only repackages the same missing common-bound statement;
  the finite-net and pointwise-convergence inputs are already discharged;
- production Lean edit verdict:
  none in this bounded pass;
- exact reason no Lean theorem landed:
  the shortest proof route is the SCV tube-slice uniform-growth argument already implemented privately in the SCV layer, so there was no short honest local theorem extraction available here without reopening a larger non-bounded proof move.

Supervision correction (`2026-04-15 23:59 UTC`): the live route is now post-Schwartz-specialization. `OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean` already contains the compile-clean theorem `finite_net_of_schwartz_seminorm_of_isVonNBounded`, so the active theorem-3 seam is no longer “derive the Schwartz finite-net theorem from the generic nuclear theorem”; it is the strong-dual existence theorem `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T23:59:59Z`):

- re-check confirmed again:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- compile-clean producer correction:
  the full Schwartz specialization theorem is already present live as
  `finite_net_of_schwartz_seminorm_of_isVonNBounded`;
- unsupported residue verdict:
  yes;
  the route text that still treated the Schwartz specialization as the next missing theorem was unsupported by live Lean;
- production Lean edit verdict:
  none in this bounded pass;
- theorem-sized decision:
  no genuinely smaller distinct bridge remains below the specialization;
  the boundedness-transfer substep and the specialization call are already internal to the landed theorem;
- exact next honest theorem statement now required:
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  with hypotheses
  `OS`, `lgc`, `hm`, `t`, `ht`
  and existentially packaged witness
  `T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ`;
- exact first internal proof obligation beneath that theorem:
  promote the fixed-time raw shell family to a convergent net in the strong dual by combining
  bounded-set finite-net control
  (`finite_net_of_schwartz_seminorm_of_isVonNBounded`),
  the finite-net-to-`MapsTo` shell consumer
  (`tempered_eventually_mapsTo_sub_of_finite_seminorm_net`),
  strong-topology neighbourhood basis facts for continuous linear maps,
  and the pointwise shell convergence / Cauchy input on each ambient test function;
- why this is now the honest minimal blocker:
  the entire nuclear-space route through the Schwartz specialization is already compile-clean;
  the first missing mathematics is the strong-dual limit-existence step, not another boundedness theorem;
- exact downstream chain is now:
  `finite_net_of_schwartz_seminorm_of_isVonNBounded`
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  / strong-topology basis
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  ->
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- witness decision:
  keep `T_t` existential;
  early extraction remains wrapper drift only;

Supervision correction (`2026-04-15 23:59 UTC`): the live producer frontier has advanced beyond the stale trust frontier supplied to this pass. `OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean` already contains the full compile-clean generic theorem `finite_net_of_nuclear_seminorm_of_q_bounded`; the active missing seam is no longer the generic nuclear-seminorm finite-net theorem itself, but the Schwartz shell-facing specialization that turns bounded Schwartz sets into finite nets for a fixed finite supremum seminorm.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T23:59:59Z`):

- re-check confirmed again:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- compile-clean shell consumer re-check:
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  remains present compile-clean in `SchwartzComplete.lean`;
- compile-clean producer correction:
  `uniform_nuclear_tail_lt_of_q_bounded`,
  `uniform_nuclear_tail_lt_of_q_bounded_sub`,
  `finite_coordinate_image_totallyBounded_of_q_bounded`,
  `finite_weighted_coordinate_net_of_q_bounded`,
  `finite_weighted_coordinate_net_mem_of_q_bounded`,
  `finite_net_of_nuclear_head_tail`,
  and
  `finite_net_of_nuclear_seminorm_of_q_bounded`
  are all already present compile-clean in live `NuclearSpace.lean`;
- unsupported residue verdict:
  yes;
  the route text treating the full generic theorem as still missing was unsupported and is corrected here;
- production Lean edit verdict:
  none in this bounded pass;
- whether any genuinely smaller compile-clean theorem/local lemma landed:
  no;
  there is no remaining honest theorem-sized split *below* the full generic theorem because that theorem is already live;
- whether the full generic nuclear-seminorm theorem now lowers one more step:
  yes;
  the theorem has already been fully assembled and verified live;
- exact first internal proof obligation still beneath it:
  none on this producer seam;
  the old beneath-generic obligations are discharged by the live theorem chain;
- whether that obligation is genuinely smaller or just the same blocker restated:
  neither;
  there is no residual below-generic blocker left to split;
- exact best Lean-ready theorem statement now required:

  `∀ {d n m : ℕ} (s : Finset (ℕ × ℕ))
      (B : Set (SchwartzNPoint d (n + m))),
      Bornology.IsVonNBounded ℂ B ->
        ∀ ε > 0, ∃ t : Finset (SchwartzNPoint d (n + m)),
          ∀ h ∈ B, ∃ g ∈ t,
            (s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ))
              (h - g) < ε`;
- exact minimal hypotheses and codomain objects:
  fixed `d n m`,
  fixed finite `s`,
  bounded `B : Set (SchwartzNPoint d (n + m))`,
  and `Bornology.IsVonNBounded ℂ B`;
  no quotient object is needed in the immediate theorem statement;
- intended insertion surface:
  the Schwartz-shell specialization layer immediately above the generic producer theorem and immediately below
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`;
- exact first internal proof obligation beneath that next theorem:
  derive `q`-boundedness of `B` for the dominating seminorm `q` coming from nuclear dominance, then invoke the already-landed generic theorem at
  `p := s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)`;
- why that obligation is genuinely smaller than the former generic theorem:
  it is pure specialization / boundedness transfer;
  it no longer includes any tail-series estimate, finite-dimensional coordinate net extraction, representative selection, or `tsum` splitting;
- exact downstream chain:
  Schwartz finite-net theorem
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  strong-topology basis / `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  ->
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- witness decision:
  keep `T_t` existential;
  early extraction remains wrapper drift only.

Supervision correction (`2026-04-15 11:08 UTC`): the last bounded Codex pass left a compile-clean Lean landing that must be treated as the live frontier correction, not as a docs-only verdict. `OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean` now contains `finite_net_of_nuclear_head_tail`, the genuine direct head-plus-tail assembly theorem below the full generic nuclear-seminorm finite-net theorem. It is not wrapper drift: it assembles the already-landed finite-head net, the pairwise-difference tail bound, and the nuclear-dominance inequality into the final `p`-estimate after splitting the `tsum` at `N`. Fresh supervision re-checks also confirmed that `section43_fixedTimeShellRawCLM` is still immediately followed by `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`, the live theorem-level `sorry`s are unchanged, and `lake env lean OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean` exits `0` with only the pre-existing unrelated theorem-level `sorry` warnings. The next active producer seam is therefore the full generic nuclear-seminorm finite-net / precompactness theorem immediately above `finite_net_of_nuclear_head_tail`.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether the generic nuclear-seminorm finite-net theorem really lowers one more
theorem-sized step (`2026-04-15`, current bounded pass,
`2026-04-15T23:59:59Z`):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- compile-clean consumer re-check:
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  remains present compile-clean in `SchwartzComplete.lean`;
- compile-clean infrastructure re-check:
  `uniform_nuclear_tail_lt_of_q_bounded`,
  `finite_coordinate_image_totallyBounded_of_q_bounded`,
  and
  `finite_weighted_coordinate_net_of_q_bounded`
  remain present compile-clean in `NuclearSpace.lean`;
- verification re-check:
  `lake env lean OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean`
  exited `0` with only the pre-existing unrelated theorem-level `sorry`
  warnings at
  `gauge_dominates_on_subspace_of_convex_nhd`
  and
  `product_seminorm_dominated`;
  `lake env lean OSReconstruction/SCV/SchwartzComplete.lean`
  exited `0` with only pre-existing `push_neg` deprecation warnings;
- unsupported residue verdict:
  yes;
  the previous claim that the active generic theorem honestly decomposes one
  theorem-sized step smaller was not supported after re-checking the live
  theorem statements and the actual data exposed by the landed finite-family
  theorem;
- no production Lean edit landed in this pass;
- decomposition result:
  the active generic theorem does not presently lower to a distinct honest
  theorem surface below itself;
- exact first internal proof obligation beneath it:
  the truncation-and-assembly proof itself:
  choose `N` from the uniform tail lemma,
  get a finite net for the truncated weighted seminorm,
  then control `p (x - y)` by splitting the nuclear `tsum` at `N` and bounding
  the tail using
  `q (x - y) ≤ q x + q y`
  with a finite `q`-bound on the chosen net centers;
- whether that obligation is genuinely smaller:
  no;
  it is the same generic precompactness blocker in proof form rather than a new
  theorem-sized subproblem;
- exact best Lean-ready theorem statement now required:
  the generic `p`-finite-net theorem on a `q`-bounded set with hypotheses
  `c ≥ 0`,
  `Summable c`,
  `‖f n x‖ ≤ q x`,
  and
  `p x ≤ ∑' n, ‖f n x‖ * c n`,
  producing
  `∀ ε > 0, ∃ t : Finset V, ∀ x ∈ B, ∃ y ∈ t, p (x - y) < ε`;
- exact minimal hypotheses:
  seminorms `p q`,
  scalar-valued `f : ℕ → V →L[ℝ] ℝ`,
  nonnegative summable `c`,
  the nuclear-dominance inequality for `p`,
  and `q`-boundedness of `B`;
- exact codomain/quotient objects:
  none;
  internally only a finite product `ι → ℝ` and the scalar tail function are
  needed;
- exact insertion surface:
  `OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean`,
  immediately below
  `finite_weighted_coordinate_net_of_q_bounded`;
- exact downstream chain:
  generic nuclear-seminorm finite-net theorem
  ->
  Schwartz finite-net theorem
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  strong-topology basis / `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  ->
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell witness existence,
  pointwise convergence,
  finite-seminorm shell control,
  and local nuclear-dominance data;
  already landed new infrastructure:
  uniform tail truncation and the full finite-family weighted-coordinate net
  theorem;
  genuinely new:
  only the full generic nuclear-seminorm finite-net theorem,
  then the Schwartz specialization;
- witness decision:
  keep `T_t` existential;
  early extraction is still wrapper drift only.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
directly in live Lean whether
`finite_weighted_coordinate_net_of_q_bounded`
is still an unfinished producer theorem surface (`2026-04-15`, current bounded
pass, `2026-04-15T23:59:59Z`):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- compile-clean consumer re-check:
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  remains present compile-clean in `SchwartzComplete.lean`;
- live infrastructure correction:
  `finite_weighted_coordinate_net_of_q_bounded`
  is already present with a complete proof in `NuclearSpace.lean`;
  the active unfinished producer surface is therefore not that theorem;
- verification re-check:
  `lake env lean OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean`
  exited `0` with only the pre-existing unrelated theorem-level `sorry`
  warnings at
  `gauge_dominates_on_subspace_of_convex_nhd`
  and
  `product_seminorm_dominated`;
- unsupported residue verdict:
  yes;
  the old frontier claim that
  `finite_weighted_coordinate_net_of_q_bounded`
  was still unfinished was unsupported by live Lean and is removed here;
- no production Lean edit landed in this pass;
- decomposition result:
  the finite-family scalar-coordinate theorem still lowers one honest step
  smaller;
- exact first internal proof obligation beneath it:
  pull back a finite `δ`-net in the coordinate image
  `coord '' B ⊆ (ι → ℝ)` to a finite subset of `B`, then convert
  coordinatewise
  `dist (g i x) (g i y) < δ`
  into
  `∑ i, ‖g i (x - y)‖ * a i < ε`;
- whether that obligation is genuinely smaller:
  yes in theorem shape;
  no as an active blocker, because the live theorem proof already discharges
  it inline;
- exact active-surface correction:
  the next genuinely new theorem required is now the generic nuclear-seminorm
  finite-net theorem above the already-landed tail and finite-family inputs;
- exact best Lean-ready theorem statement now required:
  the generic `p`-finite-net theorem on a `q`-bounded set with hypotheses
  `c ≥ 0`,
  `Summable c`,
  `‖f n x‖ ≤ q x`,
  and
  `p x ≤ ∑' n, ‖f n x‖ * c n`,
  producing
  `∀ ε > 0, ∃ t : Finset V, ∀ x ∈ B, ∃ y ∈ t, p (x - y) < ε`;
- exact minimal hypotheses:
  seminorms `p q`,
  scalar-valued `f : ℕ → V →L[ℝ] ℝ`,
  nonnegative summable `c`,
  the nuclear-dominance inequality for `p`,
  and `q`-boundedness of `B`;
- exact codomain/quotient objects:
  none;
  internally only a finite product `ι → ℝ` is needed;
- exact insertion surface:
  `OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean`,
  immediately below
  `finite_weighted_coordinate_net_of_q_bounded`;
- exact downstream chain:
  generic nuclear-seminorm finite-net theorem
  ->
  Schwartz finite-net theorem
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  strong-topology basis / `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  ->
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell witness existence,
  pointwise convergence,
  finite-seminorm shell control,
  and local nuclear-dominance data;
  already landed new infrastructure:
  uniform tail truncation and the full finite-family weighted-coordinate net
  theorem;
  genuinely new:
  the generic nuclear-seminorm finite-net theorem,
  then the Schwartz specialization.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
the finite-family scalar-coordinate finite-net theorem source-first against the
live shell and Lean files (`2026-04-15`, current bounded pass,
`2026-04-15T23:59:59Z`):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- compile-clean consumer re-check:
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  remains present compile-clean in `SchwartzComplete.lean`;
- live infrastructure re-check:
  `uniform_nuclear_tail_lt_of_q_bounded`,
  `finite_coordinate_image_totallyBounded_of_q_bounded`,
  and
  `finite_weighted_coordinate_net_of_q_bounded`
  are all already present compile-clean in `NuclearSpace.lean`;
- verification re-check:
  `lake env lean OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean`
  exited `0` with only the pre-existing unrelated theorem-level `sorry`
  warnings at
  `gauge_dominates_on_subspace_of_convex_nhd`
  and
  `product_seminorm_dominated`;
- unsupported residue verdict:
  none found and none removed;
- no production Lean edit landed in this pass;
- decomposition result:
  the finite-family scalar-coordinate theorem does lower one honest step
  smaller;
- exact first internal proof obligation beneath it:
  pull back a finite `δ`-net in the coordinate image
  `coord '' B ⊆ (ι → ℝ)` to a finite subset of `B`, then convert the resulting
  coordinatewise estimate
  `∀ i, dist (g i x) (g i y) < δ`
  into
  `∑ i, ‖g i (x - y)‖ * a i < ε`;
- whether that obligation is genuinely smaller:
  yes in theorem shape;
  no as an active blocker, because the live proof of
  `finite_weighted_coordinate_net_of_q_bounded`
  already discharges it;
- exact active-surface correction:
  the finite-family theorem is no longer the missing surface on the route;
  the next genuinely new theorem required is now the generic nuclear-seminorm
  finite-net theorem above the already-landed tail and finite-family inputs;
- exact best Lean-ready theorem statement now required:
  the generic `p`-finite-net theorem on a `q`-bounded set with hypotheses
  `c ≥ 0`,
  `Summable c`,
  `‖f n x‖ ≤ q x`,
  and
  `p x ≤ ∑' n, ‖f n x‖ * c n`,
  producing
  `∀ ε > 0, ∃ t : Finset V, ∀ x ∈ B, ∃ y ∈ t, p (x - y) < ε`;
- exact minimal hypotheses:
  seminorms `p q`,
  scalar-valued `f : ℕ → V →L[ℝ] ℝ`,
  nonnegative summable `c`,
  the nuclear-dominance inequality for `p`,
  and `q`-boundedness of `B`;
- exact codomain/quotient objects:
  none;
  internally only a finite product `ι → ℝ` is needed;
- exact insertion surface:
  `OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean`,
  immediately below
  `finite_weighted_coordinate_net_of_q_bounded`;
- exact downstream chain:
  generic nuclear-seminorm finite-net theorem
  ->
  Schwartz finite-net theorem
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  strong-topology basis / `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  ->
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell witness existence,
  pointwise convergence,
  finite-seminorm shell control,
  and local nuclear-dominance data;
  already landed new infrastructure:
  uniform tail truncation and the full finite-family weighted-coordinate net
  theorem;
  genuinely new:
  the generic nuclear-seminorm finite-net theorem,
  then the Schwartz specialization.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether the uniform nuclear-tail truncation lemma itself still lowers one
honest theorem-sized step smaller (`2026-04-15`, current bounded pass,
`2026-04-15T23:59:00Z`):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- compile-clean consumer re-check:
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  remains present compile-clean in `SchwartzComplete.lean`;
- live infrastructure refinement:
  the smaller scalar-tail ingredients are already present compile-clean in
  `NuclearSpace.lean` as
  `nuclear_tail_le_of_q_bounded`
  and
  `mul_tsum_nat_add_lt_of_summable_nonneg`;
- unsupported residue verdict:
  none found and none removed;
  those two scalar lemmas are genuine infrastructure, not dead wrappers;
- no production Lean edit landed in this pass;
- decomposition result:
  the uniform nuclear-tail truncation lemma lowers one step smaller only to the
  already-landed pair
  `nuclear_tail_le_of_q_bounded`
  +
  `mul_tsum_nat_add_lt_of_summable_nonneg`;
  therefore the first genuinely new theorem surface is now the assembled
  uniform tail lemma itself;
- exact first internal proof obligation under the generic precompactness theorem:
  still the full uniform nuclear-tail truncation lemma

  `∃ M > 0, ∀ x ∈ B, q x < M`
  +
  `Summable c`
  +
  `∀ n, 0 ≤ c n`
  +
  `∀ n x, ‖f n x‖ ≤ q x`
  =>
  `∀ ε > 0, ∃ N, ∀ x ∈ B, ∑' n, ‖f (n + N) x‖ * c (n + N) < ε`;
- whether the lower obligation is genuinely smaller:
  the two scalar pieces are genuinely smaller;
  no further honest smaller new theorem remains beneath them, because the next
  assembly step is exactly the missing uniform tail lemma rather than a
  reusable theorem distinct from it;
- exact best Lean-ready theorem statement now required:
  the uniform tail lemma in `NuclearSpace.lean` with the statement recorded in
  `docs/theorem3_os_route_blueprint.md`;
- exact minimal hypotheses:
  fixed seminorm `q`,
  scalar-valued `f : ℕ → E →L[ℝ] ℝ`,
  `c ≥ 0`,
  `Summable c`,
  `‖f n x‖ ≤ q x`,
  and `q`-boundedness of `B`;
- exact codomain/quotient objects:
  none;
  this theorem is still purely scalar-tail control on the original space;
- exact next obligation after the tail lemma:
  finite-family scalar-coordinate finite nets on `q`-bounded sets;
- why no smaller compile-clean result landed:
  the only honest smaller results were already present in production;
  no new theorem smaller than the full uniform tail lemma surfaced without just
  rephrasing the same assembly blocker.

Fresh bounded theorem-3 audit on the same live actual-shell route, testing
whether the generic nuclear-seminorm precompactness theorem splits one honest
theorem-sized step smaller (`2026-04-15`, current bounded pass,
`2026-04-15T23:30:00Z`):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- compile-clean consumer re-check:
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  remains present compile-clean in `SchwartzComplete.lean`;
- unsupported residue verdict:
  none found and none removed;
- no production Lean edit landed in this pass;
- decomposition result:
  the generic nuclear-seminorm precompactness theorem **does** lower one step
  further;
- exact first internal proof obligation beneath it:
  uniform truncation of the nuclear tail on a `q`-bounded set:

  `∃ M > 0, ∀ x ∈ B, q x < M`
  +
  `Summable c`
  +
  `∀ n x, ‖f n x‖ ≤ q x`
  =>
  `∀ ε > 0, ∃ N, ∀ x ∈ B, ∑' n, ‖f (n + N) x‖ * c (n + N) < ε`;
- whether that obligation is genuinely smaller:
  yes;
  it only controls the infinite-series tail uniformly on `B` and leaves the
  finite-dimensional covering argument untouched;
- exact best Lean-ready theorem statement now required:
  a tail lemma in `NuclearSpace.lean` with the statement recorded in
  `docs/theorem3_os_route_blueprint.md`;
- exact minimal hypotheses:
  fixed seminorm `q`,
  scalar-valued `f : ℕ → E →L[ℝ] ℝ`,
  `c ≥ 0`,
  `Summable c`,
  `‖f n x‖ ≤ q x`,
  and `q`-boundedness of `B`;
- exact codomain/quotient objects:
  none;
  the lemma is purely scalar-tail control;
- exact next obligation after the tail lemma:
  finite-family scalar-coordinate finite nets for bounded images in a compact
  box;
- why no smaller compile-clean result landed anyway:
  the tail lemma is honest, but the bounded pass did not close the required
  coercion/tsum proof plumbing cleanly enough for a production landing;
- intended insertion surface:
  `NuclearSpace.lean`,
  then the next internal theorem remains the finite-family coordinate-net
  theorem, then the full generic precompactness theorem, then the Schwartz
  specialization in `SchwartzComplete.lean`;
- exact downstream chain:
  uniform nuclear-tail truncation
  ->
  finite-family scalar-coordinate net
  ->
  generic nuclear-seminorm precompactness
  ->
  Schwartz finite-net theorem
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  strong-topology basis / `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  ->
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell witness existence, pointwise convergence, finite-seminorm shell
  control, and nuclear dominance data already exported by the local
  infrastructure;
  genuinely new:
  uniform nuclear-tail truncation,
  finite-family scalar-coordinate nets,
  and then generic nuclear-seminorm precompactness;
- witness packaging decision:
  keep `T_t` existentially packaged;
  early extraction remains wrapper drift only.

Fresh bounded theorem-3 audit on the same live actual-shell route, refining
the exact first internal proof obligation strictly below the shell producer
theorem (`2026-04-15`, current bounded pass, `2026-04-15T20:30:00Z`):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- compile-clean consumer re-check:
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  remains present compile-clean in `SchwartzComplete.lean`;
- unsupported residue verdict:
  none found and none removed;
- no production Lean edit landed in this pass;
- exact best shell producer theorem remains:

  `Bornology.IsVonNBounded ℂ B ->
    ∀ ε > 0, ∃ t : Finset (SchwartzNPoint d (n + m)),
      ∀ h ∈ B, ∃ g ∈ t,
        p (h - g) < ε`,

  for
  `p := s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)`;
- exact first internal proof obligation beneath that theorem has improved:
  because
  `schwartz_withSeminorms`
  plus
  `WithSeminorms.isVonNBounded_iff_seminorm_bounded`
  already upgrade a bounded Schwartz set to boundedness in any fixed
  continuous seminorm `q`,
  the first real missing package is now:

  `q`-bounded `B`
  +
  nuclear decomposition of `p` dominated by `q`
  =>
  finite `p`-nets on `B`;
- whether this is genuinely smaller than the full theorem:
  yes;
  it removes the Schwartz-specific bornology step and isolates the new
  mathematics in a generic nuclear-space theorem;
- exact best Lean-ready theorem statement now required:
  a generic theorem in `NuclearSpace.lean` asserting finite `p`-nets for
  `q`-bounded sets from nuclear decomposition data for `p`;
- exact minimal hypotheses:
  fixed `p q : Seminorm ℝ E`,
  `Continuous p`,
  `Continuous q`,
  nuclear decomposition data
  `(f, c)` with
  `‖f n x‖ ≤ q x`
  and
  `p x ≤ ∑' n, ‖f n x‖ * c n`,
  and `∃ M > 0, ∀ x ∈ B, q x < M`;
- exact codomain/quotient objects:
  none are needed in this theorem;
  the only codomain used internally is the finite-dimensional scalar coordinate
  space coming from finitely many functionals;
- why no smaller compile-clean result landed:
  no theorem below this generic nuclear-seminorm precompactness step surfaced
  that would be both immediately shell-consumable and not just a wrapper over
  the same tail-truncation + finite-dimensional cover argument;
- why the older Montel route is no longer the right first obligation:
  `Bornology.IsVonNBounded ℂ B -> IsCompact (closure B)` remains stronger than
  needed and is no longer the nearest honest seam once `q`-boundedness is
  available directly from `WithSeminorms`;
- intended insertion surface:
  generic theorem in `NuclearSpace.lean`,
  short Schwartz specialization in `SchwartzComplete.lean`,
  then immediate use in
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- exact downstream chain:
  generic nuclear-seminorm precompactness
  ->
  Schwartz finite-net theorem
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  strong-topology basis / `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  ->
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell witness existence,
  pointwise convergence,
  finite-seminorm shell control,
  and the strong-topology basis API;
  genuinely new:
  generic nuclear-seminorm precompactness
  and its Schwartz specialization;
- witness packaging decision:
  keep `T_t` existentially packaged;
  early extraction remains wrapper drift only.

Fresh bounded theorem-3 audit on the same live actual-shell route, pinning the
exact shell-facing producer theorem statement rather than testing a stronger
Montel lane (`2026-04-15`, current bounded pass, `2026-04-15T16:00:00Z`):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- compile-clean consumer re-check:
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  remains present compile-clean in `SchwartzComplete.lean`;
- unsupported residue verdict:
  none found and none removed;
- no production Lean edit landed in this pass;
- exact best Lean-ready producer theorem now required:

  `Bornology.IsVonNBounded ℂ B ->
    ∀ ε > 0, ∃ t : Finset (SchwartzNPoint d (n + m)),
      ∀ h ∈ B, ∃ g ∈ t,
        p (h - g) < ε`,

  for
  `p := s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)`;
- exact minimal hypotheses:
  fixed `d n m`,
  fixed finite index set `s`,
  arbitrary `B : Set (SchwartzNPoint d (n + m))`,
  and only
  `Bornology.IsVonNBounded ℂ B`;
- exact codomain/quotient objects:
  none are needed in the immediate theorem statement consumed by the shell;
  the equivalent quotient-language reformulation
  `TotallyBounded ((q_p) '' B)` uses the seminorm quotient/pseudometric object
  for `p`, but that quotient surface is not the cleanest first landing because
  the shell consumer already expects `hnet`;
- exact first internal proof obligation beneath it:
  no honest smaller theorem-sized consumer-ready lemma surfaced;
  every honest reformulation is still the same fixed-seminorm precompactness
  problem for bounded Schwartz sets;
- whether any smaller reusable ingredient exists:
  no immediately consumable one;
  closure of bounded sets, boundedness of `p '' B`, or generic seminorm
  continuity do not supply finite `p`-nets on `B`;
- why the Montel route is stronger rather than smaller:
  its first missing theorem is
  `Bornology.IsVonNBounded ℂ B -> IsCompact (closure B)`,
  a full Heine-Borel statement for Schwartz space, stronger than the fixed
  seminorm shell producer theorem;
- intended insertion surface:
  producer theorem in `SchwartzComplete.lean` or a nearby compactness file,
  then immediate specialization inside
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- exact downstream chain:
  producer finite-net theorem
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  strong-topology basis / `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  ->
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell witness existence,
  pointwise convergence,
  common finite seminorm control,
  and the strong-topology basis API;
  genuinely new:
  the fixed-seminorm finite-net theorem for bounded Schwartz sets;
- witness packaging decision:
  keep `T_t` existentially packaged;
  early extraction remains wrapper drift only.

Fresh bounded theorem-3 audit on the same live actual-shell route, after a
direct source/Lean check that no smaller producer-side compactness ingredient
has yet landed below the fixed finite-seminorm theorem (`2026-04-15`, current
bounded pass, `2026-04-15T00:00:00Z`):

Fresh bounded theorem-3 audit on the same live actual-shell route, testing the
Montel/closure/compact-image route rather than introducing any new theorem
surface (`2026-04-15`, current bounded pass, `2026-04-15T12:00:00Z`):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- compile-clean consumer re-check:
  `SchwartzMap.tempered_tendstoUniformlyOn_of_finite_seminorm_net`
  and
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  remain present compile-clean in `SchwartzComplete.lean`;
- unsupported residue verdict:
  none found and none removed;
- no production Lean edit landed in this pass;
- exact route result:
  the tested Montel/closure/compact-image route fails with current
  infrastructure;
- exact progress on that route:
  `Bornology.IsVonNBounded.closure` does exist, so
  `bounded B -> bounded (closure B)` is available;
  the route then stops because no existing Schwartz-space Montel / Heine-Borel
  theorem was found that upgrades bounded closed sets, or specifically
  `closure B`, to compact sets;
- exact first missing proof obligation on that route:

  `Bornology.IsVonNBounded ℂ B -> IsCompact (closure B)`

  for
  `B : Set (SchwartzNPoint d (n + m))`;
- whether this is genuinely smaller than the producer theorem:
  no;
  it is stronger than the fixed-seminorm producer theorem and therefore not an
  honest theorem-sized landing below the producer seam;
- exact best Lean-ready producer theorem still required:

  `Bornology.IsVonNBounded ℂ B ->
    TotallyBounded ((q_p) '' B)`,

  equivalently finite `p`-`ε` nets on bounded `B`, for
  `p := s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)`;
- compact-image continuation status:
  even after hypothetical compactness of `closure B`, the route would still
  need a seminorm quotient / pseudometric codomain and continuous quotient map
  `q_p` with enough API to read total boundedness as finite `p`-nets;
  no ready-made route for that was found in the inspected infrastructure, but
  that is not the first blocker on the tested route;
- intended insertion surface:
  unchanged;
  the producer theorem belongs in `SchwartzComplete.lean` or a nearby
  compactness file,
  then immediate specialization inside
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- exact downstream chain:
  producer theorem
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  strong-topology basis / `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell witness existence,
  pointwise convergence,
  common finite seminorm control,
  and the strong-topology basis API;
  genuinely new:
  the fixed-seminorm bounded-set finite-net / quotient-total-boundedness
  theorem;
  stronger-than-needed and also absent:
  a Schwartz-space Montel / compact-closure theorem;
- witness packaging decision:
  keep `T_t` existentially packaged;
  early extraction remains wrapper drift only.

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- compile-clean consumer re-check:
  `SchwartzMap.tempered_tendstoUniformlyOn_of_finite_seminorm_net`
  and
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  are still present in `OSReconstruction/SCV/SchwartzComplete.lean`,
  and the file still compiles cleanly;
- unsupported residue verdict:
  none found and none removed;
- no production Lean edit landed in this pass;
- exact best Lean-ready producer theorem still required:

  `Bornology.IsVonNBounded ℂ B ->
    TotallyBounded ((q_p) '' B)`,

  equivalently finite `p`-`ε` nets on bounded `B`, for
  `p := s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)`;
- whether this truly lowers to a smaller reusable ingredient:
  no;
  every honest reformulation is still the same compactness blocker;
- exact first missing proof obligation beneath it:
  prove that a von Neumann bounded
  `B : Set (SchwartzNPoint d (n + m))`
  admits finite `p`-nets for the fixed finite seminorm `p`;
- why no smaller compile-clean theorem landed:
  the audited nuclear/Montel infrastructure gives only
  seminorm-domination,
  nuclearity,
  strong-topology basis lemmas,
  and compactness consequences of already-known total boundedness;
  it does not provide the missing boundedness `->` quotient total boundedness
  upgrade for a fixed Schwartz seminorm;
- intended insertion surface:
  the producer theorem in `SchwartzComplete.lean` or a nearby compactness file,
  then immediate specialization inside
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- exact downstream chain:
  producer theorem
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  strong-topology basis / `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell witness existence,
  pointwise convergence,
  common finite seminorm control,
  and the strong-topology basis API;
  genuinely new:
  the fixed-seminorm bounded-set finite-net / quotient-total-boundedness
  producer theorem;
- witness packaging decision:
  keep `T_t` existentially packaged;
  early extraction remains wrapper drift only.

Fresh bounded theorem-3 audit on the same live actual-shell route, after
re-checking the actual shell-facing consumer now present in
`SchwartzComplete.lean` (`2026-04-15`, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact shell-facing consumer now available compile-clean:
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  at
  `OSReconstruction/SCV/SchwartzComplete.lean:537`;
- exact theorem surface:
  from
  common `p`-control,
  pointwise convergence,
  finite `p`-nets on `B`,
  and `hU : U ∈ nhds (0 : G)`,
  conclude
  `∀ᶠ a in l, Set.MapsTo (fun φ => (T a - L) φ) B U`;
- exact minimal hypotheses:
  `p : Seminorm ℝ (SchwartzMap E F)`,
  `C : ℝ` with `0 ≤ C`,
  `B : Set (SchwartzMap E F)`,
  arbitrary filter `l`,
  codomain `G` only assumed normed over `ℝ`,
  the eventual common bound,
  pointwise convergence,
  and finite `p`-nets on `B`;
- audit verdict on theorem size:
  this consumer theorem is genuinely smaller than the producer compactness
  theorem because it assumes `hnet` rather than proving it;
- first exact missing proof beneath that consumer:
  prove finite `p`-nets / quotient total boundedness for bounded Schwartz sets
  under the fixed finite controlling seminorm;
- intended insertion surface:
  no new theorem should be inserted between
  `tempered_tendstoUniformlyOn_of_finite_seminorm_net`
  and the shell file;
  the next insertion remains directly inside
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- downstream chain:
  packaged `T_t`
  ->
  common finite controlling seminorm `p`
  ->
  producer finite-net theorem
  ->
  `tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  strong-topology neighbourhood basis / `eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  witness existence and pointwise convergence from OS II Section `V`,
  plus mathlib strong-topology basis lemmas;
  genuinely new:
  the finite-net consumer theorem and the still-missing producer theorem;
- witness packaging decision:
  keep `T_t` existentially packaged;
  early extraction remains wrapper drift only;
- unsupported residue verdict for this pass:
  no Lean residue found;
  older doc notes that said no shell-facing consumer had landed are now
  superseded by this audit entry.

Fresh bounded theorem-3 audit on the same live actual-shell route, refining the
decomposition below the fixed-seminorm compactness seam
(`2026-04-15`, current bounded pass, `2026-04-15T08:09:20Z`):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact strongest new producer theorem still required:

  `Bornology.IsVonNBounded ℂ B ->
    TotallyBounded ((q_p) '' B)`

  for
  `p := s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)`,
  with `q_p` the canonical quotient / pseudometric map induced by `p`;
- exact minimal hypotheses:
  fixed finite seminorm family index set `s`,
  induced seminorm `p`,
  arbitrary set `B : Set (SchwartzNPoint d (n + m))`,
  and only
  `Bornology.IsVonNBounded ℂ B`;
- refined blocker decomposition:
  a genuinely smaller **consumer** exists beneath that theorem surface, namely
  a finite-net uniformization lemma turning
  common `p`-Lipschitz control
  +
  pointwise convergence
  +
  finite `p`-nets on `B`
  into
  `TendstoUniformlyOn` / eventual bounded-set `MapsTo`;
- but this does not shrink the new producer theorem:
  the first exact proof obligation on the producer side is still to show that
  bounded Schwartz sets admit finite `p`-nets, equivalently are totally bounded
  in the quotient induced by `p`;
- audit verdict:
  no compile-clean abstract theorem, local lemma, or honest theorem skeleton
  landed in production Lean in this pass;
- why not:
  the smaller consumer was identified only as a route decomposition;
  no verified theorem proof was completed, so landing a theorem statement would
  have been wrapper drift;
- clean insertion surface if later formalized:
  producer theorem in `OSReconstruction/SCV/SchwartzComplete.lean`
  immediately below the Banach-Steinhaus finite-seminorm package;
  consumer lemma beside
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`;
- exact specialization path:
  packaged witness `T_t`
  ->
  common finite controlling seminorm `p`
  ->
  bounded-set quotient total boundedness / finite `p`-nets
  ->
  finite-net uniformization consumer
  ->
  bounded-set `MapsTo`
  ->
  `TendstoUniformlyOn`
  ->
  strong convergence
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell growth,
  witness existence with pointwise convergence,
  common finite seminorm control,
  and the strong-topology / Montel API surface;
  genuinely new:
  the bounded-set finite-net / quotient-total-boundedness producer theorem;
  also new but downstream:
  the finite-net uniformization consumer lemma;
- witness packaging decision:
  keep `T_t` existentially packaged;
  early extraction remains wrapper drift only;
- unsupported residue verdict:
  none found and none removed.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether the exact fixed-seminorm total-boundedness theorem surface now supports
one honest compile-clean theorem statement or skeleton below the shell seam
(`2026-04-15`, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages `289`, `290-291`, `293-295`;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `gauge_dominates_on_subspace_of_convex_nhd`,
  `subspace_seminorm_dominated`,
  and `product_seminorm_dominated`;
  mathlib declarations
  `schwartzSeminormFamily`,
  `schwartzSeminormFamily_apply`,
  `MontelSpace.isCompact_of_isClosed_of_isVonNBounded`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `TotallyBounded.isVonNBounded`,
  and `isCompact_closure_of_totallyBounded_quasiComplete`;
- audit verdict:
  no compile-clean abstract theorem, local lemma, or honest theorem skeleton
  landed in `SchwartzComplete.lean` or nearby files;
- exact strongest abstract theorem statement still worth naming:

  `Bornology.IsVonNBounded ℂ B ->
    TotallyBounded ((q_p) '' B)`

  for
  `p := s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)`,
  equivalently finite `p`-nets on bounded `B`;
- whether that theorem surface is honest progress:
  no;
  on the current route it is only the shell bounded-set compactness upgrade
  rewritten in quotient / pseudometric language;
- first exact proof obligation beneath that surface:
  prove bounded Schwartz sets are totally bounded in the quotient induced by the
  fixed controlling seminorm `p`;
- why that obligation is identical to the unresolved shell seam:
  once finite `p`-nets are available, the shell route gets eventual `MapsTo`,
  then `TendstoUniformlyOn`, then strong convergence immediately;
  nothing genuinely smaller has been isolated;
- exact specialization path:
  packaged witness `T_t`
  ->
  common finite controlling seminorm `p`
  ->
  finite `p`-nets / quotient total boundedness
  ->
  bounded-set `MapsTo`
  ->
  `TendstoUniformlyOn`
  ->
  strong convergence
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell growth,
  witness existence with pointwise convergence,
  common finite seminorm control,
  and the strong-topology / Montel API surface;
  genuinely new:
  the fixed-seminorm quotient-total-boundedness theorem itself;
- witness packaging decision:
  keep `T_t` existentially packaged;
  early extraction remains wrapper drift only;
- unsupported residue verdict:
  none found and none removed;

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether one honest compile-clean fixed-seminorm total-boundedness theorem or
skeleton can now be landed below the shell bounded-set seam
(`2026-04-15`, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages `289`, `290-291`, `293-295`;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `gauge_dominates_on_subspace_of_convex_nhd`,
  `subspace_seminorm_dominated`,
  and `product_seminorm_dominated`;
  mathlib declarations
  `schwartzSeminormFamily`,
  `MontelSpace.isCompact_of_isClosed_of_isVonNBounded`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `TotallyBounded.isVonNBounded`,
  and `isCompact_closure_of_totallyBounded_quasiComplete`;
- audit verdict:
  no compile-clean abstract theorem, local lemma, or honest theorem skeleton
  landed in `SchwartzComplete.lean` or nearby files;
- exact strongest abstract theorem statement still worth naming:

  `Bornology.IsVonNBounded ℂ B ->
    TotallyBounded ((q_p) '' B)`

  for
  `p := s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)`,
  equivalently finite `p`-nets on bounded `B`;
- whether that statement is honest progress:
  not by itself on the current route;
  it is only a renamed form of the same shell bounded-set compactness upgrade,
  not a smaller theorem;
- first exact proof obligation beneath that theorem surface:
  turn common fixed-seminorm control on a bounded set into total boundedness in
  the induced quotient / pseudometric, hence into eventual bounded-set `MapsTo`;
- why that obligation is identical to the unresolved shell compactness upgrade:
  once finite `p`-nets are available, the shell route gets eventual `MapsTo`
  immediately, then `TendstoUniformlyOn`, then strong convergence;
  so the theorem surface does not cut below the blocker;
- exact specialization path:
  packaged witness `T_t`
  ->
  common finite controlling seminorm `p`
  ->
  finite `p`-nets / quotient total boundedness
  ->
  eventual `MapsTo`
  ->
  `TendstoUniformlyOn`
  ->
  strong convergence
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell growth,
  witness existence with pointwise convergence,
  common finite seminorm control,
  and the strong-topology / Montel API surface;
  genuinely new:
  the fixed-seminorm quotient-total-boundedness theorem itself;
- witness packaging decision:
  keep `T_t` existentially packaged;
  early extraction remains wrapper drift only;
- unsupported residue verdict:
  none found and none removed;

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether a reusable Schwartz/Montel/nuclear theorem now sits strictly below the
shell bounded-set theorem (2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages `289`, `290-291`, `293-295`;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `gauge_dominates_on_subspace_of_convex_nhd`,
  `subspace_seminorm_dominated`,
  and `product_seminorm_dominated`;
  mathlib declarations
  `schwartzSeminormFamily`,
  `MontelSpace.isCompact_of_isClosed_of_isVonNBounded`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `TotallyBounded.isVonNBounded`,
  and `isCompact_closure_of_totallyBounded_quasiComplete`;
- audit verdict:
  no compile-clean abstract Montel/nuclear theorem,
  no honest infrastructure theorem statement/skeleton,
  and no local lemma/instance landed;
- unsupported residue verdict:
  none found and none removed;
- exact strongest abstract theorem statement now worth pursuing:

  `Bornology.IsVonNBounded ℂ B ->
    ∀ εp > 0, ∃ t : Finset (SchwartzNPoint d (n + m)),
      ∀ h ∈ B, ∃ g ∈ t, p (h - g) < εp`

  for
  `p := s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)`,
  equivalently
  `Bornology.IsVonNBounded ℂ B -> TotallyBounded ((q_p) '' B)`;
- whether that theorem is genuinely smaller than the shell bounded-set theorem:
  no;
  it is only the same missing bounded-set `MapsTo` upgrade in
  quotient/precompactness language;
- exact specialization path:
  pointwise convergence to packaged witness `T_t`
  +
  common finite `p`-control
  +
  total boundedness / finite `p`-nets on bounded `B`
  =>
  eventual `MapsTo`
  =>
  `TendstoUniformlyOn`
  =>
  strong convergence
  =>
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell growth,
  witness existence with pointwise convergence,
  common finite seminorm control,
  and the strong-topology / Montel API surface;
  genuinely new:
  the Schwartz fixed-seminorm total-boundedness theorem itself;
- witness packaging decision:
  keep `T_t` existentially packaged;
  early extraction is still wrapper drift only;
- first exact proof obligation still blocking even the infrastructure landing:
  neither repo nor mathlib currently supplies a Schwartz `MontelSpace`
  instance or a theorem proving fixed finite-seminorm quotient images of
  bounded Schwartz sets are totally bounded,
  and the local nuclear-space lane still has its own theorem-level `sorry`s at
  `gauge_dominates_on_subspace_of_convex_nhd`
  and
  `product_seminorm_dominated`;
- bounded-pass verification commands:
  `rg -n "section43_fixedTimeShellRawCLM|section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime|section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime|sorry" .../OSToWightmanPositivity.lean`
  confirmed the insertion surface and two live theorem-level `sorry`s;
  `mutool draw -F txt -o - references/'reconstruction theorem II.pdf' 9-15`
  confirmed the paper objects;
  `rg -n "sorry" OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean`
  confirmed the nuclear-space blockers;
  no Lean build command was run because no production Lean file changed.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether the fixed controlling-seminorm quotient/precompactness seam honestly
lands as one smaller reusable theorem below the shell bounded-set theorem
(2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages `289`, `290-291`, `293-295`;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`;
  mathlib declarations
  `schwartzSeminormFamily`,
  `MontelSpace.isCompact_of_isClosed_of_isVonNBounded`,
  `ContinuousLinearMap.hasBasis_nhds_zero`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `TotallyBounded.isVonNBounded`,
  `isCompact_closure_of_totallyBounded_quasiComplete`;
  nearby repo file checked and rejected as a supplier:
  `OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean`;
- audit verdict:
  no compile-clean abstract Montel / quotient-precompactness theorem,
  no honest infrastructure theorem statement/skeleton,
  and no local lemma/instance landed;
- unsupported residue verdict:
  none found and none removed;
- exact smallest abstract theorem statement now required:

  `Bornology.IsVonNBounded ℂ B ->
    TotallyBounded ((q_p) '' B)`

  for
  `p := s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)`,
  equivalently bounded sets admit finite `p`-ε-nets;
- whether that theorem is genuinely smaller than the shell bounded-set theorem:
  no;
  this is only the same missing bounded-set `MapsTo` upgrade in fixed-seminorm
  quotient language;
- exact specialization path:
  pointwise convergence to packaged witness `T_t`
  +
  common finite `p`-control
  +
  total boundedness / finite `p`-nets on bounded `B`
  =>
  eventual `MapsTo`
  =>
  `TendstoUniformlyOn`
  =>
  strong convergence
  =>
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell growth,
  existential witness with pointwise convergence,
  and common finite seminorm control;
  genuinely new:
  the Schwartz bounded-set quotient-total-boundedness theorem for a fixed
  controlling finite seminorm;
- witness packaging decision:
  keep `T_t` existentially packaged;
  early extraction is still wrapper drift only;
- first exact proof obligation still blocking even the infrastructure landing:
  no inspected theorem proves
  `Bornology.IsVonNBounded ℂ B -> TotallyBounded ((q_p) '' B)` for Schwartz
  space, and the current local nuclear-space file is still blocked on its own
  independent `sorry` seams before any Montel consequence could be used here;
- bounded-pass verification commands:
  `rg -n "section43_fixedTimeShellRawCLM|section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime|section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime|sorry" .../OSToWightmanPositivity.lean`
  confirmed the insertion surface and two live theorem-level `sorry`s;
  `mutool draw -F txt -o - references/'reconstruction theorem II.pdf' 9-15`
  confirmed the paper objects;
  no Lean build command was run because no production Lean file changed.

Fresh bounded theorem-3 audit on the same live actual-shell route, deciding
whether one honest Schwartz/Montel quotient-precompactness theorem now lands
strictly below the shell bounded-set theorem (2026-04-15, current bounded
pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages `289`, `290-291`, `293-295`;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`;
  mathlib declarations
  `schwartzSeminormFamily`,
  `MontelSpace.isCompact_of_isClosed_of_isVonNBounded`,
  `ContinuousLinearMap.hasBasis_nhds_zero`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`;
  nearby repo file checked and rejected as a supplier:
  `OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean`;
- audit verdict:
  no compile-clean abstract Montel / quotient-precompactness theorem or honest
  infrastructure skeleton landed;
- unsupported residue verdict:
  none found and none removed;
- exact smallest abstract theorem statement now required:

  `Bornology.IsVonNBounded ℂ B ->
    ∀ εp > 0, ∃ t : Finset (SchwartzNPoint d (n + m)),
      ∀ h ∈ B, ∃ g ∈ t, p (h - g) < εp`

  for
  `p := s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)`,
  equivalently
  `Bornology.IsVonNBounded ℂ B -> TotallyBounded ((q_p) '' B)`;
- whether that theorem is genuinely smaller than the shell bounded-set theorem:
  no;
  it is only the same bounded-set `MapsTo` upgrade in fixed-seminorm quotient
  language;
- exact specialization path:
  pointwise convergence to packaged witness `T_t`
  +
  common finite `p`-control
  +
  finite `p`-nets for bounded `B`
  =>
  eventual `MapsTo`
  =>
  `TendstoUniformlyOn`
  =>
  strong convergence
  =>
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  holomorphy,
  shell growth,
  existential boundary-value witness with pointwise convergence,
  and common finite seminorm control;
  genuinely new:
  the Schwartz bounded-set finite-net / quotient-total-boundedness theorem;
- witness packaging decision:
  keep `T_t` existentially packaged;
  early extraction is still wrapper drift only;
- first exact proof obligation still blocking even the infrastructure landing:
  no inspected theorem turns bounded subsets of Schwartz space into finite nets
  for an arbitrary fixed finite Schwartz seminorm, and the local nuclear-space
  file does not yet prove a reusable Montel consequence;
- bounded-pass verification commands:
  `rg -n "section43_fixedTimeShellRawCLM|section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime|section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime|sorry" .../OSToWightmanPositivity.lean`
  confirmed the insertion surface and two live theorem-level `sorry`s;
  `mutool draw -F txt -o - references/'reconstruction theorem II.pdf' 9-15`
  confirmed the paper objects;
  no Lean build command was run because no production Lean file changed.

Fresh bounded theorem-3 audit on the same live actual-shell route, deciding
whether the controlling finite seminorm admits an honest finite-dimensional
coordinate reduction beneath the shell theorem (2026-04-15, current bounded
pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `bvt_F_holomorphic`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `schwartzSeminormFamily`,
  `schwartzSeminormFamily_apply`,
  `ContinuousLinearMap.hasBasis_nhds_zero`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  and
  `MontelSpace.isCompact_of_isClosed_of_isVonNBounded`;
- audit verdict on coordinate reduction:
  no honest finite-dimensional coordinate package sits below this seam;
  the definition of `schwartzSeminormFamily` shows that even one controlling
  seminorm is already a global supremum over all points and derivative data, so
  it does not factor through any finite coordinate map currently formalized here;
- exact quotient-level statement that would be needed if one insists on lowering
  the seam:
  for the quotient pseudometric induced by the fixed controlling seminorm `p`,
  prove that every bounded `B ⊆ SchwartzNPoint d (n + m)` has totally bounded
  image, equivalently admits finite `p`-ε-nets;
- whether that statement is genuinely smaller than the bounded-set shell theorem:
  no;
  it is only the bounded-set `MapsTo` seam in quotient/precompactness form;
- exact specialization path:
  common finite `p`-bound on `shell_ε - T_t`
  +
  pointwise convergence on each fixed test
  +
  total boundedness of bounded sets in the `p`-quotient
  =>
  eventual `MapsTo (fun h => shell_ε h - T_t h) B U`
  =>
  bounded-set uniform convergence
  =>
  strong convergence
  =>
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- why early witness extraction still does not help:
  naming `T_t` earlier does not contribute the absent quotient/precompactness
  theorem;
  it only repackages the witness already returned by
  `forwardTube_boundaryValueData_of_polyGrowth`;
- source-backed versus genuinely new:
  source-backed:
  holomorphy, shell growth, boundary-value witness existence, pointwise
  convergence, common finite seminorm control, and the abstract Montel API;
  genuinely new:
  any Schwartz-specific theorem identifying bounded sets as totally bounded in a
  fixed seminorm quotient, or equivalently any usable Montel consequence for
  Schwartz space on this branch;
- first exact proof obligation still blocking even a theorem-skeleton landing:
  no inspected repo/mathlib declaration proves the needed bounded-set
  total-boundedness in the `p`-quotient, so a new reduction lemma would still
  have to state the whole missing compactness step and would not honestly lower
  the frontier;
- bounded-pass verdict:
  no production Lean edit was made;
  no unsupported residue was found or removed.

Fresh bounded theorem-3 audit on the same live actual-shell route, deciding
whether one honest Schwartz finite-seminorm total-boundedness lemma can now
land beneath the fixed-time shell theorem (2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `bvt_F_holomorphic`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `ContinuousLinearMap.hasBasis_nhds_zero`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  and the nearby non-supplier
  `OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean`;
- exact bridge statement required:
  for the finite seminorm `p` delivered by Banach-Steinhaus, every bounded
  `B ⊆ SchwartzNPoint d (n + m)` must admit finite `p`-ε-nets, equivalently be
  totally bounded in the pseudometric induced by `p`;
- audit verdict on size:
  this is not genuinely smaller than the bounded-set shell theorem;
  it is the same seam expressed as a Schwartz-precompactness theorem because the
  shell-to-strong-topology reduction after finite nets is routine;
- exact specialization path:
  common finite `p`-bound on `shell_ε - T_t`
  +
  pointwise convergence on each fixed test
  +
  finite `p`-ε-nets on bounded `B`
  =>
  eventual `MapsTo (fun h => shell_ε h - T_t h) B U`
  =>
  bounded-set uniform convergence
  =>
  strong convergence
  =>
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- why early witness extraction still does not help:
  the witness from
  `forwardTube_boundaryValueData_of_polyGrowth`
  is already correctly packaged;
  `Classical.choose` changes names only, not the missing compactness theorem;
- source-backed versus genuinely new:
  source-backed:
  holomorphy, shell growth, boundary-value witness existence, pointwise
  convergence, and common finite seminorm control;
  genuinely new:
  the Schwartz bounded-set precompactness / total-boundedness theorem;
- first exact proof obligation still blocking even a theorem-skeleton landing:
  no inspected repo/mathlib declaration proves that bounded subsets of Schwartz
  space are precompact for an arbitrary controlling finite seminorm, and the
  local nuclear-space file does not yet provide a Montel consequence usable here;
- bounded-pass verdict:
  no production Lean edit was made;
  no unsupported residue was found or removed.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether the first insertion surface now supports an honest compile-clean
bounded-set theorem statement/proof skeleton rather than only a rephrasing of
the same blocker (2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTime_xiShift_zero_eq_add_indicator`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `bvt_F_holomorphic`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `ContinuousLinearMap.hasBasis_nhds_zero`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  and
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`;
- exact lowest honest theorem surface:
  still the full bounded-set strong-convergence theorem for the shell family;
  there is no smaller theorem below it except the same result rewritten in the
  neighborhood-basis / `MapsTo` form;
- exact theorem statement required:

  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
      (∀ h : SchwartzNPoint d (n + m),
        Filter.Tendsto
          (fun ε : ℝ =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          (nhds (T_t h))) ∧
      ∀ s : Set (SchwartzNPoint d (n + m)),
        Bornology.IsVonNBounded ℂ s →
        TendstoUniformlyOn
          (fun ε h =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (fun h => T_t h)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          s`;

  exact internal strong-topology obligation:

  `∀ s : Set (SchwartzNPoint d (n + m)),
      Bornology.IsVonNBounded ℂ s →
      ∀ U ∈ 𝓝 (0 : ℂ),
        ∀ᶠ ε in nhdsWithin (0 : ℝ) (Set.Ioi 0),
          MapsTo
            (fun h =>
              (if hε : 0 < ε then
                section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
              else
                0) - T_t h)
            s U`;
- witness packaging decision:
  keep `T_t` existentially packaged here;
  early `Classical.choose` extraction is still wrapper drift only;
- insertion placement and immediate downstream consumer chain:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  translated-shell growth
  ->
  existential boundary-value witness with pointwise convergence
  ->
  bounded-set eventual-`MapsTo`
  ->
  bounded-set strong convergence
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  ->
  quotient/factorization
  ->
  later `hlimit_os`;
- source-backed versus genuinely new:
  source-backed:
  holomorphy on the forward tube,
  global shell growth,
  tube-slice growth,
  witness existence with pointwise convergence,
  distributional-limit packaging,
  and common finite Schwartz-seminorm control;
  genuinely new:
  eventual `MapsTo` on arbitrary bounded subsets of Schwartz space;
- first exact proof obligation still blocking even a theorem-skeleton landing:
  produce, from the common finite controlling seminorm and boundedness of
  `s`, the finite-`p`-net / eventual-`MapsTo` bridge needed to pass from
  pointwise convergence to uniform convergence on `s`;
  no current source-backed declaration supplies that bridge;
- bounded-pass verdict:
  no production Lean edit was made;
  no unsupported residue was found or removed.

Fresh bounded theorem-3 audit on the same live actual-shell route, deciding
whether the best reusable abstract strong-topology theorem genuinely shrinks
the fixed-time shell seam or merely renames it (2026-04-15, current bounded
pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `bvt_F_holomorphic`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  and mathlib
  `ContinuousLinearMap.hasBasis_nhds_zero`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`;
- exact abstract theorem statement now pinned as the strongest reusable
  sufficient package:
  pointwise convergence of a filter-family `T : α → 𝓢(E,ℂ)'` to `S`,
  plus a common finite-seminorm bound on `T a - S`,
  plus a Schwartz-specific theorem that every bounded set is totally bounded in
  the pseudometric induced by that finite seminorm,
  should imply `Filter.Tendsto T l (nhds S)` in the strong topology on
  continuous linear maps;
- exact specialization path:
  `forwardTube_boundaryValueData_of_polyGrowth`
  supplies the witness `T_t` and pointwise convergence;
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  and Banach-Steinhaus supply common finite-seminorm control of the shell
  family;
  the missing new bridge would convert bounded shell test sets into finite
  `p`-nets;
  the abstract theorem would then discharge the strong-topology limit required
  by `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- audit verdict:
  this reusable abstraction is not yet genuinely smaller than the shell theorem
  on the present branch, because the Schwartz bounded-set total-boundedness
  bridge is not already source-backed or formalized;
  without that bridge, the abstraction merely restates the same bounded-set
  `MapsTo` blocker in generic language;
- why early witness extraction still does not help:
  the missing step is unchanged after naming the witness;
  `forwardTube_boundaryValueData_of_polyGrowth`
  already returns the right existential package, so `Classical.choose` only
  renames `T_t` and does not create the absent bounded-set compactness bridge;
- source-backed versus genuinely new:
  source-backed:
  holomorphy, translated-shell growth, slice growth, boundary-value witness
  existence, pointwise convergence, and common finite seminorm control;
  genuinely new:
  the bounded-set compactness / total-boundedness theorem needed to turn those
  inputs into eventual `MapsTo` on arbitrary bounded subsets;
- first exact proof obligation still blocking even an abstract theorem-skeleton
  landing:
  for the controlling finite seminorm `p`, prove that every bounded
  `B ⊆ SchwartzNPoint d (n + m)` admits finite `p`-ε-nets uniformly enough to
  derive eventual `MapsTo (fun h => shell_ε h - T_t h) B U`;
- bounded-pass verdict:
  no production Lean edit was made;
  no unsupported residue was found or removed.

Fresh bounded theorem-3 audit on the same live actual-shell route, deciding
whether the exact new strong-topology package beneath
`section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` can now land as an
honest compile-clean theorem statement/proof skeleton
(2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `bvt_F_holomorphic`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `ContinuousLinearMap.hasBasis_nhds_zero`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  and
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`;
- exact lowest honest theorem surface:
  still the bounded-set uniform-convergence theorem for the shell family,
  equivalently its neighborhood-basis / `MapsTo` form;
  the basis reformulation does not lower the mathematics further;
- exact theorem statement still required:

  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
      (∀ h : SchwartzNPoint d (n + m),
        Filter.Tendsto
          (fun ε : ℝ =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          (nhds (T_t h))) ∧
      ∀ s : Set (SchwartzNPoint d (n + m)),
        Bornology.IsVonNBounded ℂ s →
        TendstoUniformlyOn
          (fun ε h =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (fun h => T_t h)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          s`;

  exact internal strong-topology obligation:

  `∀ s : Set (SchwartzNPoint d (n + m)),
      Bornology.IsVonNBounded ℂ s →
      ∀ U ∈ 𝓝 (0 : ℂ),
        ∀ᶠ ε in nhdsWithin (0 : ℝ) (Set.Ioi 0),
          MapsTo
            (fun h =>
              (if hε : 0 < ε then
                section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
              else
                0) - T_t h)
            s U`;
- witness packaging decision:
  keep `T_t` existentially packaged here;
  early extraction via `Classical.choose` is wrapper drift only;
- insertion placement and immediate downstream consumer chain:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  translated-shell growth
  ->
  existential boundary-value witness with pointwise convergence
  ->
  bounded-set eventual-`MapsTo`
  ->
  bounded-set uniform convergence
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  ->
  downstream quotient / `hlimit_os` use;
- source-backed versus genuinely new:
  source-backed:
  holomorphy on the forward tube,
  global shell growth,
  tube-slice growth,
  witness existence with pointwise ray convergence,
  distributional-limit packaging,
  and common finite Schwartz-seminorm control;
  genuinely new:
  eventual `MapsTo` on arbitrary bounded subsets of Schwartz space;
- first exact proof obligation still blocking even a theorem-skeleton landing:
  there is still no source-backed upgrade from common finite-seminorm control
  plus pointwise convergence on each fixed test to eventual `MapsTo` on an
  arbitrary bounded set;
  any inserted theorem statement or proof skeleton would therefore duplicate,
  not shrink, the blocker;
- bounded-pass verdict:
  no production Lean edit was made;
  no unsupported residue was found or removed.

Fresh bounded theorem-3 audit on the same live actual-shell route, re-checking
whether the first shell insertion surface now supports even an honest
compile-clean theorem skeleton for the bounded-set limit theorem
(2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  at
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:3584`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  at line `3815`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no smaller theorem surface exists below the bounded-set theorem:
  the only honest internal package is the same result rewritten in the
  strong-topology neighborhood-basis form on continuous linear maps;
- exact theorem statement still required:

  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
      (∀ h : SchwartzNPoint d (n + m),
        Filter.Tendsto
          (fun ε : ℝ =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          (nhds (T_t h))) ∧
      ∀ s : Set (SchwartzNPoint d (n + m)),
        Bornology.IsVonNBounded ℂ s →
        TendstoUniformlyOn
          (fun ε h =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (fun h => T_t h)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          s`;
- exact neighborhood-basis / `MapsTo` obligation now pinned:

  `∀ s : Set (SchwartzNPoint d (n + m)),
      Bornology.IsVonNBounded ℂ s →
      ∀ U ∈ 𝓝 (0 : ℂ),
        ∀ᶠ ε in nhdsWithin (0 : ℝ) (Set.Ioi 0),
          MapsTo
            (fun h =>
              (if hε : 0 < ε then
                section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
              else
                0) - T_t h)
            s U`;
- why early witness extraction still does not help:
  `forwardTube_boundaryValueData_of_polyGrowth`
  already returns the exact existential continuous linear witness needed here;
  extracting `T_t` first with `Classical.choose` only renames that witness and
  does not change the missing bounded-set argument;
- source-backed versus genuinely new:
  source-backed:
  forward-tube holomorphy,
  translated-shell global growth,
  slice growth,
  existential boundary-value witness with pointwise convergence,
  distributional-limit packaging,
  and common finite Schwartz-seminorm control;
  genuinely new:
  eventual `MapsTo` on arbitrary bounded test sets;
- first exact proof obligation still blocking a compile-clean theorem-skeleton
  landing:
  show that common finite-seminorm control together with pointwise convergence
  of the shell family to `T_t` implies the displayed eventual `MapsTo`
  conclusion on every bounded `s`;
  no current source theorem in the route provides that upgrade;
- bounded-pass verdict:
  no production Lean edit was made, because any new theorem statement here
  would duplicate the blocker instead of shrinking it.

Fresh bounded theorem-3 audit on the same live actual-shell route, deciding
whether the first shell insertion surface now supports an honest compile-clean
bounded-set theorem statement/proof skeleton or still exposes the same exact
strong-topology blocker (2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `bvt_F_holomorphic`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  and mathlib
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `ContinuousLinearMap.hasBasis_nhds_zero`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`;
- unsupported residue:
  none removed or introduced in this pass;
- audit conclusion:
  there is still no smaller honest theorem surface below the bounded-set
  uniform-convergence theorem;
  the exact first internal proof package is still the neighborhood-basis /
  `MapsTo` form of that same theorem;
- exact theorem statement now pinned:

  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
      (∀ h : SchwartzNPoint d (n + m),
        Filter.Tendsto
          (fun ε : ℝ =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          (nhds (T_t h))) ∧
      ∀ s : Set (SchwartzNPoint d (n + m)),
        Bornology.IsVonNBounded ℂ s →
        TendstoUniformlyOn
          (fun ε h =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (fun h => T_t h)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          s`;

  equivalently, after choosing `T_t`, prove

  `∀ s, Bornology.IsVonNBounded ℂ s → ∀ U ∈ 𝓝 (0 : ℂ),
    ∀ᶠ ε in nhdsWithin (0 : ℝ) (Set.Ioi 0),
      MapsTo
        (fun h =>
          (if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0) - T_t h)
        s U`;
- witness packaging decision:
  keep `T_t` existentially packaged at this seam;
  early extraction via `Classical.choose` is wrapper drift because the bounded
  `MapsTo` upgrade does not simplify after naming the witness;
- insertion placement and immediate downstream consumer chain:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  raw shell CLM family
  -> witness from `forwardTube_boundaryValueData_of_polyGrowth`
  -> internal `MapsTo` package
  -> bounded-set uniform convergence
  -> `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  -> downstream quotient / `hlimit_os` use;
- source-backed versus genuinely new:
  source-backed:
  holomorphy on the forward tube,
  global shell growth,
  slice growth,
  pointwise ray boundary convergence to a CLM witness,
  distributional-limit packaging,
  and common finite Schwartz-seminorm control;
  genuinely new:
  the eventual `MapsTo` conclusion on arbitrary bounded sets;
- first exact blocking proof obligation:
  no source-backed declaration currently upgrades common seminorm control plus
  pointwise convergence on each fixed test to the displayed eventual `MapsTo`
  statement on bounded sets;
  that exact obligation still blocks even a compile-clean theorem-skeleton
  landing;
- bounded-pass Lean verdict:
  no production Lean edit was made;

Fresh bounded theorem-3 audit on the same live actual-shell route, deciding
from the live source objects whether the bounded-set shell seam lowers to a
smaller theorem or only to an internal strong-topology proof package
(2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `bvt_F_holomorphic`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  and mathlib
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `ContinuousLinearMap.hasBasis_nhds_zero`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`;
- unsupported residue:
  none removed or introduced in this pass;
- audit conclusion:
  there is still no smaller honest theorem surface below the bounded-set
  uniform-convergence theorem;
  the exact first internal proof package is the same theorem rewritten in the
  neighborhood-basis / `MapsTo` language dictated by the strong topology on
  continuous linear maps;
- exact eventual-smallness obligation:
  for a witness `T_t` from
  `forwardTube_boundaryValueData_of_polyGrowth`, prove
  `∀ s, Bornology.IsVonNBounded ℂ s → ∀ U ∈ 𝓝 (0 : ℂ),
    ∀ᶠ ε in nhdsWithin (0 : ℝ) (Set.Ioi 0),
      MapsTo
        (fun h =>
          (if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0) - T_t h)
        s U`;
  this is exactly the basis-neighborhood form behind bounded-set uniform
  convergence / strong-topology convergence;
- witness packaging decision:
  keep `T_t` existentially packaged at this seam;
  extracting it earlier via `Classical.choose` would not reduce the missing
  mathematics;
- insertion placement and immediate downstream consumer chain:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  raw shell CLM family
  -> witness from `forwardTube_boundaryValueData_of_polyGrowth`
  -> internal `MapsTo` package
  -> bounded-set uniform convergence
  -> `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  -> downstream quotient / `hlimit_os` use;
- source-backed versus genuinely new:
  source-backed:
  holomorphy on the forward tube,
  global polynomial growth,
  pointwise ray boundary convergence to a CLM witness,
  and a common finite Schwartz-seminorm bound from Banach-Steinhaus;
  genuinely new:
  upgrading those data to eventual `MapsTo` on each arbitrary bounded set of
  tests;
- first exact blocking proof obligation:
  no source-backed declaration currently turns common seminorm control plus
  pointwise convergence into the displayed eventual `MapsTo` conclusion on
  bounded sets;
  that is the first exact proof obligation still blocking any compile-clean
  Lean landing;
- bounded-pass Lean verdict:
  no production Lean edit was made;
- verification commands run:
  `rg -n "section43_fixedTimeShellRawCLM|section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime|section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime|sorry" OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`
  and
  `mutool draw -F txt -o - references/'reconstruction theorem II.pdf' 9-15`;
  results:
  confirmed the live insertion surface / `sorry`s and the cited paper objects.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether the fixed-time shell seam can honestly be reduced by a compile-clean
local theorem statement or packaging lemma below the current existence theorem
(2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `bvt_F_holomorphic`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  and mathlib
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.hasBasis_nhds_zero`;
- unsupported residue:
  none removed or introduced in this pass;
- audit conclusion:
  no honest compile-clean local definition/lemma below the current theorem
  surface was found;
  the full bounded-set uniform-convergence theorem remains the exact smallest
  honest next theorem on the live route;
- exact theorem statement now pinned:

  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
      (∀ h : SchwartzNPoint d (n + m),
        Filter.Tendsto
          (fun ε : ℝ =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          (nhds (T_t h))) ∧
      ∀ s : Set (SchwartzNPoint d (n + m)),
        Bornology.IsVonNBounded ℂ s →
        TendstoUniformlyOn
          (fun ε h =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (fun h => T_t h)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          s`;
- witness packaging decision:
  keep `T_t` existentially packaged;
  this matches the output shape of
  `forwardTube_boundaryValueData_of_polyGrowth`,
  and pre-extracting the witness does not reduce the missing mathematics;
- insertion placement and immediate downstream consumer chain:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  raw shell CLM family
  -> existential witness from `forwardTube_boundaryValueData_of_polyGrowth`
  -> bounded-set uniform convergence / strong-topology convergence
  -> `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  -> downstream quotient / `hlimit_os` use;
- source-backed versus genuinely new:
  source-backed:
  forward-tube holomorphy,
  translated-shell polynomial growth,
  pointwise ray boundary convergence,
  existence of a continuous linear limit witness,
  and common finite Schwartz-seminorm bounds for the shell family;
  genuinely new:
  the bounded-set eventual `MapsTo` conclusion for the shell-difference family;
- first exact blocking proof obligation:
  for a chosen witness `T_t`, prove
  `∀ s, Bornology.IsVonNBounded ℂ s → ∀ U ∈ 𝓝 (0 : ℂ),
    ∀ᶠ ε in nhdsWithin (0 : ℝ) (Set.Ioi 0),
      MapsTo
        (fun h =>
          (if hε : 0 < ε then
            section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
          else
            0) - T_t h)
        s U`;
- bounded-pass Lean verdict:
  no production Lean edit was made.

Fresh bounded theorem-3 audit on the same live actual-shell route, fixing the
exact theorem-vs-internal-package decomposition at the shell strong-topology
seam (2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `bvt_F_holomorphic`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `distributional_limit_of_equicontinuous`,
  and mathlib
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.hasBasis_nhds_zero`;
- unsupported residue:
  none removed or introduced in this pass;
- audit conclusion:
  there is still no smaller honest theorem surface below the bounded-set
  uniform-convergence theorem;
  the only honest decomposition is internal to that theorem:
  prove the neighborhood-basis / `MapsTo` form for the shell difference, then
  read off the bounded-set uniform convergence / strong-topology conclusion;
- exact reason:
  `distributional_limit_of_equicontinuous` supplies
  `∃ T_t, ∀ h, Tendsto (...) (nhds (T_t h))`;
  `SchwartzMap.tempered_uniform_schwartz_bound` supplies a common finite
  Schwartz-seminorm bound once pointwise boundedness is available;
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter` only treats varying
  tests converging in the Schwartz topology;
  and the paper objects inspected still stop at pointwise boundary values plus
  analytic/growth control;
  therefore the first missing content is exactly the eventual
  `MapsTo` statement on arbitrary von Neumann bounded sets, which is the
  basis-neighborhood form of strong convergence;
- exact strongest honest theorem statement:

  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
      (∀ h : SchwartzNPoint d (n + m),
        Filter.Tendsto
          (fun ε : ℝ =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          (nhds (T_t h))) ∧
      ∀ s : Set (SchwartzNPoint d (n + m)),
        Bornology.IsVonNBounded ℂ s →
        TendstoUniformlyOn
          (fun ε h =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (fun h => T_t h)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          s`;
- exact internal package genuinely needed for execution:

  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
      (∀ h : SchwartzNPoint d (n + m),
        Filter.Tendsto
          (fun ε : ℝ =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          (nhds (T_t h))) ∧
      ∀ s : Set (SchwartzNPoint d (n + m)),
        Bornology.IsVonNBounded ℂ s →
        ∀ U ∈ 𝓝 (0 : ℂ),
          ∀ᶠ ε in nhdsWithin (0 : ℝ) (Set.Ioi 0),
            MapsTo
              (fun h =>
                (if hε : 0 < ε then
                  section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
                else
                  0) - T_t h)
              s U`;

  this package is equivalent in burden to the full bounded-set theorem;
  it is genuinely needed as proof organization because it is the exact basis
  obligation exposed by `ContinuousLinearMap.hasBasis_nhds_zero` and
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`;
- witness packaging decision:
  keep `T_t` existentially packaged at this seam;
  extracting it earlier via `Classical.choose` would not remove the blocker;
- insertion placement and immediate downstream consumer chain:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  raw shell CLM family
  -> existential witness from `forwardTube_boundaryValueData_of_polyGrowth`
  -> internal `MapsTo` package
  -> bounded-set uniform convergence
  -> strong-topology convergence of the CLM family
  -> downstream quotient / `hlimit_os` use;
- source-backed versus genuinely new:
  source-backed:
  forward-tube holomorphy, polynomial growth, raywise boundary convergence,
  and Banach-Steinhaus seminorm control;
  genuinely new:
  bounded-set eventual smallness of the shell difference family in the strong
  dual topology;
- first exact blocking proof obligation:
  for a chosen witness `T_t`, prove
  `∀ s, Bornology.IsVonNBounded ℂ s → ∀ U ∈ 𝓝 (0 : ℂ),
    ∀ᶠ ε in nhdsWithin (0 : ℝ) (Set.Ioi 0),
      MapsTo (fun h => (if hε : 0 < ε then section43_fixedTimeShellRawCLM ... ε hε h else 0) - T_t h) s U`;
- bounded-pass Lean verdict:
  no production Lean edit was made.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether the pinned bounded-set `MapsTo` obstruction lowers one theorem-sized
step smaller beneath `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
(2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `bvt_F_holomorphic`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `distributional_limit_of_equicontinuous`,
  and mathlib
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.hasBasis_nhds_zero`;
- unsupported residue:
  none removed or introduced in this pass;
- audit conclusion:
  the obstruction does not honestly split one theorem-sized step smaller;
  the full bounded-set uniform-convergence theorem remains the exact smallest
  honest next theorem surface on the live route;
- exact reason:
  `distributional_limit_of_equicontinuous` gives
  `∃ T_t, ∀ h, Tendsto (...) (nhds (T_t h))`;
  `SchwartzMap.tempered_uniform_schwartz_bound` gives a common finite seminorm
  bound once pointwise boundedness is known;
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter` only handles varying
  tests that themselves converge in the Schwartz topology;
  but the strong topology on
  `SchwartzNPoint d (n + m) →L[ℂ] ℂ`
  is bounded convergence, so the first genuinely missing statement is still the
  eventual `MapsTo s U` conclusion on arbitrary bounded `s`;
- strongest honest intermediate theorem:
  none strictly smaller than the full bounded-set uniform-convergence theorem;
  a general lemma
  pointwise convergence + equicontinuity + seminorm control
  `=>` bounded-set eventual `MapsTo`
  would simply restate the missing strong-topology upgrade rather than reduce
  it;
- insertion placement and immediate downstream consumer chain:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  raw shell CLM family
  -> existential witness from `forwardTube_boundaryValueData_of_polyGrowth`
  -> bounded-set eventual `MapsTo` / `TendstoUniformlyOn`
  -> strong-topology convergence of the CLM family
  -> downstream quotient / `hlimit_os` use;
- source-backed versus genuinely new:
  source-backed:
  forward-tube holomorphy, polynomial growth, raywise boundary convergence,
  and Banach-Steinhaus seminorm control;
  genuinely new:
  bounded-set eventual smallness of the shell difference family in the strong
  dual topology;
- first exact blocking proof obligation:
  for a chosen witness `T_t`, prove
  `∀ s, Bornology.IsVonNBounded ℂ s → ∀ U ∈ 𝓝 (0 : ℂ),
    ∀ᶠ ε in nhdsWithin (0 : ℝ) (Set.Ioi 0),
      MapsTo (fun h => (if hε : 0 < ε then section43_fixedTimeShellRawCLM ... ε hε h else 0) - T_t h) s U`;
- bounded-pass Lean verdict:
  no production Lean edit was made.

Fresh bounded theorem-3 audit on the same live actual-shell route, fixing the
exact statement shape and witness packaging of the next strong-topology theorem
(2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `distributional_limit_of_equicontinuous`,
  and mathlib
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.hasBasis_nhds_zero`;
- unsupported residue:
  none removed or introduced in this pass;
- audited minimal theorem surface:

  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
      (∀ h : SchwartzNPoint d (n + m),
        Filter.Tendsto
          (fun ε : ℝ =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          (nhds (T_t h))) ∧
      ∀ s : Set (SchwartzNPoint d (n + m)),
        Bornology.IsVonNBounded ℂ s →
        TendstoUniformlyOn
          (fun ε h =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (fun h => T_t h)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          s`;
- witness packaging decision:
  existential packaging is the honest choice;
  `forwardTube_boundaryValueData_of_polyGrowth` already produces the right
  witness shape, while an explicit pre-theorem `Classical.choose` extraction
  would not reduce the strong-topology blocker;
- insertion placement and immediate downstream consumer chain:
  place this theorem immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  raw shell CLM family
  -> existential witness from `forwardTube_boundaryValueData_of_polyGrowth`
  -> bounded-set uniform convergence
  -> `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  -> downstream quotient / `hlimit_os` use;
- source-backed versus genuinely new:
  source-backed:
  existence of a CLM witness and pointwise shell convergence for each fixed
  test, together with uniform seminorm control once pointwise boundedness is
  known;
  genuinely new:
  eventual uniform smallness of the shell difference on arbitrary von Neumann
  bounded sets;
- first exact blocking obligation:
  prove the bounded-set upgrade
  `∀ s, Bornology.IsVonNBounded ℂ s → TendstoUniformlyOn ... s`;
  this is exactly what is still absent after
  `forwardTube_boundaryValueData_of_polyGrowth`, because the current tools stop
  at pointwise convergence and equicontinuity;
- bounded-pass Lean verdict:
  no production Lean edit was made.

Fresh bounded theorem-3 audit on the same live actual-shell route, re-checking
whether the current strong-topology obstruction still leaves any strictly
smaller reusable theorem/local lemma beneath the bounded-set uniform-convergence
surface (2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `distributional_limit_of_equicontinuous`,
  and mathlib
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `ContinuousLinearMap.hasBasis_nhds_zero`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`;
- unsupported residue:
  none removed or introduced in this pass;
- audit conclusion:
  there is still no smaller honest theorem/local lemma below the bounded-set
  uniform-convergence theorem on the live route;
  every inspected topology helper stops at either
  pointwise convergence of scalar evaluations,
  existence of a CLM witness,
  or a common finite-seminorm bound;
  none supplies eventual smallness uniformly on arbitrary von Neumann bounded
  test sets;
- exact smallest theorem surface still exposed:
  the full bounded-set uniform-convergence theorem for
  `section43_fixedTimeShellRawCLM`:

  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
      ∀ s : Set (SchwartzNPoint d (n + m)),
        Bornology.IsVonNBounded ℂ s →
        TendstoUniformlyOn
          (fun ε h =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (fun h => T_t h)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          s`;
- exact insertion placement and consumer chain:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  translated-shell growth
  + fixed-time shell CLM packaging
  -> `forwardTube_boundaryValueData_of_polyGrowth`
  -> CLM witness `T_t` with scalar convergence
  -> bounded-set uniform convergence theorem
  -> ambient CLM limit existence;
- source-backed versus genuinely new:
  source-backed:
  the forward-tube holomorphy / growth / scalar boundary-value package,
  the slice-growth estimate in `SCV/TubeBoundaryValues.lean`,
  and the Banach-Steinhaus tools in
  `SCV/SchwartzComplete.lean` and
  `GeneralResults/DistributionalLimit.lean`;
  genuinely new:
  uniform convergence on every von Neumann bounded subset of the Schwartz test
  space;
- why the strong-topology upgrade still does not compile directly:
  `distributional_limit_of_equicontinuous` only returns
  `∃ T_t, ∀ h, Tendsto (...) (nhds (T_t h))`;
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter` only controls
  `T_ε (u_ε)` when `u_ε → h` in the Schwartz topology;
  but `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn` requires
  `∀ s`, `Bornology.IsVonNBounded ℂ s → TendstoUniformlyOn ... s`,
  which is strictly stronger than the available pointwise/equicontinuous data;
- bounded-pass Lean verdict:
  no production Lean edit was made.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether the strong-topology seam lowers one theorem-sized step smaller than the
full bounded-set uniform-convergence theorem (2026-04-15, later bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF pages 10-11 / printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_of_tendsto_filter`,
  `distributional_limit_of_equicontinuous`,
  and mathlib
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `ContinuousLinearMap.hasBasis_nhds_zero`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`;
- unsupported residue:
  none removed or introduced in this pass;
- audit conclusion:
  the strong-topology seam does not lower one theorem-sized step smaller;
  the available Banach-Steinhaus/equicontinuity infrastructure is real and
  reusable, but it does not by itself turn pointwise shell convergence into
  bounded-set uniform convergence in the strong dual topology;
- exact smallest theorem surface still exposed:
  the full bounded-set uniform-convergence theorem for
  `section43_fixedTimeShellRawCLM`:

  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
      ∀ s : Set (SchwartzNPoint d (n + m)),
        Bornology.IsVonNBounded ℂ s →
        TendstoUniformlyOn
          (fun ε h =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (fun h => T_t h)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          s`;
- exact insertion placement and consumer chain:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  translated-shell growth + composition holomorphy
  -> `forwardTube_boundaryValueData_of_polyGrowth`
  -> CLM witness `T_t` with pointwise scalar convergence
  -> bounded-set uniform convergence theorem
  -> ambient CLM limit existence;
- source-backed versus genuinely new:
  source-backed:
  the forward-tube holomorphy / growth / scalar boundary-value package,
  the slice-growth estimates in `SCV/TubeBoundaryValues.lean`,
  and the Banach-Steinhaus tools in `SCV/SchwartzComplete.lean`;
  genuinely new:
  uniform convergence on every von Neumann bounded test set, not merely a
  common seminorm bound or equicontinuity;
- why the candidate smaller bridge still fails:
  `SchwartzMap.tempered_uniform_schwartz_bound` can control all
  `section43_fixedTimeShellRawCLM ... ε` by one finite Schwartz seminorm once
  pointwise boundedness is known, and
  `distributional_limit_of_equicontinuous` can repackage a pointwise-Cauchy
  family into a CLM witness;
  but `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn` shows the target
  topology requires eventual smallness on arbitrary bounded sets, and no
  inspected source theorem upgrades pointwise convergence plus equicontinuity to
  that stronger conclusion;
- bounded-pass Lean verdict:
  no production Lean edit was made.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether
`section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
now lands directly from the translated-shell growth theorem together with the
current boundary-value package (2026-04-15, latest bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF page 10 / printed page 290 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `xiShift`,
  `norm_xiShift_le`,
  `xiShift_mem_forwardTube_of_real`,
  `bvt_F_holomorphic`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `section43_fixedTime_xiShift_zero_eq_add_indicator`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  plus mathlib
  `ContinuousLinearMap.topologicalSpace`
  and
  `UniformConvergenceCLM.tendsto_iff_tendstoUniformlyOn`;
- unsupported residue:
  none removed or introduced in this pass;
- audit conclusion:
  the direct fixed-time existence theorem still does not land directly;
  the shifted-shell analytic seam is now closed enough to produce a CLM witness
  together with scalar boundary-value convergence, but the theorem asks for
  convergence in the bounded-convergence topology on
  `SchwartzNPoint d (n + m) →L[ℂ] ℂ`;
- exact lower exposed local seam:
  the bounded-set uniform-convergence upgrade for the fixed-time shell family:

  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
      ∀ s : Set (SchwartzNPoint d (n + m)),
        Bornology.IsVonNBounded ℂ s →
        TendstoUniformlyOn
          (fun ε h =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (fun h => T_t h)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          s`;
- exact insertion placement and consumer chain:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  translated-shell growth + composition holomorphy
  -> `forwardTube_boundaryValueData_of_polyGrowth`
  -> CLM witness `T_t` with pointwise boundary convergence
  -> bounded-set uniform convergence
  -> ambient CLM limit existence;
- source-backed versus genuinely new:
  source-backed:
  the forward-tube holomorphy / growth / boundary-value package and the landed
  `xiShift` geometry / translated-growth seam;
  genuinely new:
  the strong-topology upgrade on bounded subsets of the Schwartz test space;
- why the current direct proof still fails:
  source and Lean provide
  `∀ h, Tendsto (shell_t_eval h) ... (nhds (T_t h))`,
  while mathlib identifies `nhds T_t` on `→L` with bounded convergence,
  equivalently uniform convergence on every von Neumann bounded test set;
  the missing theorem is exactly that upgrade;
- bounded-pass Lean verdict:
  no production Lean edit was made.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether
`section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
now follows directly from the current translated-shell analytic package
(2026-04-15, current bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  PDF page 9 / printed page 289 / Theorem 4.3;
  PDF page 10 / printed page 290 / `(5.2)` `(5.3)` `(5.4)`;
  PDF pages 13-15 / printed pages 293-295 / Section V.2 `(AN)` `(PN)`,
  `(5.20)` `(5.21)`, Lemma 5.2;
  Lean declarations
  `xiShift`,
  `norm_xiShift_le`,
  `xiShift_mem_forwardTube_of_real`,
  `bvt_F_holomorphic`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `section43_fixedTime_xiShift_zero_eq_add_indicator`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- unsupported residue:
  none removed or introduced in this pass;
- audit conclusion:
  the direct existence theorem still does not land directly;
  the analytic seam above `F_t` is now closed enough to produce only scalar
  boundary-value convergence, but the live theorem asks for convergence in the
  strong topology on
  `SchwartzNPoint d (n + m) →L[ℂ] ℂ`;
- exact lower exposed local seam:
  the bounded-set uniform-convergence upgrade for the fixed-time shell family:

  `∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
      ∀ s : Set (SchwartzNPoint d (n + m)),
        Bornology.IsVonNBounded ℂ s →
        TendstoUniformlyOn
          (fun ε h =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε h
            else
              0)
          (fun h => T_t h)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          s`;
- exact insertion placement and consumer chain:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  translated-shell growth + composition holomorphy
  -> scalar boundary witness from
     `forwardTube_boundaryValueData_of_polyGrowth`
  -> bounded-set uniform convergence
  -> ambient CLM limit existence;
- source-backed versus genuinely new:
  source-backed:
  the forward-tube holomorphy / growth / scalar boundary-value package;
  genuinely new:
  the strong-topology upgrade on bounded subsets of the Schwartz test space;
- why the current direct proof still fails:
  source and Lean now provide
  `∀ h, Tendsto (shell_t_eval h) ... (nhds (T_t h))`,
  but no theorem turns that pointwise family into
  `Tendsto shellCLM_t ... (nhds T_t)` in the strong CLM topology;
- bounded-pass Lean verdict:
  no production Lean edit was made.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether the translated-shell growth theorem still is the smallest honest
compile-clean landing surface (2026-04-15, current latest bounded pass):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed page 289 / Theorem 4.3;
  printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  printed pages 293-295 / Section V.2 `(AN)` `(PN)`, `(5.20)` `(5.21)`,
  Lemma 5.2;
  Lean declarations
  `xiShift`,
  `bvt_F_hasGlobalForwardTubeGrowth`,
  `bvt_F_holomorphic`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- unsupported residue:
  none from this pass;
  the attempted translated-growth theorem was removed after verification failed;
- audit conclusion:
  the translated-shell growth theorem is still the first mathematical theorem-
  sized seam, but not yet the smallest compile-clean Lean landing;
  the exact lower exposed local seam is the `xiShift` branch-normalization pair:
  preservation of imaginary parts under real `xiShift`, and the affine
  decomposition of `xiShift` into `z + shift`;
- exact insertion placement and consumer chain:
  immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  branch-normalization
  -> translated-shell growth theorem
  -> `forwardTube_boundaryValueData_of_polyGrowth`
  -> fixed-time raw-shell CLM limit existence;
- source-backed versus genuinely new:
  source-backed:
  the analytic growth package;
  genuinely new:
  the concrete `xiShift` branch normalization in this theorem-3 shell file;
- failed prior attempt:
  verification failed under
  `lake build OSReconstruction.Wightman.Reconstruction.WickRotation.OSToWightmanPositivity`
  with unresolved local goals caused by `Fin`/`Nat` comparison normalization in
  `xiShift`, not by any unsupported analytic step.

Fresh bounded theorem-3 audit on the same live actual-shell route, checking
whether the translated-shell global-growth theorem can be landed cleanly at the
first fixed-time shell seam (2026-04-15, latest bounded pass):

- `(A0)` re-check result:
  first insertion surface still
  `section43_fixedTimeShellRawCLM`
  immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  theorem-level `sorry`s in
  `OSToWightmanPositivity.lean` remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
  `norm_xiShift_le` and `xiShift_mem_forwardTube_of_real`
  are present in `OSToWightman.lean`;
- arbitrary-forward-tube geometry verdict:
  fully closed;
  the live blocker is now strictly above geometry;
- lowest honest interface now exposed:
  the translated-shell global-growth theorem for
  `F_t z := bvt_F OS lgc (n + m)
      (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 z (t : ℂ))`
  on `ForwardTube d (n + m)`,
  under hypotheses
  `OS`, `lgc`, `{n m}`, `hm : 0 < m`, `t : ℝ`;
- separate-translated-growth theorem verdict:
  still required;
  the direct `forwardTube_boundaryValueData_of_polyGrowth` consumer package is
  downstream of this theorem, not a replacement for it;
- insertion placement and consumer chain:
  insert immediately above
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
  consumer chain is
  translated-shell growth
  -> translated-shell holomorphy
  -> `forwardTube_boundaryValueData_of_polyGrowth`
  -> specialization/rewrite to `section43_fixedTimeShellRawCLM`
  -> `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  -> quotient/factorization for `T_t`;
- source-backed versus genuinely new:
  source-backed:
  OS II Theorem 4.3, Section V/V.2 growth package, and Lean
  `bvt_F_hasGlobalForwardTubeGrowth`,
  `bvt_F_holomorphic`,
  `forwardTube_boundaryValueData_of_polyGrowth`;
  genuinely new:
  the translated-shell growth transfer to `xiShift ... z (t : ℂ)`;
- bounded-pass Lean verdict:
  one honest theorem statement was attempted and then removed after
  verification exposed unresolved local proof obligations;
  no production Lean edit remains from this pass.

Fresh bounded theorem-3 audit of the live fixed-time shell existence seam,
re-checking whether `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
is statement-correct enough to support any honest bounded Lean step at its own
insertion surface (2026-04-15, later bounded pass):

Fresh bounded theorem-3 audit of the live fixed-time shell existence seam,
pinning the actual proof-route package for
`section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
(2026-04-15, current bounded pass):

- `(A0)` was re-run first and again confirmed the first live insertion surface
  is still
  `section43_fixedTimeShellRawCLM`
  immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed page 289 / Theorem 4.3;
  printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  printed pages 293-295 / Section V.2 `(AN)` `(PN)`, `(5.20)` `(5.21)`,
  Lemma 5.2;
  local declarations
  `xiShift`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `bvt_boundary_values`,
  `bvt_F_translationInvariant`,
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`,
  `canonicalXiShift_mem_forwardTube`,
  `contDiff_canonicalXiShift`,
  `section43PositiveEnergyQuotientMap`,
  `Section43PositiveEnergyComponent`,
  `evalAtZeroDescendsToSection43PositiveEnergyComponent`,
  and the local `hlimit_os` block;
- unsupported-residue verdict:
  live source still has theorem-level `sorry`s at
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
  none were removed in this pass;
- exact audit conclusion:
  the strongest source-backed package that actually applies here is
  `forwardTube_boundaryValueData_of_polyGrowth`, not a separate ambient
  convergence criterion;
  accordingly, the existing theorem should be proved by recasting the fixed-time
  shell as a boundary-value problem for the shifted continuation

  `F_t z := bvt_F OS lgc (n + m)
      (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 z (t : ℂ))`;

  once `F_t` is shown holomorphic and globally polynomially bounded on
  `ForwardTube d (n + m)`, the generic boundary-value constructor already yields
  the desired ambient `T_t`;
- exact first genuinely new theorem-sized sub-obligation below the existing
  theorem:
  the fixed-time translated-shell growth theorem

  `∃ C_bd : ℝ, ∃ N : ℕ, 0 < C_bd ∧
      ∀ z ∈ ForwardTube d (n + m),
        ‖bvt_F OS lgc (n + m)
            (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0 z (t : ℂ))‖
          ≤ C_bd * (1 + ‖z‖) ^ N`;

  the corresponding holomorphy statement for `F_t` is also needed, but source-
  first inspection classifies it as the easier composition step, not the first
  new hard theorem-sized seam;
- exact local hypotheses / new objects required:
  hypotheses:
  `OS`, `lgc`, `{n m : ℕ}`, `hm : 0 < m`, `t : ℝ`, `ht : 0 < t`;
  local names:
  `r : Fin (n + m) := ⟨n, Nat.lt_add_of_pos_right hm⟩`,
  `F_t : (Fin (n + m) → Fin (d + 1) → ℂ) → ℂ`,
  resulting witness
  `T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ`;
- exact downstream use once that sub-obligation lands:
  `forwardTube_boundaryValueData_of_polyGrowth` on `F_t`
  -> specialize to
     `η = canonicalForwardConeDirection (d := d) (n + m)`
  -> rewrite to `section43_fixedTimeShellRawCLM`
  -> conclude `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  -> proceed to quotient-factorization for `T_t`
  -> then the later outside-region branch inside `hlimit_os`;
- source-backed versus genuinely new split:
  source-backed:
  Theorem 4.3 and the Section V/V.2 continuation package, together with Lean
  `forwardTube_boundaryValueData_of_polyGrowth` and the existing shell affine
  geometry;
  genuinely new:
  the global-growth theorem for the fixed-time shifted continuation `F_t`, then
  the concrete specialization/rewrite to the live raw shell CLM family;
- bounded-pass verdict:
  no production Lean edit was made;
  one honest compile-clean skeleton was not landed.

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean; the second still contains
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence` and
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed page 289 / Theorem 4.3;
  printed pages 293-295 / Section V.2 `(AN)` `(PN)`, `(5.20)` `(5.21)`,
  Lemma 5.2;
  local declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `section43_fixedPair_shellToSlice_limit_fixedTime`,
  `section43PositiveEnergyQuotientMap`,
  `Section43PositiveEnergyComponent`,
  `evalAtZeroDescendsToSection43PositiveEnergyComponent`,
  the local `hlimit_os` block, and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- unsupported-residue verdict:
  live source still has theorem-level `sorry`s at
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
  none were removed in this pass;
- exact audit conclusion:
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` remains
  statement-correct as written;
  no hypothesis, codomain, or filter correction is needed;
  however, no smaller honest compile-clean theorem/local-lemma skeleton exists
  at that insertion surface yet, because any such helper would only rename the
  same unsupported ambient convergence existence obligation;
- exact insertion-surface location and immediate downstream consumer chain:
  the theorem still belongs immediately after
  `section43_fixedTimeShellRawCLM` and before
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  consumer chain remains
  raw-shell CLM limit existence
  -> quotient-kernel / quotient-factorization for `T_t`
  -> full shell boundary-distribution package
  -> pointwise outside-region coefficient limit
  -> product-level outside-region pointwise `hlim`
  -> `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  -> shell-integral limit `0`
  -> `hlimit_os`
  -> `tendsto_nhds_unique hlimit_w hlimit_os`;
- source-backed versus genuinely new split:
  source-backed:
  Theorem 4.3 and Section V.2 continuation/support language together with the
  live raw CLM shell family and the Section-4.3 quotient API;
  genuinely new:
  the actual proof that the family converges to an ambient `T_t`, and then the
  quotient-factorization of that `T_t`.

Fresh bounded theorem-3 audit of the live fixed-time shell existence seam,
checking whether `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
needs theorem-surface correction before proof work (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean; the second still contains
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages 288-289 / Theorem 4.1 `(4.4)` `(4.5)`, Theorem 4.2 `(4.6)`,
  Theorem 4.3;
  printed pages 290-291 / Section V `(5.2)` `(5.3)` `(5.4)`;
  printed pages 294-295 / Section V.2 `(AN)` `(PN)`, `(5.20)` `(5.21)`,
  Lemma 5.2;
  local declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `section43PositiveEnergyQuotientMap`,
  `Section43PositiveEnergyComponent`,
  `evalAtZeroDescendsToSection43PositiveEnergyComponent`,
  and the local `hlimit_os` block;
- unsupported-residue verdict:
  live source still has theorem-level `sorry`s at
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
  none were removed in this pass;
- exact audit conclusion:
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` is statement-correct
  as written;
  no hypothesis or codomain correction is needed;
  the existing one-sided `Filter.Tendsto` statement to an ambient continuous
  linear functional is the honest current theorem surface for the live OS route;
- exact next proof split:
  do not rewrite the theorem statement;
  the next bounded Lean action, once source-backed mathematics is in hand,
  should target the proof shape of this exact statement;
  only after that should the route introduce the genuinely new theorem that the
  chosen `T_t` annihilates the positive-energy vanishing submodule, or
  equivalently factors through
  `section43PositiveEnergyQuotientMap (d := d) (n + m)`;
- exact insertion-surface location and immediate downstream consumer chain:
  the theorem already sits in the correct first insertion slot, immediately
  after `section43_fixedTimeShellRawCLM` and before
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  consumer chain remains
  raw-shell CLM limit existence
  -> quotient-kernel / quotient-factorization for `T_t`
  -> full shell boundary-distribution package
  -> pointwise outside-region coefficient limit
  -> product-level outside-region pointwise `hlim`
  -> `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  -> shell-integral limit `0`
  -> `hlimit_os`
  -> `tendsto_nhds_unique hlimit_w hlimit_os`;
- source-backed versus genuinely new split:
  source-backed:
  the paper continuation/existence side and the local ambient CLM / quotient
  interfaces;
  genuinely new:
  the proof of convergence for the concrete shell family and the later
  factorization/support step for `T_t`;
- bounded-pass verdict:
  no honest compile-clean theorem/local lemma statement or skeleton was landed;
  any new production Lean statement at this surface would currently be a wrapper
  around the same unsupported proof obligation.

Fresh bounded theorem-3 audit of the live fixed-time shell package, checking
whether the boundary-distribution package itself already split in source
(2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean; the second still contains
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages 288-289 / Theorem 4.1 `(4.4)` `(4.5)`, Theorem 4.2 `(4.6)`,
  Theorem 4.3;
  printed pages 290-291 / Section V `(5.2)` `(5.3)` `(5.4)`;
  printed pages 294-295 / Section V.2 `(5.20)` `(5.21)`, Lemma 5.2, `(AN)`,
  `(PN)`;
  local declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`,
  `hasFDerivAt_bvt_F_canonicalXiShift`,
  `section43PositiveEnergyQuotientMap`,
  `evalAtZeroDescendsToSection43PositiveEnergyComponent`,
  and the local `hlimit_os` block;
- unsupported-residue verdict:
  live source currently has two theorem-level `sorry`s in
  `OSToWightmanPositivity.lean`, at
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
  none were removed in this pass;
- exact audit conclusion:
  yes, the fixed-time shell boundary-distribution package splits further;
  the exact lowest honest interface now exposed is the raw-shell CLM limit
  existence theorem already present in source:

  `∀ t : ℝ, 0 < t →
      ∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
        Filter.Tendsto
          (fun ε : ℝ =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε
            else
              0)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          (nhds T_t)`;
- exact local hypotheses genuinely required:
  `OS`, `lgc`, `{n m : ℕ}`, `hm : 0 < m`, `t : ℝ`, `ht : 0 < t`;
- exact next theorem above that lowest interface:
  the package then needs a quotient-factorization theorem for the chosen
  witness `T_t`, with strongest honest shape

  `∃ T̄_t :
      Section43PositiveEnergyComponent (d := d) (n + m) →L[ℂ] ℂ,
      T_t = T̄_t.comp (section43PositiveEnergyQuotientMap (d := d) (n + m))`;

  equivalently, one can stage this first as the kernel theorem
  `section43PositiveEnergyQuotientMap ... h = 0 -> T_t h = 0`;
- exact new object names/types required:
  witness already present:
  `T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ`;
  next genuinely new descended object:
  `T̄_t : Section43PositiveEnergyComponent (d := d) (n + m) →L[ℂ] ℂ`;
- exact insertion-surface location:
  the lowest interface already sits immediately after
  `section43_fixedTimeShellRawCLM` and before
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  the later coefficient corollary still belongs after
  `hasFDerivAt_bvt_F_canonicalXiShift`;
- exact immediate downstream consumer chain inside `hlimit_os`:
  raw-shell CLM limit existence
  -> quotient-kernel / quotient-factorization for `T_t`
  -> full shell boundary-distribution package
  -> pointwise outside-region coefficient limit
  -> product-level outside-region pointwise `hlim`
  -> full pointwise `hlim` input for
     `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  -> shell-integral limit `0`
  -> `hlimit_os`
  -> `tendsto_nhds_unique hlimit_w hlimit_os`;
- source-backed versus genuinely new split:
  source-backed:
  Section V / V.2 existence-continuation material and the current quotient
  codomain infrastructure;
  genuinely new:
  the support/factorization step for the limiting shell functional, and only
  after that the outside-region coefficient corollary;
- bounded-pass verdict:
  one honest compile-clean skeleton was not landed, because the exact lowest
  interface is already present as a theorem statement in live source and the
  next factorization theorem would still be genuinely new mathematics;
  no production Lean edits were made.

Fresh bounded theorem-3 audit of the live outside-region shell seam, checking
whether the coefficient-only theorem is still one layer above the true source
interface (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean; the second live shell-local
  insertion surface still contains
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages 288-289 / Theorem 4.1 `(4.4)` `(4.5)`, Theorem 4.2 `(4.6)`,
  Theorem 4.3;
  printed pages 290-295 / `(5.2)` `(5.3)` `(5.4)`, Section V.2 `(5.20)`
  `(5.21)`, Lemma 5.2, `(AN)`, `(PN)`;
  local declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`,
  `hasFDerivAt_bvt_F_canonicalXiShift`,
  `section43_fixedPair_canonicalShell_sliceRealization`,
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
  `section43_fixedPair_mixedOrderSlice_descendedScalar_eq_directPsiIntegral_fixedTime`,
  `section43_iteratedSlice_descendedPairing`,
  and the local `hlimit_os` block;
- no unsupported residue was found or removed at either insertion point;
- exact audit conclusion:
  before the coefficient-only outside-region theorem can be justified, the live
  route first needs a more primitive fixed-time shell boundary-distribution
  package;
- exact failure mode:
  the source-backed shell realization / slice transport cluster is only valid on
  `section43PositiveEnergyRegion d (n + m)`, because the frozen slice descent
  needs nonnegativity away from the distinguished index `r`;
  on `y ∉ section43PositiveEnergyRegion d (n + m)` there is not yet any actual-
  shell descended object to which Theorem 4.3 support applies;
- exact next genuinely new package:

  `∀ t : ℝ, 0 < t →
      ∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
        (∀ h : SchwartzNPoint d (n + m),
          Filter.Tendsto
            (fun ε : ℝ =>
              ∫ y : NPointDomain d (n + m),
                bvt_F OS lgc (n + m)
                  (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                    (fun k μ =>
                      ↑(y k μ) +
                        ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                          Complex.I)
                    (t : ℂ)) *
                  h y)
            (nhdsWithin 0 (Set.Ioi 0))
            (nhds (T_t h))) ∧
        (∀ h : SchwartzNPoint d (n + m),
          section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
            T_t h = 0)`;
- exact local hypotheses genuinely required:
  `OS`, `lgc`, `{n m : ℕ}`, `hm : 0 < m`, `t : ℝ`, `ht : 0 < t`;
- exact insertion-surface location:
  immediately after `section43_fixedTimeShellRawCLM` and before
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence` in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`;
  its pointwise outside-region corollary would then belong after
  `hasFDerivAt_bvt_F_canonicalXiShift`;
- exact immediate downstream consumer chain inside `hlimit_os`:
  fixed-time shell boundary-distribution / quotient-kernel package
  -> pointwise outside-region coefficient limit
  -> product-level outside-region pointwise `hlim`
  -> full pointwise `hlim` input for
     `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  -> shell-integral limit `0`
  -> `hlimit_os`
  -> `tendsto_nhds_unique hlimit_w hlimit_os`;
- source-backed versus genuinely new split:
  source-backed:
  Theorem 4.3, Section V `(5.2)` `(5.3)` `(5.4)`, Section V.2 `(5.20)`
  `(5.21)`, Lemma 5.2, `(AN)` `(PN)`, and the region-local shell/slice
  transport API;
  genuinely new:
  the actual-shell fixed-time boundary-distribution descent / support package;
- bounded-pass verdict:
  the new package is theorem-sized mathematically but not yet honest as a bare
  theorem/local-lemma skeleton at the second insertion surface, so no
  production Lean edits were made in this pass.

Fresh bounded theorem-3 audit of the live coefficient-only outside-region seam,
checking whether any source-backed implication package actually exists above it
(2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean; the second live shell-local
  insertion surface still contains
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages 288-289 / Theorem 4.1 `(4.4)` `(4.5)`, Theorem 4.2 `(4.6)`,
  Theorem 4.3;
  printed pages 293-295 / Section V.2, formulas `(5.20)` `(5.21)`, Lemma 5.2,
  and the `(AN)` `(PN)` continuation package;
  local declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`,
  `hasFDerivAt_bvt_F_canonicalXiShift`,
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
  and the local `hlimit_os` block;
- no unsupported residue was found or removed at either insertion point;
- exact implication conclusion:
  no source-backed package above the minimal coefficient theorem exists on the
  live route;
- exact failure mode:
  the paper support conclusion in Theorem 4.3 lives on descended `W_k` after
  Fourier-Laplace continuation, while the current Lean transport API only
  rewrites the canonical shell on
  `section43PositiveEnergyRegion d (n + m)`;
  nothing inspected converts
  `y ∉ section43PositiveEnergyRegion d (n + m)` into vanishing of the actual
  shell coefficient used by
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact next genuinely new theorem package:

  `∀ y : NPointDomain d (n + m),
      y ∉ section43PositiveEnergyRegion d (n + m) →
        Filter.Tendsto
          (fun ε : ℝ =>
            bvt_F OS lgc (n + m)
              (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                (fun k μ =>
                  ↑(y k μ) +
                    ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                      Complex.I)
                (t : ℂ)))
          (nhdsWithin 0 (Set.Ioi 0))
          (nhds 0)`;
- exact local hypotheses genuinely required:
  `OS`, `lgc`, `{n m : ℕ}`, `hm : 0 < m`, `t : ℝ`,
  `y : NPointDomain d (n + m)`,
  `hy : y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact insertion-surface location:
  immediately after `hasFDerivAt_bvt_F_canonicalXiShift` and before
  `tendsto_bvt_F_canonical_conjTensorProduct_timeShift_boundaryValue` in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`;
- exact immediate downstream consumer chain inside `hlimit_os`:
  coefficient-only outside-region pointwise `hlim`
  -> product-level outside-region pointwise `hlim`
  -> full pointwise `hlim` input for
     `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  -> shell-integral limit `0`
  -> `hlimit_os`
  -> `tendsto_nhds_unique hlimit_w hlimit_os`;
- source-backed versus genuinely new split:
  source-backed:
  Theorem 4.3, Section V.2, and the region-local quotient / fixed-time slice
  transport API;
  genuinely new:
  the actual-shell complement-control implication from those descended support
  facts to the coefficient limit on
  `((section43PositiveEnergyRegion d (n + m))ᶜ)`;
- bounded-pass verdict:
  no honest compile-clean theorem/local lemma could be landed at the second
  shell insertion surface from these source objects, so no production Lean edits
  were made in this pass.

Fresh bounded theorem-3 audit of the live complement-side shell seam, checking
whether the coefficient-only outside-region theorem lowers further
(2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean; the second live shell-local
  insertion surface still contains
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages 288-289 / Theorem 4.1 `(4.4)` `(4.5)`, Theorem 4.2 `(4.6)`,
  Theorem 4.3;
  printed pages 293-295 / Section V.2, formulas `(5.20)` `(5.21)`, Lemma 5.2,
  and the `(AN)` `(PN)` continuation package;
  local declarations
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `hasFDerivAt_bvt_F_canonicalXiShift`,
  `tendsto_bvt_F_canonical_xiShift_conjTensorProduct_timeShift_boundaryValue`,
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
  and the local `hlimit_os` block;
- no unsupported residue was found or removed at either insertion point;
- exact lowering conclusion:
  the product-level outside-region supplier lowers to the coefficient-only
  theorem, but the coefficient-only theorem does not honestly lower one theorem-
  sized step further on the live route;
- exact minimal theorem shape:

  `∀ y : NPointDomain d (n + m),
      y ∉ section43PositiveEnergyRegion d (n + m) →
        Filter.Tendsto
          (fun ε : ℝ =>
            bvt_F OS lgc (n + m)
              (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                (fun k μ =>
                  ↑(y k μ) +
                    ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                      Complex.I)
                  (t : ℂ)))
          (nhdsWithin 0 (Set.Ioi 0))
          (nhds 0)`;
- exact local hypotheses genuinely required at that minimal seam:
  `OS`, `lgc`, `{n m : ℕ}`, `hm : 0 < m`, `t : ℝ`,
  `y : NPointDomain d (n + m)`,
  `hy : y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact reason no weaker coefficient-side seam is honest here:
  the inside-region cases are already discharged by the source-backed quotient /
  slice transport cluster, and the dominated-convergence consumer still asks for
  pointwise `Tendsto` of the actual integrand; after factoring out the constant
  scalar `h y`, nothing smaller remains except the bare coefficient limit on the
  true outside-region branch;
- exact insertion-surface location:
  immediately after `hasFDerivAt_bvt_F_canonicalXiShift` and before
  `tendsto_bvt_F_canonical_conjTensorProduct_timeShift_boundaryValue` in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`;
- exact immediate downstream consumer chain inside `hlimit_os`:
  coefficient-only outside-region pointwise `hlim`
  -> product-level outside-region pointwise `hlim`
  -> full pointwise `hlim` input for
     `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  -> shell-integral limit `0`
  -> `hlimit_os`
  -> `tendsto_nhds_unique hlimit_w hlimit_os`;
- source-backed versus genuinely new split:
  source-backed:
  Theorem 4.3 plus the Chapter-V continuation package and the local quotient /
  fixed-time transport API;
  genuinely new:
  outside-region vanishing of the actual shell coefficient;
- bounded-pass verdict:
  no honest compile-clean theorem/local lemma could be landed at the second
  shell insertion surface from these source objects, so no production Lean edits
  were made in this pass.

Fresh bounded theorem-3 audit of the live complement-side pointwise `hlim`
supplier seam, now tightened to the bare shell coefficient question
(2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean; the second live shell-local
  insertion surface still contains
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages 288-289 / Theorem 4.1 `(4.4)` `(4.5)`, Theorem 4.2 `(4.6)`,
  Theorem 4.3;
  printed pages 291-295 / formulas `(5.2)` `(5.3)` `(5.4)`, Lemma 5.1, and
  the `(A0)` `(AN)` `(PN)` continuation package;
  local declarations
  `section43PositiveEnergyRegion`,
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`,
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `hasFDerivAt_bvt_F_canonicalXiShift`,
  `tendsto_bvt_F_canonical_xiShift_conjTensorProduct_timeShift_boundaryValue`,
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
  and the local `hlimit_os` block;
- no unsupported residue was found or removed at either insertion point;
- exact lowering conclusion:
  the current product-level outside-region supplier is not minimal; it lowers
  algebraically to a coefficient-only outside-region theorem because the factor
  `h y` is constant in `ε`;
- exact minimal weaker theorem shape:

  `∀ y : NPointDomain d (n + m),
      y ∉ section43PositiveEnergyRegion d (n + m) →
        Filter.Tendsto
          (fun ε : ℝ =>
            bvt_F OS lgc (n + m)
              (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                (fun k μ =>
                  ↑(y k μ) +
                    ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                      Complex.I)
                  (t : ℂ)))
          (nhdsWithin 0 (Set.Ioi 0))
          (nhds 0)`;
- exact local hypotheses genuinely required at that weaker seam:
  `OS`, `lgc`, `{n m : ℕ}`, `hm : 0 < m`, `t : ℝ`,
  `y : NPointDomain d (n + m)`,
  `hy : y ∉ section43PositiveEnergyRegion d (n + m)`;
  `h` and the surrounding kernel-zero hypothesis `hker` are not part of this
  coefficient-only statement;
- exact reason this weaker seam is sufficient but still unsupported:
  once the coefficient tends to `0`, the live outside-branch input for
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence` follows
  immediately by multiplication with the constant scalar `h y`; however the
  inspected OS II source and the current quotient/slice API still give no
  outside-region theorem forcing that coefficient limit;
- exact insertion-surface location:
  immediately after `hasFDerivAt_bvt_F_canonicalXiShift` and before
  `tendsto_bvt_F_canonical_conjTensorProduct_timeShift_boundaryValue` in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`;
- exact immediate downstream consumer chain inside `hlimit_os`:
  coefficient-only outside-region pointwise `hlim`
  -> product-level outside-region pointwise `hlim`
  -> full pointwise `hlim` input for
     `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  -> shell-integral limit `0`
  -> `hlimit_os`
  -> `tendsto_nhds_unique hlimit_w hlimit_os`;
- source-backed versus genuinely new split:
  source-backed:
  Theorem 4.3 plus the local quotient/slice package only provide region-local
  support / equality / transport on
  `section43PositiveEnergyRegion d (n + m)`;
  genuinely new:
  the coefficient-only complement-side limit theorem, and hence also the
  product-level corollary;
- bounded-pass verdict:
  no honest compile-clean theorem/local lemma could be landed at the second
  shell insertion surface from these source objects, so no production Lean edits
  were made in this pass.

Fresh bounded theorem-3 audit of the live complement-side pointwise `hlim`
supplier seam, rechecked directly from OS II and the live `hlimit_os` code
(2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean; the second live shell-local
  insertion surface still contains
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages 288-289 / Theorem 4.1 `(4.4)` `(4.5)`, Theorem 4.2 `(4.6)`,
  Theorem 4.3;
  printed pages 291-295 / formulas `(5.2)` `(5.3)` `(5.4)`, Lemma 5.1, and
  the `(A0)` `(AN)` `(PN)` continuation package;
  local declarations
  `section43PositiveEnergyRegion`,
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`,
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `hasFDerivAt_bvt_F_canonicalXiShift`,
  `tendsto_bvt_F_canonical_xiShift_conjTensorProduct_timeShift_boundaryValue`,
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
  and the local `hlimit_os` block;
- no unsupported residue was found or removed at either insertion point;
- exact minimal theorem statement shape for the current complement-side pointwise
  supplier demanded by the live branch:

  `∀ y : NPointDomain d (n + m),
      y ∉ section43PositiveEnergyRegion d (n + m) →
        Filter.Tendsto
          (fun ε : ℝ =>
            bvt_F OS lgc (n + m)
              (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                (fun k μ =>
                  ↑(y k μ) +
                    ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                      Complex.I)
                  (t : ℂ)) *
              h y)
          (nhdsWithin 0 (Set.Ioi 0))
          (nhds 0)`;
- exact local hypotheses genuinely required at that seam:
  `OS`, `lgc`, `{n m : ℕ}`, `hm : 0 < m`, `t : ℝ`,
  `h : SchwartzNPoint d (n + m)`,
  `y : NPointDomain d (n + m)`,
  `hy : y ∉ section43PositiveEnergyRegion d (n + m)`;
  on the surrounding live route this theorem is used together with
  `hker : section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`, but the
  source-backed quotient facts still only control the region branch;
- exact insertion-surface location:
  immediately after `hasFDerivAt_bvt_F_canonicalXiShift` and before
  `tendsto_bvt_F_canonical_conjTensorProduct_timeShift_boundaryValue` in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`;
- exact immediate downstream consumer chain inside `hlimit_os`:
  outside-region pointwise `hlim`
  -> full pointwise `hlim` input for
     `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  -> shell-integral limit `0`
  -> `hlimit_os`
  -> `tendsto_nhds_unique hlimit_w hlimit_os`;
- source-backed versus genuinely new split:
  source-backed:
  Theorem 4.3 plus the local quotient/slice package only provide region-local
  support / equality / transport on
  `section43PositiveEnergyRegion d (n + m)`;
  genuinely new:
  the complement-side pointwise limit itself on
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- bounded-pass verdict:
  no honest compile-clean theorem/local lemma could be landed at the second
  shell insertion surface from these source objects, so no production Lean edits
  were made in this pass.

Fresh bounded theorem-3 audit of the live complement-side pointwise `hlim`
supplier seam (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean; the second live shell-local
  insertion surface still contains
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages 288-289 / Theorem 4.1 `(4.4)` `(4.5)`, Theorem 4.2 `(4.6)`,
  Theorem 4.3;
  printed pages 291-292 / formulas `(5.2)` `(5.3)` `(5.4)`;
  printed page 292 / Lemma 5.1;
  local declarations
  `section43PositiveEnergyRegion`,
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`,
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `hasFDerivAt_bvt_F_canonicalXiShift`,
  `tendsto_bvt_F_canonical_xiShift_conjTensorProduct_timeShift_boundaryValue`,
  and the local `hlimit_os` block;
- no unsupported residue was found or removed at either insertion point;
- exact minimal theorem statement shape for the current complement-side pointwise
  supplier demanded by the live branch:

  `∀ y : NPointDomain d (n + m),
      y ∉ section43PositiveEnergyRegion d (n + m) →
        Filter.Tendsto
          (fun ε : ℝ =>
            bvt_F OS lgc (n + m)
              (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                (fun k μ =>
                  ↑(y k μ) +
                    ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                      Complex.I)
                  (t : ℂ)) *
              h y)
          (nhdsWithin 0 (Set.Ioi 0))
          (nhds 0)`;
- exact local hypotheses genuinely required at that seam:
  `OS`, `lgc`, `{n m : ℕ}`, `hm : 0 < m`, `t : ℝ`,
  `h : SchwartzNPoint d (n + m)`,
  `y : NPointDomain d (n + m)`,
  `hy : y ∉ section43PositiveEnergyRegion d (n + m)`;
  on the surrounding live route this theorem is used together with
  `hker : section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`, but the
  source-backed quotient facts still only control the region branch;
- exact insertion-surface location:
  immediately after `hasFDerivAt_bvt_F_canonicalXiShift` and before
  `tendsto_bvt_F_canonical_conjTensorProduct_timeShift_boundaryValue` in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`;
- exact immediate downstream consumer chain inside `hlimit_os`:
  outside-region pointwise `hlim`
  -> full pointwise `hlim` input for
     `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  -> shell-integral limit `0`
  -> `hlimit_os`
  -> `tendsto_nhds_unique hlimit_w hlimit_os`;
- source-backed versus genuinely new split:
  source-backed:
  Theorem 4.3 plus the local quotient/slice package only provide region-local
  support / equality / transport on
  `section43PositiveEnergyRegion d (n + m)`;
  genuinely new:
  the complement-side pointwise limit itself on
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- bounded-pass verdict:
  no honest compile-clean theorem/local lemma could be landed at the second
  shell insertion surface from these source objects, so no production Lean edits
  were made in this pass.

Fresh bounded theorem-3 source/doc pass on the live actual-shell route (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean; the second live shell-local
  insertion surface still contains
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact paper objects inspected in `reconstruction theorem II.pdf`:
  PDF pages 13-15, corresponding to printed pages 288-294:
  Theorem 4.1 `(4.4)` `(4.5)`, Theorem 4.2 `(4.6)`, Theorem 4.3,
  formulas `(5.2)` `(5.3)` `(5.4)`, and Lemma 5.1;
- exact local source objects inspected:
  `section43PositiveEnergyRegion`,
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region`,
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`,
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`,
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_section43Region`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `hasFDerivAt_bvt_F_canonicalXiShift`,
  and the local block `hlimit_os`;
- decomposition verdict:
  the complement-side pointwise `hlim` supplier for kernel elements does **not**
  decompose one theorem/local lemma smaller on the live shell route;
- strongest honest weaker-or-equivalent source-backed theorem shape found:
  OS II Theorem 4.3 gives positive-energy support / Fourier-Laplace
  reconstruction only, and the local faithful formal shadow remains region-local
  quotient control plus fixed-time slice transport on
  `section43PositiveEnergyRegion d (n + m)`;
- insertion-surface readiness verdict:
  still **too weak / misplaced** for the live shell insertion surface after
  `hasFDerivAt_bvt_F_canonicalXiShift`, because it does not produce the
  complement-side pointwise limit demanded by
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact reason the supplier remains unsupported on the actual shell route:
  quotient-zero for
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h`
  only constrains `h y` on the Section-4.3 region. On the true complement
  `y ∉ section43PositiveEnergyRegion d (n + m)`, the local route still has no
  equation for `h y`, and the inspected OS II continuation / support theorems
  do not identify the canonical actual-shell boundary value with `0` there;
- therefore no production Lean edits were made and no new compile-clean
  theorem/local lemma landed in this pass.

Fresh bounded theorem-3 insertion-surface source audit (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean; the second live shell-local
  insertion surface still contains
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact paper objects inspected in `reconstruction theorem II.pdf`:
  printed page 288 / Theorem 4.1 `(4.4)` `(4.5)` and Theorem 4.2 `(4.6)`,
  printed page 289 / Theorem 4.3 with the positive-energy support conclusion
  for `W_k`,
  printed pages 291-294 / formulas `(5.2)` `(5.3)` `(5.4)` and Lemma 5.1;
- exact local source objects inspected:
  `section43PositiveEnergyRegion`,
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region`,
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`,
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`,
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_section43Region`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `hasFDerivAt_bvt_F_canonicalXiShift`,
  and the local block `hlimit_os`;
- strongest honest weaker-or-equivalent source-backed theorem shape found below
  the failed complement-vanishing surface:
  Theorem 4.3 gives positive-energy support / Fourier-Laplace reconstruction,
  while the local formal shadow only yields region-local quotient equality and
  fixed-time slice transport on
  `section43PositiveEnergyRegion d (n + m)`;
- insertion-surface readiness verdict:
  still **too weak / misplaced** for the live shell insertion surface after
  `hasFDerivAt_bvt_F_canonicalXiShift`, because the live consumer there needs a
  complement-side pointwise `Filter.Tendsto ... (nhds 0)` input;
- exact reason this weaker source-backed package still does not discharge
  `hlimit_os`:
  quotient-zero for a kernel element controls `h y` only on the Section-4.3
  region, not on the true complement, and the inspected OS II continuation and
  support theorems do not identify the canonical actual-shell boundary value
  with `0` at complement points;
- no production Lean edits were made and no new compile-clean theorem/local
  lemma landed in this pass.

Fresh bounded theorem-3 audit of the fixed-seminorm Montel/quotient seam on the
same live actual-shell route (2026-04-15):

- `(A0)` re-check confirmed again:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed page 289 / Theorem 4.3;
  printed pages 290-291 / `(5.2)` `(5.3)` `(5.4)`;
  printed pages 293-295 / Section V.2 `(AN)` `(PN)`, `(5.20)` `(5.21)`,
  Lemma 5.2;
  Lean declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `bvt_F_holomorphic`,
  `SCV.tubeSlice_uniformPolyGrowth_of_polyGrowth`,
  `distributional_limit_of_equicontinuous`,
  `SchwartzMap.tempered_uniform_schwartz_bound`,
  `SchwartzMap.tempered_apply_tendsto_zero_of_tendsto`;
  mathlib declarations
  `schwartzSeminormFamily`,
  `schwartzSeminormFamily_apply`,
  `ContinuousLinearMap.hasBasis_nhds_zero`,
  `ContinuousLinearMap.eventually_nhds_zero_mapsTo`,
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`,
  `MontelSpace.isCompact_of_isClosed_of_isVonNBounded`;
- unsupported residue verdict:
  no unsupported residue was found or removed in this pass;
- exact smallest abstract theorem now exposed beneath the shell theorem:
  for the controlling finite seminorm
  `p := s.sup (schwartzSeminormFamily ℂ (NPointDomain d (n + m)) ℂ)`,
  bounded sets in Schwartz space must admit finite `p`-ε-nets, equivalently
  their image in the quotient/pseudometric induced by `p` must be totally
  bounded;
- exact theorem statement now required:

  `Bornology.IsVonNBounded ℂ B ->
    ∀ εp > 0, ∃ t : Finset (SchwartzNPoint d (n + m)),
      ∀ h ∈ B, ∃ g ∈ t, p (h - g) < εp`;

  equivalent quotient form:

  `Bornology.IsVonNBounded ℂ B ->
    TotallyBounded ((q_p) '' B)`;
- audit verdict on size:
  this is not genuinely smaller than the shell bounded-set `MapsTo` theorem;
  after common `p`-control and pointwise convergence are in hand, the deduction
  finite `p`-nets
  ->
  eventual `MapsTo`
  ->
  `TendstoUniformlyOn`
  ->
  strong convergence
  is routine, so the abstract theorem is only an equivalent restatement;
- exact specialization path:
  translated-shell growth
  ->
  `forwardTube_boundaryValueData_of_polyGrowth` gives packaged witness `T_t`
  with pointwise convergence
  ->
  Banach-Steinhaus / `SchwartzMap.tempered_uniform_schwartz_bound`
  gives common finite `p`
  ->
  bounded-set finite `p`-nets
  ->
  eventual `MapsTo`
  ->
  `TendstoUniformlyOn`
  ->
  `ContinuousLinearMap.tendsto_iff_tendstoUniformlyOn`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  holomorphy,
  shell growth,
  boundary-value witness existence,
  pointwise convergence,
  common finite-seminorm control,
  and the abstract strong-topology / Montel APIs;
  genuinely new:
  the Schwartz bounded-set precompactness theorem for a fixed finite seminorm;
- why early extraction of `T_t` still does not help:
  the witness is already packaged by
  `forwardTube_boundaryValueData_of_polyGrowth`;
  extracting it earlier only renames the same object and does not supply the
  missing precompactness theorem;
- first exact proof obligation still blocking even an infrastructure landing:
  prove that bounded subsets of
  `SchwartzNPoint d (n + m)` are totally bounded for the pseudometric induced
  by an arbitrary finite sup of Schwartz seminorms;
  no inspected repo/mathlib theorem supplies this, and no nearby
  nuclear-space theorem converts current source into a usable Montel consequence;
- Lean landing verdict:
  no compile-clean abstract theorem,
  theorem statement/skeleton,
  local lemma,
  or instance landed;
  no production Lean edit was made in this pass.

Fresh bounded theorem-3 raw-shell package decomposition audit (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean; the second still contains
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact source objects inspected:
  `reconstruction theorem II.pdf`
  printed pages 288-289 / Theorem 4.1 `(4.4)` `(4.5)`, Theorem 4.2 `(4.6)`,
  Theorem 4.3;
  printed pages 290-295 / Section V `(5.2)` `(5.3)` `(5.4)`, Section V.2
  `(5.20)` `(5.21)`, Lemma 5.2, `(AN)`, `(PN)`;
  local declarations
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`,
  `hasFDerivAt_bvt_F_canonicalXiShift`,
  `section43_iteratedSlice_descendedPairing`,
  `section43_fixedPair_mixedOrderSlice_descendedScalar_eq_directPsiIntegral_fixedTime`,
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  the local `hlimit_os` block, and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- unsupported-residue conclusion:
  yes;
  live source now contains two production `sorry`s:
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
  none were removed in this bounded doc-only pass;
- exact decomposition conclusion:
  the previously documented raw-family boundary-distribution package does
  decompose one theorem-sized step smaller;
  the first honest smaller theorem is limit existence of the raw fixed-time
  shell CLM family in the ambient Schwartz dual;
- exact weaker theorem shape:

  `∀ t : ℝ, 0 < t →
      ∃ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
        Filter.Tendsto
          (fun ε : ℝ =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε
            else
              0)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          (nhds T_t)`;

- exact local hypotheses genuinely required:
  `OS`, `lgc`, `{n m : ℕ}`, `hm : 0 < m`, `t : ℝ`, `ht : 0 < t`;
- exact next theorem shape above that but still below the old full package:

  `∀ t : ℝ, 0 < t →
      ∀ T_t : SchwartzNPoint d (n + m) →L[ℂ] ℂ,
        Filter.Tendsto
          (fun ε : ℝ =>
            if hε : 0 < ε then
              section43_fixedTimeShellRawCLM (d := d) OS lgc hm t ε hε
            else
              0)
          (nhdsWithin (0 : ℝ) (Set.Ioi 0))
          (nhds T_t) →
        ∀ h : SchwartzNPoint d (n + m),
          section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
            T_t h = 0`;

  this is the quotient-kernel annihilation / factorization theorem;
- exact reason that existence theorem is the first smaller honest unit:
  it is the first statement not already present beneath
  `section43_fixedTimeShellRawCLM`;
  continuity, measurability, growth, and the raw CLM family itself are already
  source-backed, while quotient-kernel annihilation needs an additional support
  argument and the pointwise outside-region coefficient corollary needs a still
  later bridge from the supported limit distribution back to the actual-shell
  coefficient branch in `hlimit_os`;
- exact insertion-surface location:
  immediately after `section43_fixedTimeShellRawCLM` and before
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  the later coefficient corollary still belongs after
  `hasFDerivAt_bvt_F_canonicalXiShift`;
- exact immediate downstream consumer chain:
  raw-shell CLM limit existence
  -> quotient-kernel annihilation / factorization
  -> full fixed-time shell boundary-distribution package
  -> outside-region coefficient corollary
  -> product-level pointwise `hlim`
  -> dominated-convergence theorem
  -> `hlimit_os`;
- source-backed versus genuinely new split:
  source-backed:
  Theorem 4.3, Section V / V.2 continuation/support package, and the current
  region-local shell/slice transport plus descended-pairing theorems;
  genuinely new:
  ambient-shell limit existence for the raw family, then quotient-kernel
  annihilation of that limit, then the outside-region actual-shell corollary;
- theorem-sized landing verdict:
  the existence-only statement is honest and already present as a live local
  theorem skeleton, but not compile-clean because it still carries `sorry`;
  no production Lean theorem/local lemma landed compile-clean in this pass.

Fresh bounded theorem-3 source-backed coefficient-complement re-audit (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while the second live shell-local cluster
  directly after `bvt_F_canonicalXiShift_hasPolynomialGrowth` still contains
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact paper objects inspected in `reconstruction theorem II.pdf`:
  printed page 288 / Theorem 4.1 `(4.4)` `(4.5)` and Theorem 4.2 `(4.6)`,
  printed page 289 / Theorem 4.3 including the support conclusion
  `W_k` supported in `ℝ_+^k`,
  printed pages 291-294 / formulas `(5.2)` `(5.3)` `(5.4)`, Lemma 5.1, and the
  Chapter V `(A0)` `(AN)` `(PN)` continuation package;
- exact local source objects inspected:
  `section43PositiveEnergyRegion`,
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region`,
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`,
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`,
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_section43Region`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`,
  `hasFDerivAt_bvt_F_canonicalXiShift`,
  and the local block `hlimit_os`;
- source-backed verdict:
  OS II / current local source do **not** justify the complement-vanishing
  theorem itself for the live coefficient
  `ε ↦ bvt_F OS lgc (n + m) (xiShift ... ε ... (t : ℂ))`
  at points
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- strongest honest weaker-or-equivalent source-backed theorem shape found:
  the paper gives only positive-energy support / Fourier-Laplace reconstruction
  of `W_k`, while the local faithful Lean surface is only region-local quotient
  control:
  if two ambient tests agree on
  `section43PositiveEnergyRegion d (n + m)`, then their Section-4.3 quotient
  classes agree; in particular quotient-zero implies vanishing on the region;
- exact negative conclusion about the other inspected Chapter V source:
  formulas `(5.2)` `(5.3)` `(5.4)` and Lemma 5.1 provide semigroup matrix
  elements, one-variable continuation steps, and local analyticity radius, but
  no theorem identifying the actual-shell complement boundary value with `0`;
- exact reason that weaker source-backed shape is still insufficient beneath
  `hlimit_os`:
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence` needs a
  pointwise complement-side `hlim` supplier. The quotient/source package kills
  the region branch only; on the true complement it leaves `h y` uncontrolled,
  and the paper support/continuation theorem does not produce
  `Filter.Tendsto (...) (nhds 0)` for the canonical-shell coefficient itself;
- no production Lean edits were made and no new compile-clean theorem/local
  lemma landed in this pass.

Fresh bounded theorem-3 OS-II/source complement audit (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact paper objects inspected in `reconstruction theorem II.pdf`:
  page 288 / Theorem 4.1 `(4.4)` `(4.5)`,
  page 289 / Theorem 4.2 `(4.6)` and Theorem 4.3 with the support conclusion
  `W_k` supported in `ℝ_+^k`,
  pages 291-293 / Chapter V formulas `(5.2)` `(5.3)` `(5.4)` and Lemma 5.1;
- exact local source objects inspected:
  `section43PositiveEnergyRegion`,
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region`,
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`,
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`,
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_section43Region`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  and the local block `hlimit_os`;
- source-backed verdict:
  OS II / current local source do **not** justify the complement-vanishing
  theorem itself for the live coefficient
  `ε ↦ bvt_F OS lgc (n + m) (xiShift ... ε ... (t : ℂ))`
  at points
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- strongest honest weaker-or-equivalent source-backed theorem shape found:
  the paper gives only the positive-energy support / Fourier-Laplace theorem,
  and locally this descends only to region-local quotient control:
  if two ambient tests agree on
  `section43PositiveEnergyRegion d (n + m)`, then their Section-4.3 quotient
  classes agree; in particular quotient-zero implies vanishing on the region;
- exact reason that weaker source-backed shape is still insufficient beneath
  `hlimit_os`:
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence` needs a
  pointwise complement-side `hlim` supplier. The quotient/source package kills
  the region branch only; on the true complement it leaves `h y` uncontrolled,
  and the paper support theorem does not produce
  `Filter.Tendsto (...) (nhds 0)` for the canonical-shell coefficient itself;
- no production Lean edits were made and no new compile-clean theorem/local
  lemma landed in this pass.

Fresh bounded theorem-3 coefficient-complement insertion-surface verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact theorem statement shape now pinned for the first genuinely new package
  on the live actual-shell route:

  `∀ y : NPointDomain d (n + m),
      y ∉ section43PositiveEnergyRegion d (n + m) →
        Filter.Tendsto
          (fun ε : ℝ =>
            bvt_F OS lgc (n + m)
              (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                (fun k μ =>
                  ↑(y k μ) +
                    ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                      Complex.I)
                (t : ℂ)))
          (nhdsWithin 0 (Set.Ioi 0))
          (nhds 0)`;
- exact insertion surface/file:
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  at the second live insertion surface, immediately after
  `hasFDerivAt_bvt_F_canonicalXiShift`;
- exact immediate consumer chain to `hlimit_os`:
  1. coefficient-side complement coefficient vanishing;
  2. kernel complement-side pointwise `hlim`;
  3. complement-sensitive `hbound`;
  4. `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  5. canonical-shell kernel annihilation in the limit;
  6. specialization to
     `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`;
  7. `hlimit_os`;
- exact reason weaker statements are still insufficient:
  region-only or source-support-only statements do not touch the true
  complement branch, while `section43PositiveEnergyQuotientMap = 0` for a
  kernel element only implies `h = 0` on the region. Therefore no weaker
  quotient/source theorem can yield the needed complement-side pointwise `hlim`
  contract;
- no production Lean edits were made and no new compile-clean theorem/local
  lemma landed in this pass.

Fresh bounded theorem-3 complement-package-above-supplier verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  both live insertion-point theorem clusters,
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  `hlimit_os`,
  the fixed-pair region transport and tensor-equality theorems, and the
  Section-4.3 quotient codomain API in `Section43Codomain.lean`;
- exact corrected minimal package above the exposed complement-side pointwise
  supplier:
  the coefficient-side complement boundary-vanishing theorem for the canonical
  shell coefficient, namely for
  `y ∉ section43PositiveEnergyRegion d (n + m)` prove
  `ε ↦ bvt_F OS lgc (n + m) (xiShift ... ε ... (t : ℂ))` tends to `0` as
  `ε → 0+`;
- exact side classification: coefficient-side;
- exact reason this is the smallest honest package above the supplier:
  the supplier must hold for arbitrary kernel elements `h`, and quotient-zero
  says nothing about `h y` on the true complement. So no source-side theorem
  from the current quotient API can imply that supplier there. If the
  coefficient itself tends to `0`, the supplier follows immediately by
  multiplication by the fixed scalar `h y`;
- exact source reason this package still does not follow from current source:
  current source has no complement-side boundary theorem for `bvt_F` on the
  actual canonical shell. The quotient/support package is region-local only,
  while the shell source only provides open-tube holomorphy and polynomial
  growth at positive imaginary height, not vanishing of the boundary value at
  complement points;
- exact package order now exposed:
  1. coefficient-side complement pointwise coefficient vanishing;
  2. kernel complement-side pointwise `hlim` supplier;
  3. complement-sensitive `hbound`;
  4. limit-level canonical-shell kernel annihilation;
  5. specialization to the actual tensor difference;
  6. `hlimit_os`;
- no production Lean edits were made and no new compile-clean theorem/local
  lemma landed in this pass.

Fresh bounded theorem-3 coefficient-package recheck verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact minimal package above the exposed complement-side supplier remains the
  coefficient-side theorem asserting complement boundary-value vanishing of the
  canonical shell coefficient `ε ↦ bvt_F OS lgc (n + m) (xiShift ... ε ...)`;
- exact side classification: coefficient-side;
- exact source reason this is minimal:
  the supplier beneath it is quantified over arbitrary kernel elements `h`,
  but quotient-zero only forces `h = 0` on
  `section43PositiveEnergyRegion d (n + m)`. Therefore no source-side theorem
  from the present quotient API can imply the supplier on the true complement;
  coefficient-side vanishing can;
- exact source reason it still does not follow from current source:
  the current shell package supplies holomorphy and polynomial growth on the
  open forward tube and positive-height shell, not complement-side boundary
  vanishing at real shell points. The Section-4.3 quotient/support theorems
  remain region-local only;
- exact package order now fixed:
  1. coefficient-side complement pointwise coefficient vanishing;
  2. kernel complement-side pointwise `hlim` supplier;
  3. complement-sensitive `hbound`;
  4. limit-level canonical-shell kernel annihilation;
  5. specialization to the actual tensor difference;
  6. `hlimit_os`;
- no production Lean edits were made and no new compile-clean theorem/local
  lemma landed in this pass.

Fresh bounded theorem-3 sub-complement-`hlim` verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  both live insertion-point theorem clusters,
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  `hlimit_os`,
  the fixed-pair region transport and tensor-equality theorems, and the
  Section-4.3 quotient codomain API in `Section43Codomain.lean`;
- exact theorem-search conclusion:
  no honest new theorem/local lemma strictly below the complement-side
  pointwise `hlim` supplier is implementable from current live source;
- exact smallest remaining theorem-sized package is still that
  complement-side pointwise `hlim` supplier for kernel elements;
- exact input category: shell-level input to
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact source reason:
  quotient-zero already kills the region branch, so no smaller shell-side
  branch remains there; on the true complement
  `y ∉ section43PositiveEnergyRegion d (n + m)`, current source still has no
  theorem giving pointwise canonical-shell vanishing, canonical-shell
  localization, or limit-to-zero;
- exact package order now exposed:
  1. kernel complement-side pointwise `hlim`;
  2. complement-sensitive `hbound`;
  3. limit-level canonical-shell kernel annihilation;
  4. specialization to the actual tensor difference;
  5. `hlimit_os`;
- no production Lean edits were made and no new compile-clean theorem/local
  lemma landed in this pass.

Fresh bounded theorem-3 kernel-complement-`hlim` verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. both live insertion-point theorem clusters;
  2. `section43_fixedTimeShellRawCLM`;
  3. `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  4. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  5. the fixed-pair region transport and tensor-equality theorems;
  6. the Section-4.3 quotient codomain API in `Section43Codomain.lean`;
- exact theorem-sized package strictly beneath the current kernel-annihilation
  frontier:
  the first smaller shell-side target is the complement-side pointwise `hlim`
  supplier for kernel elements, i.e. for
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0` and
  `y ∉ section43PositiveEnergyRegion d (n + m)`, prove the live integrand
  against `h y` tends to `0` as `ε → 0+`;
- exact dominated-convergence input targeted: `hlim`;
- exact source reason this is the smallest honest step:
  the kernel hypothesis already gives `h = 0` on the region, so the pointwise
  `hlim` branch there is immediate; no smaller shell-side substatement remains
  before the true complement branch;
- exact source reason it still does not follow from current source:
  the quotient package is purely region-local, while the shell coefficient is
  ambient; current source contains no complement theorem controlling
  `y ∉ section43PositiveEnergyRegion d (n + m)` pointwise in the `ε → 0+`
  limit, so the kernel-side `hlim` supplier is still unsupported;
- exact package order now exposed:
  1. kernel complement-side pointwise `hlim`;
  2. any complement-sensitive `hbound`;
  3. limit-level canonical-shell kernel annihilation;
  4. specialization to the actual tensor difference;
  5. `hlimit_os`;
- no production Lean edits were made and no new compile-clean theorem/local
  lemma landed in this pass.

Fresh bounded theorem-3 kernel-annihilation verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. both live insertion-point theorem clusters;
  2. `section43_fixedTimeShellRawCLM`;
  3. `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  4. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  5. the fixed-pair region transport and tensor-equality theorems;
  6. the Section-4.3 quotient codomain API in `Section43Codomain.lean`;
- exact next theorem-sized package beneath the ambient shell-comparison limit:
  the right immediate surface is the limit-level canonical-shell
  quotient-kernel annihilation theorem, not fixed-`ε` shell localization and
  not fixed-`ε` quotient descent of `section43_fixedTimeShellRawCLM`;
- exact statement shape now isolated:
  for any ambient `h : SchwartzNPoint d (n + m)` with
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`,
  prove

  `Filter.Tendsto
    (fun ε : ℝ =>
      section43_fixedTimeShellRawCLM OS lgc hm t ε hε h)
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0)`;

- exact source reason this is the smallest honest step:
  the Section-4.3 transport package already gives the kernel hypothesis for
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`, so the remaining
  missing mathematics is shell-side annihilation of that kernel only in the
  `ε → 0+` limit; asking for fixed-`ε` descent/localization would impose a
  stronger theorem shape than the live proof actually needs;
- exact source reason it still does not follow from current source:
  the quotient package is purely region-local, while
  `section43_fixedTimeShellRawCLM` is still an ambient integral over all `y`;
  current source contains no theorem descending that raw shell through the
  quotient and no complement theorem controlling
  `y ∉ section43PositiveEnergyRegion d (n + m)` well enough to supply the
  `hbound`/`hlim` hypotheses of the dominated-convergence theorem;
- exact package order now exposed:
  1. canonical-shell kernel-annihilation in the limit;
  2. specialize to the actual tensor difference;
  3. obtain the ambient shell-comparison limit / `hlimit_os`;
  4. only then continue to any later direct-`psiZ` comparison;
- no production Lean edits were made and no new compile-clean theorem/local
  lemma landed in this pass.

Fresh bounded theorem-3 minimal-contract verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. both live insertion-point theorem clusters;
  2. `section43_fixedTimeShellRawCLM`;
  3. `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  4. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  5. the fixed-pair region transport and tensor-equality theorems;
  6. the Section-4.3 quotient codomain API in `Section43Codomain.lean`;
- exact minimal-contract conclusion:
  the earlier informal contract
  "prove the complement-side pointwise supplier beneath `hlimit_os`"
  is not the right theorem-sized surface to record anymore;
- exact corrected theorem-sized contract:
  the first honest theorem-sized contract is the ambient fixed-time shell
  comparison limit for the actual shell difference, equivalently equality of
  the two shell limits against `(φ.conjTensorProduct ψ)` and
  `(f.conjTensorProduct g)`;
- exact source reason:
  the current Section-4.3 package is purely region-local. It proves only
  quotient equality / region equality and region-side vanishing, but the live
  shell functional in `hlimit_os` is an ambient integral over all `y`. So the
  missing mathematics is not merely one pointwise `hlim` branch; it is the
  absent theorem that the ambient shell ignores, localizes, or null-limits the
  true complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact package order now exposed:
  1. ambient shell-comparison / complement-localization theorem on the actual
     fixed-time shell;
  2. `hlimit_os`;
  3. only afterward any later source-shell versus direct-`psiZ` comparison;
- no production Lean edits were made and no new compile-clean theorem/local
  lemma landed in this pass.

Fresh bounded theorem-3 complement-supplier theorem-sized verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. both live insertion-point theorem clusters;
  2. `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  3. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  4. the fixed-pair region transport and tensor-equality theorems;
  5. the Section-4.3 quotient codomain API in `Section43Codomain.lean`;
- exact theorem-sized conclusion:
  the complement-side pointwise supplier itself is not yet implementable from
  current source as one honest theorem/local lemma on the live route;
- exact blocker status:
  the complement-side pointwise supplier itself is now confirmed as the first
  theorem-sized new-math contract on the branch;
- exact source reason:
  current Section-4.3 source only gives region-local equality/zero on
  `section43PositiveEnergyRegion d (n + m)` and says nothing analytic about the
  true complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  in particular there is still no theorem providing complement-shell
  localization, complement-side pointwise vanishing of the live integrand, or
  a direct pointwise limit-to-zero supplier there;
- `hbound` remains unsupported afterward too, but only after that first
  complement-side supplier;
- no new compile-clean theorem/local lemma landed in production Lean in this
  pass.

Fresh bounded theorem-3 complement-branch source verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. both live insertion-point theorem clusters;
  2. `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  3. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  4. the fixed-pair region transport and tensor-equality theorems;
  5. the Section-4.3 quotient codomain API in `Section43Codomain.lean`;
- exact theorem-search conclusion:
  no honest new theorem/local lemma strictly below the current complement-side
  pointwise `hlim` branch is implementable on the live route;
- exact post-pass blocker status:
  the first remaining honest theorem-sized contract is still the complement-
  side pointwise limit-to-zero supplier itself for the actual shell integrand
  on `y ∉ section43PositiveEnergyRegion d (n + m)`;
- no still-smaller source-backed explicit hypothesis beneath that supplier was
  found in this pass;
- `hbound` remains unsupported too, but only after that first complement-side
  pointwise branch;
- no new compile-clean theorem/local lemma landed in production Lean in this
  pass.

Fresh bounded theorem-3 complement-`hlim` supplier verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. both live insertion-point theorem clusters;
  2. `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  3. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  4. the fixed-pair region transport and tensor-equality theorems;
  5. the Section-4.3 quotient codomain API in `Section43Codomain.lean`;
- exact theorem-search conclusion:
  no honest new theorem/local lemma strictly below the current
  complement-side pointwise `hlim` branch is implementable on the live route;
- exact post-pass blocker status:
  the first remaining honest theorem-sized contract is now the complement-side
  pointwise limit-to-zero supplier itself for the actual shell integrand on
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- no still-smaller source-backed explicit hypothesis beneath that supplier was
  found in this pass;
- `hbound` remains unsupported too, but only after that first complement-side
  pointwise branch;
- no new compile-clean theorem/local lemma landed in production Lean in this
  pass.

Fresh bounded theorem-3 hypothesis-supplier verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. `section43_fixedTimeShellRawCLM`;
  2. `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  3. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  4. the fixed-pair region transport and tensor-equality theorems;
  5. the Section-4.3 quotient codomain API in `Section43Codomain.lean`;
- exact theorem-search conclusion:
  no honest new theorem/local lemma strictly below
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  is implementable on the live branch without changing route, because the
  current toolkit still has no theorem for the true complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact post-landing contract now recorded:
  the shell limit under `hlimit_os` must still be discharged by supplying
  `hbound` and `hlim` for the actual shell difference
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`;
- exact first unsupported branch:
  a smaller explicit hypothesis beneath `hlim`, namely the complement-side
  pointwise limit-to-zero statement for the actual shell integrand;
- `hbound` remains unsupported too, but only after that first pointwise branch,
  since current source already annihilates the difference on source support and
  on already-in-region outside-source points;
- no new compile-clean theorem/local lemma landed in production Lean in this
  pass.

Fresh bounded theorem-3 actual-shell dominated-convergence verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. `section43_fixedTimeShellRawCLM` and its unfolded ambient coefficient
     `coeff`;
  2. the live shell parameters `xiShift`, `t`, and `ε`;
  3. the live shell difference
     `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`;
  4. `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
  5. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  6. the current fixed-time region transport and tensor-equality theorems;
  7. the Section-4.3 quotient codomain API in `Section43Codomain.lean`;
- exact theorem-shape conclusion:
  the first missing mathematics is no longer the absence of a writable ambient
  theorem statement; the exact dominated-convergence theorem already exists in
  source as
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`;
- exact analytic contract exposed by the actual shell is now the need to
  instantiate that theorem with the actual shell difference, i.e. to prove the
  hypotheses
  `hbound` and `hlim` for

  `bvt_F OS lgc (n + m)
    (xiShift ... (t : ℂ)) *
    (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y)`;

- exact obstruction:
  current source still controls that integrand only on source support and on
  already-in-region outside-source points; the true complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)` still provides neither
  complement localization nor an ambient integrable majorant uniform in `ε`;
- no new compile-clean theorem/local lemma landed in production Lean in this
  pass.

Fresh bounded theorem-3 `hlimit_os` contract verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. `section43_fixedTimeShellRawCLM` and its unfolded ambient coefficient
     `coeff`;
  2. the live shell parameters `xiShift`, `t`, and `ε`;
  3. the live shell difference
     `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`;
  4. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  5. the current fixed-time region transport and tensor-equality theorems;
  6. the Section-4.3 quotient codomain API in `Section43Codomain.lean`;
- exact analytic contract exposed by the actual shell supplier remains

  `Filter.Tendsto
    (fun ε : ℝ =>
      section43_fixedTimeShellRawCLM OS lgc hm t ε hε
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0))`;

- exact shell-domain decomposition now verified from live source:
  1. source support is handled by
     `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`;
  2. the already-in-region outside-source branch is handled by
     `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;
  3. the true complement branch
     `y ∉ section43PositiveEnergyRegion d (n + m)` remains completely
     uncontrolled by current source;
- exact conclusion:
  the first missing mathematics is no longer best described as a generic
  transport/scalarization blocker, but as one ambient fixed-time shell
  comparison contract for the actual raw CLM, with hypotheses strong enough to
  remove or dominate the complement contribution uniformly in `ε`;
- why current Section-4.3 transport still does not supply that contract:
  1. shell/slice realization and fixed-time transport remain explicitly
     region-local;
  2. the quotient codomain still records only region equality / region zero;
  3. no current theorem descends the ambient shell integral over all `y` from
     those region-only facts;
- no new compile-clean theorem/local lemma landed in production Lean in this
  pass.

Fresh bounded theorem-3 coefficient-contract verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. `section43_fixedTimeShellRawCLM` and its unfolded ambient coefficient
     `coeff`;
  2. the live shell parameters `xiShift`, `t`, and `ε`;
  3. the live shell difference
     `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`;
  4. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  5. the current fixed-time region transport and tensor-equality theorems;
  6. the Section-4.3 quotient codomain API in `Section43Codomain.lean`;
- exact analytic contract exposed by the actual shell supplier:
  `section43_fixedTimeShellRawCLM OS lgc hm t ε hε` is still the ambient
  integral `h ↦ ∫ y, coeff y * h y`, hence the first missing statement remains

  `Filter.Tendsto
    (fun ε : ℝ =>
      section43_fixedTimeShellRawCLM OS lgc hm t ε hε
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0))`;

- exact hypotheses any honest smaller theorem would need:
  1. complement-localization of the actual shell integrand on
     `y ∉ section43PositiveEnergyRegion d (n + m)`, or
  2. an ambient dominated-convergence package with domination uniform in `ε`
     near `0` and pointwise convergence strong enough to kill that same
     outside-region branch;
- why current Section-4.3 transport still does not supply those hypotheses:
  1. the shell/slice realization and fixed-time transport theorems remain
     explicitly region-local;
  2. the fixed-pair tensor lemmas still kill the difference only on source
     support or on already-in-region outside-source points;
  3. the quotient codomain still records only region equality / region zero,
     so it cannot descend an ambient shell CLM taken over all `y`;
- no new compile-clean theorem/local lemma landed in production Lean in this
  pass.

Fresh bounded theorem-3 actual-shell ambient-contract verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. `section43_fixedTimeShellRawCLM` and its unfolded ambient coefficient
     `coeff`;
  2. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  3. `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`;
  4. `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`;
  5. `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;
  6. `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`;
  7. `section43PositiveEnergyVanishingSubmodule`,
     `section43PositiveEnergyQuotientMap`,
     `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`, and
     `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`;
- exact analytic contract exposed by the live shell supplier:
  `section43_fixedTimeShellRawCLM OS lgc hm t ε hε` is the ambient integral
  `h ↦ ∫ y, coeff y * h y`, with
  `coeff y = bvt_F OS lgc (n + m) (xiShift ... (t : ℂ))`;
- exact first missing theorem-sized contract remains

  `Filter.Tendsto
    (fun ε : ℝ =>
      section43_fixedTimeShellRawCLM OS lgc hm t ε hε
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0))`;

- exact hypotheses any honest smaller ambient theorem would need:
  1. complement-sensitive localization of the actual shell coefficient, or
  2. an ambient dominated-convergence package for the actual shell integrand,
     with a dominating function independent of `ε` near `0` and pointwise
     control on the true complement branch
     `y ∉ section43PositiveEnergyRegion d (n + m)`;
- why current Section-4.3 transport theorems still do not provide those
  hypotheses:
  1. shell/slice realization and fixed-time transport are explicitly
     restricted to `section43PositiveEnergyRegion d (n + m)`;
  2. the fixed-pair tensor lemmas still kill the difference only on source
     support or on already-in-region outside-source points;
  3. the quotient codomain theorems still encode only region equality /
     region-zero information, so they cannot descend an ambient shell CLM that
     integrates over all `y`;
- no new compile-clean theorem/local lemma landed in production Lean in this
  pass.

Fresh bounded theorem-3 actual-shell live-source verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  2. `section43_fixedTimeShellRawCLM` with its unfolded ambient coefficient
     `coeff`;
  3. `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`;
  4. `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`;
  5. `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;
  6. `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`;
  7. `section43PositiveEnergyVanishingSubmodule`,
     `section43PositiveEnergyQuotientMap`,
     `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`, and
     `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`;
- exact source-level conclusion:
  1. `section43_fixedTimeShellRawCLM` is still an ambient integral over all
     `y`, so it does not descend automatically from Section-4.3 quotient
     equality on `section43PositiveEnergyRegion d (n + m)`;
  2. the currently landed realization and transport theorems remain explicitly
     region-local and therefore never discharge the true complement branch;
  3. the fixed-pair tensor lemmas still kill the shell difference only on
     source support or on already-in-region outside-source points;
  4. therefore region-only equality remains insufficient, and no honest
     smaller theorem currently removes the outside-region contribution in
     `hlimit_os`;
- exact minimal missing contract remains

  `Filter.Tendsto
    (fun ε : ℝ =>
      section43_fixedTimeShellRawCLM OS lgc hm t ε hε
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0))`;

- why the smaller candidate formulations still collapse to that same missing
  contract:
  1. fixed-`ε` raw-CLM annihilation is the same complement-sensitive theorem
     before the limit;
  2. coefficient-side complement vanishing still needs a theorem on
     `y ∉ section43PositiveEnergyRegion d (n + m)`;
  3. quotient-kernel or quotient-zero descent still cannot see that
     complement branch because the quotient only remembers region equality;
- no new compile-clean theorem/local lemma landed in production Lean in this
  pass.

Fresh bounded OS-I / OS-II direct raw-CLM source verdict (2026-04-14):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact local source objects rechecked:
  1. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  2. `section43_fixedTimeShellRawCLM` and its local coefficient `coeff`;
  3. `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`;
  4. `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`;
  5. `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;
  6. `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`;
  7. `section43PositiveEnergyVanishingSubmodule`,
     `section43PositiveEnergyQuotientMap`,
     `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`, and
     `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`;
- exact paper trail inspected for this seam:
  1. OS I Section 4.1, formulas `(4.10)` and `(4.11)`, pp. 92-93;
  2. OS I Section 8, Lemmas `8.5`-`8.8`, pp. 108-110;
  3. OS II Section IV.2, Theorems `4.1`-`4.3`, pp. 288-289;
  4. OS II Section V preamble with `(5.2)`-`(5.4)`, pp. 290-291;
  5. OS II Section V.1, pp. 291-294;
  6. OS II Section VI.1, pp. 297-300;
- exact paper-source conclusion:
  1. the papers provide semigroup matrix elements, one-variable analytic
     continuation, Fourier-Laplace support, and temperedness bounds;
  2. they do not provide an ambient shell-functional descent theorem that turns
     region equality / region vanishing into equality of the actual shell
     integral over all `y`;
  3. consequently they do not supply an honest theorem-sized contract for the
     direct raw-CLM comparison limit at `hlimit_os`;
- exact missing contract remains the direct raw-CLM comparison limit

  `Filter.Tendsto
    (fun ε : ℝ =>
      section43_fixedTimeShellRawCLM OS lgc hm t ε hε
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0))`;

- no new compile-clean theorem/local lemma landed in production Lean in this
  pass.

Fresh bounded direct raw-CLM comparison-seam pass (2026-04-14):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact source objects rechecked:
  1. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;
  2. `section43_fixedTimeShellRawCLM` and its local coefficient `coeff`;
  3. `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`;
  4. `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`;
  5. `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;
  6. `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`;
  7. `section43PositiveEnergyVanishingSubmodule`,
     `section43PositiveEnergyQuotientMap`,
     `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`, and
     `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`;
- exact theorem-sized candidates checked:
  1. a fixed-`ε` complement-sensitive annihilation/localization theorem for
     `section43_fixedTimeShellRawCLM`;
  2. the equivalent complement-vanishing theorem for `coeff`;
  3. a quotient-kernel / quotient-zero descent theorem for the ambient shell
     functional;
  4. the direct raw-CLM comparison limit itself;
- why the smaller candidates still do not land:
  1. the realization/transport theorems checked above are region-local, so
     they never discharge the true complement branch;
  2. the fixed-pair tensor-weight lemmas only handle source support and
     already-in-region outside-source points;
  3. the quotient API still provides only region equality/vanishing and does
     not descend an ambient integral taken over all `y`;
  4. hence no smaller source-backed theorem currently removes the outside-
     region shell contribution in `hlimit_os`;
- exact conclusion for this pass:
  the first missing honest contract beneath `hlimit_os` is still the direct
  raw-CLM comparison limit

  `Filter.Tendsto
    (fun ε : ℝ =>
      section43_fixedTimeShellRawCLM OS lgc hm t ε hε
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0))`,

  and no new compile-clean theorem/local lemma landed in production Lean.

# OS II Detailed Proof Audit

Purpose: this note is a detailed proof-oriented reading of
`references/reconstruction theorem II.pdf`. It is written to serve as a
route-discipline document for the `E -> R` formalization and, in particular, for
the active one-gap / `k = 2` frontier. It aims to make explicit not only the
headline theorems but the hidden sublemmas, proof packaging, and chapter split
that the Lean development must respect.

This note assumes familiarity with the notation in:
- `docs/os1_detailed_proof_audit.md`
- `docs/os_reconstruction_reading_notes.md`
- `docs/openclaw_comprehensive_reading_note.md`

Current fixed-time Section-4.3 seam note for theorem 3:

- bounded coefficient-contract pass re-ran `(A0)` first and again confirmed
  both live insertion surfaces in `OSToWightmanPositivity.lean` are still
  clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- exact current coefficient-level obstruction:
  the live shell supplier is still the ambient CLM
  `h ↦ ∫ y, coeff y * h y`, so the first honest missing theorem remains the
  raw-shell limit for the actual difference
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`, not a smaller
  quotient-side restatement;
- exact missing contract still exposed by `hlimit_os`:

  `Filter.Tendsto
    (fun ε : ℝ =>
      section43_fixedTimeShellRawCLM OS lgc hm t ε hε
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0))`;
- exact ambient hypotheses still absent from current source:
  complement localization of the actual shell integrand, or a dominated-
  convergence theorem with domination uniform in `ε` near `0` on the true
  outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;

- bounded complement-branch contract pass re-ran `(A0)` first and again
  confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded source check:
  inspect the exact complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)` inside `hlimit_os` together
  with the current fixed-time shell supplier and Section-4.3 quotient API to
  decide whether exactly one honest complement-sensitive theorem-sized
  contract already lands strictly below the raw comparison limit;
- exact source surfaces checked:

  1. `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
     especially `hlimit_os`;

  2. `section43_fixedTimeShellRawCLM` and its local coefficient `coeff`;

  3. `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`;

  4. `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;

  5. `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`;

  6. `section43PositiveEnergyVanishingSubmodule`,
     `section43PositiveEnergyQuotientMap`,
     `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`, and
     `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`;
- exact candidate contracts checked:

  1. complement-sensitive fixed-`ε` annihilation/localization of the actual
     shell functional;

  2. the equivalent coefficient-side complement-vanishing statement;

  3. a quotient-kernel / quotient-zero descent theorem for the raw shell
     functional;

  4. the direct raw-CLM comparison limit itself;
- why none of the smaller candidates lands on source:

  1. the shell-to-slice realization theorem is explicitly restricted to
     `section43PositiveEnergyRegion d (n + m)`;

  2. the fixed-pair weight-difference lemmas do not touch the true complement
     branch, only source support and already-in-region outside-source points;

  3. the quotient API still records only region equality / region vanishing,
     not complement-sensitive annihilation of the ambient shell coefficient;

  4. therefore no smaller theorem presently removes the outside-region shell
     contribution in `hlimit_os`;
- exact conclusion for this pass:
  the first missing honest contract beneath `hlimit_os` is still the direct
  raw-CLM comparison limit, not a strictly smaller complement-sensitive local
  theorem.

- fresh bounded theorem-3 actual-shell shell-functional / quotient-descent
  pass re-ran `(A0)` first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded source check:
  inspect the live `hlimit_os` body together with
  `section43_fixedTimeShellRawCLM` and the current Section-4.3 quotient API to
  decide whether exactly one honest theorem-sized shell-functional /
  quotient-descent ingredient already lands strictly below the direct raw-CLM
  comparison limit;
- exact candidates checked:

  1. the existing fixed-`ε` ambient shell functional
     `section43_fixedTimeShellRawCLM OS lgc hm t ε hε`;

  2. the raw kernel inclusion
     `section43PositiveEnergyVanishingSubmodule (d := d) (n + m) ≤
       LinearMap.ker
         (section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`;

  3. the equivalent quotient-zero descent form
     `∀ h,
        section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
          section43_fixedTimeShellRawCLM OS lgc hm t ε hε h = 0`;

  4. the fixed-pair shell-difference specialization
     `section43_fixedTimeShellRawCLM OS lgc hm t ε hε
       (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))) = 0`;

  5. the same targets after unfolding through the local coefficient `coeff`
     and the `SchwartzMap` CLM packaging;
- why each still fails or is only a wrapper:

  1. `(1)` is already present only as ambient packaging; it is not a new
     theorem and does not localize or descend the shell functional;

  2. `(2)` fails because the quotient machinery controls only equality on
     `section43PositiveEnergyRegion d (n + m)`, not the ambient complement
     contribution in the shell integral;

  3. `(3)` is equivalent to `(2)` and still leaves
     `y ∉ section43PositiveEnergyRegion d (n + m)` untouched;

  4. `(4)` fails because the fixed-pair transport lemmas only kill source
     support or already-in-region outside-source points, not the true
     outside-region branch;

  5. `(5)` is definition-level repackaging of the same ambient coefficient
     integral and therefore not an honest new theorem-sized reduction;
- exact conclusion for this pass:
  there is still no single source-backed theorem-sized shell-functional /
  quotient-descent theorem strictly below the direct raw-CLM comparison limit
  that lands honestly at the true insertion point;
  the missing mathematics remains complement-sensitive annihilation/localization
  of the actual shell coefficient on the outside-region branch in `hlimit_os`.

- fresh bounded fixed-`ε` complement-sensitive actual-shell pass re-ran `(A0)`
  first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded source check:
  inspect the exact body of `section43_fixedTimeShellRawCLM`, especially its
  local coefficient `coeff`, together with the outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)` inside `hlimit_os`, to decide
  whether exactly one honest theorem-sized fixed-`ε` complement-sensitive
  actual-shell annihilation/localization statement already lands at the true
  insertion point;
- exact candidates checked:

  1. coefficient-side complement vanishing of the shell coefficient itself;

  2. pointwise complement annihilation of the actual shell integrand
     `bvt_F ... (xiShift ...) *
       (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y)`;

  3. a complement-sensitive localization theorem saying the exact shell
     functional `section43_fixedTimeShellRawCLM OS lgc hm t ε hε` is already
     determined by Section-4.3 quotient/region data on the ambient difference;

  4. the same targets after definition-level unfolding through `coeff`,
     `SchwartzMap.smulLeftCLM`, and `SchwartzMap.integralCLM`;
- why each still fails on source:

  1. there is still no theorem that the actual shell coefficient vanishes on
     `y ∉ section43PositiveEnergyRegion d (n + m)`;

  2. the available fixed-pair weight-difference lemmas stop at source support
     or already-on-region outside-source points and therefore do not kill the
     true complement branch;

  3. the quotient API still gives only region equality and no complement-side
     localization theorem for `section43_fixedTimeShellRawCLM`;

  4. CLM unfolding only restates the same ambient coefficient integral and
     does not produce any new annihilation/localization theorem;
- exact conclusion for this pass:
  there is still no single source-backed fixed-`ε` complement-sensitive
  actual-shell annihilation/localization theorem that lands honestly at the
  true insertion point;
  the missing mathematics remains complement-sensitive control of the actual
  shell coefficient on the outside-region branch in `hlimit_os`.

- fresh bounded fixed-pair actual-shell comparison-limit pass re-ran `(A0)`
  first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded source check:
  inspect the exact proof body of
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  especially `hlimit_os`, together with
  `section43_fixedTimeShellRawCLM`, to decide whether exactly one honest
  theorem-sized fixed-pair actual-shell comparison-limit statement already
  lands at the true insertion point;
- exact candidates checked:

  1. the live raw-CLM comparison limit

     `Filter.Tendsto
       (fun ε : ℝ =>
         section43_fixedTimeShellRawCLM OS lgc hm t ε hε
           (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
       (nhdsWithin 0 (Set.Ioi 0))
       (nhds 0)`;

  2. the same target after unfolding
     `section43_fixedTimeShellRawCLM` to the ambient shell integral against
     its local coefficient `coeff`;

  3. the equal-limit form comparing the same shell against
     `(φ.conjTensorProduct ψ)` and `(f.conjTensorProduct g)`;

  4. the fixed-`ε` vanishing precursor
     `section43_fixedTimeShellRawCLM ... (((φ.conjTensorProduct ψ) -
       (f.conjTensorProduct g))) = 0`;
- why each still fails on source:

  1. `(1)` is the exact first missing contract; no smaller honest theorem at
     the true insertion point currently discharges it;

  2. `(2)` is only a definition-level repackaging of `(1)`, so it adds no new
     mathematics;

  3. `(3)` is equivalent to `(1)` and still fails at the same complement
     branch `y ∉ section43PositiveEnergyRegion d (n + m)`;

  4. `(4)` would require a complement-sensitive annihilation theorem for the
     actual shell coefficient which the branch still does not have;
- exact conclusion for this pass:
  there is still no single source-backed theorem-sized fixed-pair actual-shell
  comparison-limit lemma that lands honestly at the true insertion point;
  the missing mathematics remains control of the outside-region contribution in
  `hlimit_os`.

- fresh bounded coeff-level actual-shell pass re-ran `(A0)` first and again
  confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded source check:
  inspect the exact ambient coefficient `coeff` inside
  `section43_fixedTimeShellRawCLM` together with the outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)` in `hlimit_os`, to determine
  whether exactly one honest pointwise/geometric complement-sensitive
  supplier theorem already lands below the earlier quotient/descent
  candidates;
- exact coeff-level supplier candidates checked:

  1. pointwise complement vanishing of `coeff`;

  2. pointwise complement annihilation of
     `bvt_F ... (xiShift ...) *
       (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y)`;

  3. complement-side extension of
     `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`;

  4. the same target after unfolding through `coeff`,
     `SchwartzMap.smulLeftCLM`, and `SchwartzMap.integralCLM`;
- why each still fails on source:

  1. there is still no theorem that the shell coefficient itself vanishes on
     `y ∉ section43PositiveEnergyRegion d (n + m)`;

  2. the fixed-pair weight-difference lemmas still only kill source-support
     points or already-on-region outside-source points;

  3. the available shell-to-slice realization theorem is explicitly region-
     local and supplies nothing on the complement;

  4. CLM unfolding only restates the same ambient coefficient integral and
     does not produce any geometric annihilation theorem;
- exact conclusion for this pass:
  there is still no source-backed single coeff-level pointwise/geometric
  supplier theorem that lands honestly on the present branch;
  the missing mathematics remains complement-sensitive control of the actual
  fixed-`ε` shell coefficient on the outside-region branch.

- fresh bounded theorem-3 actual-shell pass re-ran `(A0)` first and again
  confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded source check:
  inspect the live `hlimit_os` body, `section43_fixedTimeShellRawCLM`, and the
  current quotient/descent API to determine whether exactly one honest
  complement-sensitive supplier theorem already lands strictly beneath raw
  quotient descent;
- exact candidate supplier statements checked:

  1. the raw kernel inclusion

     `section43PositiveEnergyVanishingSubmodule (d := d) (n + m) ≤
       LinearMap.ker
         (section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`;

  2. the equivalent quotient-zero descent statement

     `∀ h : SchwartzNPoint d (n + m),
         section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
           section43_fixedTimeShellRawCLM OS lgc hm t ε hε h = 0`;

  3. the actual shell-difference specialization

     `section43_fixedTimeShellRawCLM OS lgc hm t ε hε
       (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))) = 0`;

  4. the same target after definition-level unfolding through
     `SchwartzMap.smulLeftCLM` and `SchwartzMap.integralCLM`;
- why each still fails on source:

  1. quotient-zero gives only equality on
     `section43PositiveEnergyRegion d (n + m)`;

  2. the quotient-zero reformulation does not add any new theorem for the
     ambient complement branch;

  3. the fixed-pair specialization still leaves
     `y ∉ section43PositiveEnergyRegion d (n + m)` untouched;

  4. CLM unfolding only restates the same ambient integral and does not
     produce any complement-sensitive annihilation statement;
- exact conclusion for this pass:
  there is still no source-backed single theorem-sized supplier one step below
  raw quotient descent that lands honestly on the present branch;
  the missing mathematics remains complement-sensitive annihilation of the
  actual fixed-`ε` shell coefficient on the outside-region branch.

- fresh bounded theorem-3 actual-shell pass re-ran `(A0)` first and again
  confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded source check:
  inspect the live `hlimit_os` body, `section43_fixedTimeShellRawCLM`, and the
  current quotient/descent API to determine whether the raw shell kernel
  inclusion

  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m) ≤
    LinearMap.ker
      (section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`

  already lands honestly on the present branch;
- exact source surfaces checked:

  1. the live `hlimit_os` branch split in
     `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;

  2. the raw shell functional definition
     `section43_fixedTimeShellRawCLM`;

  3. the quotient-side equal-on-region API
     `section43PositiveEnergyVanishingSubmodule`,
     `section43PositiveEnergyQuotientMap`,
     `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`, and
     `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`;

  4. the current transport/equality suppliers under the live seam:
     `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
     `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_section43Region`,
     `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
     and
     `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`;

  5. the existing descended-model examples in `Section43Codomain.lean`
     (`evalAtZero...`, `fourierPairingDescends...`) used as the only honest
     comparison for a possible `liftQ` route;
- exact candidate reductions checked:

  1. prove the raw kernel inclusion directly from quotient-zero;

  2. specialize it first to the actual shell difference
     `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`;

  3. unfold through `SchwartzMap.smulLeftCLM` and
     `SchwartzMap.integralCLM`;

  4. reuse the already-landed region/source-support annihilation lemmas to
     discharge the complement branch;

  5. imitate the nearby `liftQ` descents whose raw functionals are already
     determined by region-side information;
- exact negative verdict:
  every candidate still fails at the same outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  the quotient API gives only region equality,
  the transport lemmas still stop at source support or prior region
  membership,
  the CLM unfolding only restates the same ambient integral,
  and the existing `liftQ` examples do not transfer because they have an
  honest region-side annihilation theorem that `section43_fixedTimeShellRawCLM`
  does not yet have;
- exact smaller missing surface now in force:
  the precise missing theorem is still complement-sensitive annihilation of the
  actual fixed-`ε` shell functional on quotient-zero inputs, equivalently the
  raw kernel inclusion / quotient-descent contract above;
- exact conclusion for this pass:
  no honest compile-clean non-wrapper theorem/local lemma landed,
  and the raw shell kernel inclusion / quotient-descent theorem still fails to
  land on the present branch.

- fresh bounded theorem-3 actual-shell pass re-ran `(A0)` first and again
  confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded source check:
  inspect the live `hlimit_os` body together with
  `section43_fixedTimeShellRawCLM` itself to decide whether the current first
  missing theorem contract

  `Filter.Tendsto
    (fun ε : ℝ =>
      section43_fixedTimeShellRawCLM OS lgc hm t ε hε
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0))`

  admits any honest one-theorem decomposition strictly below that surface;
- exact candidate statements checked:

  1. fixed-`ε` raw-shell annihilation of the actual fixed-pair difference;

  2. the same claim after unfolding
     `section43_fixedTimeShellRawCLM` through
     `SchwartzMap.smulLeftCLM` and `SchwartzMap.integralCLM`,
     i.e. vanishing of the fixed-`ε` multiplied-Schwartz ambient integral;

  3. a dominated / temperate-convergence theorem for the exact family
     `bvt_F(... xiShift ... ε ...) * (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y)`;

  4. a localization theorem for the exact fixed-pair difference family that
     removes the complement branch without any false whole-shell ordered-
     support claim;
- exact negative verdict:
  none of those candidates is source-backed below the live shell-comparison
  limit;
  the existing transport / equality lemmas only control source support or
  already-on-region points,
  `smulLeftCLM` / `integralCLM` only repackage the same ambient integral, and
  a dominated / temperate-convergence theorem would still need new
  complement-sensitive estimates or localization on
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact sharper obstruction now in force:
  the fixed-pair shell-comparison limit itself does not decompose one theorem
  smaller on the present branch and remains the first honest missing contract
  consumed by `hlimit_os`.

- fresh bounded theorem-3 actual-shell pass re-ran `(A0)` first and again
  confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded source check:
  inspect the precise `hlimit_os` complement split together with the true
  insertion point beneath `section43_fixedTimeShellRawCLM`, to determine
  whether the outside-region branch can already be reduced by exactly one
  honest theorem-sized step either to the fixed-pair limit theorem

  `Filter.Tendsto
    (fun ε : ℝ =>
      section43_fixedTimeShellRawCLM OS lgc hm t ε hε
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0))`

  or further down to a shell-coefficient / raw-CLM complement-vanishing lemma;
- exact source surfaces checked:

  1. the live `hlimit_os` branch decomposition itself;

  2. the strongest already-landed region-local comparison theorems
     `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
     `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
     and
     `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`;

  3. the fixed-time shell functional definitions
     `section43_fixedTimeShellRawCLM`,
     `SchwartzMap.smulLeftCLM`, and
     `SchwartzMap.integralCLM`;
- exact negative verdict:
  none of those sources provides any already-supported complement-sensitive
  theorem below the live branch;
  the existing transport/equality theorems stop at source support or at prior
  region membership, while the raw-CLM unfolding only restates the same fixed-
  `ε` ambient integral;
  therefore the outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)` still has no smaller honest
  supplier either at the true insertion point or immediately beneath
  `hlimit_os`;
- exact sharper obstruction now in force:
  the first missing theorem contract is the fixed-pair ambient-vs-source shell
  comparison limit

  `Filter.Tendsto
    (fun ε : ℝ =>
      section43_fixedTimeShellRawCLM OS lgc hm t ε hε
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0))`;

  the stronger pointwise-in-`ε` quotient-kernel route

  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m) ≤
    LinearMap.ker
      (section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`

  remains unsupplied as well, but source inspection in this pass shows that it
  is not the smallest theorem needed by the live `hlimit_os` seam.

- fresh bounded true-live-route pass re-ran `(A0)` first and again confirmed
  both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded specialization checked beneath
  `section43_fixedTimeShellRawCLM`:
  whether the missing raw kernel inclusion already specializes to the actual
  fixed-pair shell-difference annihilation target

  `section43_fixedTimeShellRawCLM OS lgc hm t ε hε
    ((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) = 0`;
- exact source surfaces checked for that specialization:

  1. quotient-zero for the shell difference via
     `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region` plus
     `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`;

  2. source-support equality via
     `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`;

  3. on-region off-source vanishing via
     `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;

  4. raw-CLM definition-unfolding through `SchwartzMap.smulLeftCLM` and
     `SchwartzMap.integralCLM`;
- exact negative verdict:
  each candidate still removes only region or source-support pieces;
  none supplies any theorem for the live outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  the CLM unfolding itself only restates the same problem as vanishing of the
  fixed-`ε` ambient integral or of the multiplied Schwartz test, so no honest
  compile-clean non-wrapper theorem/local lemma currently exists that proves
  the specialized raw shell coefficient annihilates the actual shell
  difference on the complement;
- exact smallest supplier verdict now in force:
  even the fixed-pair-specialized raw-CLM zero target is not yet source-backed,
  so the first missing contract remains the general kernel inclusion

  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m) ≤
    LinearMap.ker
      (section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`,

  equivalently

  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
        section43_fixedTimeShellRawCLM OS lgc hm t ε hε h = 0`.

- fresh bounded theorem-3 actual-shell pass re-ran `(A0)` first and again
  confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded specialization checked beneath
  `section43_fixedTimeShellRawCLM`:
  whether the missing raw kernel inclusion already specializes to the actual
  fixed-pair shell-difference annihilation target

  `section43_fixedTimeShellRawCLM OS lgc hm t ε hε
    ((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) = 0`;
- exact source surfaces checked for that specialization:

  1. quotient-zero for the shell difference via
     `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region` plus
     `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`;

  2. source-support equality via
     `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`;

  3. on-region off-source vanishing via
     `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;

  4. direct raw-CLM unfolding to the fixed-`ε` ambient integral, equivalently
     the factorization through `SchwartzMap.smulLeftCLM` followed by
     `SchwartzMap.integralCLM`;
- exact negative verdict:
  each candidate still removes only region or source-support pieces;
  none supplies any theorem for the live outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  the raw-CLM unfolding itself only repackages the same missing claim as
  vanishing of the multiplied Schwartz test, so no honest compile-clean
  non-wrapper theorem/local lemma currently exists that proves the specialized
  raw shell coefficient annihilates the actual shell difference on the
  complement;
- exact smallest supplier verdict now in force:
  even the fixed-pair-specialized raw-CLM zero target is not yet source-backed,
  so the first missing contract remains the general kernel inclusion

  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m) ≤
    LinearMap.ker
      (section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`,

  equivalently

  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
        section43_fixedTimeShellRawCLM OS lgc hm t ε hε h = 0`.

- fresh bounded fixed-pair actual-shell pass re-ran `(A0)` first and again
  confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded specialization checked beneath
  `section43_fixedTimeShellRawCLM`:
  whether the missing raw kernel inclusion can already be replaced by the
  fixed-pair actual-shell annihilation target

  `section43_fixedTimeShellRawCLM OS lgc hm t ε hε
    ((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) = 0`;
- exact source surfaces checked for that specialization:

  1. quotient-zero for the shell difference via
     `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region` plus
     `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`;

  2. source-support equality via
     `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`;

  3. on-region off-source vanishing via
     `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;
- exact negative verdict:
  each specialization still removes only region or source-support pieces;
  none supplies any theorem for the live outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  hence no honest compile-clean non-wrapper theorem/local lemma currently
  exists that proves the specialized raw shell coefficient annihilates the
  actual shell difference on the complement;
- exact smallest supplier verdict now in force:
  even the fixed-pair-specialized raw-CLM zero target is not yet source-backed,
  so the first missing contract remains the general kernel inclusion

  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m) ≤
    LinearMap.ker
      (section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`,

  equivalently

  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
        section43_fixedTimeShellRawCLM OS lgc hm t ε hε h = 0`.

- fresh bounded true-insertion-surface pass re-ran `(A0)` first and again
  confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- no unsupported residue was found or removed at either insertion point;
- exact supplier check performed one theorem smaller at the true insertion
  surface beneath `section43_fixedTimeShellRawCLM`:
  after unfolding the landed raw CLM, the present branch still only exposes

  `section43_fixedTimeShellRawCLM OS lgc hm t ε hε h =
    ∫ y : NPointDomain d (n + m), coeff y * h y`,

  with fixed shell coefficient

  `coeff y =
    bvt_F OS lgc (n + m)
      (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
        (fun k μ =>
          ↑(y k μ) +
            ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
              Complex.I)
        (t : ℂ))`;
- exact candidate supplier surfaces checked:

  1. the local multiplied-Schwartz theorem

     `∀ h : SchwartzNPoint d (n + m),
        section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
          SchwartzMap.smulLeftCLM ℂ coeff h = 0`;

  2. quotient-zero used only as on-region vanishing, followed by a
     fixed-`ε` region/complement integral split;

  3. the fixed-pair-specialized route through
     `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
     source-side support vanishing for `f.conjTensorProduct g`, and
     `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;
- exact negative verdict:
  (1) is not smaller, because it restates the same kernel inclusion as
  vanishing of the multiplied Schwartz test;
  (2) kills only the region contribution and leaves the complement contribution
  completely uncontrolled;
  (3) still applies only to source-side or on-region pieces and therefore does
  not produce the missing complement annihilation for a general ambient
  quotient-zero test;
- therefore no honest compile-clean non-wrapper theorem/local lemma is
  currently supportable strictly below `section43_fixedTimeShellRawCLM`
  descent on the present branch;
- the smallest supplier verdict remains exactly:

  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m) ≤
    LinearMap.ker
      (section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`,

  equivalently

  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
        section43_fixedTimeShellRawCLM OS lgc hm t ε hε h = 0`.

- fresh bounded definition-unfolding pass re-ran `(A0)` first and again
  confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- no unsupported residue was found or removed at either insertion point;
- exact source check added in this pass:
  unfold the landed raw shell functional
  `section43_fixedTimeShellRawCLM`, and compare it with Mathlib's
  `SchwartzMap.smulLeftCLM` and `SchwartzMap.integralCLM`;
  this shows only the expression-level normal form

  `section43_fixedTimeShellRawCLM OS lgc hm t ε hε h =
    ∫ y : NPointDomain d (n + m), coeff y * h y`,

  equivalently the CLM factorization through
  `SchwartzMap.smulLeftCLM ℂ coeff` followed by
  `SchwartzMap.integralCLM ℂ volume`,
  where `coeff` is the fixed-`ε` shell coefficient
  `y ↦ bvt_F OS lgc (n + m) (xiShift ... y ... (t : ℂ))`;
- exact candidate integral-level reductions checked from that unfolding:

  1. reduce quotient-zero annihilation to
     `SchwartzMap.smulLeftCLM ℂ coeff h = 0`;
  2. use quotient-zero only on-region, i.e.
     `∀ y ∈ section43PositiveEnergyRegion d (n + m), h y = 0`,
     and split the integral into region and complement contributions;
  3. isolate the fixed-`ε` complement product-zero target

     `∀ y, y ∉ section43PositiveEnergyRegion d (n + m) →
        coeff y * h y = 0`;
- exact verdict:
  (1) is not smaller, because it is just the same kernel problem rewritten as
  vanishing of the multiplied Schwartz test;
  (2) removes only the region contribution and leaves the full complement
  integral untouched;
  (3) is the real missing content and already equivalent in force to the raw
  kernel/descent theorem, since the quotient relation carries no complement
  data and the current source has no theorem forcing the fixed-time shell
  coefficient to annihilate all such `h` there;
- therefore the present branch still does not support any honest compile-clean
  non-wrapper theorem/local lemma strictly below
  `section43_fixedTimeShellRawCLM` descent;
- the exact smallest raw-CLM supplier verdict remains:

  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m) ≤
    LinearMap.ker
      (section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`,

  equivalently

  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
        section43_fixedTimeShellRawCLM OS lgc hm t ε hε h = 0`.
- fresh bounded actual-shell pass re-ran `(A0)` first and again confirmed both
  live insertion surfaces in `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- no unsupported residue was found or removed at either insertion point;
- bounded raw-CLM supplier check on the present branch tested whether any
  strictly smaller honest theorem already reduces

  `section43_fixedTimeShellRawCLM OS lgc hm t ε hε h = 0`

  from a source-backed complement-sensitive hypothesis weaker than
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`;
- exact candidate reductions checked:

  1. fixed-pair specialization to
     `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`;
  2. the shell-weighted source-side complement vanishing

     `∀ ε y, y ∉ section43PositiveEnergyRegion d (n + m) →
        bvt_F OS lgc (n + m)
          (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
            (fun k μ =>
              ↑(y k μ) +
                ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                  Complex.I)
            (t : ℂ)) *
          (f.conjTensorProduct g) y = 0`;

  3. the already-landed region-local vanishing theorem
     `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;
- exact negative verdict:
  no compile-clean non-wrapper theorem/local lemma is supportable from those
  inputs on the current branch;
  the fixed-pair specialization is not smaller, because the specialized
  quotient-zero hypothesis is already available and the remaining work is still
  the same ambient complement annihilation;
  the shell-weighted source vanishing is not genuinely complement-sensitive,
  because it only removes the source summand and leaves the ambient factor
  `(φ.conjTensorProduct ψ) y` uncontrolled;
  the region-local vanishing theorem cannot fire on the live complement branch
  because it assumes prior region membership;
- so every honest reduction still collapses back to the same first kernel
  contract for the landed raw CLM:

  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m) ≤
    LinearMap.ker
      (section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`,

  equivalently

  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
        section43_fixedTimeShellRawCLM OS lgc hm t ε hε h = 0`.

- bounded supervision-pass recheck on 2026-04-14 re-ran `(A0)` first and
  confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `differentiableAt_bvt_F_restrictScalars` and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- so the previously active shell-local real differentiability / chain-rule
  seam is closed on the current branch and is not the live blocker any more;
- fresh supervision at 21:38 UTC also verified that the raw fixed-`ε` ambient
  shell functional itself is now actually landed in production as the private
  non-wrapper definition `section43_fixedTimeShellRawCLM` in
  `OSToWightmanPositivity.lean`; a fresh exact file check
  `lake env lean OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`
  exited `0` with warnings only;
- re-reading the actual `hlimit_os` body on the same branch therefore finds no
  smaller honest non-wrapper theorem/local lemma beneath that body beyond the
  quotient-descent/kernel theorem for this newly landed CLM;
- the exact next-smaller theorem surface is now the ambient
  complement-localization contract already displayed below, read one notch more
  concretely: either quotient descent of `section43_fixedTimeShellRawCLM`, or
  equivalently the statement that the shell-difference integral tends to `0` as
  `ε → 0+`.

- in the live local block `hlimit_os` inside
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`
  (`OSToWightmanPositivity.lean:8445` on the present branch), the first lower
  unsupported statement beneath the desired shell-limit comparison is not a new
  theorem about the complex canonical shell point itself;
- the proof first fails on the ambient complement branch for the real
  integration variable `y`, namely outside
  `section43PositiveEnergyRegion d (n + m)`;
- the sharp formulation of that first lower failing statement is ambient
  outside-region representative comparison

  `∀ y, y ∉ section43PositiveEnergyRegion d (n + m) →
      (φ.conjTensorProduct ψ) y = (f.conjTensorProduct g) y`;

- the nearby ambient-vanishing phrasing is not genuinely different on the
  current route, because the source tensor already vanishes there by
  `orderedPositiveTimeRegion_subset_section43PositiveEnergyRegion` plus
  `hf_ord`, `hg_ord`;
- a shell-local representative-choice theorem on the actual fixed-time
  canonical shell is likewise not genuinely smaller: the shell appears only in
  the `bvt_F` factor, while the unresolved branch is the ambient weight
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y`, so any shell-local
  theorem strong enough to help still collapses to the same ambient
  outside-region representative control.
- exact current theorem skeleton for the first honest lower step on this seam:
- exact first missing theorem contract on the current branch:

  `Filter.Tendsto
    (fun ε : ℝ =>
      ∫ y : NPointDomain d (n + m),
        bvt_F OS lgc (n + m)
          (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
            (fun k μ =>
              ↑(y k μ) +
                ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                  Complex.I)
            (t : ℂ)) *
          (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0)`;

  equivalently, the canonical fixed-time shell has the same limit against the
  ambient tensor and the explicit source tensor;
- no smaller already-landed theorem removes the outside-region branch, because
  the current Section-4.3 quotient transport only controls representatives on
  `section43PositiveEnergyRegion d (n + m)`.
- exact weaker statement tested source-first on the present branch:

  `∀ y, y ∉ section43PositiveEnergyRegion d (n + m) →
      (f.conjTensorProduct g) y = 0`.

  This is genuinely provable from existing source support geometry:
  `conjTensorProduct_tsupport_subset_fixedPairMixedOrderTimeRegion` gives
  support inside the mixed-order region, the first component of
  `FixedPairMixedOrderTimeRegion` forces all time coordinates nonnegative, and
  `image_eq_zero_of_notMem_tsupport` then yields pointwise vanishing off the
  Section-4.3 region.
  On the current branch, the nearest usable production consumer is already
  present as
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  so there is no smaller new Lean theorem to land here without merely
  duplicating existing surface.
  But it still fails to start the live complement branch in Lean, because
  `hlimit_os` is weighted by
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y`; on
  `y ∉ section43PositiveEnergyRegion d (n + m)` this weaker theorem only
  removes the source term and gives no control at all on the ambient factor
  `(φ.conjTensorProduct ψ) y`.
- no strictly smaller honest actual-shell localization theorem is available on
  the present source:
  any theorem strong enough to help must kill the branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`, but on the live shell that
  branch is seen only through the ambient factor
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y`, so every candidate
  collapses back to the same complement-localization contract above.
- bounded actual-shell recheck one notch lower confirms the same verdict for
  raw-CLM annihilation: there is still no compile-clean non-wrapper theorem on
  present source proving
  `section43_fixedTimeShellRawCLM OS lgc hm t ε hε h = 0`
  from any genuinely complement-sensitive source-backed hypothesis weaker than
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`;
- the only source-backed weaker fact near that seam remains

  `∀ y, y ∉ section43PositiveEnergyRegion d (n + m) →
      (f.conjTensorProduct g) y = 0`,

  which removes only the source term on the live complement branch and leaves
  the ambient factor `(φ.conjTensorProduct ψ) y` uncontrolled;
- hence this branch still does not yield even a smaller fixed-`ε`
  localization theorem below the raw kernel/descent statement itself.
- bounded actual-shell micro-pass after that audit tested the only immediate
  shell-strengthening that source support supplies:

  `∀ ε y, y ∉ section43PositiveEnergyRegion d (n + m) →
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
        (f.conjTensorProduct g) y = 0`;

  equivalently, for fixed `ε` the source-side shell integral already trims away
  the complement contribution;
- this is source-backed but not an honest new production target: it is only the
  previously-recorded source complement vanishing multiplied by the live shell
  factor, so it adds no new complement-sensitive mathematics;
- and it still fails at the same place in `hlimit_os`, because the unresolved
  live integrand there is

  `bvt_F ... (xiShift ...) * (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y)`;

  on the complement, the shell-weighted source-zero theorem removes only the
  source summand and leaves
  `bvt_F ... (xiShift ...) * (φ.conjTensorProduct ψ) y`
  completely uncontrolled.
- first concrete source obstacle at the live proof body is now explicit:
  immediately after `have hweightEqOnSource := ...` in `hlimit_os`,
  the proof has no theorem that applies on
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  `hweightEqOnSource` needs source support,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`
  needs region membership,
  and the shell/slice rewrites `hshellToSlice` and `hsliceTransport` do not
  eliminate the ambient factor
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y`;
  so the shell-limit theorem is blocked first by absence of any complement
  control theorem at that exact branch point, not by a later scalar/Fourier
  identity.
- exact earliest reusable API boundary where new mathematics would have to
  enter is `Section43Codomain.lean`:
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region` and
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` stop at region
  equality on `section43PositiveEnergyRegion d (n + m)`;
- source-first, this boundary is forced by the codomain definition itself:
  `section43PositiveEnergyVanishingSubmodule` quotients by ambient Schwartz
  functions that vanish on that region, so current quotient equality carries no
  complement data at all;
- therefore the alternative insertion point "strengthen the codomain API first"
  fails on the present branch unless one first proves genuinely new
  complement-sensitive mathematics, not just another transport lemma;
- bounded refinement after Entry #504:
  the cleanest equivalent theorem surface for that missing complement
  mathematics is quotient descent of the shell-limit functional itself.
  Concretely, for fixed `t > 0` one would need:

  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
        Filter.Tendsto
          (fun ε : ℝ =>
            ∫ y : NPointDomain d (n + m),
              bvt_F OS lgc (n + m)
                (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                  (fun k μ =>
                    ↑(y k μ) +
                      ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                        Complex.I)
                  (t : ℂ)) *
                h y)
          (nhdsWithin 0 (Set.Ioi 0))
          (nhds 0)`;
- for the live proof, apply this to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`;
  the required zero-class hypothesis is already source-backed by
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
  so this theorem is equivalent to the current shell-difference limit
  contract rather than a route change;
- bounded fixed-`ε` reformulation, now settled source-first:

  `theorem section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero
      (t ε : ℝ) (ht : 0 < t) (hε : 0 < ε) :
      ∀ h : SchwartzNPoint d (n + m),
        section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
          ∫ y : NPointDomain d (n + m),
            bvt_F OS lgc (n + m)
              (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                (fun k μ =>
                  ↑(y k μ) +
                    ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                      Complex.I)
                (t : ℂ)) *
              h y = 0`;

  this is not genuinely smaller than the limit theorem above:
  both formulations need the same missing kernel/complement-localization
  theorem for the multivariate fixed-time shell functional;
- exact first missing proof ingredient at the true insertion surface:
  not another shell-local identity, but a theorem that the ambient fixed-`ε`
  shell functional descends through
  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m)`;
  equivalently, its kernel must contain every Schwartz test vanishing on
  `section43PositiveEnergyRegion d (n + m)`;
- bounded live-branch recheck now sharpens this from a theorem sketch to an
  exact negative source verdict:
  the branch already contains the raw ambient CLM
  `section43_fixedTimeShellRawCLM OS lgc hm t ε hε`, but no compile-clean
  theorem currently proves the kernel inclusion

  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m) ≤
    LinearMap.ker
      (section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`;

  hence there is still no honest `liftQ` descent available for that CLM on the
  present source;
- this is not a packaging omission.
  The one-variable `liftQ` consumers in `Section43Codomain.lean` descend only
  after kernel containment is proved, and the live multivariate fixed-time
  shell route still lacks exactly that complement-sensitive annihilation input;
- bounded fixed-pair-specialized verdict after re-checking the live body:
  specializing the general-ambient theorem to the actual live difference

  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`

  does not produce a smaller honest theorem surface.
  The strongest honest fixed-pair-specialized shell-localization contract is

  `theorem section43_fixedTime_shellIntegral_fixedPairDifference_eq_zero
      (t ε : ℝ) (ht : 0 < t) (hε : 0 < ε) :
      ∫ y : NPointDomain d (n + m),
        bvt_F OS lgc (n + m)
          (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
            (fun k μ =>
              ↑(y k μ) +
                ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                  Complex.I)
            (t : ℂ)) *
          (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y) = 0`;

  equivalently, the same shell-difference integral tends to `0` as
  `ε → 0+`.
  Its quotient-zero premise is already discharged by
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
- fresh bounded actual-shell supervision after that verdict re-ran `(A0)` and
  confirmed the same exact live file state remains in force:
  the insertion point after `contDiff_canonicalXiShift` is still clean with
  only `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift`, and the insertion point after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth` is still clean with
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift`;
- no unsupported residue was found or removed on that recheck;
- source-first reinspection still finds no smaller already-supportable
  complement theorem below the fixed-`ε` kernel surface.
  Neither a general-ambient annihilation theorem nor the fixed-pair
  specialization already lands compile-clean on the present branch, because the
  current source has no theorem controlling the ambient factor on
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- so the exact first missing contract remains unchanged and should now be read
  in raw-CLM form:

  `∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
        section43_fixedTimeShellRawCLM OS lgc hm t ε hε h = 0`;

  equivalently, kernel containment for
  `(section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`;
- therefore this bounded pass makes no further production Lean edit on the
  theorem-3 route and only records the exact still-missing complement-sensitive
  kernel/localization contract beneath the already-landed raw shell packaging.
  but the proof still needs exactly the same absent complement control as the
  general-ambient theorem, because current source only gives:

  `∀ y, y ∈ section43PositiveEnergyRegion d (n + m) →
      ((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y = 0`

  and source-support-local refinements of that statement;
  none of these touches the live complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)` in `hlimit_os`;
- source-first, the attempted move fails immediately because every existing
  descended theorem in the fixed-time chain still lives at the 1D quotient
  level after a frozen-slice reduction, whereas the live blocker is the
  ambient `(n + m)`-variable shell integral before any such quotient descent
  has been proved;
- exact current proof-body stopping point remains the same after the raw-CLM
  landing:
  in `hlimit_os`, the source-support equality theorem and the region-local
  vanishing theorem already discharge the first two branches of the ambient
  decomposition, but there is still no theorem for
  `y ∉ section43PositiveEnergyRegion d (n + m)` that annihilates
  `bvt_F ... (xiShift ...) * (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y)`;
- so the strict first missing kernel/localization contract beneath the landed
  raw CLM is unchanged and exact:
  prove that the CLM kills quotient-zero ambient tests, equivalently prove the
  same-shell difference integral is `0` for every quotient-zero test, before
  any further `liftQ` or limit consumer is attempted.
- more precisely, the current source still has no ambient-shell continuous
  linear functional whose kernel has been shown to contain
  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m)`;
- bounded source-first functional-packaging recheck sharpens this one step:
  the generic machinery to build such a raw `ContinuousLinearMap` already
  exists, via `SchwartzMap.smulLeftCLM` and `SchwartzMap.integralCLM`, exactly
  as used locally for `fourierInvPairingCLM` and
  `horizontalPhasePairingCLM` in `OSToWightmanPositivity.lean`;
  however, the current branch still has no theorem with the fixed-time shell
  coefficient itself proved to have `Function.HasTemperateGrowth`, and no
  named definition bundling the resulting multivariate fixed-`ε` shell
  integral on `SchwartzNPoint d (n + m)`;
  so even the raw packaged-functional step is not already landed as a concrete
  source object, let alone descended through the Section-4.3 quotient;
- exact verdict from the present bounded pass:
  the first missing sub-contract below
  `section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero`
  is still the raw shell-coefficient packaging theorem

  `Function.HasTemperateGrowth
    (fun y : NPointDomain d (n + m) =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))`;

  without that theorem there is still no honest named ambient fixed-`ε` shell
  `ContinuousLinearMap` whose kernel could then be compared with
  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m)`;
- this is why the compile-clean theorem skeleton
  `section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero` is not yet an
  honest Lean item to land on the current branch:
  the statement shape is clear, but the first missing proof ingredient is still
  the raw ambient-shell kernel theorem above, and that missing ingredient sits
  exactly at the live insertion boundary inside `hlimit_os`.
- re-reading `Section43Codomain.lean` makes the missing insertion point
  explicit: the current `liftQ` descendants are only the multivariate boundary
  evaluation map `evalAtZeroDescendsToSection43PositiveEnergyComponent` and
  the 1D half-line descended functionals
  `fourierPairingDescendsToSection43PositiveEnergy1D` /
  `evalAtZeroDescendsToSection43PositiveEnergy1D`;
  none of them packages the multivariate fixed-time shell integral or its
  `ε → 0+` limit;
  without that, the displayed quotient-descent theorem is itself the first
  missing descent theorem rather than a consequence of existing descended APIs;
- the already-proved
  `section43_iteratedSlice_descendedPairing` sits strictly downstream of this:
  it only compares frozen one-variable slice extensions after the proof has
  already reduced to the 1D quotient and positive-time representatives, so it
  cannot start the ambient shell descent needed in `hlimit_os`;
- bounded specialization check continuing from Entry #656 now fixes the exact
  first obstacle to even the raw packaging substep:
  `bvt_F_hasGlobalForwardTubeGrowth` only supplies a zeroth-order polynomial
  bound on

  `bvt_F OS lgc (n + m) : (Fin (n + m) → Fin (d + 1) → ℂ) → ℂ`

  over `ForwardTube d (n + m)`;
- the fixed-time shell coefficient

  `y ↦ bvt_F OS lgc (n + m)
      (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
        (fun k μ =>
          ↑(y k μ) + ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
            Complex.I)
        (t : ℂ))`

  does land in that tube by `canonicalXiShift_mem_forwardTube`, but
  `Function.HasTemperateGrowth` in Mathlib additionally requires
  `ContDiff ℝ ∞` and polynomial bounds for every iterated derivative;
- current source has no theorem upgrading the existing tube-side data
  `DifferentiableOn ℂ ... (ForwardTube d (n + m))` plus the value-growth bound
  to derivative-growth bounds on that affine shell map;
- source-first recheck against local Mathlib confirms the gap is still exactly
  there:
  `Mathlib/Analysis/Distribution/TemperateGrowth.lean` provides
  `Function.HasTemperateGrowth` and composition machinery such as
  `Function.HasTemperateGrowth.comp'`, but those APIs require the derivative
  bounds to already be available;
  `Mathlib/Analysis/Complex/Liouville.lean` and
  `Mathlib/Analysis/Complex/CauchyIntegral.lean` provide one-variable disc
  Cauchy estimates and power-series control, while
  `Mathlib/Analysis/Calculus/ContDiff/Defs.lean` gives the analytic-to-`ContDiff`
  bridge;
  however, neither the repo nor Mathlib currently contains a theorem that
  turns holomorphy of `bvt_F` on the multivariate forward tube together with
  the zeroth-order polynomial bound from `bvt_F_hasGlobalForwardTubeGrowth`
  into polynomial bounds for every iterated real derivative of the restricted
  affine shell coefficient;
- so the branch still has no named theorem asserting this shell coefficient is
  `Function.HasTemperateGrowth`, and for that reason no concrete raw packaged
  multivariate shell `ContinuousLinearMap` exists yet by source name;
- exact Piece-(1) verdict from the bounded 2026-04-14 source-check:
  not landable compile-clean on the current branch.
  The existing source is sufficient to identify the raw shell coefficient,
  prove it lands in `ForwardTube d (n + m)`, prove outer holomorphy of `bvt_F`,
  and prove the zeroth-order shell bound
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
  it is not sufficient to instantiate `SchwartzMap.smulLeftCLM` for that
  coefficient, because the required theorem
  `shellCoeff.HasTemperateGrowth` is still absent by exact source name;
- exact API/dependency checklist for Piece (1):
  1. coefficient formula:

     `y ↦ bvt_F OS lgc (n + m)
         (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
           (fun k μ =>
             ↑(y k μ) +
               ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                 Complex.I)
           (t : ℂ))`;
  2. forward-tube membership:
     `canonicalXiShift_mem_forwardTube`;
  3. outer holomorphy:
     `bvt_F_holomorphic` / the corresponding `DifferentiableOn` theorem;
  4. value-growth input:
     `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
  5. missing prerequisite:
     a theorem turning items 2-4 into
     `Function.HasTemperateGrowth` for the coefficient in item 1;
  6. packaging step once item 5 exists:
     define the raw functional via
     `(SchwartzMap.integralCLM ℂ volume).comp
       (SchwartzMap.smulLeftCLM ℂ shellCoeff)`;
- exact minimal theorem contract for that missing upgrade is:

  ```lean
  theorem section43_fixedTime_shellCoefficient_hasTemperateGrowth
      (t ε : ℝ) (ht : 0 < t) (hε : 0 < ε) :
      Function.HasTemperateGrowth
        (fun y : NPointDomain d (n + m) =>
          bvt_F OS lgc (n + m)
            (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
              (fun k μ =>
                ↑(y k μ) +
                  ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                    Complex.I)
              (t : ℂ)))
  ```

  This is the smallest honest theorem surface that would let the raw fixed-`ε`
  shell integral compile-clean as a `ContinuousLinearMap`.
- exact first missing derivative-growth ingredient beneath that theorem is the
  positive-order all-orders estimate:

  ```lean
  ∀ r : ℕ, 1 ≤ r → ∃ k : ℕ, ∃ C : ℝ, 0 ≤ C ∧
    ∀ y : NPointDomain d (n + m),
      ‖iteratedFDeriv ℝ r
          (fun y : NPointDomain d (n + m) =>
            bvt_F OS lgc (n + m)
              (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                (fun k μ =>
                  ↑(y k μ) +
                    ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                      Complex.I)
                (t : ℂ))) y‖
        ≤ C * (1 + ‖y‖) ^ k
  ```

  In particular, the `r = 0` case is already covered by
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`; the first absent ingredient is
  the `r ≥ 1` derivative-growth half alone.
- hence the first honest obstacle between
  `bvt_F_hasGlobalForwardTubeGrowth` and the raw CLM packaging step is
  precisely the absent derivative-growth upgrade, not the later quotient
  kernel theorem.
- bounded quotient-descent test after Entry #504:
  the first attempted move was to imitate the existing `liftQ` constructions
  directly at the fixed-time shell by freezing `t > 0` and a fixed `ε > 0` and
  treating

  `h ↦ ∫ y : NPointDomain d (n + m),
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
        h y`

  as the raw functional to descend through
  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m)`;
- this fails at the first kernel obligation:
  current source has no theorem that every `h` with
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0` is annihilated by
  that fixed-`ε` shell integral;
- in contrast to the 1D descended Fourier/evaluation maps, there is no theorem
  reducing this shell functional to data only on
  `section43PositiveEnergyRegion d (n + m)`, and no theorem that its shell
  coefficient kills the complement branch;
- bounded one-layer-below pass continuing from Entry #505:
  source-first, the strongest current theorem strictly below this missing
  quotient-kernel statement is still only region-local pointwise transport for
  the fixed-pair difference
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`;
- concretely,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_section43Region`
  gives

  `∀ y, y ∈ section43PositiveEnergyRegion d (n + m) →
      ((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y = 0`,

  so for each fixed `ε` the live shell integrand is pointwise zero on the
  region branch:

  `∀ y, y ∈ section43PositiveEnergyRegion d (n + m) →
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y) = 0`;
- this is the exact minimal stronger hypothesis current source supports at the
  ambient shell seam: fixed-pair transport data plus explicit region
  membership of `y`;
- there is still no honest shell-functional descent/vanishing theorem on the
  present branch under any stronger explicit hypothesis that is weaker than
  full ambient representative equality:
  source-first, every candidate below the quotient-kernel theorem still
  reduces to this same pointwise region-local branch statement and therefore
  still carries the explicit hypothesis
  `y ∈ section43PositiveEnergyRegion d (n + m)`;
- it is weaker than global ambient representative equality, but it still does
  not yield shell-functional descent or any ambient shell integral reduction,
  because the integral in `hlimit_os` still sees the uncontrolled complement
  branch `y ∉ section43PositiveEnergyRegion d (n + m)`;
- that is the exact first missing proof ingredient now:
  a theorem that the fixed-time shell-limit functional annihilates
  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m)`.
- exact first still-missing theorem contract after this pass remains the
  kernel-inclusion / quotient-descent statement, equivalently its fixed-`ε`
  form:

  `theorem section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero
      (t ε : ℝ) (ht : 0 < t) (hε : 0 < ε) :
      ∀ h : SchwartzNPoint d (n + m),
        section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
          ∫ y : NPointDomain d (n + m),
            bvt_F OS lgc (n + m)
              (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                (fun k μ =>
                  ↑(y k μ) +
                    ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                      Complex.I)
                (t : ℂ)) *
              h y = 0`;
- but source-first this remains blocked one packaging layer earlier in Lean,
  because Piece (1) still has no shell-coefficient `Function.HasTemperateGrowth`
  theorem and therefore no raw multivariate shell `ContinuousLinearMap` yet.
- exact clean theorem contract to record for the live branch:

  `theorem section43_fixedTime_shellLimit_tendsto_zero_of_quotient_eq_zero
      (t : ℝ) (ht : 0 < t) :
      ∀ h : SchwartzNPoint d (n + m),
        section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
          Filter.Tendsto
            (fun ε : ℝ =>
              ∫ y : NPointDomain d (n + m),
                bvt_F OS lgc (n + m)
                  (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                    (fun k μ =>
                      ↑(y k μ) +
                        ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                          Complex.I)
                    (t : ℂ)) *
                  h y)
            (nhdsWithin 0 (Set.Ioi 0))
            (nhds 0)`;
- this is the exact quotient-kernel/descent theorem needed under
  `hlimit_os`: with
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`, the zero-class
  hypothesis is already supplied by
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`;
- exact already-landed dependency boundary on the present branch:
  `section43FixedPair` region transport stops at
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_section43Region` and
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;
  codomain descent stops at
  `evalAtZeroDescendsToSection43PositiveEnergyComponent`,
  `fourierPairingDescendsToSection43PositiveEnergy1D`, and
  `evalAtZeroDescendsToSection43PositiveEnergy1D`;
  downstream slice comparison starts only at
  `section43_iteratedSlice_descendedPairing`;
- exact first genuinely new mathematical ingredient still absent:
  kernel containment for the multivariate fixed-time shell functional,
  equivalently an ambient complement-trimming theorem for general
  `h : SchwartzNPoint d (n + m)` saying the shell integral ignores
  `((section43PositiveEnergyRegion d (n + m))ᶜ)`;
- continuing the 2026-04-14 quotient-kernel pass after Entries #509, #511,
  and #512, the earliest honest fixed-`ε` theorem surface on a general
  ambient test function is the per-shell kernel statement

  `theorem section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero
      (t ε : ℝ) (ht : 0 < t) (hε : 0 < ε) :
      ∀ h : SchwartzNPoint d (n + m),
        section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
          ∫ y : NPointDomain d (n + m),
            bvt_F OS lgc (n + m)
              (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                (fun k μ =>
                  ↑(y k μ) +
                    ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                      Complex.I)
                (t : ℂ)) *
              h y = 0`;
- this fixed-`ε` theorem is not a genuinely smaller route-level target than
  the limit theorem
  `section43_fixedTime_shellLimit_tendsto_zero_of_quotient_eq_zero`;
  source-first, both formulations demand the same absent complement-
  localization theorem for the ambient shell functional on arbitrary `h`, and
  the repo still has no non-wrapper theorem trimming that complement even
  under an explicit complement-vanishing hypothesis on general `h`;
- bounded source recheck after Entry #511 found no non-wrapper theorem in the
  repo already proving that stronger ambient trimming statement under an
  explicit complement-vanishing hypothesis on general `h`; current source only
  proves fixed-pair, region-gated pointwise vanishing, not shell-functional
  descent.
- bounded 2026-04-14 explicit-complement-vanishing source pass sharpened that
  verdict further:
  even after adding the stronger input hypothesis

  `∀ y : NPointDomain d (n + m),
      y ∉ section43PositiveEnergyRegion d (n + m) →
        h y = 0`

  on a general ambient
  `h : SchwartzNPoint d (n + m)`,
  there is still no already-landed non-wrapper theorem or local lemma showing
  that the multivariate fixed-time shell functional vanishes on such `h`;
  the current source only knows fixed-pair pointwise complement facts, and any
  shell-weighted restatement of those would still be wrapper-only because it
  adds no new kernel/complement-localization mathematics;
- therefore this explicit-complement-vanishing variant is not a genuine
  intermediate theorem surface between
  `section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero`
  and the live `hlimit_os` obligation;
  it remains blocked by the same missing shell-functional kernel theorem.
- bounded continuation pass after Entry #511 re-confirmed the comparison
  exactly at the intended insertion point:
  the fixed-`ε` kernel statement
  `section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero` is the
  earliest honest per-shell formulation on a general ambient `h`, but it is
  not route-smaller than the limit theorem
  `section43_fixedTime_shellLimit_tendsto_zero_of_quotient_eq_zero`;
  both formulations still require the same missing complement-localization
  theorem for the multivariate fixed-time shell functional, namely that the
  shell integral depends only on the quotient class
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h`;
- equivalently, the first missing mathematics can be recorded in either of two
  honest contracts, neither smaller than the other on present source:

  `theorem section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero
      (t ε : ℝ) (ht : 0 < t) (hε : 0 < ε) :
      ∀ h : SchwartzNPoint d (n + m),
        section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
          ∫ y : NPointDomain d (n + m),
            bvt_F OS lgc (n + m)
              (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                (fun k μ =>
                  ↑(y k μ) +
                    ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                      Complex.I)
                (t : ℂ)) *
              h y = 0`

  or

  `theorem section43_fixedTime_shellLimit_tendsto_zero_of_quotient_eq_zero
      (t : ℝ) (ht : 0 < t) :
      ∀ h : SchwartzNPoint d (n + m),
        section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
          Filter.Tendsto
            (fun ε : ℝ =>
              ∫ y : NPointDomain d (n + m),
                bvt_F OS lgc (n + m)
                  (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                    (fun k μ =>
                      ↑(y k μ) +
                        ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                          Complex.I)
                    (t : ℂ)) *
                  h y)
            (nhdsWithin 0 (Set.Ioi 0))
            (nhds 0)`;
- exact obligation graph between these two theorem surfaces on current source:
  the fixed-`ε` theorem implies the limit theorem directly, with no further
  shell-localization result;
  indeed, after fixing `h` with
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0`, the fixed-`ε`
  theorem yields
  `∀ ε > 0, ∫ y, bvt_F ... * h y = 0`,
  so the shell-integral function is pointwise equal to `0` on `Set.Ioi 0`,
  and the limit statement follows immediately from equality on
  `nhdsWithin 0 (Set.Ioi 0)` plus `tendsto_const_nhds`;
- thus no additional theorem beyond the fixed-`ε` kernel/descent contract is
  needed to derive
  `section43_fixedTime_shellLimit_tendsto_zero_of_quotient_eq_zero`;
- conversely, the limit theorem does not imply the fixed-`ε` equality for each
  positive `ε`, so the two statements are not logically equivalent;
- the honest current-frontier verdict is therefore:
  fixed-`ε` kernel form is logically stronger, but not mathematically smaller
  on this branch, because the missing proof of the fixed-`ε` theorem is
  already exactly the missing complement-localization/kernel theorem for the
  multivariate fixed-time shell functional.
- bounded source-first continuation after the post-Entry-#511 tightening
  rechecked whether that fixed-`ε` kernel statement already exists in the repo
  in non-wrapper form; source inspection of
  `OSToWightmanPositivity.lean`, `Section43Codomain.lean`, and the nearby
  descended-functional files found no such theorem or local lemma;
- exact current classification is therefore stable:
  the fixed-`ε` shell-kernel theorem
  `section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero` is the
  earliest honest per-shell formulation on a general ambient `h`, and it is
  logically stronger than
  `section43_fixedTime_shellLimit_tendsto_zero_of_quotient_eq_zero`;
  but it is not route-smaller, because proving it still needs exactly the same
  missing shell-functional complement-localization input as the limit theorem,
  namely that the ambient fixed-time shell integral depends only on the
  quotient class
  `section43PositiveEnergyQuotientMap (d := d) (n + m) h`;
- no explicit-complement-vanishing, fixed-pair-specialized, or shell-weighted
  restatement currently lowers that mathematical burden on this branch.
- bounded insertion-point verdict continuing from Entry #500:
  the next honest theorem-sized move therefore belongs directly in
  `OSToWightmanPositivity.lean` at the live fixed-time shell seam, as either
  the displayed shell-difference limit theorem or an equivalent actual-shell
  complement-localization statement removing the outside-region contribution;
  therefore the missing step cannot be obtained by further transport
  bookkeeping alone below `hlimit_os`.

## 1. Executive Summary

OS II corrects exactly one part of OS I:
- not the semigroup construction,
- not the one-variable continuation,
- not the positivity mechanism,
- but the unjustified leap from separate one-variable continuation to a full
  many-variable Fourier-Laplace statement.

The corrected theorem shape is:

1. Build one-variable continuation objects from the OS semigroup.
2. Use Euclidean covariance plus local geometry to obtain local analyticity in
   all time variables.
3. Enlarge the domain inductively via the `(A_N)/(P_N)` ladder.
4. Only afterwards prove polynomial growth / temperedness estimates using the
   linear growth condition `E0'`.
5. Finally take tempered boundary values to recover Wightman distributions.

The most important discipline consequence is:
- Chapter V is continuation.
- Chapter VI is growth control.
- these must not be blurred.

## 2. Chapter I: What Was Wrong and What Is Being Fixed

OS II states plainly that OS I Lemma 8.8 is wrong. The counterexample
`F(x,y)=exp(-xy)` shows that being a Laplace transform in each variable
separately does not imply being a joint Laplace transform.

This has three direct consequences for formalization:

1. Separate one-variable holomorphicity is not enough.
2. The corrected proof must control the analytic domain explicitly.
3. Many-variable continuation must be built, not guessed.

The paper then offers two repaired Euclidean hypotheses:
- `Ě0`, a strong distribution condition restoring a quick equivalence theorem;
- `E0'`, the linear growth condition, which is closer to applications and is the
  real target of the paper.

## 3. Chapter II: New Test-Function Norms and Distribution Surfaces

OS II introduces the `|.|_p` and `|.|_{p,O}` norms and the space `S(O)` for open
sets `O`, together with a sharpened Whitney-extension statement.

The role of this chapter is not decorative. It feeds directly into:
- the strong condition `Ě0` in Chapter III,
- the comparison between distribution control on `R_+^{4n}` and tempered
  estimates after continuation,
- the constant-tracking needed in Chapter VI.

Two hidden facts matter:

1. Lemma 2.2 is a quantitative extension theorem with constants controlled by
   `gamma(n,p) = [c^2 (p+1)]^{2p+1}`.
2. Lemma 2.3 converts support-control plus `|.|_p` bounds into a distribution on
   the positive cone with explicit constant inflation.

These are part of why OS II can transport estimates carefully rather than only
qualitatively.

## 4. Chapter III: The Strong Equivalence Theorem

The strong condition `Ě0` says, morally, that the difference-variable Schwinger
functions already live on the Fourier-Laplace-friendly test-function side.

The proof structure is simple:
- under `Ě0`, the Fourier-Laplace transform to supported Wightman data is
  immediate;
- the reverse direction from Wightman data to `Ě0` is also straightforward.

This chapter is not the active route for the current Lean frontier, but it
clarifies a conceptual point:
- if one makes the Euclidean input strong enough, the hard continuation problem
  disappears;
- OS II deliberately avoids doing that in the main theorem because it is too
  strong for applications.

So when the Lean development uses a stronger theorem surface than OS II needs,
that should be understood as a convenience detour, not the paper route.

## 5. Chapter IV: The Real Main Theorem

`E0'` says the Euclidean Green's functions satisfy a linear growth estimate

`|S_n(f)| <= sigma_n |f|_{n s}`

with `sigma_n` of factorial growth.

The chapter then states the roadmap:
- Theorem 4.1 `(A_0)`: real analyticity.
- Theorem 4.2 `(A_r)` and `(TE_r)`: inductive continuation and matching bounds
  on larger domains.
- Theorem 4.3: full continuation on the product right half-plane with
  polynomial bounds.

The important strategic point is already visible here:
- continuation and bounds are proved together only at the statement level;
- in the proof, continuation is conceptually Chapter V and bounds are Chapter
  VI.

The end of Chapter IV also explains the extraction of Wightman distributions:
- Theorem 4.3 gives a holomorphic function on `C_+^k` with polynomial growth.
- Standard several-complex-variable Fourier-Laplace theory produces a tempered
  distribution supported in the forward cone.

So the real frontier is never "produce some holomorphic function." It is
"produce the specific OS II holomorphic function with the correct domain and
growth."

## 6. Chapter V Overview: Continuation Without `E0'`

OS II begins Chapter V by stressing that `E0'` is not used here. The inputs are
only:
- temperedness `E0`,
- Euclidean covariance `E1`,
- reflection positivity `E2`.

This means:
- the semigroup continuation mechanism is independent of the later growth
  estimates;
- any proof surface that imports `E0'` into the continuation theorem itself is
  not following the OS II chapter split.

This is especially important for the current repo because some earlier attempts
blurred "analytic continuation" with "boundary-value temperedness" or "growth
    package" statements. OS II does not.

## 7. Chapter V.1: Real Analyticity

### 7.1. Inputs

The paper starts from the Hilbert-space-valued distributions `Ψ_n(x,ξ)` and the
semigroup identity

`e^{-tH} Ψ_n(x,ξ) = Ψ_n(x+t, ξ)`.

Then, for a chosen time variable, a scalar product of `Ψ`-vectors gives a
one-variable analytic continuation, exactly as in OS I.

### 7.1.1. What is fixed and what varies

The continuation theorem in Chapter V.1 is frequently misunderstood because the
paper suppresses the spatial variables after introducing them.

What is fixed:
- the spatial variables `ξ`,
- the real positive-time basepoint `ξ^0`,
- the cone angle `γ`,
- the dual basis vectors `e_μ`.

What varies:
- the nonnegative coefficients `u_i^μ` in the chosen dual directions.

So the theorem is not "joint analyticity in all variables from nothing". It is:
- fix a real positive-time configuration,
- move it in carefully chosen complex directions,
- and prove local analyticity around that fixed real point.

### 7.2. Cone geometry

For a fixed real positive-time configuration `ξ`, choose:
- a cone `R_+^k(γ)` around the positive real axis in the time variables,
- in the 4-dimensional paper, four linearly independent vectors
  `e_1,...,e_4` in the dual cone;
- for the repo's general-`d` implementation, read this as a choice of
  `d + 1` linearly independent dual-cone vectors.

The key use of these directions is:
- for each direction separately, one can run the one-variable semigroup
  continuation;
- these directional continuations are compatible on overlaps because they come
  from the same distributional object.

### 7.2.1. Why the dual cone is used

The vectors `e_μ` are chosen in the dual cone of `R_+^k(γ)` so that:
- adding `u^μ e_μ` with `u^μ >= 0` keeps the imaginary part inside the
  admissible forward region,
- after rotation by the corresponding `A_μ`, the distinguished direction
  becomes the time axis.

This is what makes the OS I one-variable semigroup theorem applicable in each
chosen direction.

So the geometry is serving a very specific analytic purpose:
- it identifies finitely many directions in which the old one-gap theorem is
  valid,
- and it does so uniformly near the chosen real point.

### 7.3. Malgrange-Zerner

This is the first decisive several-complex-variable ingredient.

The paper introduces logarithmic coordinates `r_i^μ = s_i^μ + i t_i^μ` and uses
the directional continuations to build analytic functions on several flat tube
domains. Malgrange-Zerner then says these glue to an analytic function on the
convex envelope.

This proves local analyticity in all time variables around the chosen real
positive-time point.

### 7.3.1. Detailed proof transcript of the glue step

The glue step can be unpacked as:

1. For each pair `(i, μ)`, hold all other variables fixed and continue in the
   one variable `u_i^μ` by the OS I semigroup theorem.
2. Pass to logarithmic coordinates `s_i^μ = log u_i^μ`.
3. In those coordinates, each one-variable continuation becomes an analytic
   function on a flat tube `|Im r_i^μ| < π/2`.
4. All these partially continued functions agree on the common real slice
   because they come from the same original distribution.
5. Malgrange-Zerner then provides a single analytic continuation on the convex
   envelope of the union of these flat tubes.
6. Returning to the original variables gives analyticity in all chosen
   direction variables simultaneously.

So the role of Malgrange-Zerner is very concrete:
- it replaces the false "separately holomorphic implies jointly Laplace" step
  by an actual several-complex-variable continuation theorem on a specific
  family of tube domains.

### 7.4. Quantitative radius

Lemma 5.1 extracts an explicit polydisc of analyticity around each real point:
the radius is controlled by the positive time component divided by the Euclidean
size of the point.

This lemma matters a great deal for formalization:
- it is not just a pleasant side estimate;
- it is the entry point for the VI.1 regularization scale.

If the local analyticity radius is treated as a purely qualitative theorem, the
later VI.1 argument loses its scale.

### 7.4.1. Detailed proof transcript of Lemma 5.1

Lemma 5.1 is a coordinate estimate, not an abstract SCV theorem.

The proof is:

1. Define `γ` by the ratio of time component to Euclidean size, via `(5.10)`.
2. In the 4-dimensional paper, choose explicit dual-cone vectors
   `e_1, ..., e_4` as in `(5.11)`.
3. For the repo's general-`d` implementation, this becomes:
   write the standard basis vectors of `R^(d+1)` as linear combinations of the
   chosen dual-cone basis; this is the role of `(5.11')`.
4. Express the perturbed point `ξ_i + ζ_i` in the `e_μ` basis; this is
   `(5.12)`.
5. Require each resulting coordinate `w_i^μ` to stay in the sector
   `|arg w_i^μ| < π/2`.
6. Reduce this to the concrete inequalities `(5.13)` on the components of `ζ`.
7. Use the relation between `tan γ` and the original real point `(5.10)` to
   turn `(5.13)` into the explicit radius estimate `(5.14)`.

So the analyticity radius comes from a linear-algebra calculation in the chosen
dual basis, not from a generic compactness argument.

## 8. Chapter V.2: The `(A_N)/(P_N)` Ladder

This is the corrected replacement for the bad OS I Lemma 8.8 step.

Current formalization note (2026-04-14 09:40 UTC): source-first re-reading of
the live proof body in
`OSToWightmanPositivity.lean`, specifically the local block `hlimit_os` inside
`section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`, shows
that the first missing object is still **earlier** than the fixed-time scalar
assembly. The branch already has:

- the canonical-shell/slice realization theorems
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`
  and
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
- the region-level representative comparison theorems
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_section43Region`,
  and
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`.

But every one of those theorems only applies on
`section43PositiveEnergyRegion d (n + m)` or on the actual source support
`tsupport (f.conjTensorProduct g)`. The live shell variable in `hlimit_os`
ranges over arbitrary `y : NPointDomain d (n + m)`, and current source has no
theorem controlling the outside-region contribution
`y ∉ section43PositiveEnergyRegion d (n + m)`.

So the first missing mathematics is not a lower fixed-time scalarization lemma.
It is a theorem or representation-level choice that removes the outside-region
ambient representative branch on the actual canonical shell. Only after that
ambient-vs-source shell comparison is available does the later scalar assembly
below `hsliceTransport` become the next honest blocker.

### 8.1. Statements

`(A_N)`:
- scalar analytic continuation on time domains `C_k^(N)`.

`(P_N)`:
- Hilbert-space-valued vectors `Ψ_n(x,ζ)` on mixed domains `D_n^(N)` whose
  scalar products reproduce the analytically continued Schwinger functions.

### 8.2. Direction of implication

The proof alternates:
- `(P_N)` gives `(A_{N+1})`,
- then `(A_N)` gives `(P_N)`.

The mechanism is:
- scalar continuation is generated from matrix elements of the `Ψ`-vectors;
- once the scalar continuation exists, one reconstructs new `Ψ`-vectors by
  Taylor expansion in Hilbert space.

### 8.3. Hidden sublemma: Hilbert-space Taylor control

The construction of `Ψ_n(x,ζ)` from nearby real points is not formal handwaving.
The paper uses norm convergence of the Taylor series, and the scalar product
identity follows by passing to the limit from the already-continued scalar
functions.

So the induction uses two nontrivial facts:
- local holomorphic expansion with Hilbert-space values,
- compatibility of scalar products with that expansion.

### 8.4. Domain growth

Lemma 5.2 and Corollary 5.3 are the combinatorial/geometric engine:
- the domains `C_k^(N)` strictly enlarge at each stage;
- the union covers all of `C_+^k`.

This is the precise replacement for the false "separate continuation implies
joint continuation" leap.

Formalization lesson:
- one cannot replace this with a generic Hartogs slogan;
- one needs either these domains or a mathematically equivalent alternative
  continuation ladder.

### 8.5. Base step `(A_0) => (P_0)`

The paper is brief here, but the proof contains a real hidden step.

What is known at this point:
- `(A_0)` gives a scalar function `S_k(ζ)` locally analytic near the real
  positive-time domain;
- equation `(5.2)` is already available on the real side, but only in the sense
  of distributions.

What must be shown:
- the scalar-product identity `(5.17)` holds pointwise for the newly continued
  scalar function, not merely after smearing.

The paper's maneuver is:
1. choose approximate identity test functions on the positive-time side;
2. smear both sides of `(5.2)`;
3. use continuity of the locally analytic representative;
4. let the smearing shrink to a delta function.

So `(P_0)` is not a tautological rephrasing of `(5.2)`. It uses:
- compatibility of the local analytic representative with the distribution;
- a delta-sequence passage on the positive cone.

This is a useful Lean warning:
- whenever a theorem upgrades a distributional equality to a pointwise equality
  of an analytic representative, there is a hidden approximate-identity step.

### 8.5.1. Why the delta-sequence step is legitimate

The paper only says "smear with delta-sequences and take the limit", but the
actual justification requires:

1. both sides define continuous functionals on the relevant positive-time test
   space,
2. the locally analytic representative is continuous on the real slice,
3. approximate identities remain inside the positive-time region,
4. the scalar-product formula `(5.2)` is stable under the smearing limit.

So the base step is already using a boundary-regularity principle:
- distribution identity on a real slice
  +
  continuity of the analytic representative
  =>
  pointwise scalar-product identity.

Exact theorem-slot inventory for the base step:

```lean
lemma positive_time_delta_family_exists
    (τ : PositiveTimePoint k) :
    ∃ ρ : ℕ → PositiveTimeTestFamily k,
      approximateIdentityAt τ ρ := by
  -- choose compactly supported positive-time mollifiers centered at `τ`

lemma scalar_distribution_identity_stable_under_smearing
    (hdist : DistributionalEqualityOnPositiveTimeSlice S Ψ) :
    ∀ N,
      smearingPairing (ρ N) S = smearingPairing (ρ N) Ψ := by
  -- this is the smeared form of `(5.2)`

lemma analytic_representative_smearing_tends_to_point_value
    (hcont : ContinuousOn S positiveTimeSlice)
    (hρ : approximateIdentityAt τ ρ) :
    Tendsto (fun N => smearingPairing (ρ N) S) atTop (nhds (S τ)) := by
  -- continuity + approximate identity

lemma semigroup_side_smearing_tends_to_scalar_product
    (hΨcont : ContinuousOn (fun τ => scalarProductSide Ψ τ) positiveTimeSlice)
    (hρ : approximateIdentityAt τ ρ) :
    Tendsto (fun N => smearingPairing (ρ N) (scalarProductSide Ψ)) atTop
      (nhds (scalarProductSide Ψ τ)) := by
  -- same argument on the scalar-product side

theorem base_step_pointwise_scalar_product_identity
    (hA0 : AnalyticRepresentativeNearPositiveSlice S)
    (hdist : DistributionalEqualityOnPositiveTimeSlice S Ψ) :
    ∀ τ ∈ positiveTimeSlice, S τ = scalarProductSide Ψ τ := by
  -- choose `ρ`, compare the smeared expressions termwise,
  -- take the limit on both sides
```

So the later Lean port should see `(A_0) => (P_0)` as a four-stage package:

1. construct positive-time delta families,
2. smear the distribution identity,
3. pass to the limit on the analytic side,
4. pass to the limit on the scalar-product side.

The final pointwise identity is not a one-line "approximate by deltas" slogan.
It is the output of those four explicit lemmas.

There is one more bridge hidden here that should be named explicitly. The OS II
base step is not merely "use OS I semigroup continuation and then smear".
It uses the following three-layer input:

1. OS II Theorem 4.1 provides real analyticity on the ordered positive-time
   slice;
2. OS II Lemma 5.1, via the Malgrange-Zerner theorem, promotes that local
   analyticity to a genuine complex neighborhood / local polydisc continuation;
3. OS I Section 4.1 provides the semigroup scalar-product formula on the real
   positive-time slice.

Only after those three pieces are in place does the delta-family argument turn
distributional equality into the pointwise `(P_0)` scalar-product identity.

So the implementation-level theorem slots should also include:

```lean
lemma local_polydisc_extension_from_A0_and_lemma51
    (hA0 : RealAnalyticOnPositiveSlice S)
    (h51 : LocalTubeNeighborhoodControl S) :
    AnalyticRepresentativeNearPositiveSlice S := by
  -- this is the point where Lemma 5.1 / Malgrange-Zerner enters

lemma real_slice_semigroup_scalar_product_formula
    (hOSI : OSISemigroupPackage Ψ) :
    ∀ τ ∈ positiveTimeSlice, scalarBoundaryValue S τ = scalarProductSide Ψ τ := by
  -- this is the OS I one-gap semigroup theorem on the real slice
```

This matters because otherwise the later Lean port could still say "base step =
OS I semigroup construction", which is too coarse and hides the local SCV input
coming from OS II itself.

### 8.5.2. Exact correspondence between the OS II variable `ζ` and the OS I
semigroup parameter

This is the precise hidden bridge between Chapter V and OS I Section 4.1.

At stage `(A_0)` / `(P_0)`, the variables `ζ_1, ..., ζ_{n-1}` are not new
mathematical objects. They are the complexified versions of the real positive
time gaps already used in OS I.

The correspondence is:

1. on the real slice one has `ζ_i = τ_i` with `τ_i > 0`;
2. each `τ_i` is a positive time difference between consecutive Euclidean
   arguments;
3. OS I Section 4.1 packages such a positive time difference as the parameter
   of the contraction semigroup `e^{-τ_i H}`;
4. OS II Chapter V then allows `τ_i` to move off the positive real axis to a
   complex variable `ζ_i` in the right half-plane.

So the mathematical content of the base step is:

- OS I gives the real-parameter semigroup/scalar-product formula;
- OS II reinterprets the same real parameter as the boundary value of a
  holomorphic variable `ζ_i`;
- `(P_0)` is the statement that the already-continued scalar function agrees
  pointwise with the semigroup/scalar-product formula on that real boundary
  slice.

For later Lean implementation, the bridge must therefore be written with two
separate theorem slots:

1. a real positive-time theorem
   `F(τ) = (Ψ_left, e^{-τH} Ψ_right)` for `τ > 0`;
2. a boundary identification theorem saying that the holomorphic function of
   `ζ` has real boundary values equal to the same scalar-product expression.

Only after those are both present can one honestly say that the OS II
continuation variable `ζ` is "the same parameter" as the OS I semigroup time.

### 8.6. Inductive step `(P_{M-1}) => (A_M)`

This is the easier half of the induction, but it still has a very specific
shape.

Assume `(P_{M-1})`. Then for admissible mixed-domain points
`(x,ζ) in D_n^(M-1)` and `(x',ζ') in D_m^(M-1)` the scalar product

`(Ψ_n(x,ζ), Ψ_m(x',ζ'))`

is already defined and analytic in each block of variables. By `(5.17)` it
agrees on the real slice with `S_{n+m-1}` evaluated on a specific tuple of time
differences, namely

`(-\bar ζ, x+x', ζ')`.

So the paper defines `S_k` first on a union of mixed domains obtained from
those scalar products. The crucial point is:
- this union is not yet the final domain `C_k^(M)`;
- the paper then invokes the *definition* of `C_k^(M)` as the envelope of
  holomorphy of that union.

Therefore the logical structure is:
1. scalar products from `(P_{M-1})` produce analyticity on a concrete union of
   tube-like pieces;
2. the envelope-of-holomorphy construction expands that analyticity to the full
   `C_k^(M)`.

This matters for formalization because the second step is not free:
- either the concrete envelope theorem must be available,
- or the Lean theorem surface must build `C_k^(M)` in a way that already
  packages this extension.

### 8.6.2. Exact variable bookkeeping in `(P_{M-1}) => (A_M)`

For later implementation, the scalar-product continuation should be remembered
with the following bookkeeping:

- if `k = n + m - 1`,
- the left vector contributes `n-1` continued time variables `ζ`,
- the right vector contributes `m-1` continued time variables `ζ'`,
- the middle real variable is `x + x'`,
- the resulting scalar point in `C_k^(M)` is
  `(-\bar ζ, x + x', ζ')`.

So the continuation step is not symmetric in all variables. It is built from
two continued blocks with a real bridge coordinate in the middle. That pattern
must be preserved if one later recreates this step in Lean.

### 8.6.1. Why the scalar-product formula has the right variable pattern

The scalar-product formula uses

`(-\bar ζ, x+x', ζ')`.

This pattern is not arbitrary. It is the same one already present in `(5.17)`:

- the left block enters with reflection and complex conjugation,
- the middle variable is the positive real bridge variable `x + x'`,
- the right block enters unchanged.

So the continuation from `(P_{M-1})` to `(A_M)` preserves the OS positivity
geometry. This is another reason one cannot replace it by a generic SCV
assertion detached from the Hilbert-space scalar product.

For later Lean implementation, the notation `-\bar ζ` must also be translated
into the repo's conjugation conventions explicitly.

Paper notation:
- `ζ` is a block of complexified positive-time parameters;
- `\bar ζ` means coordinatewise complex conjugation;
- the leading minus sign is the reflection of the left block under OS
  positivity.

Repo-side semantic translation:

1. on the Euclidean / Schwinger side, the reflected-conjugated left block is
   represented by `osConjTensorProduct`;
2. on the reconstructed Wightman side, the corresponding reflected-conjugated
   left block is represented by `conjTensorProduct`;
3. the scalar-product pattern `(-\bar ζ, x + x', ζ')` is therefore the
   time-parameter analogue of the same left-reflection/right-identity geometry
   encoded by those two tensor-product constructions.

So the later Lean port should not treat `-\bar ζ` as anonymous notation.
Whenever Chapter V or VI uses that pattern, the implementation notes should
explicitly ask:

1. which side is playing the role of the reflected left block?
2. is the current formula on the Euclidean side (`osConjTensorProduct`) or the
   Wightman side (`conjTensorProduct`)?
3. which real bridge variable is playing the role of `x + x'`?

This notation dictionary matters even if the eventual theorem-3 port avoids the
full general `(P_N)/(A_N)` machinery, because it prevents the common error of
flattening OS reflection/conjugation into an ordinary complex conjugation with
no tensorial meaning.

### 8.7. Inductive step `(A_M) => (P_M)`

This is the subtler half and the part that is easiest to under-document in a
formalization.

Fix `(x,ζ) in D_n^(M)`. The paper first chooses:
- real positive times `ξ_i > 0`,
- radii `r_i > 0`,
such that the polydisc

`P = {(x,ζ) | x = x, |ζ_i - ξ_i| < r_i}`

lies entirely inside `D_n^(M)`.

Then `Ψ_n(x,ζ)` is defined by the strong-Hilbert-space Taylor series

`Σ_α ((ζ-ξ)^α / α!) ∂^α Ψ_n(x,ξ)`.

Two hidden facts are packed into the next sentence of the paper:

1. The derivatives `∂^α Ψ_n(x,ξ)` already make sense as Hilbert-space vectors.
   This comes from the previous stage, because the scalar products of those
   derivatives are read off from derivatives of the already-continued scalar
   functions.

2. The series converges in norm. The proof is the estimate `(5.21)`:
   the squared norm of the remainder is identified with the remainder term in
   the Taylor expansion of the scalar function
   `S_{2n-1}(-\bar ζ, 2x, ζ)` around
   the real point `(ξ,2x,ξ)`.

So the vector-valued analyticity is not postulated. It is reconstructed from
scalar analyticity plus positivity of the scalar-product formula.

This is one of the clearest paper precedents for the current project's
discipline:
- scalar continuation data should be turned into Hilbert-space or kernel data
  only through explicit norm-control and scalar-product identities.

### 8.7.1. Why the remainder norm equals a scalar Taylor remainder

The key estimate `(5.21)` identifies the square of the norm of the vector
Taylor remainder with the scalar Taylor remainder of

`S_{2n-1}(-\bar ζ, 2x, ζ)`.

This works because:

1. scalar products of derivatives of `Ψ_n` can be computed by differentiating
   the scalar-product identity `(5.17)`;
2. the Hilbert norm squared of the remainder is the scalar product of the
   remainder with itself;
3. after expanding that scalar product, one gets exactly the scalar Taylor
   remainder of the continued Schwinger function at the doubled point.

So `(5.21)` is not an analogy. It is a literal equality produced by the
scalar-product formula.

The minus sign on the left block is essential: it is the time-reflection sign
coming from the OS inner product / reflected first block in `(5.17)`. So this
Taylor expansion is a real-analytic expansion of the norm-squared scalar
function built from the OS-reflected argument pattern, not a holomorphic Taylor
expansion of an unrelated `S_{2n-1}(ζ,2x,\bar ζ)` expression.

### 8.7.2. Why the polydisc is chosen around a real point

The paper chooses the center `ξ` of the Taylor expansion on the real positive
slice rather than at the target complex point `ζ`. This is essential:

- derivatives of `Ψ_n` are already known there from the previous stage;
- the scalar-product formula is already available there in a concrete Hilbert
  sense;
- the scalar Taylor remainder can be controlled there by the already-proved
  scalar analyticity.

So the vector-valued continuation is anchored at a real point where the Hilbert
structure is concrete, not at an arbitrary interior complex point.

### 8.7.3. Full theorem-slot inventory for `(A_M) => (P_M)`

For a later Lean port, the previous prose should be read as the following
explicit chain of local obligations.

Fix `(x,ζ) ∈ D_n^(M)`. Choose a real basepoint `ξ` and radii `r_i > 0` such
that the closed polydisc

`P(ξ,r) = {w | ∀ i, ‖w_i - ξ_i‖ < r_i}`

lies in `D_n^(M)`.

The hidden local theorems are then:

```lean
lemma scalar_product_of_vector_derivatives_from_PM
    (hPM : P_M)
    (x : RealSpaceTuple n)
    (ξ : PositiveTimeTuple (n - 1))
    :
    ∀ α β,
      inner
        (vectorDerivativeAt (Ψ_n := Ψ_n) x ξ α)
        (vectorDerivativeAt (Ψ_n := Ψ_n) x ξ β)
      =
      scalarDerivativeAt
        (fun (u,v) => S_(2 * n - 1) (-conj u, 2 * x, v))
        (ξ, ξ) (α, β) := by
  -- differentiate the real-slice pairing identity `(5.17)` in both blocks

lemma vector_taylor_series_defined_on_real_basepoint
    (hPM : P_M)
    (x : RealSpaceTuple n)
    (ξ : PositiveTimeTuple (n - 1))
    :
    FormalMultilinearSeries ℂ (HilbertSpaceField d) := by
  -- package `∂^α Ψ_n(x, ξ)` into the candidate Taylor coefficients

lemma vector_remainder_norm_sq_eq_scalar_remainder
    (hPM : P_M)
    (x : RealSpaceTuple n)
    (ξ : PositiveTimeTuple (n - 1))
    (r : PositiveRadiusTuple (n - 1))
    :
    ∀ N w,
      ‖vectorTaylorRemainder (Ψ_n := Ψ_n) x ξ N w‖^2 =
        scalarTaylorRemainder
          (fun z => S_(2 * n - 1) (-conj z, 2 * x, z))
          ξ N w := by
  -- expand the remainder norm squared and rewrite every scalar product using `(5.17)`

lemma scalar_remainder_tends_to_zero_on_internal_polydisc
    (hAM : A_M)
    (x : RealSpaceTuple n)
    (ξ : PositiveTimeTuple (n - 1))
    (r : PositiveRadiusTuple (n - 1))
    :
    ∀ w ∈ closedPolydisc ξ r,
      Tendsto
        (fun N =>
          scalarTaylorRemainder
            (fun z => S_(2 * n - 1) (-conj z, 2 * x, z))
            ξ N w)
        atTop (nhds 0) := by
  -- ordinary scalar holomorphic Taylor theorem on the polydisc from `A_M`

lemma strong_taylor_series_converges_on_internal_polydisc
    (hAM : A_M) (hPM : P_M)
    (x : RealSpaceTuple n)
    (ξ : PositiveTimeTuple (n - 1))
    (r : PositiveRadiusTuple (n - 1))
    :
    ∀ w ∈ closedPolydisc ξ r,
      Summable (vectorTaylorSeriesTerms (Ψ_n := Ψ_n) x ξ w) := by
  -- use the previous two lemmas and positivity of the Hilbert norm

lemma vector_continuation_from_polydisc_taylor_series
    (hAM : A_M) (hPM : P_M)
    :
    ∃ Φ : D_n^(M) → HilbertSpaceField d,
      (∀ (x,ζ) ∈ realPositiveSlice, Φ (x,ζ) = Ψ_n x ζ) ∧
      HolomorphicOn Φ D_n^(M) := by
  -- define Φ by the convergent local Taylor series and glue overlapping discs

lemma scalar_pairing_formula_extends_from_real_slice
    (hAM : A_M) (hPM : P_M)
    :
    ∀ (x,ζ) ∈ D_n^(M),
      inner (Φ x ζ) (Φ x ζ) =
        S_(2 * n - 1) (-conj ζ, 2 * x, ζ) := by
  -- identity theorem: both sides are holomorphic/real-analytic and agree on the real slice
```

The point of writing the proof this way is to make the dependency direction
unambiguous:

1. `P_M` gives the scalar-product identity on the real slice and therefore
   gives scalar products of derivatives there.
2. `A_M` gives scalar analyticity on the polydisc, hence Taylor convergence for
   the scalar function `S_{2n-1}(-conj z, 2x, z)`.
3. The norm-remainder identity transfers scalar Taylor convergence into
   norm-convergence of the vector Taylor series.
4. Only after that is a genuine vector-valued continuation on `D_n^(M)`
   defined.

So the proof does not assume a Hilbert-valued holomorphic function at the start.
It manufactures one from scalar Taylor control.

### 8.7.4. Detailed micro-proof of the norm estimate `(5.21)`

The paper writes `(5.21)` in a compressed form. The full proof should be read
as follows.

1. Let
   `R_N(ζ) := Ψ_n(x,ζ) - Σ_{|α| ≤ N} ((ζ-ξ)^α / α!) ∂^α Ψ_n(x,ξ)`.
2. Expand `‖R_N(ζ)‖^2 = ⟪R_N(ζ), R_N(ζ)⟫`.
3. Expand both copies of `R_N(ζ)` as finite sums plus the full vector.
4. Rewrite each scalar product of derivatives by differentiating `(5.17)`:
   every term becomes a scalar derivative of
   `S_{2n-1}(-conj z, 2x, z)` at `ξ`.
5. Collect the resulting coefficients. They are exactly the scalar Taylor
   remainder of that scalar function about `ξ`.
6. Conclude
   `‖R_N(ζ)‖^2 = R_N^scalar(ζ)`.
7. Because `R_N^scalar(ζ) → 0` on the polydisc by ordinary holomorphic Taylor
   theory, the vector remainder converges to zero in norm.

The delicate point is step 4. There is no independent vector derivative theory
being invoked. The derivative data are valid only because `(5.17)` already
encodes all scalar products of the would-be derivatives.

### 8.7.5. Exact Lean-style pseudocode for `(A_M) => (P_M)`

```lean
theorem inductive_step_AM_to_PM
    (hAM : A_M) (hPM : P_M) :
    P_{M+1} := by
  intro n x ζ hζ
  obtain ⟨ξ, r, hξ_real, hdisc⟩ :=
    choose_real_basepoint_and_polydisc_inside_DnM hζ
  let coeffs :=
    vector_taylor_series_defined_on_real_basepoint
      (hPM := hPM) (x := x) (ξ := ξ)
  have hrem :
      ∀ N w ∈ closedPolydisc ξ r,
        ‖vectorTaylorRemainder (Ψ_n := Ψ_n) x ξ N w‖^2 =
          scalarTaylorRemainder
            (fun z => S_(2 * n - 1) (-conj z, 2 * x, z))
            ξ N w :=
    vector_remainder_norm_sq_eq_scalar_remainder
      (hPM := hPM) (x := x) (ξ := ξ) (r := r)
  have hscalar :
      ∀ w ∈ closedPolydisc ξ r,
        Tendsto
          (fun N =>
            scalarTaylorRemainder
              (fun z => S_(2 * n - 1) (-conj z, 2 * x, z))
              ξ N w)
          atTop (nhds 0) :=
    scalar_remainder_tends_to_zero_on_internal_polydisc
      (hAM := hAM) (x := x) (ξ := ξ) (r := r)
  have hsum :
      ∀ w ∈ closedPolydisc ξ r,
        Summable (vectorTaylorSeriesTerms (Ψ_n := Ψ_n) x ξ w) :=
    strong_taylor_series_converges_on_internal_polydisc
      (hAM := hAM) (hPM := hPM) (x := x) (ξ := ξ) (r := r)
  let Φ :=
    vectorContinuationFromTaylorSeries
      (Ψ_n := Ψ_n) x ξ r coeffs hsum
  have hpair :
      ∀ w ∈ closedPolydisc ξ r,
        inner (Φ w) (Φ w) =
          S_(2 * n - 1) (-conj w, 2 * x, w) :=
    scalar_pairing_formula_extends_from_real_slice
      (hAM := hAM) (hPM := hPM)
  exact package_local_PM_data_from_vector_continuation Φ hpair
```

This is the level of detail the later Lean port should preserve. Nothing about
the Hilbert-valued continuation should be left as "standard" or "formal."

### 8.8. Proof skeleton of Lemma 5.2

Lemma 5.2 is the real replacement for the false OS I many-variable jump. The
paper proves it by introducing recursively defined numbers, written in the
paper as `ρ(i,N)`, satisfying the recursion encoded in `(5.27)`. In our local
notes we may temporarily write them as `h_i^(N)`, but the important point is
that they are defined recursively from the convex-hull step and *not* by a
standalone closed formula.

`h_i^(N+1) = (i / (2i-1)) * (h_{i-1}^(N) + π/2)`,

with `h_i^(0) = 0`, and then showing that the points

`(0, h_1^(N), ..., h_{s-1}^(N))`

lie in the mixed domains `d_s^(N)`.

There are three structural ideas hidden here:

1. Convexity and sign symmetry of the logarithmic tube bases `c_k^(N)` and
   `d_n^(N)` let the proof work with a single positive orthant representative.
2. The recursive formula is obtained by averaging two extreme points already
   known to lie in the previous-stage convex set.
3. What the paper actually uses later is not a closed form for `ρ(i,N)` but an
   explicit lower bound `(5.28)` strong enough for Corollary 5.3.

Important correction:

- the earlier local note claiming the exact closed form
  `h_i^(N) = (π/2) * (1 - (2i-1)^(-N))`
  was incorrect and should not be used;
- the associated claim that `h_i^(N) -> π/2` for each fixed `i` was therefore
  also incorrect as stated;
- for the formalization, the mathematically relevant output is the paper's
  explicit quantitative lower bound `(5.28)` and the resulting exhaustion
  statement in Corollary 5.3, not the false closed-form shortcut.

So the domain-exhaustion theorem is not qualitative. It is a very explicit
combinatorial induction.

Implementation-level theorem-slot inventory for Lemma 5.2 / Corollary 5.3:

```lean
lemma recursive_angle_parameters_exist
    :
    ∃ ρ : ℕ → ℕ → ℝ,
      (∀ i, ρ i 0 = 0) ∧
      (∀ i N, ρ (i + 1) (N + 1) =
        ((i + 1 : ℝ) / (2 * i + 1)) * (ρ i N + π / 2)) := by
  -- recursive angle parameters from the convex-hull step `(5.27)`

lemma orthant_point_in_mixed_domain_of_recursive_angles
    (ρ : ℕ → ℕ → ℝ)
    (hρ : RecursiveAngleSystem ρ) :
    ∀ s N, (0, ρ 1 N, ..., ρ (s - 1) N) ∈ d_s^(N) := by
  -- this is the actual inductive content of Lemma 5.2(a)

lemma ck_contains_sector_box_of_recursive_angles
    (ρ : ℕ → ℕ → ℝ)
    (hρ : RecursiveAngleSystem ρ) :
    ∀ k N, sectorBoxFromAngles (fun i => ρ i N) ⊆ C_k^(N) := by
  -- this is Lemma 5.2(b)

lemma recursive_angles_admit_quantitative_lower_bound
    :
    ∃ γ : ℕ → ℝ,
      ∀ i N, lowerBoundFromFormula528 γ i N ≤ ρ i N := by
  -- paper's explicit lower bound `(5.28)`, without pretending a false exact formula

lemma choose_stage_from_argument_bounds
    (ζ : CPlusTuple k) :
    ∃ N, ζ ∈ C_k^(N) := by
  -- Corollary 5.3 + the argument comparison used later in VI.2
```

The critical point is that the later Lean port should *not* try to prove a
closed form for `ρ(i,N)` unless the exact paper formula has first been checked
from the source. The formalization only needs:

1. the recursive construction,
2. the domain-membership induction,
3. the quantitative lower bound used in Corollary 5.3,
4. the stage-selection theorem `N = N(ζ)` used in VI.2.

### 8.8.1. Why Lemma 5.2 is enough to reach all of `C_+^k`

The key output of Lemma 5.2 is that for every fixed angle away from the
boundary of the right half-plane, some finite stage `N` already captures that
angle. Corollary 5.3 then quantifies this using the lower bound `(5.28)` in the
form used later by VI.2.

So the union

`⋃_N C_k^(N) = C_+^k`

is not merely topological. It is quantitatively controlled by the recursively
defined angle parameters from `(5.27)` together with the explicit lower bound
`(5.28)` and the derived constants `γ_k`.

## 9. Chapter VI Overview: The Role of `E0'`

Chapter VI starts only after continuation is in place.

Problem:
- a real-analytic or holomorphic continuation need not satisfy polynomial
  bounds.

Goal:
- prove the real-domain estimate `(4.5)`,
- then transport it along the continuation ladder.

The paper is very clear that `E0'` is used here to control the size of
regularized Hilbert-space vectors, not to produce the continuation itself.

## 10. Chapter VI.1: From Distributions to Functions

This is the hardest technical chapter relevant to the current Lean frontier.

### 10.1. Why a naive argument fails

The paper explicitly warns that even if `S_k` is distributionally tempered and
real analytic, it does not follow that the resulting function is polynomially
bounded. The counterexample is a smooth function with huge spikes on tiny
intervals.

So one must not argue:
- "the Schwinger function is a tempered distribution and also a function,
  therefore it has polynomial growth."

OS II rejects exactly that shortcut.

### 10.2. Regularization

Fix a real point `ξ` and the local analyticity radius `ρ(ξ)` from Lemma 5.1.
Construct:
- a bump `h`,
- a regularizing function `g_ρ`,
- a radial kernel `k_ρ`,
- then a regularized object `T_k`.

The mean-value theorem for harmonic functions gives

`S_k(ξ+ζ)` as an average of `T_k` over a small complex neighborhood.

This is the first absolutely decisive proof-shape lesson for the current
formalization:
- VI.1 does not estimate the raw boundary-value function directly;
- it estimates a regularized auxiliary object that still preserves positivity.

More concretely, the paper's definitions are:

- `(6.1)`: the mean-value identity for `S_k(ξ+ζ)` on the local polydisc;
- `(6.2)`: choose a positive `C_0^∞` bump `h` with normalization
  `∫ h(r) r^{2(d+1)-1} dr = 1` and controlled derivatives;
- choose the radial support convention explicitly as `supp h ⊆ [0, 1/8]`,
  since this is what later forces the rescaled kernels to stay inside the
  `ρ/8` and `ρ/4` balls;
- in the 4-dimensional paper this is `∫ h(r) r^7 dr = 1`;
- define `g_ρ(z) = c ρ^{-2(d+1)} h_ρ(|z|)` on `C^(d+1)`, with the scaling
  chosen so that `supp g_ρ ⊆ {|z| < ρ/8}`;
- define `k_ρ = g_ρ * g_ρ`, so `supp k_ρ ⊆ {|z| < ρ/4}` and `∫ k_ρ = 1`;
- define `k_ρ^k` as the product kernel in `k` blocks;
- `(6.5)`: define the regularized object `T_k` by integrating the shifted
  values of `S_k` against `g_ρ` and `k_ρ`.

Then `(6.6)` rewrites the original function as

`S_k(ξ+ζ) = ∬ T_k(ξ+ζ+iy') d y d y'`

over a small region, and `(6.7)` turns this into a supremum bound on `T_k`.

So the regularization package uses:
- mean-value theorem for harmonic functions,
- support bookkeeping for `g_ρ` and `k_ρ`,
- Fubini to rearrange the averaging integrals.

If a later Lean formalization wants VI.1 in a faithful way, those ingredients
need explicit local lemmas. They are not optional cosmetic details.

### 10.2.1. Derivation of `(6.6)` from `(6.1)`-`(6.5)`

The paper compresses several integral manipulations into one displayed formula.
Unpacked, the derivation is:

1. Start from the mean-value theorem `(6.1)` for the holomorphic function
   `S_k(ξ+ζ)`.
2. Replace the sphere average by a radial average using the mollifier `h` from
   `(6.2)`.
3. Normalize the radial average to obtain the compactly supported smooth kernel
   `g_ρ`.
4. Convolve `g_ρ` with itself to form `k_ρ`; this improves support bookkeeping
   and gives a symmetric kernel with total mass `1`.
5. Because the support of `k_ρ` sits inside the analyticity polydisc, inserting
   the convolution does not change the value of the averaged holomorphic
   function.
6. Apply Fubini to rearrange the resulting nested integrals blockwise.
7. Define the inner integral to be `T_k`.
8. The remaining outer integral is exactly `(6.6)`.

So `(6.6)` is not a mysterious formula; it is a reorganized mean-value theorem
after inserting a compactly supported approximate identity that is compatible
with the local analyticity radius.

### 10.2.2. Derivation of `(6.7)`

Once `(6.6)` is written down, `(6.7)` is obtained by:

1. taking absolute values of both sides,
2. using positivity of the regularizing kernels,
3. using that the outer integration region is contained in the support region
   where `|y_i| < ρ/4` and `|y'_i| < ρ/8`,
4. bounding the weighted average by the supremum of `|T_k|` on that region.

So `(6.7)` is the precise point where the argument turns the average formula
into a supremum estimate. This is why all later effort goes into bounding `T_k`
rather than `S_k` directly.

### 10.3. Positivity channeling

`T_k` is then written as a Hilbert-space scalar product of two vectors `Ψ_1` and
`Ψ_2`. Once in that form, the semigroup yields an analytic continuation in one
chosen variable and the uniform bound

`|<Ψ_1, e^{-zH} Ψ_2>| <= ||Ψ_1|| ||Ψ_2||`.

This is the second decisive lesson:
- the growth estimate is obtained by routing through the same positive semigroup
  used for continuation, but only after regularization.

Formulaically, `(6.8)` is the key bridge. After rotating to the preferred
direction `e_μ`, the regularized object at `ξ + u e_μ` is represented as an
integral pairing that has exactly the OS-positivity shape. The paper then reads
that integral as a scalar product

`T_k(ξ + u e_μ) = (Ψ_1, Ψ_2)`.

At that point the semigroup inserts a complex translation in the distinguished
direction and gives the one-variable analytic function

`(Ψ_1, e^{-zH} Ψ_2)`.

This is the exact place where the proof returns to the OS I semigroup mechanism.
Everything before it is packaging; everything after it is norm estimation.

### 10.3.1. The vectors `Ψ_1`, `Ψ_2` from `(6.10)`

The paper's formula `(6.10)` should be read extremely literally. The vectors
`Ψ_1` and `Ψ_2` are obtained by feeding specific Euclidean test data into the
same OS Hilbert-space map constructed earlier:

- the first vector uses the first `n-1` difference variables together with the
  mollifier `g_ρ` in the distinguished block;
- the second vector uses the remaining `k-n` difference variables together with
  the kernel `k_ρ` and the shifted real point.

The conceptual point is:
- `T_k` is not an abstract scalar product representation theorem;
- the vectors are written down explicitly from the regularization kernels.

So a faithful later formalization should not merely prove existence of two
vectors. It should define the vectors and then prove the scalar-product
identity.

### 10.3.2. The one-variable semigroup insertion `(6.11)`

Once `T_k(ξ+u e_μ)` is rewritten as `(Ψ_1, Ψ_2)`, the paper inserts the
semigroup exactly as in OS I:

1. vary the chosen directional parameter `u_{nμ}`;
2. interpret that variation as a positive-time shift in the distinguished OS
   variable;
3. obtain an analytic continuation by sandwiching `e^{-wH}` between `Ψ_1` and
   `Ψ_2`;
4. use contractivity to bound the resulting analytic function uniformly in the
   auxiliary parameters `y`, `y'`.

The important detail is the phrase "uniformly in `y`, `y'`". This means the
semigroup bound is not merely pointwise in the regularization parameters; it is
stable across the whole support region of the regularizing kernels. That
uniformity is what later allows the supremum estimate in `(6.7)` to be
effective.

### 10.3.3. Relationship between `T_k` in OS II and the repo's `bvt_F`

This is exactly where the current project must avoid conflating two different
analytic objects.

In the paper:

- `S_k` is the Schwinger function already continued to an interior domain;
- `T_k` is a *regularized* auxiliary function obtained by averaging `S_k`
  against the kernels `g_ρ` and `k_ρ`;
- the semigroup estimate is proved for `T_k`, not directly for `S_k`.

In the repo:

- `bvt_F` is the unregularized common holomorphic tube object coming from the
  BHW / boundary-value construction;
- the positive-real theorem-3 shell compares special evaluations or pairings of
  `bvt_F`, `OS.S`, and `bvt_W`;
- there is not yet a separate production object named `T_k` playing the exact
  role of OS II's mollified auxiliary kernel.

So the honest correspondence is:

1. `bvt_F` is analogous to the *pre-regularization* common holomorphic object;
2. `T_k` is analogous to a local average or mollified slice built from that
   object when one needs VI.1-style bounds;
3. therefore one must never write "`T_k = bvt_F`" in the documentation;
4. if a future Lean step needs the full VI.1 estimate mechanism, it must define
   and analyze an explicit regularized kernel on top of `bvt_F`, not pretend
   that the unregularized object already has the same quantitative properties.

For theorem 3 specifically, this means:

- the current route may still work without introducing a full `T_k` analogue if
  the one-variable boundary-value identity is obtained through the existing K2
  uniqueness/transport machinery;
- but any later attempt to imitate VI.1 for estimates must introduce a genuine
  regularized object and prove its relation to `bvt_F` first.

### 10.4. Where `E0'` enters

The norms `||Ψ_1||`, `||Ψ_2||` are controlled using the linear growth condition.
The linear dependence of the Schwartz order on the number of variables is what
makes the constant bookkeeping manageable.

So in formalization terms:
- `E0'` feeds the bound on regularized Hilbert vectors;
- it is not a replacement for the semigroup or the continuation domains.

The paper's displayed inequality `(6.9)` is already part of this bookkeeping:
it controls how the rotated/shifted configuration grows in terms of the local
radius `ρ(ξ)` and the added directional parameter `u`.

That estimate matters because it is what lets the eventual `E0'` seminorm bound
see only polynomial growth in `ξ`. In other words:
- geometric control of the shifted points comes first,
- seminorm control is applied second.

### 10.4.1. How `(6.12)` is proved from `(6.17)`-`(6.19)`

The paper defers the proof of the norm estimate `(6.12)` and then gives it at
the end of VI.1. Since this is exactly the sort of hidden step that causes
later confusion, it should be unpacked in full.

The proof of `(6.12)` is:

1. Start from the scalar-product formula `(5.2)` for the OS Hilbert norm and
   insert the explicit vectors `(6.10)`.
2. This yields the long integral expression `(6.17)` for `‖Ψ_1‖^2`.
3. Apply the linear growth condition `E0'` to the resulting test function.
   This is where the sequence `σ_{2n-1}` appears.
4. Estimate the support of the regularizing kernels:
   - `|x_n - λ_n| < ρ/8`,
   - `|x_i - x_{i+1} - λ_i| < ρ/4`,
   and similarly for the `y`-variables.
5. Deduce the polynomial control `(6.18)` on the full tuple
   `1 + Σ x_i^2 + Σ y_i^2` in terms of the shifted geometric variables `λ_i`.
6. Estimate derivatives of the regularizers using `(6.2)` and `(6.3)`.
7. Feed those support and derivative bounds into the `E0'` seminorm estimate.
8. Conclude `(6.12)` for `‖Ψ_1‖`, and analogously for `‖Ψ_2‖`.

So the order of dependence is:
- kernel support bounds,
- then Schwartz seminorm bounds on the regularizers,
- then `E0'`,
- then the Hilbert norm estimate.

### 10.4.2. Why `(6.9)` and `(6.18)` are separate

The paper uses two different geometric inequalities:

- `(6.9)` controls the rotated/shifted point appearing in the analytic
  continuation step;
- `(6.18)` controls the actual variables in the regularizer norm estimate.

They serve different purposes:
- `(6.9)` feeds the bound on the analytically continued function;
- `(6.18)` feeds the bound on the `E0'` seminorm of the explicit vectors.

This distinction is easy to miss because both are "polynomial growth"
inequalities, but they live in different parts of the argument.

### 10.5. Four-direction continuation again

The paper repeats the directional continuation idea from Chapter V, but now on
the regularized object `T_k`. Each direction yields a bound on a flat tube, and
Malgrange-Zerner plus the maximum principle propagate the bound to the whole
local analytic neighborhood.

This is a characteristic OS II pattern:
- continue in finitely many geometrically chosen directions,
- glue to the convex envelope,
- then use a maximum principle to transport estimates.

### 10.6. Hidden constant tracking

The inequalities `(6.9)`-`(6.16)` are not cosmetic:
- they show exactly how the local radius `ρ(ξ)` and the Euclidean size `|ξ|`
  control the bound;
- they are what eventually produce the concrete polynomial estimate `(4.5)`.

A Lean route that skips explicit dependence on the analyticity radius is not yet
following VI.1 faithfully.

### 10.6.1. The chain `(6.13)` -> `(6.14)` -> `(6.15)` -> `(6.16)`

The displayed inequalities here are the real analytic heart of VI.1.

1. `(6.13)` combines:
   - the semigroup bound `(6.11)`,
   - the Hilbert norm estimate `(6.12)`,
   - the geometric control `(6.9)`.

   The output is a uniform bound on the directional continuation
   `T_k(ξ + u e_μ)`.

2. Introduce logarithmic coordinates `r = log w`. This converts the wedge-type
   domain in `w` into a flat tube domain in `r`.

3. `(6.14)` rewrites the previous bound in the logarithmic variables.

4. Since all the directional continuations coincide on the common real slice,
   Malgrange-Zerner gives a single analytic continuation on the convex envelope
   of the union of the flat tubes.

5. `(6.15)` uses the envelope-of-holomorphy / maximum-principle argument: the
   supremum on the convex envelope equals the supremum on the original union of
   flat tubes.

6. Return from `r` to `w`, then from `w` to `ζ`, to obtain the local bound on
   `T_k`.

7. Finally combine this with `(6.7)` and the coordinate decomposition `(5.12)`
   to obtain `(6.16)`, which is exactly the desired real-domain estimate
   `(4.5)`.

So the proof is not "apply maximum principle to the original `S_k`". It is:
- regularize,
- continue directionally,
- pass to log coordinates,
- glue,
- maximize,
- unroll coordinates,
- then unregularize.

### 10.6.2. Why logarithmic coordinates are essential

The passage `w -> r = log w` is not a change of notation. It changes the domain
from:
- a product of wedge-type multiplicative regions in the `w`-variables

to
- a union of flat additive tube domains in the `r`-variables.

That transformation is exactly what allows Malgrange-Zerner to apply in the
form the paper uses. Without it, one does not have the right convexity theorem
available on the original multiplicative domain.

So if later Lean work ever tries to bypass the logarithmic-coordinate step, it
must replace it with a theorem of comparable strength on the original wedge
domain; otherwise it is not following the paper proof.

### 10.7. Step-by-step proof skeleton of VI.1

To make later implementation straightforward, it is useful to isolate the exact
flow of VI.1 as a sequence of proof obligations.

For fixed `k` and real positive-time point `ξ`:

1. Use Lemma 5.1 to choose a radius `ρ(ξ)` on which `S_k` is holomorphic.
2. Choose the auxiliary cutoff data `h`, `g_ρ`, `k_ρ` with the support and
   normalization properties listed in the paper.
3. Define the regularized object `T_k` by convolving / averaging the analytic
   continuation against those kernels.
4. Prove the mean-value identity expressing `S_k(ξ+ζ)` as an average of `T_k`
   over a smaller complex neighborhood.
5. Rewrite `T_k` as an OS Hilbert-space scalar product
   `<Ψ_1, Ψ_2>`.
6. Insert the semigroup in one chosen direction and obtain
   `<Ψ_1, e^{-zH} Ψ_2>`.
7. Use contractivity of `e^{-zH}` in the right half-plane to bound the scalar
   product by `||Ψ_1|| ||Ψ_2||`.
8. Use `E0'` to estimate `||Ψ_1||` and `||Ψ_2||` in terms of the local scale
   `ρ(ξ)` and the Euclidean size of `ξ`.
9. Transfer the directional estimates to the full local neighborhood by the
   multi-direction continuation argument.
10. Convert the local bound into the global polynomial estimate `(4.5)`.

Every later formalization of VI.1 should be auditable against those ten steps.
If a proposed proof does not exhibit all of them, it is suppressing an
analytic input.

### 10.8. What VI.1 does *not* allow

Because the paper is so explicit here, we can safely rule out several tempting
shortcuts:

- It does not permit concluding polynomial bounds directly from real
  analyticity.
- It does not permit a proof that never introduces a regularized auxiliary
  object.
- It does not permit replacing the Hilbert-space norm estimate by a bare
  distributional uniqueness argument.
- It does not permit sliding between boundary values and interior evaluations
  without first establishing the correct local continuation object.

Those prohibitions are especially relevant to the current theorem-3 frontier.

### 10.9. Minimal local lemma inventory suggested by VI.1

If VI.1 ever has to be formalized more completely in Lean, the paper suggests a
very specific local lemma inventory:

1. quantitative local analyticity radius,
2. normalized radial bump with derivative bounds,
3. support and normalization properties of the convolution kernel,
4. mean-value identity for holomorphic functions on a polydisc,
5. Fubini-safe regularization identity,
6. positivity/scalar-product representation of the regularized kernel,
7. semigroup bound on that scalar product,
8. polynomial comparison between the shifted geometric point and the original
   real point,
9. maximum-principle propagation from directional bounds to full local bounds.

Writing those down now is useful even before formalization, because it prevents
future proof attempts from silently collapsing several genuinely different
analytic steps into one slogan.

## 11. Chapter VI.2: Continuing the Estimates

Once `(4.5)` holds on the real domain, the paper packages the continued
functions as taking values in Banach spaces of spatial-variable functions with
polynomial weights.

Then it repeats the induction:
- define `S_{k,ε}` with a compensating denominator;
- use the `(P_N)` scalar-product representation and previously obtained bounds
  to prove estimates on the boundary of the next domain;
- apply the maximum principle to extend those estimates to the entire enlarged
  domain.

This is conceptually cleaner than VI.1:
- no new analyticity theorem is proved here;
- only the bounds are propagated along the Chapter V continuation ladder.

That distinction should be maintained in the repo's proof decomposition.

### 11.1. Exact proof chain of VI.2

The paper's proof of VI.2 can be decomposed into the following explicit steps.

1. Define the Banach spaces `B_{k,t}` by the weighted sup norm `(6.19)` in the
   spatial variables.
2. Regard `S_k(ζ^0 | ·)` as a function of the time variables with values in
   that Banach space.
3. Record the real-domain estimate `(6.20)` coming from VI.1.
4. For `ε > 0`, define the renormalized family `S_{k,ε}` by dividing by a
   compensating factor as in `(6.21)`.
5. Use `(P_N)` and Cauchy-Schwarz to obtain the Banach-valued scalar-product
   estimate `(6.22)` on mixed domains.
6. Convert `(6.22)` into the renormalized estimate `(6.23)`.
7. Verify the purely algebraic inequalities `(6.24)`-`(6.27)` needed in that
   conversion.
8. Apply the induction hypothesis to the lower-order factors on the right-hand
   side of `(6.23)`, obtaining `(6.29)`.
9. Use the maximum principle on the envelope-of-holomorphy domain to propagate
   the estimate from the generating region to all of `C_k^(M+1)`.
10. Eliminate the dependence on `N` using Corollary 5.3 and the choice rule
    `(6.30)`.
11. Undo the renormalization to reach `(6.31)`, i.e. the final estimate
    `(4.6)`.

This is a fully closed induction. There is no hidden new analyticity theorem in
VI.2; only a carefully normalized maximum-principle argument.

### 11.1.1. Detailed derivation of `(6.22)` and `(6.23)`

The estimate `(6.22)` comes directly from `(P_N)` and the Banach-space norm:

1. `(P_N)` writes `S_k(\bar ζ, 2x, ζ')` as the scalar product
   `(Ψ_n(x,ζ), Ψ_m(x,ζ'))`.
2. Apply Cauchy-Schwarz in the Hilbert space.
3. Interpret the result as a pointwise bound in the spatial variables.
4. Take the weighted supremum over the spatial variables to obtain a bound in
   the Banach norm `‖·‖_p`.

Then `(6.23)` is obtained by inserting the renormalization factor from
`S_{k,ε}`. The only extra work is to verify that the denominator splits in a
way compatible with the decomposition `k = n + m - 1`. That is exactly the role
of `(6.24)` and `(6.25)`.

So the structure is:
- `(P_N)` gives a Hilbert-space factorization,
- the renormalization turns that factorization into a multiplicative Banach
  estimate.

### 11.1.2. Detailed role of `(6.29)`

The estimate `(6.29)` is the actual induction-closing inequality. It is
obtained by:

1. applying the induction hypothesis separately to the lower-order factors
   `S_{2n-1,ε}` and `S_{2m-1,ε}`,
2. multiplying the resulting bounds,
3. simplifying the exponents using the normalization identities already
   established.

This is the point where the lower-order information is lifted to the
next-stage domain. Everything after `(6.29)` is propagation by the maximum
principle; everything before it is algebraic preparation.

### 11.2. Why the renormalized family `S_{k,ε}` is necessary

The denominator in `(6.21)` is not a technical nuisance. It is what makes the
induction compatible with the convexity estimates `(6.24)`-`(6.27)`.

Its role is:
- compensate for the growth in the real parts of the time variables,
- make the boundary estimates multiplicative under the `(P_N)` scalar-product
  decomposition,
- and allow the lower-order induction hypothesis to close.

So if one later formalizes VI.2, the renormalized family should be treated as a
central object, not as a convenience wrapper.

### 11.3. The algebraic heart: `(6.24)`-`(6.27)`

The inequalities `(6.24)`-`(6.27)` are where the normalization is proved to be
compatible with the decomposition `k = n + m - 1`.

The paper reduces both required inequalities to the convexity of the function
`x ↦ ln x`. Concretely:

- `(6.24)` is a weighted geometric-arithmetic mean type estimate for the real
  parts of the time variables;
- `(6.25)` is the corresponding estimate for the epsilon-regularization factor;
- `(6.27)` is the common convexity template from which both follow.

Thus the later Lean proof should isolate these as explicit analytic inequalities
on positive reals, not bury them in ring/algebra manipulations inside the main
induction theorem.

### 11.4. Eliminating `N` via Corollary 5.3

The last step of VI.2 is often under-emphasized. The induction gives an
estimate on `C_k^(N)` that still visibly depends on `N`. To get the final
statement, the paper:

1. fixes a target point `ζ ∈ C_+^k`,
2. chooses an index `s` so that `|arg ζ_r| ≤ |arg ζ_s|` for all `r`,
3. chooses `N = N(ζ)` so that Corollary 5.3 guarantees `ζ ∈ C_k^(N)`,
4. inserts that choice into `(6.28)`,
5. uses the explicit bound on `γ_k` from `(5.28)`,
6. then undoes the Banach norm and the `ε`-shift.

So the final estimate `(6.31)` is not an abstract consequence of the induction.
It depends quantitatively on the explicit domain-growth control from Chapter V.

### 11.4.1. Exact recovery chain from the normalized family back to the
original distributions

The phrase "undo the renormalization" hides a real theorem package. A later
Lean port should expose the following exact recovery chain:

```lean
lemma normalized_family_agrees_with_original_on_stage
    (ζ : CkN N) (ε : ℝ) (hε : 0 < ε) :
    S_{k,ε} ζ = renormFactor k ε ζ * S_k (stageShift ε ζ)

lemma normalized_family_estimate_imported_from_VI1
    (ζ : CkN N) :
    ‖S_{k,ε} ζ‖ ≤ stageBound k N ζ

lemma stage_selection_for_target_configuration
    (ζ : CPlus k) :
    ∃ N, ζ ∈ CkN N

lemma estimate_descends_from_selected_stage
    (ζ : CPlus k) :
    ‖S_{k,ε} ζ‖ ≤ selectedStageBound k ζ

lemma epsilon_limit_exists_for_normalized_family
    (ζ : CPlus k) :
    Tendsto (fun ε : ℝ => S_{k,ε} ζ) (nhdsWithin 0 (Set.Ioi 0))
      (nhds (boundaryCandidate k ζ))

lemma epsilon_limit_matches_original_distribution
    (ζ : CPlus k) :
    Tendsto (fun ε : ℝ => renormFactor k ε ζ * S_k (stageShift ε ζ))
      (nhdsWithin 0 (Set.Ioi 0))
      (nhds (boundaryCandidate k ζ))

lemma recovered_boundary_value_is_tempered
    (k : ℕ) :
    TemperedDistributionBoundaryValue (boundaryCandidate k)

theorem vi2_temperedness_and_boundary_recovery
    (k : ℕ) :
    FinalChapterVI2Conclusion k
```

The roles are:

1. algebraic comparison with the original family,
2. import the fixed-stage estimate,
3. choose a stage using Corollary 5.3,
4. remove the visible `N`-dependence,
5. take the `ε -> 0` boundary limit,
6. identify the limit with the original distributions,
7. package the result as tempered boundary-value recovery.

Without those items, VI.2 is still only a proof sketch. With them, the later
Lean file can be organized as an explicit recovery package.

### 11.5. Why VI.2 is still tied to Chapter V

VI.2 may look like a pure estimate-propagation theorem, but it still depends on
Chapter V in two essential ways:

1. the domain `C_k^(N)` is defined through the Chapter V induction;
2. Corollary 5.3 supplies the quantitative exhaustion of the full right
   half-plane by those domains.

So the papers do not prove continuation first and then forget the continuation
domains. The estimates are literally propagated along the same domains that
were created in the continuation proof.

## 12. Appendix: `E0'' => E0'`

The appendix proves the stronger pointwise/distribution condition `E0''`
implies the linear growth condition `E0'` by Hermite expansion.

This is mostly auxiliary for the current frontier, but it reveals a useful
pattern:
- when one has strong pointwise control, Hermite coefficients convert it into
  Schwartz seminorm bounds with explicit order growth.

So if a future Lean route temporarily obtains an `E0''`-style theorem, the
appendix explains how to reconnect it to the official `E0'` surface.

### 12.1. Exact proof chain of the appendix

The appendix proves:
- pointwise/derivative control `E0''` on Schwartz test functions implies
  the linear-growth seminorm estimate `E0'`.

The proof is:

1. Expand the distribution `T` in the Hermite basis:
   `T = Σ T_i H_i`.
2. Express `T(g)` as `Σ T_i γ_i`, where `γ_i = ∫ H_i g`.
3. Apply Cauchy-Schwarz, obtaining `(A3)`.
4. Bound the Hermite coefficients `T_i` using the assumed `E0''` estimate and
   Sobolev control on Hermite functions; this yields `(A5)`.
5. Bound the coefficients `γ_i` by repeated action of the harmonic-oscillator
   operator `(1 + x^2 - d^2/dx^2)` on `g`; this yields `(A6)`.
6. Choose the exponent `s = r + 1` so that the weighted Hermite series in
   `(A3)` converges uniformly.
7. Combine the bounds to obtain `(A2)`, i.e. `E0'`.

So the appendix is really a spectral estimate for the harmonic oscillator in
disguise.

### 12.1.1. Detailed derivation of `(A3)`-`(A6)`

The appendix proof is short, but it contains three separate estimates.

First estimate: `(A3)` by Cauchy-Schwarz
1. Expand `T(g)` in the Hermite basis.
2. Insert the weights `(1+i)^s` and `(1+i)^{-s}`.
3. Apply Cauchy-Schwarz.
4. This produces two factors:
   - the coefficient factor involving `T_i`,
   - the test-function factor involving `γ_i`.

Second estimate: `(A5)` for `T_i`
1. Express `H_i` in terms of creation/annihilation operators.
2. Use Sobolev inequality to control the supremum norm of derivatives of
   Hermite functions.
3. Apply the `E0''` hypothesis `(A1)` to those derivatives.
4. Conclude a polynomial bound in the Hermite index `i`.

Third estimate: `(A6)` for `γ_i`
1. Apply the harmonic-oscillator operator repeatedly to `g`.
2. Use the fact that `H_i` is an eigenfunction of that operator.
3. Shift the powers of `(1+i)` from the Hermite basis onto derivatives of `g`.
4. Bound the result by a Schwartz seminorm `|g|_{n,t}`.

The final step is to choose `s = r+1` so that the weighted Hermite-coefficient
series converges uniformly. Then multiplying `(A5)` and `(A6)` inside `(A3)`
gives the desired estimate `(A2)`.

So the appendix really has the same pattern as VI.1:
- isolate coefficients,
- estimate one factor by structural spectral information,
- estimate the other by Schwartz seminorm control,
- then reassemble.

### 12.2. Why the appendix matters for formalization

The point of the appendix is not merely historical. It gives a robust template:

- strong pointwise/derivative control,
- orthogonal expansion,
- coefficient decay,
- then recovery of Schwartz seminorm bounds.

This pattern is exactly the kind of route one might later use in Lean if a
stronger theorem surface appears first and must be converted back to the
official `E0'` format.

## 13. What This Imposes on the Current Lean Frontier

### 13.1. Theorem-shape discipline

The live `E -> R` development should prefer the following theorem shapes:

1. one-gap semigroup matrix-element theorem with parameters,
2. local analyticity / domain-enlargement theorem,
3. VI.1 regularization-and-bound theorem,
4. boundary-value extraction theorem.

It should avoid:
- direct many-gap continuation from separate one-gap facts,
- same-test-function Euclidean/Wightman identities,
- operator-theoretic shortcuts that bypass the semigroup-to-boundary-value
  object,
- reductions that change the seam without making it more honest.

### 13.2. Meaning for the current `k = 2` / theorem-3 lane

For `k = 2`, there is only one true time-difference variable. The OS II reading
therefore suggests:
- the correct scalar object is already one-variable,
- all remaining complications are parameter packaging and matching of shell
  representatives,
- the missing theorem should look like an Input-A / fixed-time extension /
  difference-variable bridge, not like a generic many-variable continuation
  theorem.

This is exactly why the current project's `K2VI1` and route-1 language is not
off-paper. It is a Lean implementation of the OS "treat all other variables as
parameters" philosophy.

### 13.3. Meaning for theorem 3 in `OSToWightmanBoundaryValues`

The current theorem-3 seam should be read as a local version of:
- identify the correct one-variable scalar continuation object,
- prove the corresponding fixed-time common/probe or common/explicit shell
  comparison,
- then transport positivity through the already-built reduction chain.

What OS II does not support is:
- repeated abstract reductions with no closure,
- proving positivity by comparing unrelated Euclidean and Minkowski pairings on
  the same literal test function,
- using continuity/uniqueness on too large a domain without first proving the
  correct local analyticity and extension package.

### 13.4. Current theorem-3 state after the latest scratch corrections

The most current scratch state in the repo is more modest than some recent
`agents_chat` optimism suggested.

What is genuinely proved in `test/proofideas_bvt_route1_semigroup_strip.lean`:
- the explicit descended `bvt_F` kernel matches the theorem-3 semigroup shell
  on the positive strip;
- this lifts to integral identities for positive-support tests;
- the strengthened common fixed-time kernel and common zero-center section have
  the same positive-support pairings as that explicit descended kernel.

What is *not* currently proved there:
- a pointwise equality theorem on the whole positive-time strip.

Why the stronger claim was removed:
- the attempted pointwise upgrade incorrectly treated the descended absolute
  forward-tube input as if it were already defined and continuous on a domain
  larger than the reduced forward tube.

So the documentation-standard current reading is:
- the functional-level bridge is real progress;
- the pointwise strip equality is not yet part of the trusted scratch state;
- any future port-to-production plan must begin by repairing that domain issue,
  not by assuming the pointwise theorem already exists.

### 13.5. Exact theorem shape still missing for theorem 3

Combining OS II with the current repo state, the missing theorem should be read
as follows.

Input:
- a one-variable common kernel supplied by the strengthened `K2VI1` /
  `InputA*` package;
- the explicit descended `bvt_F` kernel already identified with the semigroup
  shell on positive-support pairings.

Goal:
- prove the two kernels agree on the exact local positive-time domain where the
  boundary-value reduction needs them.

Acceptable proof shapes:
1. a fixed-time / partial-boundary-value extension theorem on the common kernel,
2. a uniqueness theorem on the correct one-variable domain, once continuity and
   common pairing identities are fully established there,
3. a direct instantiation of the existing `InputAHeadBlockTransport` style
   machinery if it produces the same identification.

Unacceptable proof shapes:
- generic contour-shift slogans with unchanged test function,
- direct operator equalities `e^{-tH} = e^{itH}` or similar circular rewrites,
- further reduction lemmas that merely rename the same missing bridge.

Current local correction for the production route:

- on the present branch, the weaker fixed-time shell-limit theorem is already
  landed in `OSToWightmanPositivity.lean` as
  `section43_fixedPair_shellToSlice_limit_fixedTime`;
- so the exact remaining theorem-shaped content is not an earlier
  shell-assembly existence statement, but the direct identification of that
  same canonical shell limit with the chosen upper-half-plane witness value
  `Hs ((t : ℂ) * Complex.I)`;
- source inspection tightens this one notch further: because the only local use
  of the witness value is through `hHs_imag_os`, the inline scalar there is
  already fixed as
  `OSInnerProductTimeShiftHolomorphicValue ... (t : ℂ)`, so the missing
  content is exactly the ambient fixed-time bridge to that semigroup scalar,
  not a smaller frozen-slice object below it;
- this is why the lone remaining blocker is exactly
  `section43_fixedPair_shellToSlice_limit_upperHalfPlaneWitness_fixedTime`, and
  why no smaller honest theorem exists below it on the current route without
  wrapper drift.
- a fresh bounded compile-checked pass on 2026-04-13 did not change that
  verdict: the file still has exactly one `sorry`, and the first missing
  non-circular bridge is still the fixed-time target identity above rather than
  a smaller paper-backed theorem slot underneath it.

## 14. Recommended Operational Use

Before changing a live `E -> R` theorem surface, ask:

1. Is this a Chapter V continuation statement or a Chapter VI estimate
   statement?
2. If it is Chapter V, where is the one distinguished continuation variable and
   where are the packaged parameters?
3. If it is Chapter VI, where is the regularized object and where is the
   Hilbert-space norm estimate?
4. If neither answer is clear, stop and document the missing paper theorem slot
   before proceeding.

## 15. Bottom Line

OS II is not merely a correction of one bad lemma. It is a disciplined proof
architecture.

The main architectural rules are:
- one-variable continuation first,
- parameters fixed throughout,
- many-variable continuation only through the induction ladder,
- growth only after regularization,
- boundary values only after growth.

Those rules should be treated as binding route guidance for the Lean
formalization until a fully proved alternative theorem surface exists in the
repository.

For the implementation dependency graph extracted from these theorem-slot
inventories, see `docs/general_k_continuation_blueprint.md`.

## Appendix A. Lean-Pseudocode Skeletons for OS II

This appendix is deliberately implementation-shaped. The statements below are
not intended to compile as-is, but they are meant to be close enough to the
actual mathematical proof that later Lean porting becomes straightforward.

They should be read as theorem-slot inventories, not as optional sketches. Each
slot corresponds to a mathematically real proof obligation in the paper.

### A.1. Local analyticity radius from Chapter V.1

Mathematical content:
- fix a real positive-time point `ξ`;
- choose the cone angle `γ` and dual vectors `e_μ`;
- obtain an explicit polydisc `Γ(ξ)` from Lemma 5.1.

Lean pseudocode:

```lean
/-- OS II Lemma 5.1: quantitative local polydisc of analyticity. -/
theorem local_polydisc_of_real_positive_time
    (ξ : TimeDiffConfig k)
    (hξ : ξ ∈ positiveRealTimeRegion) :
    ∃ ρ > 0,
      IsHolomorphicOn
        (fun ζ => S_k (ξ + ζ))
        (polydisc (0 : TimeDiffConfig k) ρ) := by
  -- 1. choose γ from the ratio min ξ_i^0 / |ξ_i|
  -- 2. in the paper, build e_1,...,e_4 in the dual cone;
  --    for the repo's general-`d` implementation, use d+1 dual-cone vectors
  -- 3. use directional one-variable continuation in each e_μ direction
  -- 4. glue by Malgrange-Zerner
  -- 5. extract the explicit radius from the coordinate inequalities
```

Local sublemmas later Lean will need:
- `cone_angle_from_positive_time`,
- `dual_cone_basis_exists`,
- `directional_semigroup_continuation`,
- `malgrange_zerner_glue_flat_tubes`,
- `explicit_radius_from_coordinate_estimates`.

More explicit theorem-slot inventory for A.1:

```lean
lemma cone_angle_from_positive_time
    (ξ : TimeDiffConfig k)
    (hξ : ξ ∈ positiveRealTimeRegion) :
    ∃ γ > 0, admissibleConeAngle ξ γ := by
  -- choose `γ` so that each positive real time component of `ξ` dominates the
  -- allowed angular variation

lemma dual_cone_basis_exists
    (hγ : admissibleConeAngle ξ γ) :
    ∃ e : Fin (d + 1) → TimeDiffConfig k,
      dualConeBasisFor hγ e := by
  -- this is the finite-direction geometry behind `(5.10)`-`(5.14)`

lemma directional_semigroup_continuation
    (hξ : ξ ∈ positiveRealTimeRegion)
    (he : dualConeDirection ξ γ eμ) :
    IsHolomorphicOn
      (fun u : ℂ => S_k (ξ + u • eμ))
      rightHalfPlane := by
  -- apply the OS I one-gap semigroup theorem in the chosen direction

lemma malgrange_zerner_glue_directional_pieces
    (hdir : ∀ μ, directionalContinuationData μ) :
    ∃ F, IsHolomorphicOn F (localTubeDomain ξ γ) := by
  -- glue the flat-tube continuations on overlaps by Malgrange-Zerner

lemma explicit_radius_from_coordinate_estimates
    (hF : IsHolomorphicOn F (localTubeDomain ξ γ)) :
    ∃ ρ > 0, polydisc (0 : TimeDiffConfig k) ρ ⊆ localTubeDomain ξ γ := by
  -- extract the quantitative radius from the coordinate inequalities in Lemma
  -- 5.1
```

These five theorem slots are the exact proof skeleton of Lemma 5.1. The
general A.1 theorem should be regarded as a thin wrapper around them.

### A.2. The `(A_N) -> (P_N)` vector reconstruction step

Mathematical content:
- choose a real basepoint `ξ` and a polydisc inside `D_n^(N)`;
- define the vector-valued Taylor series;
- prove norm convergence via scalar-product remainders.

Lean pseudocode:

```lean
/-- OS II `(A_N) -> (P_N)`: reconstruct a Hilbert-valued analytic vector
from scalar analyticity on a mixed domain. -/
theorem hilbert_vector_from_scalar_taylor
    (hA : ScalarContinuationOn DnN)
    (x : ℝ)
    (ζ : TimeVector (n - 1))
    (hxζ : (x, ζ) ∈ DnN) :
    ∃ Ψ : HilbertSpaceVector,
      HasStrongTaylorExpansionAt
        (fun w => PsiField n x w)
        Ψ ζ := by
  -- 1. choose a real point ξ and radii r_i with closed polydisc ⊆ DnN
  -- 2. define the formal Taylor series in Hilbert space
  -- 3. compute the squared norm of the remainder via scalar products
  -- 4. identify that scalar remainder with a scalar Taylor remainder
  -- 5. use scalar analyticity to conclude norm convergence
```

The key hidden theorem to formalize is the remainder identity:

```lean
lemma hilbert_remainder_norm_sq_eq_scalar_remainder
    (ξ : TimeVector (n - 1))
    (r : MultiRadius (n - 1))
    (hpolydisc : closedPolydisc ξ r ⊆ DnN)
    (N : ℕ) :
    ‖vectorTaylorRemainder (Ψn := Ψ_n) ξ r N‖^2 =
      scalarTaylorRemainder
        (fun z => S_(2 * n - 1) (-conj z, 2 * x, z)) ξ r N := by
  -- expand the squared norm as an inner product of remainder vectors,
  -- replace all scalar products by `(5.17)`,
  -- observe that the resulting double sum is exactly the scalar Taylor
  -- remainder of `S_{2n-1}(-\bar ζ, 2x, ζ)`
```

Without that lemma, `(A_N) -> (P_N)` is not a proof, only a slogan.

Additional theorem-slot inventory for A.2:

```lean
lemma real_polydisc_inside_mixed_domain
    (hxζ : (x, ζ) ∈ DnN) :
    ∃ ξ r, closedPolydisc ξ r ⊆ DnN := by
  -- choose the real basepoint and coordinate radii used in the paper

lemma scalar_derivatives_control_hilbert_derivatives
    (hA : ScalarContinuationOn DnN)
    (hpolydisc : closedPolydisc ξ r ⊆ DnN) :
    ∀ α, derivativeScalarProductFormula α := by
  -- differentiate `(5.17)` at the real basepoint

lemma vector_taylor_series_norm_converges
    (hA : ScalarContinuationOn DnN)
    (hrem :
      ∀ N, ‖vectorTaylorRemainder (Ψn := Ψ_n) ξ r N‖^2 =
        scalarTaylorRemainder (fun z => S_(2 * n - 1) (-conj z, 2 * x, z)) ξ r N) :
    ∃ Ψ, HasStrongTaylorExpansionAt (fun w => PsiField n x w) Ψ ζ := by
  -- use scalar analyticity of `S_{2n-1}` to make the scalar remainder tend to
  -- zero, hence the vector remainder tends to zero in norm
```

This is the exact mathematical content of `(A_N) -> (P_N)`: one geometric
polydisc lemma, one differentiated scalar-product identity, and one norm
convergence theorem.

### A.3. The `(P_N) -> (A_{N+1})` scalar-product continuation step

Mathematical content:
- form scalar products of the already-continued `Ψ`-vectors;
- read them as values of `S_k` on a union of tube-like sets;
- extend to the envelope of holomorphy, using the SCV/tube-domain input
  hidden in the paper's definition of `Ck^(N+1)`.

Lean pseudocode:

```lean
/-- OS II `(P_N) -> (A_{N+1})`: scalar continuation from Hilbert vectors. -/
theorem scalar_continuation_from_hilbert_vectors
    (hP : VectorContinuationOn DnN)
    (k : ℕ) :
    ScalarContinuationOn (Ck (N + 1)) := by
  -- 1. define the scalar candidate by
  --    `(Ψ_n(x, ζ), Ψ_m(x', ζ'))` on the mixed domain with `k = n + m - 1`
  -- 2. show agreement with the old real slice
  -- 3. obtain analyticity on the generating union of mixed domains
  -- 4. extend to the envelope of holomorphy using the tube-domain envelope
  --    theorem packaged into the domain definition
```

Explicit theorem-slot inventory for A.3:

```lean
lemma scalar_candidate_from_vector_pairing
    (hP : VectorContinuationOn DnN)
    (hnm : k = n + m - 1) :
    ∃ Fnm,
      ∀ (x ζ x' ζ'),
        Fnm (x, ζ, x', ζ') = inner (Ψ_n x ζ) (Ψ_m x' ζ') := by
  -- define the scalar candidate on the mixed-domain generators

lemma scalar_candidate_agrees_on_real_slice
    (hP : VectorContinuationOn DnN)
    (hFnm : scalarCandidateData Fnm) :
    Fnm.restrictToRealSlice = oldSchwingerData := by
  -- this is the real-slice content of `(5.17)`

lemma scalar_candidate_holomorphic_on_generating_union
    (hP : VectorContinuationOn DnN)
    (hFnm : scalarCandidateData Fnm) :
    IsHolomorphicOn Fnm (generatingMixedUnion (N := N) (k := k)) := by
  -- holomorphy in each vector block gives holomorphy on the generating union

lemma scalar_candidate_extends_to_envelope
    (hFnm_holo : IsHolomorphicOn Fnm (generatingMixedUnion (N := N) (k := k))) :
    ∃ Fk, IsHolomorphicOn Fk (Ck (N + 1)) := by
  -- use the tube-domain / logarithmic-tube envelope theorem behind the
  -- definition of `Ck^(N+1)`

lemma generating_union_has_required_envelope
    :
    EnvelopeOfHolomorphy (generatingMixedUnion (N := N) (k := k)) (Ck (N + 1)) := by
  -- this is the SCV input hidden in the paper's domain definition
```

This is the exact proof structure of `(P_N) -> (A_{N+1})`: define the scalar
candidate, identify its real slice, prove holomorphy on the generating union,
then invoke the envelope definition.

The important point is that this is not a free abstract SCV step. The domains
here are tube / logarithmic-tube type domains, so the later Lean port should
either import the corresponding Bochner-style envelope theorem or package the
domains so that the envelope extension is already part of their definition.

### A.4. Chapter VI.1 regularization package

Mathematical content:
- construct `g_ρ`, `k_ρ`, and `T_k`;
- bound `S_k` by a supremum of `T_k`;
- rewrite `T_k` as a scalar product.

Lean pseudocode:

```lean
/-- Complexified Euclidean spacetime in general dimension `d+1`. -/
abbrev ComplexSpacetime (d : ℕ) := Fin (d + 1) → ℂ

/-- `k` complexified spacetime blocks. -/
abbrev ComplexSpacetimeBlocks (d k : ℕ) := Fin k → Fin (d + 1) → ℂ

/-- Auxiliary radial mollifier used in OS II VI.1. -/
def radialMollifier (ρ : ℝ) : ComplexSpacetime d → ℝ :=
  -- `g_ρ(z) = c ρ^{-2(d+1)} h_ρ(|z|)` where the radial scaling is fixed so
  -- that:
  -- 1. `supp g_ρ ⊆ {|z| < ρ/8}`,
  -- 2. the convolution kernel `k_ρ = g_ρ * g_ρ` satisfies
  --    `supp k_ρ ⊆ {|z| < ρ/4}`,
  -- 3. the normalization from `(6.2)` holds in complex dimension `d+1`
  scaledRadialBump ρ

/-- Convolution kernel `k_ρ = g_ρ * g_ρ`. -/
def regularizingKernel (ρ : ℝ) : ComplexSpacetime d → ℝ :=
  convolutionKernelFrom radialMollifier ρ

/-- The regularized analytic object `T_k`. -/
def regularizedSchwinger
    (ρ : ℝ) (ξ : TimeDiffConfig k) : ComplexSpacetimeBlocks d k → ℂ :=
  -- the `T_k` of `(6.5)`: integrate the shifted Schwinger function against the
  -- product kernel built from `g_ρ` and `k_ρ`
  regularizedSchwingerIntegral ρ ξ

theorem schwinger_eq_average_of_regularized
    (hρ : 0 < ρ)
    (hζ : ζ ∈ smallPolydisc ρ) :
    S_k (ξ + ζ) =
      ∬ regularizedSchwinger ρ ξ (ζ + i * y')
          ∂μ := by
  -- mean-value theorem + support of the kernels + Fubini

theorem abs_schwinger_le_sup_regularized
    (hρ : 0 < ρ)
    (hζ : ζ ∈ smallPolydisc ρ) :
    ‖S_k (ξ + ζ)‖ ≤
      supOn (smallImaginaryBox ρ) (fun y => ‖regularizedSchwinger ρ ξ (i * y)‖) := by
  -- rewrite `S_k` as the double average from `(6.6)`,
  -- take absolute values,
  -- dominate the average by the supremum of the regularized kernel on the
  -- support box from `(6.7)`

theorem regularized_eq_hilbert_pairing
    (hρ : 0 < ρ)
    (hξ : ξ ∈ positiveRealTimeRegion)
    (hdir : μ ∈ Fin 4) :
    regularizedSchwinger ρ ξ (u • e_μ) =
      inner Ψ₁ Ψ₂ := by
  -- choose the vectors `Ψ₁`, `Ψ₂` exactly as in `(6.10)`,
  -- use Euclidean covariance to rewrite the regularized integral,
  -- identify the resulting expression with the OS Hilbert-space scalar product
```

Explicit theorem-slot inventory for A.4:

```lean
lemma radial_bump_exists_with_osii_normalization :
    ∃ h : ℝ → ℝ,
      radialBumpData h := by
  -- this is the input behind `(6.2)`

lemma regularizingKernel_support_and_mass
    (hρ : 0 < ρ) :
    supportAndMassData (regularizingKernel (d := d) ρ) := by
  -- compact support and total mass 1 of `k_ρ`

lemma schwinger_mean_value_identity
    (hρ : 0 < ρ)
    (hζ : ζ ∈ smallPolydisc ρ) :
    S_k (ξ + ζ) =
      doubleAverageOfRegularizedKernel (regularizedSchwinger (d := d) ρ ξ) ζ := by
  -- this is `(6.6)` after the mean-value and Fubini manipulations

lemma regularized_pairing_vectors_exist
    (hρ : 0 < ρ)
    (hξ : ξ ∈ positiveRealTimeRegion)
    (hdir : μ ∈ Fin 4) :
    ∃ Ψ₁ Ψ₂,
      regularizedSchwinger (d := d) ρ ξ (u • e_μ) = inner Ψ₁ Ψ₂ := by
  -- explicit OS Hilbert-space vectors from `(6.10)`
```

So A.4 really has four independent proof packages:
1. build the radial bump,
2. prove the support/mass bookkeeping,
3. derive the mean-value / average identity,
4. convert the regularized kernel to an OS Hilbert pairing.

### A.5. Chapter VI.1 norm estimate and propagation

Mathematical content:
- estimate the Hilbert vectors via `E0'`;
- continue in finitely many directions;
- propagate bounds by maximum principle.

Lean pseudocode:

```lean
theorem regularized_hilbert_norm_bound
    (hE0' : LinearGrowthCondition)
    (hρ : 0 < ρ)
    (hξ : ξ ∈ positiveRealTimeRegion) :
    ‖Ψ₁‖ * ‖Ψ₂‖ ≤
      C k * (1 + euclideanSize ξ)^M * ρ^(-M') := by
  -- geometric estimate (6.9) + `E0'`

theorem directional_bound_for_regularized
    (hE0' : LinearGrowthCondition)
    (hρ : 0 < ρ)
    (hξ : ξ ∈ positiveRealTimeRegion) :
    ∀ u ≥ 0, ‖regularizedSchwinger ρ ξ (u • e_μ)‖ ≤ bound ξ ρ := by
  -- semigroup matrix-element bound

theorem local_polydisc_bound_from_directional_bounds
    (hρ : 0 < ρ)
    (hξ : ξ ∈ positiveRealTimeRegion)
    (hdir : ∀ μ, directionalBound μ) :
    ∀ ζ ∈ smallPolydisc ρ, ‖regularizedSchwinger ρ ξ ζ‖ ≤ bound ξ ρ := by
  -- continue in each of the four dual-cone directions,
  -- glue the directional functions by Malgrange-Zerner,
  -- transfer the directional bounds to the full polydisc by the maximum
  -- principle on the envelope of holomorphy

theorem real_domain_temperedness_estimate
    (hE0' : LinearGrowthCondition) :
    ∃ C M, ∀ ξ ∈ positiveRealTimeRegion,
      ‖S_k ξ‖ ≤ C k * (1 + euclideanSize ξ)^M := by
  -- combine `(6.6)`-`(6.7)` with the bound on the regularized kernel,
  -- then rewrite the resulting estimate into the polynomial-growth format
  -- `(4.5)`
```

Explicit theorem-slot inventory for A.5:

```lean
lemma regularizer_support_bounds
    (hρ : 0 < ρ) :
    supportBoundsForRegularizingVectors ρ := by
  -- this is the geometric content of `(6.17)` and `(6.18)`

lemma regularizer_derivative_bounds
    (hρ : 0 < ρ) :
    derivativeBoundsForRegularizers ρ := by
  -- this is the `C_0^∞` control from `(6.2)` and `(6.3)`

lemma e0prime_norm_bound_for_regularized_vectors
    (hE0' : LinearGrowthCondition)
    (hρ : 0 < ρ)
    (hξ : ξ ∈ positiveRealTimeRegion) :
    regularizedVectorNormBound ρ ξ := by
  -- apply `E0'` to the explicit test functions appearing in `(6.17)`

lemma directional_regularized_bound
    (hE0' : LinearGrowthCondition)
    (hρ : 0 < ρ)
    (hξ : ξ ∈ positiveRealTimeRegion) :
    ∀ μ, directionalBoundForRegularizedKernel μ := by
  -- combine the semigroup matrix-element estimate with the norm bounds

lemma local_bound_from_directional_bounds
    (hdir : ∀ μ, directionalBoundForRegularizedKernel μ) :
    localRegularizedKernelBound := by
  -- this is `(6.13)`-`(6.15)` plus the logarithmic-coordinate / maximum
  -- principle step

lemma real_domain_estimate_from_local_bound
    (hlocal : localRegularizedKernelBound) :
    realDomainTemperedEstimate := by
  -- this is `(6.7)` plus the coordinate unrolling yielding `(6.16)`
```

This is the exact structure of VI.1:
1. support bounds,
2. derivative bounds,
3. `E0'` norm estimate,
4. directional regularized bounds,
5. glued local bound,
6. real-domain estimate.

### A.5.1. Exact dependency chain for `(6.12)`-`(6.18)`

The later Lean port should treat the displayed VI.1 inequalities as a genuine
dependency chain rather than as one large estimate:

```lean
lemma support_bound_implies_test_function_localization
lemma localization_implies_E0prime_applicable
lemma E0prime_applied_to_regularized_test_functions
lemma regularized_vector_norm_bound_from_E0prime
lemma scalar_pairing_bound_from_vector_norms
lemma directional_bound_from_scalar_pairing_bound
lemma local_polydisc_bound_from_directional_maximum_principle
lemma real_domain_bound_from_mean_value_identity
```

The mathematical order is:

1. `(6.17)`-`(6.18)` localize the explicit test functions;
2. localization allows the `E0'` seminorm estimate;
3. `E0'` bounds `‖Ψ₁‖` and `‖Ψ₂‖`;
4. Cauchy-Schwarz bounds the scalar pairing `T_k`;
5. directional bounds feed the maximum-principle step;
6. the mean-value identity converts the regularized bound into the raw bound.

If a later Lean proof skips any one of those arrows, the docs should be
updated first rather than letting the hidden step reappear inside a large proof
term.

### A.6. The current theorem-3 lesson extracted from OS II

For the repo's current `k = 2` frontier, the paper suggests the following
implementation-shaped target:

```lean
/-- Domain used by the current theorem-3 pairing theorems. -/
def positiveTimeRegion : Set (Spacetime d) := {ξ | 0 < ξ 0}

/-- Desired theorem shape for the remaining theorem-3 bridge. -/
theorem k2_fixed_time_boundary_value_bridge
    (commonKernel explicitKernel : Spacetime d → ℂ)
    (hcommon_cont : ContinuousOn commonKernel positiveTimeRegion)
    (hexplicit_cont : ContinuousOn explicitKernel positiveTimeRegion)
    (hpair :
      ∀ h : SchwartzSpacetime d,
        HasCompactSupport (h : Spacetime d → ℂ) →
        tsupport (h : Spacetime d → ℂ) ⊆ positiveTimeRegion →
          ∫ ξ, commonKernel ξ * h ξ =
          ∫ ξ, explicitKernel ξ * h ξ) :
    EqOn commonKernel explicitKernel positiveTimeRegion := by
  -- apply
  -- `eqOn_positiveTime_of_compactSupport_schwartz_integral_eq_continuousOn_local`
  -- on the open set `{ξ | 0 < ξ 0}` after proving:
  -- 1. both kernels are restrictions of genuinely defined strip kernels,
  -- 2. both are continuous there,
  -- 3. the pairing theorem quantifies over all compactly supported tests in
  --    `{ξ | 0 < ξ 0}` and not merely over one shell family
```

The note above is intentionally conditional. At the time of writing, the repo
does not yet have all those hypotheses in trusted form for the current common
kernel / descended-`bvt_F` comparison. That is exactly the remaining theorem-3
work.

### A.7. Bounded fixed-time bridge audit status

The current repo-local fixed-time bridge was re-audited source-first against
the directly relevant Wick-rotation files only.

What was checked:

- in `OSToWightmanPositivity.lean`:
  `section43_fixedPair_shellToSlice_limit_fixedTime`,
  `section43_fixedPair_upperHalfPlaneScalarization`,
  `rightHalfPlaneWitness_of_upperHalfPlaneScalarization`,
  `lemma42_matrix_element_time_interchange`,
  `one_variable_time_interchange_for_wightman_pairing`,
  `bvt_xiShift_eq_osInnerProduct_holomorphicValue_single`;
- in sibling Wick-rotation files:
  `exists_singleSplit_xiShift_holomorphicValue`,
  `bvt_exists_singleSplit_xiShift_holomorphicValue`,
  `bvt_exists_singleSplit_xiShift_holomorphicValue_with_limit`,
  and the surviving comparison-layer notes in
  `OSToWightmanBoundaryValuesComparison.lean`.

Why they are insufficient:

1. the ambient canonical-shell limit theorem already lands, but only with
   target `bvt_W ... (φ.conjTensorProduct (timeShiftSchwartzNPoint ... t ψ))`;
2. the semigroup-side scalarization already lands, but only with target
   `OSInnerProductTimeShiftHolomorphicValue ... (t : ℂ)`;
3. the later uniqueness/consumer theorems assume exactly this positive-real
   bridge, so using them here would be circular;
4. the `singleSplit` witness theorems live on the preimage shell
   `f.osConjTensorProduct g` and do not identify the ambient canonical shell
   for `(φ, ψ, hφf, hψg)` with that witness.

Therefore the OS-II-style continuation package is still missing exactly the
first transport-backed fixed-time bridge between those two already-landed
targets; no smaller honest theorem surfaced below that seam on this bounded
audit.

Verification-state note (2026-04-13 16:25 UTC, rechecked later the same day):
the earlier recorded semigroup/import failure was transient or stale relative
to the present working tree. The exact file check
`timeout 60s lake env lean
OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`
now succeeds, and source inspection confirms that
`selfAdjointSpectralBoundaryValueOffdiag` and
`tendsto_rotated_OSInnerProductTimeShiftHolomorphicValue_boundaryValue_fourierTransform`
are exported at top level from `OSToWightmanSemigroup.lean`. So the current
frontier is again purely mathematical.

Latest source-backed refinement from the current dirty tree:

- the live production blocker remains
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`, but a
  direct bounded pass inside that theorem body has now reduced it honestly to a
  single local subclaim `hlimit_os` plus a final `tendsto_nhds_unique` step;
- concretely, `hlimit_w` is already discharged in the body by
  `tendsto_bvt_F_canonical_xiShift_conjTensorProduct_timeShift_boundaryValue`,
  so the first remaining mathematics is the same-shell fixed-time limit from
  the canonical boundary-value shell to
  `OSInnerProductTimeShiftHolomorphicValue ... (t : ℂ)`;
- source-first inspection shows the branch already contains the no-compact
  witness constructor
  `exists_bvt_W_conjTensorProduct_timeShift_temperedFunctional_noCompact_psiZ`
  and the horizontal-kernel evaluation theorem
  `ambient_noCompact_witness_apply_horizontalPsiZ_fixedPair`, but both still
  stop short of a public transport-backed theorem that discharges `hlimit_os`
  from the ambient fixed-pair data `(φ, ψ, f, g, hφf, hψg)`;
- the older diagnosis that the first honest missing statement was simply the
  ambient descended `ψ_{2πit}` comparison is therefore now too coarse: that is
  still the likely mathematical content, but the exact surfaced frontier is the
  local shell-to-OS limit `hlimit_os`, and any lower theorem-sized split must
  be justified as the first *public* ingredient strictly beneath that local
  subclaim;
- the compact-support-free slice chain
  `fourierInvPairingCLM_partialFourierSpatial_timeSlice_eq_of_repr_eq_transport`,
  `fourierInvPairingCLM_opposite_partialFourierSpatial_timeSlice_eq_of_repr_eq_transport`,
  and `section43_iteratedSlice_descendedPairing`
  still only gives frozen slice equalities indexed by `(r, t, ht, ξ, w)`;
- source-first inspection now sharpens the exact first lower ingredient
  strictly beneath that frozen public slice chain: the private theorem
  `partialFourierSpatial_timeSlice_eqOn_nonneg_of_repr_eq_transport`, because
  the first public frozen-slice edge
  `section43PositiveEnergyQuotientMap1D_partialFourierSpatial_timeSlice_eq_of_repr_eq_transport`
  is just `section43PositiveEnergyQuotientMap1D_eq_of_eqOn_nonneg` applied to
  that `Set.EqOn` statement on `[0,∞)`;
- the canonical fixed-time parameter identification `(r, tSlice, ht, ξ)` is
  now already landed locally via
  `section43_fixedPair_nonnegTimeCoords_of_mem_tsupport_conjTensorProduct`,
  `section43_fixedPair_nonnegAwayFrom_r_of_mem_tsupport_conjTensorProduct`, and
  `section43_fixedPair_canonicalShell_sliceRealization`;
- the sharpened obstruction is one category lower: the existing frozen-slice
  transport entry points still require a total-arity positive-time source
  object routed through `os1TransportComponent` /
  `EuclideanPositiveTimeComponent`, but the actual tensor
  `f.conjTensorProduct g` does not satisfy that source geometry;
- source-first, the left block is reversed by `conjTensorProduct`, and the
  theorem
  `section43_fixedPair_conjTensorProduct_not_mem_orderedPositiveTimeRegion_of_leftBlockPair`
  rules out the tempting package
  `tsupport (f.conjTensorProduct g) ⊆ OrderedPositiveTimeRegion d (n + m)`;
- therefore the honest next OS-II continuation task was no longer to keep
  splitting `hlimit_os` inside `OSToWightmanPositivity.lean`, but to build the
  exact mixed-order total-arity source surface in
  `EuclideanPositiveTime.lean`; that source seam is now landed via
  `FixedPairMixedOrderTimeRegion`, `FixedPairMixedOrderComponent`,
  `conjTensorProduct_tsupport_subset_fixedPairMixedOrderTimeRegion`, and
  `FixedPairMixedOrderComponent.ofConjTensorProduct`;
- the new live lower blocker is now the corresponding mixed-order transport
  theorem that can feed the frozen-slice chain without wrapper drift.
- a fresh source-first audit on 2026-04-14 03:30 UTC tightens that diagnosis one
  notch further. The mixed-order transport chain is already landed on-branch:
  `partialFourierSpatial_timeSlice_eqOn_nonneg_of_repr_eq_fixedPairMixedOrder`,
  `section43PositiveEnergyQuotientMap1D_partialFourierSpatial_timeSlice_eq_of_repr_eq_fixedPairMixedOrder`,
  `fourierInvPairingCLM_partialFourierSpatial_timeSlice_eq_of_repr_eq_fixedPairMixedOrder`,
  `fourierInvPairingCLM_opposite_partialFourierSpatial_timeSlice_eq_of_repr_eq_transport`,
  and `section43_iteratedSlice_descendedPairing`.
- the exact remaining lower seam is therefore the pointwise fixed-time
  shell-to-slice scalarization beneath the local subclaim `hlimit_os`, i.e.
  the intended private theorem
  `section43_fixedPair_shellToSlice_integrand_descendedScalar_fixedTime`
  in `OSToWightmanPositivity.lean`; the later theorem
  `section43_fixedPair_shellToSlice_assembledScalar_fixedTime` is downstream
  shell aggregation, not the first blocker anymore.
- the mismatch is now explicitly representation/assembly rather than geometry:
  `hshellToSlice` only gives a pointwise identification of the ambient
  canonical shell with a frozen one-variable slice of
  `φ.conjTensorProduct ψ`, while the landed mixed-order transport chain only
  compares already-frozen slice tests and their descended scalar pairings after
  routing through `FixedPairMixedOrderComponent.ofConjTensorProduct f hf_ord g
  hg_ord`.
- the first exact lower obligation is therefore the pointwise-in-`y`
  integrand scalarization inserted immediately after the local
  `hshellToSlice` block inside
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`:
  for fixed `t > 0` and `ε > 0`, rewrite the shell integrand
  `y ↦ bvt_F ... (xiShift ... (t : ℂ))`
  as the descended slice scalar obtained by composing
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
  `section43PositiveEnergyQuotientMap1D_partialFourierSpatial_timeSlice_eq_of_repr_eq_fixedPairMixedOrder`,
  `fourierInvPairingCLM_partialFourierSpatial_timeSlice_eq_of_repr_eq_fixedPairMixedOrder`,
  `fourierInvPairingCLM_opposite_partialFourierSpatial_timeSlice_eq_of_repr_eq_transport`,
  and `section43_iteratedSlice_descendedPairing` on the canonical frozen slice
  parameters coming from
  `section43_fixedPair_canonicalShell_sliceRealization`.
- the exact local insertion point is the current `sorry` proving
  `hlimit_os` in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`:
  after
  `have hshellToSlice := ...`,
  but before the final
  `exact tendsto_nhds_unique hlimit_w hlimit_os`.
  That is where the missing pointwise theorem
  `section43_fixedPair_shellToSlice_integrand_descendedScalar_fixedTime`
  must be called before any later fixed-`ε` shell assembly.

Update after live source recheck (2026-04-14 08:10 UTC): that diagnosis is not
source-correct for the current workspace. The declaration
`section43_fixedPair_shellToSlice_integrand_descendedScalar_fixedTime` is not
present in
`OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`;
only comment/doc references to the name remain. The frontier file still
compile-checks with `EXIT=0` and the known single production `sorry` in
`section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`.

So the active obstruction first appeared to move back up one notch: the first
exact remaining lower obligation looked like the theorem insertion itself,
under that exact name and planned statement shape, between the already-landed
feeder theorems
`section43_fixedPair_mixedOrderSlice_weightedRawScalarKernel_fixedTime` and
`section43_fixedPair_mixedOrderSliceCanonicalExtension_descendedScalar_fixedTime`
and the live `hlimit_os` seam. But the latest source-first recheck sharpens the
frontier again: even that provisional theorem is slightly too high as the first
missing source object.

What is actually missing first on the current branch is the fixed-parameter
one-variable opposite-pairing bridge at
`w := ((y r 0 + t : ℂ) + ε * Complex.I)` connecting the descended scalar
produced by
`section43_fixedPair_mixedOrderSliceCanonicalExtension_descendedScalar_fixedTime`
to the explicit `ψ_{2πw}` kernel pairing on the same mixed-order slice. In
other words, the live missing step is the identification

```lean
OSReconstruction.fourierPairingDescendsToSection43PositiveEnergy1D ...
  (section43PositiveEnergyQuotientMap1D
    (SCV.schwartzPsiZ (((2 * Real.pi : ℂ) * w)) ...))
```

with the opposite pairing

```lean
fourierInvPairingCLM
  (SCV.schwartzPsiZ (((2 * Real.pi : ℂ) * w)) ...)
  ((SchwartzMap.fourierTransformCLM ℂ)
    (partialFourierSpatial_timeSliceSchwartz ... F.1 r (tSlice y) (ξ y)))
```

at those frozen parameters.

Once that exact bridge is in hand, `SCV.psiZ_twoPi_pairing_formula` plus
`FourierTransform.fourierInv_fourier_eq` gives the true raw scalar shape on the
branch,

```lean
Complex.exp (-(2 * Real.pi * ε : ℂ) * (y r 0 + t)) *
  ((partialFourierSpatial_timeSliceSchwartz ... F.1 r (tSlice y) (ξ y)
    : SchwartzMap ℝ ℂ) (y r 0 + t)),
```

with the cutoff simplifying to `1` because `y r 0 + t ≥ 0` on support. So the
stale would-be equality with unchanged raw evaluation was not merely unproved;
it had the wrong scalar normalization for `ε > 0`.
- stated source-first, the theorem shape still missing there is:

```lean
private theorem section43_fixedPair_shellToSlice_assembledScalar_fixedTime
    ... (t : ℝ) (ht : 0 < t) :
    let r : Fin (n + m) := ⟨n, Nat.lt_add_of_pos_right hm⟩
    let shell_t : ℝ → ℂ := fun ε =>
      ∫ y : NPointDomain d (n + m),
        bvt_F OS lgc (n + m)
          (xiShift r 0
            (fun k μ =>
              ↑(y k μ) +
                ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                  Complex.I)
            (t : ℂ)) *
          (φ.conjTensorProduct ψ) y
    ∀ ε : ℝ, 0 < ε →
      shell_t ε =
        scalar_tε
```

  where `scalar_tε` is not an existential placeholder, but the exact
  descended/canonical-slice value at the frozen parameters
  `(r, tSlice y, ht, ξ y)` supplied by
  `section43_fixedPair_canonicalShell_sliceRealization`.
- a sharper source-first diagnosis from the theorem body is that even this
  `y`-parametrized descended-scalar bridge is still one notch too high on the
  current branch. The local fact `hshellToSlice` produced by
  `section43_fixedPair_canonicalShell_sliceRealization`
  does **not** rewrite the shell integrand to a canonical-extension value or a
  descended scalar; it only rewrites the ambient shell geometry to the frozen
  raw slice evaluation
  `partialFourierSpatial_timeSliceSchwartz ... (y r 0 + t)`.
- the fixed-time specialization of that private transport bridge has now
  landed as
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime`. Its statement
  shape is:

```lean
private theorem section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime
    ... (t ε : ℝ) (ht : 0 < t) :
    let r : Fin (n + m) := ⟨n, Nat.lt_add_of_pos_right hm⟩
    let tSlice : NPointDomain d (n + m) → Fin (n + m) → ℝ := ...
    let ξ : NPointDomain d (n + m) →
      EuclideanSpace ℝ (Fin (n + m) × Fin d) := ...
    ∀ y : NPointDomain d (n + m),
      y ∈ tsupport
        (((f.conjTensorProduct g : SchwartzNPoint d (n + m)) :
          NPointDomain d (n + m) → ℂ)) →
      ((partialFourierSpatial_timeSliceSchwartz (d := d) (n := n + m)
          (φ.conjTensorProduct ψ) r (tSlice y) (ξ y) : SchwartzMap ℝ ℂ)
          (y r 0 + t)) =
      ((partialFourierSpatial_timeSliceSchwartz (d := d) (n := n + m)
          (OSReconstruction.FixedPairMixedOrderComponent.ofConjTensorProduct
            (d := d) (n := n) (m := m) f hf_ord g hg_ord).1
          r (tSlice y) (ξ y) : SchwartzMap ℝ ℂ)
          (y r 0 + t))
```

  obtained by composing
  `section43_fixedPair_canonicalShell_sliceRealization`,
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
  `FixedPairMixedOrderComponent.ofConjTensorProduct`,
  and
  `partialFourierSpatial_timeSlice_eqOn_nonneg_of_repr_eq_fixedPairMixedOrder`.
- the exact insertion point is unchanged:
  immediately after `have hshellToSlice := ...` inside the proof of `hlimit_os`
  in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`.
  There this theorem is now the direct transport rewrite from the ambient
  slice to the mixed-order source slice.
- the branch has now progressed one notch further on the pairing side: the
  honest lower theorem
  `section43_fixedPair_mixedOrderSliceCanonicalExtension_descendedScalar_fixedTime`
  is landed in `OSToWightmanPositivity.lean`.
  It packages exactly the source-backed corollary obtained from
  `SCV.fourierLaplaceExt_eq` together with
  `OSReconstruction.fourierPairingDescendsToSection43PositiveEnergy1D_apply`:
  once the mixed-order frozen slice is already expressed by its canonical
  extension at
  `w := ((y r 0 + t : ℂ) + ε * Complex.I)`, the corresponding descended
  `ψ_Z` scalar follows immediately.
- an attempted refinement through
  `section43_fixedPair_mixedOrderSlice_eval_eq_canonicalExtension_fixedTime`
  is not source-correct on the present branch:
  `partialFourierSpatial_timeSliceCanonicalExtension` is defined for an
  `EuclideanPositiveTimeComponent`, whereas the mixed-order source data below
  `hsliceTransport` only provide the one-variable test object
  `partialFourierSpatial_timeSliceTest_fixedPairMixedOrder`;
- more sharply, the theorem
  `partialFourierSpatial_timeSliceCanonicalExtension_eq_complexLaplaceTransform`
  shows that the canonical extension is a Fourier-Laplace / Laplace-transform
  object on the positive imaginary axis, not an analytic continuation of the
  raw point-evaluation map
  `s ↦ ((partialFourierSpatial_timeSliceSchwartz ...) s)`;
- so the first exact remaining lower obligation is **not** honestly an
  evaluation-to-`partialFourierSpatial_timeSliceCanonicalExtension` rewrite for
  the mixed-order frozen slice;
- and a fresh source-first re-read of the current `hlimit_os` proof body shows
  that the still earlier blocker is no longer the pointwise scalarization
  theorem `section43_fixedPair_shellToSlice_integrand_descendedScalar_fixedTime`
  either;
- the current live blocker sits one notch lower: after
  `hshellToSlice`, `hsliceTransport`, and the landed region-level shell
  theorems, the proof is still integrating against the ambient weight
  `(φ.conjTensorProduct ψ) y`, while the already-landed source-shell slice
  theorems and source-support weight comparison only fire on
  `tsupport (f.conjTensorProduct g)`, and the strongest region-level vanishing
  theorem still needs
  `hy_region : y ∈ section43PositiveEnergyRegion d (n + m)`;
- therefore the exact first remaining lower obligation is now the
  ambient-to-source shell comparison on the actual fixed-time canonical shell
  used in `hlimit_os`: a theorem that trims the ambient shell integral to the
  source shell, or equivalently shows the shell integral against
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))` tends to `0`, while
  preserving the already-landed region-level shell/slice rewrites on that same
  domain;
- only after that shell-domain promotion theorem lands does it become honest
  to state the later fixed-parameter shell-minus-direct-`psiZ` scalar bridge on
  the mixed-order source inputs.
- if even this direct pointwise scalarization theorem is still too high, the
  next honest lower source object must be exposed directly on the actual mixed-
  order slice functional
  `SCV.fourierLaplaceExt (fourierInvPairingCLM
    (partialFourierSpatial_timeSliceSchwartz ... F.1 ...)) (((2 * Real.pi : ℂ) * w))`;
  more sharply, `SCV.fourierLaplaceExt_eq` only rewrites that Fourier-Laplace
  term to the explicit `ψ_{2πw}` pairing scalar and leaves the raw point
  evaluation
  `((partialFourierSpatial_timeSliceSchwartz ... F.1 ...) (y r 0 + t))`
  untouched; no wrapper theorem, fake density route, or same-input shortcut is
  permitted.

Current source-first update (2026-04-14 09:50 UTC):

- `OSToWightmanPositivity.lean` now contains the private theorem
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`;
- this theorem is the exact lower weight-comparison fact the current source
  honestly supports: on
  `y ∈ tsupport (f.conjTensorProduct g)`, it identifies the ambient weight
  `(φ.conjTensorProduct ψ) y` with the explicit source weight
  `(f.conjTensorProduct g) y`;
- however, the live seam remains limit-level rather than pointwise-global,
  because the fixed-time shell/slice rewrites below `hlimit_os` still consume a
  source-shell membership hypothesis before that identity can be used;
- so the next honest theorem-sized target is now the ambient-domain shell
  comparison showing that the fixed-time canonical shell contribution of
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))` vanishes in the
  `ε → 0+` limit, or the first exact EqOn/support theorem strictly beneath that
  claim on the actual shell domain.

Current source-first recheck (2026-04-14 ambient-domain shell-vanishing pass):

- no theorem of that ambient shell-vanishing shape lands cleanly on the current
  source in this bounded pass;
- the stale advertised next theorem
  `section43_fixedPair_shellToSlice_integrand_descendedScalar_fixedTime`
  is therefore still one notch too high as a first target on the present
  branch;
- the reason is not just “the limit argument is still large”: it is a sharper
  domain mismatch already visible one level lower in the available pointwise
  theorems;
- specifically, the current off-source vanishing theorem
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`
  only applies after supplying
  `y ∈ section43PositiveEnergyRegion d (n + m)`;
- but the actual feeders consumed under `hlimit_os`,
  `section43_fixedPair_canonicalShell_sliceRealization` and
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime`, are gated
  instead by source-shell membership
  `y ∈ tsupport (f.conjTensorProduct g)`;
- source-first rereading of the lower geometry lemmas shows that
  `section43_fixedPair_nonnegTimeCoords_of_mem_tsupport_conjTensorProduct` and
  `section43_fixedPair_nonnegAwayFrom_r_of_mem_tsupport_conjTensorProduct`
  are themselves only consequences of that same source-shell membership, so
  they do not yield the missing ambient-shell promotion theorem either;
- current source provides no theorem connecting the ambient shell integration
  domain of `hlimit_os` to either of those two gated domains, and in
  particular no theorem trimming the ambient shell integral to the source
  `tsupport`;
- therefore the exact first remaining lower obligation is now the support-
  promotion step itself: an EqOn/support/integrand-vanishing theorem on the
  actual fixed-time canonical shell strong enough to move the ambient weight
  `(φ.conjTensorProduct ψ)` onto the source-shell domain where the existing
  fixed-time rewrites apply.

Current source-first recheck (2026-04-14 actual-shell comparison pass):

- the live theorem body in
  `OSToWightmanPositivity.lean` still bottoms out exactly at the local claim
  `hlimit_os` inside
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- the strongest lower shell inputs actually available there are now
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  and
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`;
- but no theorem yet trims the actual fixed-time canonical shell integral in
  `hlimit_os` to `tsupport (f.conjTensorProduct g)`, and no theorem directly
  proves that the same shell integral against
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))` tends to `0`;
- so the exact first remaining lower obligation stays limit-level on that
  actual shell, rather than any new pointwise fixed-`(y, ε)` shell theorem.

Current source-first recheck (2026-04-14 14:35 UTC, bounded theorem-sized pass):

- re-reading the exact `hlimit_os` body confirms those three lower theorems are
  not just nearby infrastructure but the direct current inputs under the live
  `sorry`:
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  and
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`;
- no additional honest actual-shell `EqOn`, support-promotion, or
  integrand-vanishing theorem is available from current source without
  fabricating the still-missing localization step;
- therefore this bounded pass did not add a new theorem surface below
  `hlimit_os`: the exact first remaining lower obligation is still the
  ambient-to-source comparison on the actual fixed-time canonical shell,
  either as a shell-trimming theorem to
  `tsupport (f.conjTensorProduct g)` or as the limit theorem that the shell
  integral against
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))` tends to `0`.

Current source-first recheck (2026-04-14 14:55 UTC, actual-shell measure/support pass):

- descending one notch lower into the canonical-shell domain did not expose any
  honest already-landed measure-theoretic shortcut beneath `hlimit_os`;
- the nearby generic tools in `OSToWightmanPositivity.lean`
  `MeasureTheory.setIntegral_tsupport`,
  `image_eq_zero_of_notMem_tsupport`,
  `tsupport_mul_subset_left`,
  `tsupport_mul_subset_right`,
  and
  `MeasureTheory.integral_eq_zero_of_ae`
  are real, but they do not discharge the current seam because the live shell
  integrand is still weighted by `(φ.conjTensorProduct ψ)`, not by
  `(f.conjTensorProduct g)`;
- that is the exact hypothesis mismatch:
  the support lemmas become useful only after an ambient-to-source replacement
  has already occurred, while the landed fixed-time shell/slice rewrites still
  require either
  `hy_region : y ∈ section43PositiveEnergyRegion d (n + m)` or
  `hy : y ∈ tsupport (((f.conjTensorProduct g : SchwartzNPoint d (n + m)) :
    NPointDomain d (n + m) → ℂ))`;
- current source still has no theorem promoting the actual fixed-time canonical
  shell points in `hlimit_os` to those hypotheses, no theorem trimming that
  shell integral to `tsupport (f.conjTensorProduct g)`, and no theorem proving
  directly that the shell integral against
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))` tends to `0`;
- so no new lower theorem compiled in this pass, and the exact first remaining
  lower obligation is unchanged: the ambient-vs-source comparison on the actual
  fixed-time canonical shell, at the level of the missing support/localization
  step rather than any new pointwise scalar theorem.

Current source-first recheck (2026-04-14 11:57 UTC, actual-shell localization pass):

- pushing one notch lower inside the same local proof body of
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`
  sharpens the failure point beneath the ambient-vs-source limit candidate;
- the intended limit theorem
  “the shell against
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))` tends to `0`”
  first decomposes into:
  1. `y ∈ tsupport (f.conjTensorProduct g)`,
  2. `y ∈ section43PositiveEnergyRegion d (n + m)` but outside that source
     support,
  3. `y ∉ section43PositiveEnergyRegion d (n + m)`;
- cases (1) and (2) are exactly the branches already handled by
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`
  and
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`;
- the first branch that still has no supporting theorem is therefore case (3),
  not any later limit manipulation;
- the exact first lower statement that fails is an outside-region
  annihilation/localization theorem for the actual canonical shell integrand,
  for example:

```lean
∀ ε : ℝ, ∀ y : NPointDomain d (n + m),
  y ∉ section43PositiveEnergyRegion d (n + m) →
    bvt_F OS lgc (n + m)
      (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
        (fun k μ =>
          ↑(y k μ) +
            ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
              Complex.I)
        (t : ℂ)) *
      (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y) = 0
```

  or any equivalent theorem that removes the outside-
  `section43PositiveEnergyRegion` contribution before integration;
- no such theorem is currently present in source, so no new lower theorem
  compiled in this pass.
- Pushing one step lower in the same branch shows that this product-level
  localization statement is already not the first failing subgoal: because
  `hf_ord`, `hg_ord` place `tsupport (f.conjTensorProduct g)` inside
  `section43PositiveEnergyRegion d (n + m)`, the source tensor term already
  vanishes for `y ∉ section43PositiveEnergyRegion d (n + m)`.
- So the exact first unsupported lower statement on that branch is the ambient
  vanishing claim

```lean
∀ y : NPointDomain d (n + m),
  y ∉ section43PositiveEnergyRegion d (n + m) →
    (φ.conjTensorProduct ψ) y = 0
```

  and one step lower still the first strictly-smaller theorem statement that
  fails is the outside-region representative comparison

```lean
∀ y : NPointDomain d (n + m),
  y ∉ section43PositiveEnergyRegion d (n + m) →
    (φ.conjTensorProduct ψ) y = (f.conjTensorProduct g) y
```

  equivalently `Set.EqOn (φ.conjTensorProduct ψ) (f.conjTensorProduct g)` on
  the complement of `section43PositiveEnergyRegion d (n + m)`;
- this is the first exact lower comparison statement that the current route
  cannot supply: the proof body
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`
  in `OSToWightmanPositivity.lean` only reaches total-arity quotient equality,
  while `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region` in
  `Section43Codomain.lean` only compare representatives on
  `section43PositiveEnergyRegion d (n + m)`, never on its complement;
- therefore the route stops before ambient outside-region vanishing is even in
  range: the first unsupported proof step is already the attempt to compare the
  ambient and source representatives outside the Section-4.3 region.
- Current bounded-pass conclusion (2026-04-14): there is still no honest
  theorem-sized step strictly below that comparison claim on the present route.
  Any proposed outside-region shell annihilation, ambient outside-region
  vanishing, or limit-level trimming theorem immediately reduces back to that
  same unsupported outside-region representative comparison, because the
  source tensor already vanishes off `section43PositiveEnergyRegion d (n + m)`.
- Exact body-location sharpening from the live source:
  the first forced use of that unsupported comparison occurs inside
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  local proof `hlimit_os`, immediately after the helper
  `hweightEqOnSource` at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:8445).
  That helper only proves
  `(φ.conjTensorProduct ψ) y = (f.conjTensorProduct g) y`
  on
  `y ∈ tsupport (f.conjTensorProduct g)`;
  the next proof step would have to extend the shell-weight comparison to
  arbitrary integration points `y`, and its first unresolved branch is exactly
  `y ∉ section43PositiveEnergyRegion d (n + m)`.
- Exact candidate audit on current source:
  `partialFourierSpatial_fun_eq_of_eqOn_section43PositiveEnergyRegion`,
  `partialFourierSpatial_timeSlice_eqOn_nonneg_of_section43PositiveEnergyQuotientMap_eq`,
  `partialFourierSpatial_timeSlice_eqOn_nonneg_of_repr_eq_transport`,
  `partialFourierSpatial_timeSlice_eqOn_nonneg_of_repr_eq_fixedPairMixedOrder`,
  `section43PositiveEnergyQuotientMap1D_partialFourierSpatial_timeSlice_eq_of_repr_eq_fixedPairMixedOrder`,
  `fourierInvPairingCLM_partialFourierSpatial_timeSlice_eq_of_repr_eq_fixedPairMixedOrder`,
  `fourierInvPairingCLM_opposite_partialFourierSpatial_timeSlice_eq_of_repr_eq_transport`,
  and `section43_iteratedSlice_descendedPairing`
  all remain gated by the Section-4.3 region or by the one-variable
  nonnegative-time condition `Set.Ici (0 : ℝ)`, so they never start on the
  complement branch that is still live in `hlimit_os`;
  `nPointTimeSpatialSchwartzCLE_eq_zero_of_neg_time_of_fixedPairMixedOrder`,
  `partialFourierSpatial_fun_eq_zero_of_neg_time`, and
  `tsupport_partialFourierSpatial_timeSlice_subset_Ici_of_fixedPairMixedOrder`
  are source-side mixed-order slice support/vanishing lemmas only, so they do
  not remove the ambient outside-region term;
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
  and
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`
  all still require region membership and therefore do not touch that branch;
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`
  reaches only quotient equality; and the only codomain comparison theorems,
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region`,
  restrict comparison to `section43PositiveEnergyRegion d (n + m)` itself.
  So no honest actual-shell or integral-level lemma strictly below
  `hlimit_os` removes the outside-region contribution on present source
  without new mathematics beyond complement-of-region representative control.
- a second bounded 2026-04-14 probe checked the seemingly smaller shell-local
  derivative statement
  `Differentiable ℝ (fun y => bvt_F OS lgc (n + m) (xiShift ... y ...))`
  directly at the production insertion point below
  `contDiff_canonicalXiShift`;
  this also failed source-first, but one layer earlier than derivative growth:
  the attempt to restrict the outer `DifferentiableAt ℂ` statement for
  `bvt_F` to an `ℝ`-differentiable statement on the ambient shell codomain
  `Fin (n + m) → Fin (d + 1) → ℂ` hit a missing compile-clean scalar-tower
  setup;
- so the exact next missing sub-contract below the derivative-growth target is
  now sharpened further:
  compile-clean real/complex scalar restriction infrastructure for the ambient
  shell codomain, sufficient to transport forward-tube differentiability of
  `bvt_F` onto the real shell map before any `HasFDerivAt` / `fderiv` growth
  theorem can even be stated honestly in production.

Addendum (2026-04-14, earlier-route-repair audit):

- the same source-first pass also checked whether OS II already supplies an
  earlier representation-choice/input-data repair that would change the
  ambient representatives before the live local shell comparison;
- exact current-source candidates were
  `FixedPairMixedOrderComponent.ofConjTensorProduct` in
  `EuclideanPositiveTime.lean`,
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`
  and its region-level consumers in `OSToWightmanPositivity.lean`,
  and the codomain comparison theorem
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` in
  `Section43Codomain.lean`;
- none of these changes the ambient data:
  the first only packages the source tensor,
  the second class proves quotient equality and then equality only on
  `section43PositiveEnergyRegion d (n + m)`,
  and the later fixed-time slice / pairing theorems consume that already gated
  equality rather than producing a new representative;
- so the obstruction is not “we missed an earlier repair theorem already on the
  route” but “the current route has no such theorem”;
- relative to the OS II chapter structure described above, this missing input
  would have to be inserted as new mathematics immediately before the local
  fixed-time shell comparison inside the theorem-3 Lemma-4.2 adapter, not
  extracted from the existing Chapter-V/Section-4.3 transport package.
- paper-facing sharpening:
  the audited OS II slots above are Chapter V.2 `(A_N)/(P_N)` and Theorem 4.3,
  while the audited OS I slots are Section 4.3 Lemma 4.1, Lemma 4.2, and
  `(4.28)`;
  all of them work on the positive-time / positive-energy transform side and
  never claim equality of ambient representatives off
  `section43PositiveEnergyRegion d (n + m)`;
  so, on the present source package, the route has reached a genuine
  mathematical gap rather than a merely unencoded earlier paper lemma.

Addendum (2026-04-14, theorem-contract gap audit):

- exact current Lean demand was rechecked at the first live body location:
  inside
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  local block `hlimit_os`,
  immediately after
  `have hweightEqOnSource := ...`
  at
  `OSToWightmanPositivity.lean:8445`;
- exact missing statement there is still the complement branch of the ambient
  shell-weight comparison, equivalently
  `∀ y, y ∉ section43PositiveEnergyRegion d (n + m) →
    (φ.conjTensorProduct ψ) y = (f.conjTensorProduct g) y`,
  or an actual-shell localization theorem strong enough to remove that branch
  from the shell integral without asserting pointwise complement equality;
- exact reason the audited paper slots do not supply it:
  OS II Chapter V.2 `(A_N)/(P_N)` and Theorem 4.3 only compare data on the
  positive-energy transform side, and the current Lean codomain theorems
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region` /
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` in
  `Section43Codomain.lean` faithfully encode only that region-restricted
  comparison;
- so the present blockage is an explicit theorem-contract gap between the
  paper-backed transformed-image comparison package and the stronger
  complement-of-region ambient representative control now demanded by
  `hlimit_os`.
- best reading of that gap on the current source:
  it is a route-contract mismatch at the Lean `Section43Codomain` interface;
  the encoded OS II / Section-4.3 package honestly stops at positive-energy
  region comparison, whereas `hlimit_os` now needs ambient shell control beyond
  that interface;
  consequently, any repair without a route change would have to be new
  mathematics in the form of an actual-shell localization/comparison theorem,
  not a smaller theorem already latent in the current quotient package.

Addendum (2026-04-14, weaker-than-global repair audit):

- the smallest new theorem surface still compatible with the theorem-3 route is
  not full complement-of-region `EqOn`;
  it is the actual-shell localization/comparison statement that the fixed-time
  canonical shell integral against
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`
  tends to `0` as `ε → 0+`;
- that statement is strictly weaker than global complement representative
  equality because it concerns only the specific shell and limit used in
  `hlimit_os`;
- however, source-first rechecking shows that every proof attempt for that
  weaker theorem on the present package still decomposes first into the same
  outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`,
  and there the current codomain comparison theorems
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region` and
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  give no control on the ambient representative;
- so OS II Chapter V.2 / Theorem 4.3 and OS I Section 4.3 Lemma 4.1,
  Lemma 4.2, `(4.28)` do not merely lack an encoded proof here:
  on the current Lean surface they stop short of the weaker shell theorem
  itself, because its first proof branch already collapses to unsupported
  complement-of-region representative control.

Addendum (2026-04-14, paper-contract source extraction audit):

- exact paper labels re-read directly from the PDFs:
  OS II Theorem 4.3 states only that there are analytic functions
  `S_k(ζ° | ξ)` and then, “by standard arguments,” unique distributions `W_k`
  with support in `ℝ^k_+` whose Fourier-Laplace transforms are those boundary
  values;
  the preceding Chapter V.2 induction surfaces are `(A_N)` and `(P_N)`;
  OS I Section 4.3 states positivity through Lemma 4.1, Lemma 4.2, the dense
  map `u` in `(4.27)`, and the quadratic identity `(4.28)`;
- exact paper-vs-Lean verdict sharpened by that reread:
  none of those paper statements is a theorem about the fixed-time canonical
  shell integral already written in Lean `hlimit_os`, and none states that the
  shell integral of
  `((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))`
  tends to `0`;
- in paper language, OS II Theorem 4.3 only yields the usual
  Fourier-Laplace boundary-value support theorem after the global continuation
  has already been built, while OS I Section 4.3 only identifies the
  Wightman quadratic form with a Hilbert-space norm on the transformed-image
  side;
- in current Lean language, those two paper contracts are exactly reflected by
  quotient/region-side theorems such as
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region`,
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`,
  and
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`;
  no existing encoded theorem was found that upgrades them to the weaker
  actual-shell localization target;
- therefore the weaker shell-local statement is itself new mathematics relative
  to the present OS I / OS II route package, not a merely unencoded corollary
  of the existing Section-4.3 quotient interface.

Addendum (2026-04-14, insertion-point audit):

- a bounded pass tested whether the next honest theorem-sized move should enter
  upstream through `Section43Codomain.lean` rather than directly at the live
  fixed-time shell seam in `OSToWightmanPositivity.lean`;
- source-first, the codomain insertion point fails for structural reasons:
  `section43PositiveEnergyVanishingSubmodule` quotients by ambient Schwartz
  functions vanishing on `section43PositiveEnergyRegion d (n + m)`, so current
  quotient equality intentionally carries no complement-of-region information;
- hence the reusable theorems
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region` and
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`
  faithfully encode only region-restricted representative comparison and
  cannot be pushed to ambient complement control without genuinely new
  complement-sensitive mathematics;
- the earliest failing API boundary therefore remains exactly the current one:
  representative transport stops at
  `section43PositiveEnergyRegion d (n + m)`, while `hlimit_os` still needs
  control of the outside-region branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- so on the present theorem-3 route the next honest theorem skeleton still
  belongs at the live shell seam, not as a transport wrapper upstream:

```lean
Filter.Tendsto
  (fun ε : ℝ =>
    ∫ y : NPointDomain d (n + m),
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y))
  (nhdsWithin 0 (Set.Ioi 0))
  (nhds 0)
```

  equivalently, equality of the actual fixed-time shell limits against the
  ambient tensor and the source tensor.

Addendum (2026-04-14, bounded actual-shell specialization audit):

- source-first recheck of the live body after `have hweightEqOnSource := ...`
  confirms that the candidate fixed-pair actual-shell contract

```lean
theorem section43_fixedTime_shellIntegral_fixedPairDifference_eq_zero
    (t ε : ℝ) (ht : 0 < t) (hε : 0 < ε) :
    ∫ y : NPointDomain d (n + m),
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y) = 0
```

  is not a strictly earlier theorem than
  `section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero` or
  `section43_fixedTime_shellLimit_tendsto_zero_of_quotient_eq_zero`;
- it is only the specialization of those contracts to
  `h := (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)`, and the required
  quotient-zero hypothesis is already available from
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`;
- the proof obstruction is unchanged: on the complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`, present source still provides
  no theorem that annihilates the shell coefficient against that specialized
  weight, so the specialization needs the same missing kernel containment for
  the shell functional as the general ambient theorem;
- bounded source search found no existing non-wrapper local lemma/theorem
  strictly below `hlimit_os` that packages this specialization honestly;
- therefore the earliest honest shell-functional statement remains the general
  quotient-kernel/descent contract, while the actual-shell fixed-pair theorem
  is recorded only as an equivalent specialization of the same missing
  complement-localization mathematics.

Addendum (2026-04-14, bounded strengthened-specialization audit):

- source-first, the only remaining candidate strictly between the earlier
  explicit-complement-vanishing variant and the general quotient-kernel
  contracts is to impose both:
  1. explicit complement vanishing
     `∀ y, y ∉ section43PositiveEnergyRegion d (n + m) → h y = 0`, and
  2. fixed-pair transport provenance for `h`, for example
     `h = (φ.conjTensorProduct ψ) - (f.conjTensorProduct g)` with quotient-zero
     given by
     `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`;
- exact verdict: this strengthened specialization is still not a genuinely
  smaller honest theorem surface;
- if explicit complement vanishing is assumed, the fixed-pair provenance adds
  no new shell-functional mathematics beyond identifying one particular source
  of such `h`; the needed theorem would still be the same ambient shell
  kernel/localization statement;
- if explicit complement vanishing is not assumed, then the fixed-pair
  specialization reduces immediately to the already-audited actual-shell
  difference theorem and still dies on the complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- the current fixed-pair lemmas remain only pointwise region/source-support
  controls:
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_section43Region`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  and
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`;
  none of these descends the ambient shell integral or shell limit through the
  Section-4.3 quotient;
- therefore no new non-wrapper local lemma/theorem exists below `hlimit_os`
  from this extra structure layer, and the earliest honest missing contracts
  remain exactly
  `section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero` and
  `section43_fixedTime_shellLimit_tendsto_zero_of_quotient_eq_zero`.

Addendum (2026-04-14, bounded derivative-growth route audit):

- source-first theorem package order from forward-tube facts to the fixed-time
  shell coefficient is now:
  1. `bvt_F_holomorphic`
     (`OSToWightmanBoundaryValuesBase.lean`) gives complex holomorphy of
     `bvt_F` on `ForwardTube d (n + m)`;
  2. `canonicalXiShift_mem_forwardTube`
     (`OSToWightmanPositivity.lean`) gives range inclusion of the affine shell
     map into that tube for `ε > 0`;
  3. `contDiff_canonicalXiShift`
     (`OSToWightmanPositivity.lean`, landed in this bounded pass) gives
     `ContDiff ℝ (⊤ : ℕ∞)` for that affine shell map;
  4. local Mathlib already supplies the open-set smoothness bridge
     `DifferentiableOn.contDiffOn` on `isOpen_forwardTube`, so the
     smoothness-only part of the restricted shell coefficient is not the live
     gap;
  5. `bvt_F_hasGlobalForwardTubeGrowth` supplies the tube-level value bound;
  6. `bvt_F_canonicalXiShift_hasPolynomialGrowth`
     (`OSToWightmanPositivity.lean`) now lands the shell-specialized
     zeroth-order bound, i.e. the `r = 0` case after restriction.
- exact first missing theorem contract is therefore the all-orders derivative
  bound for positive derivative order on the restricted shell coefficient, not
  range inclusion, not shell smoothness, not the `r = 0` bound, and not raw
  CLM packaging:

```lean
∀ r : ℕ, 1 ≤ r → ∃ k : ℕ, ∃ C : ℝ, 0 ≤ C ∧
  ∀ y : NPointDomain d (n + m),
    ‖iteratedFDeriv ℝ r
        (fun y : NPointDomain d (n + m) =>
          bvt_F OS lgc (n + m)
            (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
              (fun k μ =>
                ↑(y k μ) +
                  ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                    Complex.I)
              (t : ℂ))) y‖
      ≤ C * (1 + ‖y‖) ^ k
```

- with that one theorem in hand, the target shell-coefficient
  `Function.HasTemperateGrowth` theorem becomes routine packaging; without it,
  current source still lacks the multivariate Cauchy / iterated-derivative
  estimate that upgrades tube holomorphy plus zeroth-order growth to the
  positive-order derivative-growth package.

Addendum (2026-04-14, bounded positive-order derivative-growth API audit):

- source-first audit after the landed shell-specialized
  `bvt_F_canonicalXiShift_hasPolynomialGrowth` confirms that the first missing
  theorem contract is unchanged: positive-order polynomial bounds for
  `iteratedFDeriv ℝ r` of the fixed-time shell coefficient for `r ≥ 1`;
- no smaller honest source-backed theorem was found between the current package
  and that contract:
  the available local / Mathlib ingredients only cover
  tube holomorphy, shell-image membership, shell smoothness, zeroth-order
  value growth, and downstream composition once derivative bounds are already
  known;
- exact APIs inspected in this bounded pass:
  1. `AnalyticOnNhd.iteratedFDeriv_of_isOpen`
     (`Mathlib/Analysis/Calculus/FDeriv/Analytic.lean`)
     and `AnalyticOn.contDiffOn`
     (`Mathlib/Analysis/Calculus/ContDiff/Defs.lean`) only yield analyticity /
     `ContDiff`, not polynomial derivative bounds;
  2. `Complex.norm_iteratedDeriv_le_of_forall_mem_sphere_norm_le`
     (`Mathlib/Analysis/Complex/Liouville.lean`) is one-variable and requires
     boundary-circle sup bounds, so it does not directly furnish the needed
     multivariate affine-shell estimate;
  3. `norm_iteratedFDeriv_comp_le'`
     (`Mathlib/Analysis/Calculus/ContDiff/Bounds.lean`)
     and `Function.HasTemperateGrowth.comp'`
     (`Mathlib/Analysis/Distribution/TemperateGrowth.lean`) are only usable
     after the outer derivative-growth bounds are already available.
- hence the package order remains exactly:
  `bvt_F_holomorphic`
  -> `canonicalXiShift_mem_forwardTube`
  -> `contDiff_canonicalXiShift`
  -> `DifferentiableOn.contDiffOn` on `isOpen_forwardTube`
  -> `bvt_F_hasGlobalForwardTubeGrowth`
  -> `bvt_F_canonicalXiShift_hasPolynomialGrowth`
  -> first missing positive-order theorem contract
  `section43_fixedTime_shellCoefficient_iteratedFDeriv_polynomialBound`;
- no production Lean theorem/local lemma was added in this bounded pass,
  because no honest non-wrapper intermediate theorem below that contract was
  already source-backed.

Addendum (2026-04-14, bounded OS-route supervision pass after Entry #659):

- the present branch plus local Mathlib still contains no honest already-landed
  theorem/API upgrading the shell-specialized `r = 0` bound
  `bvt_F_canonicalXiShift_hasPolynomialGrowth` to any positive-order
  polynomial bound for `iteratedFDeriv ℝ r` of the fixed-time shell
  coefficient;
- exact already-landed derivative-growth package/order remains:
  `bvt_F_holomorphic`
  -> `canonicalXiShift_mem_forwardTube`
  -> `contDiff_canonicalXiShift`
  -> `DifferentiableOn.contDiffOn` on `isOpen_forwardTube`
  -> `bvt_F_hasGlobalForwardTubeGrowth`
  -> `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `DifferentiableOn.contDiffOn` and
  `AnalyticOnNhd.iteratedFDeriv_of_isOpen` only control smoothness / existence
  of iterated derivatives, not polynomial growth of those derivatives;
- the one-variable complex estimates
  `Complex.norm_iteratedDeriv_le_of_forall_mem_sphere_norm_le` and the Cauchy
  circle-integral formulas still do not provide the required multivariate
  affine-shell estimate out of the box;
- the downstream tools `norm_iteratedFDeriv_comp_le'` and
  `Function.HasTemperateGrowth.comp'` remain unusable until the outer
  derivative-growth bounds are already proved;
- therefore the exact first missing positive-order theorem contract is still:

```lean
∀ r : ℕ, 1 ≤ r → ∃ k : ℕ, ∃ C : ℝ, 0 ≤ C ∧
  ∀ y : NPointDomain d (n + m),
    ‖iteratedFDeriv ℝ r
        (fun y : NPointDomain d (n + m) =>
          bvt_F OS lgc (n + m)
            (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
              (fun k μ =>
                ↑(y k μ) +
                  ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                    Complex.I)
              (t : ℂ))) y‖
      ≤ C * (1 + ‖y‖) ^ k
```

- no honest smaller non-wrapper theorem below that contract was found already
  source-backed, so this supervision pass makes no production Lean edit.

Addendum (2026-04-14, bounded SCV/analytic source audit continuing from Entry #661):

- rechecking the repo-local SCV/analytic files most plausibly relevant to this
  seam,
  `SCV/Analyticity.lean`,
  `SCV/IteratedCauchyIntegral.lean`,
  `SCV/TubeBoundaryValueExistence.lean`,
  against the already-imported local chain
  `OSToWightman.lean`
  -> `OSToWightmanBoundaryValuesBase.lean`
  -> `OSToWightmanPositivity.lean`,
  found no honest reusable theorem path from multivariate holomorphy on an open
  complex tube plus polynomial value growth to a positive-order polynomial bound
  for the fixed-time affine shell coefficient;
- exact already-landed package order relevant to the shell coefficient remains:
  `full_analytic_continuation_with_symmetry_growth`
  -> `bvt_F_holomorphic`
  -> `canonicalXiShift_mem_forwardTube`
  -> `contDiff_canonicalXiShift`
  -> `DifferentiableOn.contDiffOn` on `isOpen_forwardTube`
  -> `bvt_F_hasGlobalForwardTubeGrowth`
  -> `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- exact derivative-growth use-site audit:
  in `SCV/TubeBoundaryValueExistence.lean`, derivative growth is consumed only
  in the call to `hasDerivAt_schwartz_integral` inside
  `hasDerivAt_tubeSlice_ray`;
  the later theorems
  `continuous_tubeSlice_ray_deriv` and
  `cr_integration_identity`
  depend on that derivative theorem, but do not demand a stronger derivative
  package;
- therefore the first genuinely consumed contract below theorem 3’s shell
  `Function.HasTemperateGrowth` target is smaller than a global multivariate
  order-`1` shell derivative theorem:
  the SCV proof only needs the local-in-`τ` ray-direction estimate

```lean
∃ (C_bd' : ℝ) (N' : ℕ), 0 < C_bd' ∧
  ∀ τ, |τ - τ₀| < δ → ∀ x,
    ‖(fderiv ℂ F (slice τ x)) vη‖ ≤ C_bd' * (1 + ‖x‖) ^ N'
```

  with
  `slice τ x i = (x i : ℂ) + ((τ • η) i : ℝ) * Complex.I`
  and
  `vη i = (η i : ℂ) * Complex.I`;
- source-first, `SCV/TubeBoundaryValueExistence.lean` establishes that estimate
  internally by a one-variable Cauchy bound on
  `w ↦ F (sliceC w)`;
  but the current repo still has no already-landed reusable theorem
  specializing this to the theorem-3 fixed-time shell coefficient, and this
  bounded pass did not leave one compile-clean in production;
- exact import/dependency-surface verdict after rechecking
  `SCV/TubeBoundaryValueExistence.lean`,
  `SCV/TubeBoundaryValues.lean`,
  `OSToWightmanBoundaryValuesBase.lean`,
  and `OSToWightmanPositivity.lean`:
  the first mathematically honest insertion point for this sharpened contract
  is directly above `hasDerivAt_tubeSlice_ray` in
  `SCV/TubeBoundaryValueExistence.lean`, because that file proves the needed
  local Cauchy estimate inline and is the first place that actually consumes
  it;
- but that insertion point is not compile-clean on the current branch:
  theorem 3 reaches only the abstract boundary-value API through
  `SCV/TubeBoundaryValues.lean`, while the concrete existence file still fails
  `lake env lean` in the present source state;
- therefore no honest theorem landing for this contract exists at the lower
  theorem-3 import surfaces
  `OSToWightmanBoundaryValuesBase.lean` or `OSToWightmanPositivity.lean`
  without changing route or adding wrappers; the current verdict is “earliest
  honest site found, but not landable compile-clean on this branch”;
- so the first honest missing theorem contract after this audit is not
  `iteratedFDeriv ℝ 1` polynomial growth on the full shell function, but the
  shell-specialized instance of the displayed local ray-derivative estimate,
  after flattening/specializing theorem 3’s fixed-time shell coefficient to the
  `TubeBoundaryValueExistence` notation;
- once that local ray contract exists, the SCV consumer path can feed
  `hasDerivAt_schwartz_integral`;
  before it, there is still no compile-backed non-wrapper intermediate theorem
  immediately below `Function.HasTemperateGrowth` to land on the current
  branch.

Addendum (2026-04-14, bounded actual-shell insertion-point recheck after Entry #664):

- source-first rechecked the exact current proof body
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`
  in `OSToWightmanPositivity.lean`;
- exact support/shell-localization theorems rechecked there:
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_section43Region`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
  `partialFourierSpatial_timeSlice_eqOn_nonneg_of_repr_eq_fixedPairMixedOrder`,
  and
  `tsupport_partialFourierSpatial_timeSlice_subset_Ici_of_fixedPairMixedOrder`;
- codomain/support boundary rechecked in
  `Section43Codomain.lean`:
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region`,
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`,
  `orderedPositiveTimeRegion_subset_section43PositiveEnergyRegion`;
  and in `EuclideanPositiveTime.lean`:
  `FixedPairMixedOrderTimeRegion`,
  `FixedPairMixedOrderComponent.ofConjTensorProduct`;
- exact insertion-point verdict:
  the first honest complement-localization theorem surface is still the live
  local seam inside
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  immediately after `have hweightEqOnSource := ...`;

Addendum (2026-04-14, bounded compile-clean insertion-surface recheck continuing from Entry #663):

- exact files/theorems rechecked:
  `SCV/TubeBoundaryValueExistence.lean` at
  `hasDerivAt_tubeSlice_ray`,
  `continuous_tubeSlice_ray_deriv`,
  `cr_integration_identity`,
  and `tube_boundaryValueData_of_polyGrowth'`;
  `SCV/TubeBoundaryValues.lean` at
  `tube_boundaryValueData_of_polyGrowth`;
  `OSToWightmanBoundaryValuesBase.lean` at
  `forwardTube_boundaryValueData_of_polyGrowth` and
  `full_analytic_continuation_boundaryValueData`;
  `OSToWightmanPositivity.lean` at
  `bvt_F_hasGlobalForwardTubeGrowth` and
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- exact import/dependency verdict:
  the theorem-3 branch imports only
  `OSToWightmanBoundaryValuesBase.lean -> SCV/TubeBoundaryValues.lean`;
  the latter exposes `SCV.tube_boundaryValueData_of_polyGrowth` as an abstract
  `axiom`, so it is not an honest site for the missing local ray-derivative
  theorem, only the compile-clean export surface consumed downstream;
- source-first, the first honest insertion point remains directly above
  `hasDerivAt_tubeSlice_ray` in `SCV/TubeBoundaryValueExistence.lean`, where
  the local Cauchy-estimate proof is already written inline and is first
  consumed by `hasDerivAt_schwartz_integral`;
- compile-clean verdict on the current branch:
  `SCV/TubeBoundaryValueExistence.lean` still fails `lake env lean` with
  pre-existing errors, while
  `OSToWightmanBoundaryValuesBase.lean` and
  `OSToWightmanPositivity.lean` both compile;
- therefore there is no honest compile-clean insertion point on the current
  theorem-3 import path for the sharpened local ray-derivative-growth
  contract;
  the only honest site is upstream in the non-compile-clean concrete SCV
  existence file, and every downstream compile-clean file sees only the
  abstract boundary-value API;
- exact first still-missing theorem contract after this pass remains the
  shell-specific local ray estimate

```lean
∃ (C_bd' : ℝ) (N' : ℕ), 0 < C_bd' ∧
  ∀ τ, |τ - τ₀| < δ → ∀ x,
    ‖(fderiv ℂ F_shell (slice τ x)) vη‖ ≤ C_bd' * (1 + ‖x‖) ^ N'
```

  rather than any downstream wrapper theorem in
  `OSToWightmanBoundaryValuesBase.lean` or `OSToWightmanPositivity.lean`;
- no production Lean theorem/local lemma was landed in this pass.
- source branches now fully accounted for:
  on source support, `hweightEqOnSource` identifies ambient and source
  weights;
  on the region branch away from source support,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`
  kills the ambient weight;
  but on the actual remaining branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`,
  there is still no theorem that annihilates or rewrites the shell integrand
  into controlled source data;
- therefore no compile-clean non-wrapper theorem/local lemma strictly below the
  quotient-kernel shell theorem was found on current source, and this bounded
  pass made no production Lean edit;
- exact first still-missing theorem contracts remain:

```lean
theorem section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero
    (t ε : ℝ) (ht : 0 < t) (hε : 0 < ε) :
    ∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
        ∫ y : NPointDomain d (n + m),
          bvt_F OS lgc (n + m)
            (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
              (fun k μ =>
                ↑(y k μ) +
                  ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                    Complex.I)
              (t : ℂ)) *
            h y = 0
```

  and equivalently

```lean
theorem section43_fixedTime_shellLimit_tendsto_zero_of_quotient_eq_zero
    (t : ℝ) (ht : 0 < t) :
    ∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
        Filter.Tendsto
          (fun ε : ℝ =>
            ∫ y : NPointDomain d (n + m),
              bvt_F OS lgc (n + m)
                (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
                  (fun k μ =>
                    ↑(y k μ) +
                      ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                        Complex.I)
                  (t : ℂ)) *
                h y)
          (nhdsWithin 0 (Set.Ioi 0))
          (nhds 0)
```

- both formulations are equivalent witnesses of the same still-missing new
  mathematics:
  the multivariate fixed-time shell functional must descend through
  `section43PositiveEnergyQuotientMap (d := d) (n + m)`,
  equivalently its kernel must contain
  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m)`.

Addendum (2026-04-14, bounded actual-shell insertion-point recheck):

- exact source files/theorems re-read on this pass:
  `OSToWightmanPositivity.lean` at
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_section43Region`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
  and the live proof body
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
  `Section43Codomain.lean` at
  `orderedPositiveTimeRegion_subset_section43PositiveEnergyRegion`,
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region`,
  and
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`;
  `EuclideanPositiveTime.lean` at
  `FixedPairMixedOrderTimeRegion` and
  `conjTensorProduct_tsupport_subset_fixedPairMixedOrderTimeRegion`;
- exact proof-body verdict:
  immediately after `have hweightEqOnSource := ...` in `hlimit_os`,
  current source already saturates every smaller branch theorem that is honest
  on the current route;
  the only unresolved branch is the ambient complement case
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact insertion-point verdict:
  there is no compile-clean theorem/local-lemma insertion point strictly
  between the already-recorded region-local pointwise transport facts and the
  full quotient-kernel theorem for the fixed-time shell functional;
  any theorem strong enough to move the live proof must already control the
  actual-shell integral on that complement branch, so it collapses to shell
  descent through the Section-4.3 quotient;
- therefore this pass made no production Lean edit;
- exact first still-missing theorem contract after this pass remains
  unchanged:

```lean
theorem section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero
    (t ε : ℝ) (ht : 0 < t) (hε : 0 < ε) :
    ∀ h : SchwartzNPoint d (n + m),
      section43PositiveEnergyQuotientMap (d := d) (n + m) h = 0 →
        ∫ y : NPointDomain d (n + m),
          bvt_F OS lgc (n + m)
            (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
              (fun k μ =>
                ↑(y k μ) +
                  ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                    Complex.I)
              (t : ℂ)) *
            h y = 0
```

- with equivalent limit-level form
  `section43_fixedTime_shellLimit_tendsto_zero_of_quotient_eq_zero`;
- both are the same missing complement-localization/kernel theorem for the
  multivariate fixed-time shell functional.

Addendum (2026-04-14, bounded proof-body source check after the completed insertion-point audits):

- exact files/theorems inspected on this pass:
  `OSToWightmanPositivity.lean` at
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  `section43PositiveEnergyQuotientMap_conjTensorProduct_eq_of_repr_eq_transport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_section43Region`,
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  `section43_fixedPair_conjTensorProduct_eq_of_repr_eq_on_source_tsupport`,
  `section43_fixedPair_canonicalShell_sliceRealization_on_section43PositiveEnergyRegion`,
  `section43_fixedPair_canonicalSlice_eq_of_repr_eq_fixedTime_on_section43PositiveEnergyRegion`,
  `partialFourierSpatial_timeSlice_eqOn_nonneg_of_repr_eq_fixedPairMixedOrder`,
  and
  `tsupport_partialFourierSpatial_timeSlice_subset_Ici_of_fixedPairMixedOrder`;
  `Section43Codomain.lean` at
  `orderedPositiveTimeRegion_subset_section43PositiveEnergyRegion`,
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region`,
  and
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`;
  `EuclideanPositiveTime.lean` at
  `FixedPairMixedOrderTimeRegion`,
  `FixedPairMixedOrderComponent.ofConjTensorProduct`,
  and
  `conjTensorProduct_tsupport_subset_fixedPairMixedOrderTimeRegion`;
- exact proof-body / insertion-point verdict:
  the first honest complement-localization theorem surface is still the live
  local branch in `hlimit_os`, immediately after
  `have hweightEqOnSource := ...`;
  current source has no compile-clean non-wrapper theorem/local lemma strictly
  below that point;
- exact reason:
  the smaller branches are already exhausted by existing mathematics:
  source support is handled by `hweightEqOnSource`,
  the region-minus-source-support branch is handled by
  `section43_fixedPair_conjTensorProduct_eq_zero_of_repr_eq_on_section43Region_outside_source_tsupport`,
  and the unresolved branch is precisely
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
  any theorem strong enough to move the proof must already control the actual
  fixed-time shell integral on that complement branch, so it collapses to the
  quotient-kernel shell theorem itself;
- therefore this pass made no production Lean edit;
- exact first still-missing theorem contract remains unchanged:
  `section43_fixedTime_shellIntegral_eq_zero_of_quotient_eq_zero`,
  equivalently
  `section43_fixedTime_shellLimit_tendsto_zero_of_quotient_eq_zero`;
  both state that the multivariate fixed-time shell functional depends only on
  the quotient class
  `section43PositiveEnergyQuotientMap (d := d) (n + m)`,
  equivalently annihilates
  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m)`.

Addendum (2026-04-14, 18:30 UTC supervision package split):

- source-first, the quotient-descent seam has a clean package decomposition:
  (1) raw fixed-`ε` shell functional on `SchwartzNPoint d (n + m)`;
  (2) kernel inclusion of
  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m)` for that
  functional;
  (3) descended quotient-side functional via `liftQ`;
  (4) application to the live fixed-pair difference plus the final
  `ε → 0+` limit passage;
- Pieces 1, 3, and 4 are packaging/transport once the kernel statement exists;
  they mirror already-landed descent patterns in `Section43Codomain.lean`;
- Piece 2 is still the first genuinely missing mathematics and remains exactly
  the complement-localization theorem for the multivariate fixed-time shell
  functional;
- therefore the next honest theorem-sized implementation search should test
  whether Piece 1 can already be landed compile-clean as a raw continuous
  linear functional surface, while keeping the docs explicit that Piece 2 is
  the actual blocker.

Addendum (2026-04-14, bounded theorem-3 actual-shell supervision pass on the live import path):

- exact files/theorems inspected on this pass:
  `OSToWightmanPositivity.lean` at
  `contDiff_canonicalXiShift`,
  `exists_shellKernel_pairing_canonicalXiShift`,
  `bvt_F_hasGlobalForwardTubeGrowth`,
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`,
  `horizontalPhase_temperate`,
  and the live body
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
  `OSToWightmanBoundaryValuesBase.lean` at
  `boundary_values_from_flatBoundaryData`,
  `forwardTube_boundaryValueData_of_polyGrowth`,
  `full_analytic_continuation_boundaryValueData`,
  and
  `boundary_values_tempered`;
  `Section43Codomain.lean` at
  `section43PositiveEnergyQuotientMap`,
  `section43PositiveEnergyQuotientMap_eq_of_eqOn_region`,
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq`,
  `evalAtZeroDescendsToSection43PositiveEnergyComponent`,
  `fourierPairingDescendsToSection43PositiveEnergy1D`,
  and
  `evalAtZeroDescendsToSection43PositiveEnergy1D`;
  `SCV/TubeBoundaryValues.lean` at
  `tube_boundaryValueData_of_polyGrowth` plus its integral-packaging lemmas;
  Mathlib `Analysis/Distribution/TemperateGrowth.lean` at
  `Function.HasTemperateGrowth.of_fderiv`.
- exact compile-clean landing verdict:
  no new production Lean theorem/local lemma landed on this pass;
  the branch still has no already-supported non-wrapper theorem strictly
  between `bvt_F_canonicalXiShift_hasPolynomialGrowth` and the full shell
  coefficient `Function.HasTemperateGrowth` upgrade.
- exact smallest honest theorem contract below full shell-coefficient
  `Function.HasTemperateGrowth` is now sharpened one step:
  because Mathlib exposes `Function.HasTemperateGrowth.of_fderiv`, the first
  strictly smaller theorem surface on the actual packaging path is

```lean
Function.HasTemperateGrowth
  (fderiv ℝ
    (fun y : NPointDomain d (n + m) =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ))))
```

  together with the already-landed zeroth-order growth theorem
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- exact `Function.HasTemperateGrowth.of_fderiv` packaging verdict:
  for

  ```lean
  shellCoeff :=
    fun y : NPointDomain d (n + m) =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ))
  ```

  Mathlib asks for exactly:
  1. `Function.HasTemperateGrowth (fderiv ℝ shellCoeff)`,
  2. `Differentiable ℝ shellCoeff`,
  3. the zeroth-order estimate already furnished by
     `bvt_F_canonicalXiShift_hasPolynomialGrowth`.
  On the present branch, item 2 is already available from the affine shell-map
  smoothness `contDiff_canonicalXiShift` together with the outer
  differentiability of `bvt_F`, while item 1 is still completely absent by
  exact source name;
- exact insertion point:
  the first honest place this theorem can land is
  `OSToWightmanPositivity.lean`, directly between
  `bvt_F_canonicalXiShift_hasPolynomialGrowth` and the still-unwritten raw
  shell-functional packaging with `SchwartzMap.smulLeftCLM`;
  the boundary-value files and `Section43Codomain.lean` sit strictly
  downstream of that package and therefore cannot bypass it;
- source-first, this is still not already supported on the current branch:
  `contDiff_canonicalXiShift` proves only smoothness of the affine shell map,
  while the source still has no theorem converting the outer forward-tube
  holomorphy/value-growth package for `bvt_F` into any positive-order
  polynomial bound for the composed shell coefficient or its `fderiv`;
- exact theorem-3 import-path verdict:
  the abstract boundary-value API does not bypass the missing shell-coefficient
  upgrade.
  `OSToWightmanBoundaryValuesBase.lean` / `SCV/TubeBoundaryValues.lean`
  package boundary values only after global tube-growth data is already
  present, and `Section43Codomain.lean` only descends functionals after kernel
  containment has already been proved.
  So theorem 3 still cannot reach a descended fixed-time shell functional
  without first proving the shell-coefficient growth upgrade in production;
- therefore the operational blocker ordering is now exact:
  1. derivative-growth / `Function.HasTemperateGrowth.of_fderiv` reduction for
     the fixed-time shell coefficient;
  2. raw fixed-`ε` shell functional packaging via `SchwartzMap.smulLeftCLM`;
  3. kernel inclusion into
     `section43PositiveEnergyVanishingSubmodule (d := d) (n + m)`;
  4. quotient descent and the final `ε → 0+` consumer step.

Addendum (2026-04-14, bounded theorem-3 actual-shell supervision pass: strict sub-`fderiv` packaging surface):

- exact smaller honest theorem/local-lemma surface strictly below full
  `Function.HasTemperateGrowth (fderiv ℝ shellCoeff)` was initially fixed as
  the first positive-order derivative-growth theorem is the direct polynomial
  norm bound for the derivative-valued coefficient itself,

```lean
∃ (C_bd : ℝ) (N : ℕ), 0 < C_bd ∧
  ∀ y : NPointDomain d (n + m),
    ‖fderiv ℝ
        (fun y : NPointDomain d (n + m) =>
          bvt_F OS lgc (n + m)
            (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
              (fun k μ =>
                ↑(y k μ) +
                  ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                    Complex.I)
              (t : ℂ))) y‖
      ≤ C_bd * (1 + ‖y‖) ^ N
```

- this is strictly smaller than full
  `Function.HasTemperateGrowth (fderiv ℝ shellCoeff)` because it only provides
  the `n = 0` case of growth for the derivative-valued function;
- bounded insertion-point recheck now sharpens the landing verdict:
  this derivative-valued zeroth-order bound is **not yet directly landable**
  on the present branch, because the branch still lacks a compile-clean local
  theorem that identifies `fderiv ℝ shellCoeff` itself on the actual shell
  domain;
- exact `Function.HasTemperateGrowth.of_fderiv` verdict:
  the theorem above is the first missing positive-order ingredient, but it is
  not by itself enough to invoke `of_fderiv`.
  `of_fderiv` still asks for all-orders temperate growth of
  `fderiv ℝ shellCoeff`, plus the already-available differentiability of
  `shellCoeff` and the already-landed value bound
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- exact insertion point remains
  `OSToWightmanPositivity.lean` immediately after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
  analytically this is the first live branch location where the theorem-3
  shell coefficient is fully fixed and before any raw CLM packaging attempt;
- no smaller compile-clean item is already present on this branch:
  source search still finds no theorem naming `fderiv ℝ` of the actual shell
  coefficient, no positive-order polynomial bound for that derivative, and no
  packaged derivative-side `HasTemperateGrowth` theorem.

Addendum (2026-04-14, bounded theorem-3 actual-shell supervision pass: explicit real chain-rule formula recheck):

- the next one-step-smaller candidate below that derivative-growth bound was
  tested directly at the live insertion point: an explicit theorem naming
  `fderiv ℝ shellCoeff y` by a real chain-rule / composition formula through
  the affine `xiShift` shell map and the outer derivative of `bvt_F`;
- exact current verdict: this formula surface is not currently supportable as a
  compile-clean production theorem on the present branch;
- source-first reason is not mathematical falsity of the formula but missing
  local plumbing already on the theorem-3 import path:
  1. the needed open-forward-tube neighborhood step is still not available as
     a compile-clean local theorem on the exact shell codomain:
     the direct insertion-point probe failed already on
     `have hopen : IsOpen (ForwardTube d (n + m)) := BHW.isOpen_forwardTube (d := d)`,
     so the current branch still lacks the explicit local bridge from the
     BHW-side forward-tube presentation to the exact `ForwardTube d (n + m)`
     target used in the shell proof body;
  2. the real/complex derivative restriction route for the outer `bvt_F`
     derivative still hits the exact instance-synthesis failure

     `IsScalarTower ℝ ℂ (Fin (n + m) → Fin (d + 1) → ℂ)`

     on the multivariate shell codomain inside
     `OSToWightmanPositivity.lean`;
  3. the affine-shell differentiability extraction from
     `contDiff_canonicalXiShift` still lacks a compile-clean local bridge at
     the needed API surface: the direct probe failed on the naive
     `differentiableAt_univ` route, so one still has to cleanly extract a real
     `DifferentiableAt` / `HasFDerivAt` theorem for the affine shell map even
     before any derivative-growth estimate starts;
- because those are additional infrastructure steps rather than an already
  supported theorem at the requested surface, the formula theorem itself is not
  the honest next landed item for this branch;
- exact missing local contract beneath that bound is a compile-clean shell
  derivative theorem of the form

```lean
∀ y : NPointDomain d (n + m),
  HasFDerivAt shellCoeff
    ((((fderiv ℂ (bvt_F OS lgc (n + m)) (shellPoint y)).restrictScalars ℝ).comp
        (fderiv ℝ shellMap y))) y
```

  where `shellMap` is the canonical fixed-time `xiShift` map and
  `shellCoeff := fun y => bvt_F OS lgc (n + m) (shellMap y)`;
- therefore the derivative-valued zeroth-order polynomial bound recorded above
  is no longer the directly landable next theorem surface.
  The current exact verdict is sharper:
  even that bound still depends first on the absent local compile-clean
  `HasFDerivAt` / `fderiv` contract for `shellCoeff`.
- the matching source-hygiene recheck is now explicit as well:
  `OSToWightmanPositivity.lean` no longer contains unsupported residue from the
  failed `fderiv_bvt_F_canonicalXiShift_eq_chainRule` attempt at the insertion
  point, so no production Lean cleanup remains to be done before attacking that
  contract.

Addendum (2026-04-14, bounded theorem-3 actual-shell supervision pass: exact shell-local derivative-contract verdict refresh):

- the required insertion-point hygiene check was repeated first on
  `OSToWightmanPositivity.lean` immediately after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
  no unsupported derivative theorem residue was found there;
- a scratch compile probe then tested exactly the local theorem surface
  currently below the derivative-valued growth target:
  a `HasFDerivAt` theorem for the actual shell coefficient `shellCoeff` on the
  canonical fixed-time `xiShift` shell;
- exact result: the branch still does **not** already support that theorem as
  one compile-clean non-wrapper local item at the true insertion point;
- one previously recorded blocker is now source-cleared:
  the open-forward-tube neighborhood step is locally available by rewriting
  with `BHW_forwardTube_eq` and then using `BHW.isOpen_forwardTube`;
  the scratch theorem compiled through that step, so open-set bridging is no
  longer the sharp blocker for this contract;
- the sharpest current compile failure is now the real/complex restriction
  seam for the multivariate shell codomain:

  `IsScalarTower ℝ ℂ (Fin (n + m) → Fin (d + 1) → ℂ)`;

  until that instance/plumbing issue is resolved locally, the outer
  `HasFDerivAt.restrictScalars` step for `bvt_F` cannot be packaged compile-
  clean on this route;
- the domain-side affine derivative extraction is still also missing at the
  exact local API surface:
  the direct attempt to obtain
  `ContDiffAt ℝ (1 : ℕ∞) shellMap y` via

  `(contDiff_canonicalXiShift (d := d) hm t ε).contDiffAt y`

  failed by application type mismatch, so there is not yet a compile-clean
  local bridge from `contDiff_canonicalXiShift` to the needed
  `DifferentiableAt` / `HasFDerivAt` statement for `shellMap`;
- therefore the exact next-smaller theorem/local-lemma surface verdict is now:
  the shell-local `HasFDerivAt` / `fderiv` theorem for `shellCoeff` remains the
  first missing contract, but the precise missing sub-contract beneath it is no
  longer open-tube bridging; it is the pair of local derivative-plumbing seams
  above, with the scalar-tower synthesis failure the sharpest concrete compile
  stop observed in this pass;
- no production Lean edit was kept, and no new theorem/local lemma landed
  compile-clean in this pass.

Addendum (2026-04-14, bounded theorem-3 actual-shell supervision pass: affine-shell bridge state corrected to match the live branch):

- the required `(A0)` insertion-point recheck was repeated source-first in
  `OSToWightmanPositivity.lean` at both live surfaces:
  immediately after `contDiff_canonicalXiShift`, and immediately after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
  result: the live file now contains both
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift`, and the later insertion surface remains
  clean of unsupported derivative residue;
- the smallest honest build check for the touched theorem file,
  `lake env lean OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  completed with exit code `0` and only the pre-existing warnings plus the
  known single production `sorry` in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- therefore the domain-side affine-shell bridge is no longer merely a candidate
  target on the current branch:
  the exact compile-clean local theorem now already present at the true
  insertion surface is

  `hasFDerivAt_canonicalXiShift`;

- this means the earlier verdicts in the preceding addenda are now stale at
  that one specific point:
  the branch no longer lacks a local `ContDiff` ->
  `DifferentiableAt` / `HasFDerivAt` bridge for the affine shell map itself;
- the exact next-smaller theorem/local-lemma surface now in force is therefore
  shifted one notch later:
  source-first reassess the shell-coefficient-local theorem for

  `shellCoeff := fun y =>
    bvt_F OS lgc (n + m)
      (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
        (fun k μ =>
          ↑(y k μ) +
            ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
              Complex.I)
        (t : ℂ))`;

- on the current branch, the precise remaining sub-contract beneath that
  reassessment is no longer the affine shell map:
  it is the outer shell-coefficient derivative plumbing, beginning with the
  real/complex restriction step for `bvt_F` at the shell codomain
  (the `restrictScalars` / scalar-tower seam).

Addendum (2026-04-14, bounded theorem-3 actual-shell supervision pass: shell-coefficient reassessment sharpened one step smaller):

- `(A0)` was re-run first on the live file at both insertion surfaces:
  immediately after `contDiff_canonicalXiShift` the file contains
  `differentiableAt_canonicalXiShift` and `hasFDerivAt_canonicalXiShift`,
  and immediately after `bvt_F_canonicalXiShift_hasPolynomialGrowth` there is
  no unsupported derivative residue;
- the bounded compile probe in this pass then moved one theorem smaller than
  the earlier `HasFDerivAt` attempt and tested the shell-coefficient theorem

  `DifferentiableAt ℝ
    (fun y : NPointDomain d (n + m) =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ))) y`;

- source-first verdict is still negative:
  this smaller theorem is not compile-clean landable on the present branch;
- the open-forward-tube neighborhood step is no longer part of the blocker:
  rewriting with `BHW_forwardTube_eq` and using `BHW.isOpen_forwardTube`
  suffices to derive the outer
  `DifferentiableAt ℂ (bvt_F OS lgc (n + m)) (shellMap y)`;
- the exact compile stop is now the codomain-side scalar restriction line

  `exact houter_diff.restrictScalars ℝ`

  with instance synthesis failure

  `IsScalarTower ℝ ℂ (Fin (n + m) → Fin (d + 1) → ℂ)`;

- therefore the exact next-smaller theorem/local-lemma surface now in force is:
  before the shell-local `HasFDerivAt` / `fderiv` theorem for `shellCoeff`,
  and even before the smaller shell-local `DifferentiableAt` theorem, the
  branch still needs a compile-clean codomain-side real/complex derivative
  restriction bridge for the shell codomain
  `Fin (n + m) → Fin (d + 1) → ℂ`;
- no production Lean edit was kept and no new theorem/local lemma landed
  compile-clean in this pass.

Addendum (2026-04-14, bounded theorem-3 actual-shell supervision pass: shell-local derivative seam rechecked after the codomain bridge landed):

- `(A0)` was re-run first at both required insertion surfaces in
  `OSToWightmanPositivity.lean`;
  immediately after `contDiff_canonicalXiShift` the file still contains
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift`, and immediately after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth` the surface is still clean
  except for the now-landed local bridge
  `differentiableAt_bvt_F_restrictScalars`;
- source-first reinspection of the second insertion surface shows that the
  exact shell-local real chain-rule theorem requested by the current bounded
  pass is already present in production as

  `hasFDerivAt_bvt_F_canonicalXiShift`;

- this removes the previously recorded `restrictScalars` blocker at the exact
  local derivative surface;
- the smallest honest file check,
  `lake env lean OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`,
  succeeds with exit code `0`, with warnings only and the already-known single
  production `sorry`;
- exact corrected verdict now in force:
  there is no unsupported shell-local real `HasFDerivAt` / chain-rule residue
  below the shell-coefficient differentiability package on the live branch;
- exact next-smaller theorem/local-lemma surface now in force:
  the next honest local theorem below the eventual shell-coefficient
  `Function.HasTemperateGrowth` package is not another differentiability
  bridge, but the positive-order derivative-growth theorem needed to apply
  `Function.HasTemperateGrowth.of_fderiv` to the same shell coefficient;
- no production Lean edit was made in this pass, because the requested local
  theorem was already landed compile-clean.

Addendum (2026-04-14, bounded theorem-3 actual-shell pass: complement branch rechecked beneath the landed raw shell CLM):

- `(A0)` was re-run first in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`;
  both live insertion surfaces are still clean:
  immediately after `contDiff_canonicalXiShift` the file still contains
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift`, while immediately after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth` it still contains
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift`;
- no unsupported residue was found or removed at either insertion point;
- exact live branch point rechecked:
  in `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  inside the local `hlimit_os` block, immediately after
  `have hweightEqOnSource := ...`, where the proof must still eliminate the
  remaining complement branch
  `y ∉ section43PositiveEnergyRegion d (n + m)`;
- exact one-theorem-smaller candidate statements checked:

  1. source-side complement vanishing

     `∀ y, y ∉ section43PositiveEnergyRegion d (n + m) →
        (f.conjTensorProduct g) y = 0`;

  2. ambient/source representative comparison on the complement

     `∀ y, y ∉ section43PositiveEnergyRegion d (n + m) →
        (φ.conjTensorProduct ψ) y = (f.conjTensorProduct g) y`;

  3. ambient complement vanishing

     `∀ y, y ∉ section43PositiveEnergyRegion d (n + m) →
        (φ.conjTensorProduct ψ) y = 0`;

  4. fixed-`ε` shell-coefficient complement annihilation

     `∀ y, y ∉ section43PositiveEnergyRegion d (n + m) →
        coeff y * (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y) = 0`,

     where `coeff` is the actual fixed-time canonical shell coefficient used by
     `section43_fixedTimeShellRawCLM`;
- why each still falls short:

  1. (1) is source-backed from existing support geometry, but it only removes
     the source term and therefore is not the missing insertion-point theorem;

  2. (2) is unsupported because all available quotient transport theorems only
     compare representatives on
     `section43PositiveEnergyRegion d (n + m)` or on source support;

  3. (3) is unsupported for the same reason: the branch has no theorem forcing
     the ambient representative itself to vanish on the complement;

  4. (4) is exactly the sort of complement-sensitive supplier the live shell
     proof would need, but it is not already justified by the paper/docs/source
     route on the present branch.
- exact sharpened implementation-frontier verdict:
  the first missing mathematics is now exposed as ambient complement control
  for the actual fixed-`ε` shell coefficient, not another region-local or
  source-support corollary;
  no honest compile-clean non-wrapper theorem/local lemma is currently
  supportable strictly below `section43_fixedTimeShellRawCLM`;
- therefore the exact first missing contract is the fixed-pair shell-
  comparison limit already consumed by the live `hlimit_os` seam,

  `Filter.Tendsto
    (fun ε : ℝ =>
      section43_fixedTimeShellRawCLM OS lgc hm t ε hε
        (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
    (nhdsWithin 0 (Set.Ioi 0))
    (nhds 0)`;

  the stronger reusable raw-kernel inclusion

  `section43PositiveEnergyVanishingSubmodule (d := d) (n + m) ≤
    LinearMap.ker
      (section43_fixedTimeShellRawCLM OS lgc hm t ε hε).toLinearMap`

  remains unsupplied too, but this bounded source check shows it is not the
  smallest theorem contract needed to continue honestly on the current branch.

- fresh bounded fixed-pair actual-shell re-check again re-ran `(A0)` first and
  confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact bounded source check:
  re-inspect the exact proof body of
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`,
  especially `hlimit_os`, together with
  `section43_fixedTimeShellRawCLM`, to decide whether exactly one honest
  theorem-sized fixed-pair actual-shell comparison-limit statement already
  lands at the true insertion point;
- exact candidates re-checked:

  1. the direct raw-CLM comparison limit

     `Filter.Tendsto
       (fun ε : ℝ =>
         section43_fixedTimeShellRawCLM OS lgc hm t ε hε
           (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g))))
       (nhdsWithin 0 (Set.Ioi 0))
       (nhds 0)`;

  2. the same target after unfolding `section43_fixedTimeShellRawCLM` to the
     ambient shell integral against its local coefficient `coeff`;

  3. the equal-limit form comparing the same shell against
     `(φ.conjTensorProduct ψ)` and `(f.conjTensorProduct g)`;

  4. the fixed-`ε` precursor
     `section43_fixedTimeShellRawCLM ... (((φ.conjTensorProduct ψ) -
       (f.conjTensorProduct g))) = 0`;
- why each still fails on source:

  1. `(1)` is still the exact first missing contract; no smaller honest theorem
     at the true insertion point currently discharges it;

  2. `(2)` remains only a definition-level repackaging of `(1)`, so it adds no
     new mathematics;

  3. `(3)` is equivalent to `(1)` and still fails at the same complement
     branch `y ∉ section43PositiveEnergyRegion d (n + m)`;

  4. `(4)` would still require a complement-sensitive annihilation theorem for
     the actual shell coefficient which the branch does not yet have;
- exact conclusion for this pass:
  there is still no single source-backed theorem-sized fixed-pair actual-shell
  comparison-limit lemma that lands honestly at the true insertion point; no
  production Lean edit was made in this pass.
Fresh bounded theorem-3 complement-side pointwise-supplier verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact theorem-search conclusion:
  no honest new theorem/local lemma strictly below the current complement-side
  pointwise supplier is implementable on the live route;
- exact supplier contract now exposed:

  `∀ y : NPointDomain d (n + m),
      y ∉ section43PositiveEnergyRegion d (n + m) →
      Filter.Tendsto
        (fun ε : ℝ =>
          bvt_F OS lgc (n + m)
            (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
              (fun k μ =>
                ↑(y k μ) +
                  ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                    Complex.I)
              (t : ℂ)) *
            (((φ.conjTensorProduct ψ) - (f.conjTensorProduct g)) y))
        (nhdsWithin 0 (Set.Ioi 0))
        (nhds 0)`;

- why this is the first honest contract:
  1. the live raw shell functional still integrates over all `y`;
  2. the quotient codomain API only converts quotient equality into equality on
     `section43PositiveEnergyRegion d (n + m)`;
  3. the already-landed fixed-pair pointwise consequences only control source
     support and already-in-region outside-source points;
  4. the true complement branch remains uncontrolled by current source;
- no still-smaller source-backed explicit hypothesis beneath that supplier was
  found in this pass;
- `hbound` remains unsupported too, but only after that first complement-side
  pointwise branch;
- no new compile-clean theorem/local lemma landed in production Lean in this
  pass.
Fresh bounded theorem-3 coefficient-package decomposition verdict (2026-04-15):

- `(A0)` was re-run first and again confirmed both live insertion surfaces in
  `OSToWightmanPositivity.lean` are still clean:
  `differentiableAt_canonicalXiShift` and
  `hasFDerivAt_canonicalXiShift` remain directly after
  `contDiff_canonicalXiShift`, while
  `section43_fixedTimeShellRawCLM`,
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`,
  `differentiableAt_bvt_F_restrictScalars`, and
  `hasFDerivAt_bvt_F_canonicalXiShift` remain directly after
  `bvt_F_canonicalXiShift_hasPolynomialGrowth`;
- `OSToWightmanPositivity.lean` still has exactly one production `sorry`, in
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- no unsupported residue was found or removed at either insertion point;
- exact decomposition verdict:
  the coefficient-side complement boundary-vanishing package does not
  decompose into a smaller honest theorem/local lemma on the current OS-I/OS-II
  route;
- exact source reason:
  the current quotient/support API is region-local only, and the shell source
  currently provides forward-tube holomorphy, shell continuity /
  differentiability, and polynomial growth at positive imaginary height only;
  no current theorem identifies or annihilates the real-boundary canonical
  shell coefficient on the true complement, so any weaker useful supplier still
  collapses back to the same missing complement-side coefficient statement;
- exact OS-I/OS-II citation status:
  OS I Section 4.3 Lemma 4.1, Lemma 4.2, `(4.27)`, `(4.28)`, plus
  OS II Theorems 4.1-4.3 and Chapter V `(5.2)`-`(5.4)`, cover the
  positive-energy / semigroup / Fourier-Laplace side of the route only;
  they do not already provide complement-side boundary vanishing for the live
  canonical-shell coefficient;
- exact present-source / paper status:
  this package is not already present in current source or the cited paper
  slots and is therefore the first genuinely new mathematical package on the
  live route;
- exact package order from that package to `hlimit_os`:
  1. coefficient-side complement pointwise coefficient vanishing;
  2. complement-side pointwise `hlim` supplier;
  3. complement-sensitive `hbound`;
  4. limit-level canonical-shell kernel/fixed-pair annihilation;
  5. specialization to the actual tensor difference;
  6. `hlimit_os`;
- no production Lean edits were made and no new compile-clean theorem/local
  lemma landed in this pass.
Fresh bounded theorem-3 audit on the same live actual-shell route, checking the
generic nuclear-seminorm finite-net theorem source-first against the current
finite-family theorem interface (`2026-04-15`, current bounded pass,
`2026-04-15T23:59:59Z`):

- `(A0)` re-check confirmed:
  `section43_fixedTimeShellRawCLM`
  is still immediately followed by
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`,
  and the live theorem-level `sorry`s remain exactly
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  and
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- compile-clean consumer re-check:
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  remains present compile-clean in `SchwartzComplete.lean`;
- compile-clean producer-input re-check:
  `uniform_nuclear_tail_lt_of_q_bounded`,
  `finite_coordinate_image_totallyBounded_of_q_bounded`,
  and
  `finite_weighted_coordinate_net_of_q_bounded`
  remain present compile-clean in `NuclearSpace.lean`;
- verification re-check:
  `lake env lean OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean`
  exited `0` with only the pre-existing unrelated theorem-level `sorry`
  warnings at
  `gauge_dominates_on_subspace_of_convex_nhd`
  and
  `product_seminorm_dominated`;
- unsupported residue verdict:
  yes;
  the smaller-candidate notes were still suppressing the fact that the generic
  proof needs tail control on `x - y`, hence needs the finite-head net centres
  to remain certified members of `B`;
  the current theorem statement of
  `finite_weighted_coordinate_net_of_q_bounded`
  does not expose that membership information;
- no production Lean edit landed in this pass;
- decomposition result:
  the generic nuclear-seminorm finite-net theorem lowers one honest step
  smaller only to a local assembly theorem that combines:
  head-net centres explicitly in `B`
  +
  uniform tail control on differences `x - y` with `x, y ∈ B`
  +
  `Summable.sum_add_tsum_nat_add`
  +
  the nuclear-dominance inequality for `p`;
- exact first internal proof obligation beneath the full generic theorem:
  prove that assembly theorem or rederive its ingredients inline;
- whether that obligation is genuinely smaller:
  yes in theorem shape;
  no as a black-box consumer of the current finite-family theorem statement,
  because that statement forgets the representative-in-`B` witness needed to
  control the tail on `x - y`;
- exact best Lean-ready theorem statement now required:
  still the full generic `p`-finite-net theorem in `NuclearSpace.lean`;
  the smallest honest local theorem below it is the head-plus-tail assembly
  statement recorded in
  `docs/theorem3_os_route_blueprint.md`;
- exact minimal hypotheses:
  for the smaller local assembly theorem:
  fixed seminorm `p`,
  scalar-valued `f : ℕ → V →L[ℝ] ℝ`,
  coefficients `c`,
  the dominance inequality
  `∀ z, p z ≤ ∑' n, ‖f n z‖ * c n`,
  a finite head net whose centres are still known to lie in `B`,
  and a uniform tail estimate on differences of points of `B`;
- exact codomain/quotient objects:
  none;
  internally only a finite head-coordinate product `Fin N → ℝ`
  and the scalar tail series are used;
- exact insertion surface:
  `OSReconstruction/Wightman/NuclearSpaces/NuclearSpace.lean`,
  immediately below
  `finite_weighted_coordinate_net_of_q_bounded`;
- exact downstream chain:
  generic nuclear-seminorm finite-net theorem
  ->
  Schwartz finite-net theorem
  ->
  `SchwartzMap.tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  ->
  strong-topology basis / `ContinuousLinearMap.eventually_nhds_zero_mapsTo`
  ->
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`
  ->
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime`;
- source-backed versus genuinely new:
  source-backed:
  shell witness existence,
  pointwise convergence,
  finite-seminorm shell control,
  and the strong-topology bounded-set target;
  already landed new infrastructure:
  uniform tail truncation and the finite-family weighted-coordinate theorem;
  genuinely new:
  the difference-tail plus head-net assembly into the generic nuclear-seminorm
  finite-net theorem,
  then the Schwartz specialization.
Supervision correction (`2026-04-15 14:05 UTC`): a direct proof attempt at `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime` was made and then reverted after compile failure. No production Lean change was kept.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T14:05:00Z`):

- theorem surface tested:
  `section43_fixedTimeShellRawCLM_tendsto_exists_fixedTime`;
- exact proof packaging reached before failure:
  pointwise shell limit data
  + common seminorm control
  + manual `ℝ`-linear packaging of the shell family and the existential limit functional
  + planned use of `tempered_eventually_mapsTo_sub_of_finite_seminorm_net`
  + strong-topology basis on `ContinuousLinearMap`;
- exact first compile-blocking obligation:
  Lean cannot synthesize
  `NuclearSpace (SchwartzMap (NPointDomain d (n + m)) ℂ)`
  at the finite-net invocation site;
- exact blocked consumer:
  `NuclearSpace.finite_net_of_complex_schwartz_seminorm_of_isVonNBounded`;
- audit consequence:
  the route does not yet fail at the final strong-topology neighborhood packaging;
  it fails one step earlier, at the bounded-set finite-net transport from the Euclidean nuclear Schwartz instance to the actual `NPointDomain` shell test space;
- blocker classification:
  `(d)` another exact source-backed sub-obligation;
- production Lean verdict:
  none;
  the theorem remains `sorry`.
Supervision correction (`2026-04-15 20:51 UTC`): one bounded audit reread of the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body reduction through factoring the constant `h y` immediately requires the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T20:51:28Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.
Supervision correction (`2026-04-15 20:51 UTC`): one bounded audit reread of the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body reduction through factoring the constant `h y` immediately requires the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T20:51:28Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.
Supervision correction (`2026-04-15 20:56 UTC`): one bounded audit reread of the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body reduction through factoring the constant `h y` immediately requires the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T20:56:29Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.
Supervision correction (`2026-04-15 21:01 UTC`): one bounded audit reread of the exact live seam reconfirmed that no honest compile-clean local step can be kept inside the theorem body. The `hy` branch still reduces only to the same unmasked fixed-`y` complement-side scalar shell limit, and even the only plausible in-body reduction through factoring the constant `h y` immediately requires the same missing complement-side shell coefficient limit. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-15`, current bounded pass, `2026-04-15T21:01:23Z`):

- exact audited theorem:
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`;
- exact audited fixed-`y` complement-branch goal:
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4555),
  after
  `by_cases hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  and
  `simp only [F, indicator_of_mem hy]`,
  the branch goal is exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited feeder chain:
  theorem-local `F` at line `4536` defines the masked family;
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at line `4430` supplies domination only;
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at line `4566` is only the unmasked DCT consumer once pointwise limits already exist;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  do not touch the complement branch;
- exact audited blocker verdict:
  no honest retained local rewrite remains after that `simp`;
  the only plausible local in-body factorization, namely reducing to a shell coefficient limit times the constant `h y`, still demands the same missing coefficient limit
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)))
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`,
  and no live theorem proves it;
- exact audited insertion surface / consumer chain / source status:
  the unresolved theorem remains at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514)
  and is still the direct supplier for `hlimit_os` beginning at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9701);
  OS II page index `10` / printed page `291`, formula `(5.4)`, still supplies only the fixed-time analytic-continuation identity and no separate complement-localization input below this seam.
Supervision correction (`2026-04-16 01:21 UTC`, bounded audit pass): re-audited the mandated sources plus the exact live theorem body, the immediately following dominated-convergence closure, the earlier `hlimit_os` case-(3) decomposition note, the quotient API, and OS II page `291`, formula `(5.4)`. Again no honest retained theorem-body step exists inside `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`. The post-`simp only [F, indicator_of_mem hy]` obligation is still the same fixed-`y` complement-side scalar shell limit; pulling out `h y` still only renames the blocker as the canonical-shell coefficient limit; the nearby dominated-convergence theorem still assumes exactly that pointwise limit as `hlim`; the earlier `hlimit_os` analysis still fails first in outside-region case `(3)`; the quotient API still stops on-region; and OS II `(5.4)` still stops at the fixed-time semigroup / one-variable analytic-continuation identity. No production Lean edit was made.

Fresh bounded theorem-3 audit correction on the same live actual-shell route (`2026-04-16`, current bounded pass, `2026-04-16T01:21Z`):

- exact audited branch goal:
  in
  `section43_fixedTimeShellRaw_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4514),
  the branch
  `hy : y ∈ (section43PositiveEnergyRegion d (n + m))ᶜ`
  still becomes, after
  `simp only [F, indicator_of_mem hy]`,
  exactly
  `Filter.Tendsto
    (fun ε : ℝ =>
      bvt_F OS lgc (n + m)
        (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
          (fun k μ =>
            ↑(y k μ) +
              ε * ↑(canonicalForwardConeDirection (d := d) (n + m) k μ) *
                Complex.I)
          (t : ℂ)) *
      h y)
    (nhdsWithin (0 : ℝ) (Set.Ioi 0))
    (nhds 0)`;
- exact audited blocker verdict:
  no honest local `have` / `calc` / factorization lowers that goal;
  the only plausible rewrite still separates the constant `h y`, but that leaves
  the same canonical-shell coefficient limit and therefore does not shrink the
  mathematics;
  the nearby theorem
  `section43_fixedTimeShellRaw_tendsto_zero_of_dominated_convergence`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4566)
  is not a missed local step, because it assumes this same pointwise limit as
  hypothesis `hlim`;
- exact audited source boundary:
  `section43_fixedTimeShellRaw_on_section43PositiveEnergyRegion_compl_eventually_bounded`
  at [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4430)
  still supplies domination only;
  `eqOn_region_of_section43PositiveEnergyQuotientMap_eq` and
  `section43PositiveEnergyQuotientMap_sub_eq_zero_of_eqOn_region`
  at [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:106)
  and [Section43Codomain.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/Section43Codomain.lean:121)
  still stop on `section43PositiveEnergyRegion`;
  the later `hlimit_os` branch audit at
  [OSToWightmanPositivity.lean](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9813)
  still fails first in outside-region case `(3)`;
  OS II printed page `291`, formula `(5.4)`, still reaches only the fixed-time
  analytic-continuation identity in one time variable;
- exact first missing mathematics:
  the first missing theorem remains the complement-side coefficient limit for the
  canonical shell family;
  equivalently, no audited source object yet removes the outside-region case
  `(3)` on the actual shell by complement-localization or quotient-zero-on-
  complement control.

Supervision correction (`2026-04-16`, bounded theorem-(1) audit on the live complement-side seam): no honest Lean edit survived; this pass was docs-only.

- exact intended theorem-(1) surface remains:
  insert immediately above
  `section43_fixedTimeShellRaw_coefficient_tendsto_zero_on_section43PositiveEnergyRegion_compl`
  a `private theorem` producing `Uflat`, `hUflat_open`, `hyUflat`,
  `hUflat_sub`, and
  `hcontUflat : ∀ xflat ∈ Uflat, ContinuousWithinAt G_t
    (SCV.TubeDomain (ForwardConeFlat d (n + m))) (SCV.realEmbed xflat)`,
  for the flattened fixed-time shell continuation
  `G_t zflat := bvt_F OS lgc (n + m)
    (xiShift ⟨n, Nat.lt_add_of_pos_right hm⟩ 0
      ((flattenCLEquiv (n + m) (d + 1)).symm zflat) (t : ℂ))`;
- exact audited near-source package:
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime`,
  `section43_fixedTime_xiShift_hasGlobalForwardTubeGrowth`,
  `ForwardTubeDistributions.schwartz_bv_to_flat_repr`,
  `OSToWightmanBoundaryValueLimits.exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr`,
  and `SCV.fourierLaplaceExtMultiDim_continuousOn`;
- exact audited reason theorem (1) still fails:
  the current source reaches only:
  1. a weak fixed-time boundary-value functional,
  2. a flattened FL representation on the open tube,
  3. continuity of the FL extension **inside** the tube;
  it does **not** provide any theorem identifying the real complement-side
  boundary trace or upgrading that interior continuity to
  `ContinuousWithinAt` at real boundary points in the flattened complement
  image;
- blocker classification:
  missing local-neighborhood topology is not the main obstruction, since the
  complement image can be localized once boundary regularity exists;
  the live blocker is a continuity formulation mismatch at the real edge;
- route consequence:
  theorem (2) remains blocked first by theorem (1), and the compact-support
  fixed-direction DCT helper should still not be attempted yet.
Supervision correction (`2026-04-16`, source-pinned audit of the exact local-open theorem that would recover `hrepr_boundary_shifted`):
this pass is docs-only.

Exact interfaces inspected in the live source:

- `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` at
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:156](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:156);
- `SCV.eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn` at
  [OSReconstruction/SCV/DistributionalUniqueness.lean:1507](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/DistributionalUniqueness.lean:1507);
- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241);
- `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point` at
  [OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/ForwardTubeDistributions.lean:688);
- `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4686](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4686).

Audit verdict on the exact smallest theorem surface:

- the first honest theorem above the raw shifted boundary equality is an
  open-neighborhood `Set.EqOn` theorem, not a single-point continuity lemma;
- after unpacking the witness package
  `Tflat`, `hTflat_support`, `hTflat_W`, `hrepr_tube`,
  the precise candidate target is:
  `∃ Uflat, IsOpen Uflat ∧ xflat0 ∈ Uflat ∧ Set.EqOn g h Uflat`,
  where
  `xflat0 := flattenCLEquivReal q (d + 1) (xiShift j 0 y t)`,
  `g xflat :=
    bvt_F OS lgc q
      (xiShift j 0 ((flattenCLEquiv q (d + 1)).symm (SCV.realEmbed xflat))
        (t : ℂ))`,
  and
  `h xflat :=
    fourierLaplaceExtMultiDim
      (ForwardConeFlat d q)
      hCflat_open hCflat_conv hCflat_cone hCflat_salient
      Tflat (SCV.realEmbed xflat)`;
- applying `Set.EqOn` at `xflat0` gives `hrepr_boundary_shifted`.

Audit verdict on the needed hypotheses:

- yes, this route genuinely needs an open neighborhood, not only continuity at
  `xflat0`;
- yes, it needs continuity on that whole neighborhood for both traces:
  the shifted `bvt_F` side and the exact FL side;
- the SCV route uses
  `fourierLaplace_boundary_recovery_on_open_of_tempered` to obtain pairing
  equalities for compactly supported Schwartz tests on `Uflat`, then
  `eqOn_open_of_compactSupport_schwartz_integral_eq_of_continuousOn` to turn
  those pairings into `Set.EqOn`.

Audit verdict on whether current source already almost supplies the continuity package:

- on the shifted complement side, only as a theorem surface that is not
  independently proved, because the open-neighborhood theorem at positivity
  line `4686` is built directly from the live theorem-(1) `sorry`;
- on the exact FL side, no proved continuity theorem is available at all, since
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` remains `sorry`;
- therefore the first missing mathematics is the boundary continuity/trace
  theorem itself, not a later local-open equality wrapper.

Insertion site remains:

- immediately below the private witness theorem in
  `OSToWightmanBoundaryValueLimits.lean`;
- then evaluation at `xflat0` supplies `hrepr_boundary_shifted`;
- then the existing theorem-(1) consumer
  `boundary_function_continuousWithinAt_forwardTube_of_flatFLRepr_at_point`
  remains unchanged.

Supervision correction (`2026-04-16`, audit of the exact circular edge under the shifted-side local-open continuity package):
this pass is docs-only.

Exact statements and circular edge:

- `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl` at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4583](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4583)
  is the live theorem-(1) seam:
  it directly states full `ContinuousWithinAt` of raw `bvt_F` at the shifted
  real point;
- `section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl` at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4630](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4630)
  is only the flattened pointwise transport of that same continuity;
- `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl` at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4686](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4686)
  packages the open set actually needed by the local-open SCV route, but its
  last proof step is exactly the pointwise theorem at `4630`;
- therefore the exact circular chain is:
  `4686 localFlatContinuity`
  → `4630 pointwiseFlatContinuity`
  → `4600 pointwiseContinuity`
  → `4583 theorem-(1) shifted ContinuousWithinAt`
  → live `sorry`.

Audit verdict on the smallest honest non-circular replacement:

- for the local-open SCV route, the smallest useful non-circular shifted-side
  theorem is not the pointwise theorem at `4630`;
- the smallest honest replacement is the open-neighborhood continuity theorem
  with the same shape now exposed at `4686`, but proved independently of
  theorem-(1):
  after fixing
  `q := n + m`,
  `j := ⟨n, Nat.lt_add_of_pos_right hm⟩`,
  and
  `G_t zflat :=
    bvt_F OS lgc q
      (xiShift j 0 ((flattenCLEquiv q (d + 1)).symm zflat) (t : ℂ))`,
  prove
  `∃ Uflat, IsOpen Uflat ∧
      flattenCLEquivReal q (d + 1) y ∈ Uflat ∧
      Uflat ⊆ (flattenCLEquivReal q (d + 1)) ''
        ((section43PositiveEnergyRegion d q)ᶜ) ∧
      ∀ xflat ∈ Uflat, ContinuousWithinAt G_t
        (SCV.TubeDomain (ForwardConeFlat d q))
        (SCV.realEmbed xflat)`;
- this is exactly the shifted-side continuity package needed to build
  `ContinuousOn` for the local-open boundary-agreement route;
- theorem-(1) itself still must remain the full `ContinuousWithinAt` consumer
  on the current branch.

Audit verdict on nearby upstream source:

- no proved upstream source already almost supplies this package from the live
  witness data without circularity;
- the only nearby stronger theorem surface is the regular package
  `SCV.HasFourierLaplaceReprRegular` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:118](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:118),
  which includes `boundary_continuous` and `tube_continuousWithinAt` as
  assumptions, not consequences;
- on the exact FL side, the corresponding pointwise boundary continuity theorem
  `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` is still a live
  `sorry`, so there is no proved twin theorem to mirror on the shifted side.

Exact insertion site:

- if this non-circular shifted-side theorem is later proved, place it
  immediately below
  `exists_flattened_bvt_F_dualCone_distribution_with_fourierLaplace_repr` in
  `OSToWightmanBoundaryValueLimits.lean`;
- it should feed only the shifted-trace `ContinuousOn` input of the future
  local-open boundary-agreement theorem above `hrepr_boundary_shifted`,
  not theorem-(1) directly.

Supervision correction (`2026-04-16`, exact failure classification of the primitive FL direct proof):
this pass is docs-only.

Exact direct-composition audit:

- source path theorem:
  `SCV.realPlusIEpsEta_tendsto_nhdsWithin_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3510](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3510);
- source target theorem:
  `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241);
- source boundary-value theorem:
  `SCV.fourierLaplaceExtMultiDim_boundaryValue` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:4288](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4288).

Exact failed step:

- the only proved FL theorem along the interior ray
  `ε ↦ x + i ε η`
  is the distributional convergence of the integrals against Schwartz tests;
- there is no proved scalar-valued theorem asserting
  `fourierLaplaceExtMultiDim(...)(x+iεη) → fourierLaplaceExtMultiDim(...)(realEmbed x)`;
- therefore the geometric path lemma does not close the target
  `ContinuousWithinAt` theorem.

Exact smallest missing lemma:

- a raywise pointwise-limit theorem for the exact Fourier-Laplace extension:
  for all real `x` and all `η ∈ C`,
  `ε ↦ fourierLaplaceExtMultiDim(...)(x+iεη)`
  tends to
  `fourierLaplaceExtMultiDim(...)(SCV.realEmbed x)`
  as `ε → 0+`;
- this is strictly smaller than a full theorem constructing
  `HasFourierLaplaceReprRegular`;
- but it is still genuinely analytic, not a wrapper and not a topology lemma.

Audit verdict:

- `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` does not yet
  collapse from existing source;
- the blocker surface shrinks precisely to a pointwise FL boundary-ray limit
  theorem;
- no Lean artifact was landed in this pass.

Supervision correction (`2026-04-16`, exact audit refinement of the primitive FL boundary-regularity gap):
this pass is docs-only.

Exact interface extraction:

- `HasFourierLaplaceReprRegular` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:118](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:118)
  adds exactly:
  `poly_growth`,
  `uniform_bound`,
  `boundary_continuous`,
  `tube_continuousWithinAt`;
- `HasFourierLaplaceReprTempered` at
  [143](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:143)
  adds exactly:
  `poly_growth`,
  `uniform_bound`.

Exact primitive source extraction:

- `fourierLaplaceExtMultiDim_holomorphic` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3256](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3256);
- `fourierLaplaceExtMultiDim_vladimirov_growth` at
  [3304](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3304);
- `fourierLaplaceExtMultiDim_boundaryValue` at
  [4288](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4288);
- the file comments at
  [3865](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3865)
  and
  [4303](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4303)
  still explicitly classify the boundary theorem as distributional-only and
  still explicitly allow `ε⁻ᴹ` blowup along boundary rays.

Exact audit verdict on the next theorem shape:

- not theorem 1 only forever:
  a primitive `uniform_bound` theorem is the first missing estimate, but by
  itself it does not supply the boundary-continuity fields used by
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`;
- not theorem 2 only:
  a continuity-only theorem skips the first independent obstruction, because
  even the weaker tempered package is still unproved without `uniform_bound`;
- not one bundled stronger theorem:
  that would hide the estimate-level frontier behind a larger package claim;
- yes: a two-step chain.

Exact proposed chain:

1. `fourierLaplaceExtMultiDim_uniform_bound_near_boundary`
   with conclusion
   `∀ (η : Fin m → ℝ), η ∈ C →
       ∃ (C_bd : ℝ) (N : ℕ) (δ : ℝ), C_bd > 0 ∧ δ > 0 ∧
         ∀ (x : Fin m → ℝ) (ε : ℝ), 0 < ε → ε < δ →
           ‖fourierLaplaceExtMultiDim C hC_open hC_conv hC_cone hC_salient T
               (fun i => ↑(x i) + ↑ε * ↑(η i) * I)‖ ≤
             C_bd * (1 + ‖x‖) ^ N`;
2. `fourierLaplaceExtMultiDim_exists_regular_boundary_repr`
   with conclusion
   `∃ G : (Fin m → ℂ) → ℂ,
       (∀ z, z ∈ SCV.TubeDomain C →
         G z =
           fourierLaplaceExtMultiDim C hC_open hC_conv hC_cone hC_salient T z) ∧
       HasFourierLaplaceReprRegular C G`.

Why this is the first honest target:

- theorem 1 is exactly the first independent missing field already visible in
  the existing package interfaces;
- theorem 2 is then the minimal honest route to the boundary-continuity fields
  on a boundary-correct representative, rather than falsely asserting them for
  the current zero extension.

Supervision correction (`2026-04-16`, final theorem-1 audit verdict on the primitive FL source):
this pass is docs-only.

Exact audit result:

- the exact theorem-1 target is not an existing Lean theorem name but the shared
  `uniform_bound` field already required by both
  `HasFourierLaplaceReprRegular` and `HasFourierLaplaceReprTempered` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:126](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:126)
  and
  [151](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:151);
- `fourierLaplace_uniform_bound_near_boundary` at
  [263](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:263)
  is only a projection from `HasFourierLaplaceReprRegular`;
- `exists_fourierLaplaceReprTempered` at
  [907](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:907)
  still takes the same `hunif` as explicit hypothesis, so there is no missing
  tiny constructor/helper theorem;
- primitive FL source still provides only:
  holomorphicity,
  Vladimirov growth,
  ray-in-tube geometry,
  ray approach to the real boundary,
  and distributional boundary-value convergence
  at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3256](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3256),
  [3304](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3304),
  [3495](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3495),
  [3510](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3510),
  and
  [4288](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4288);
- the comments at
  [4303](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4303)
  explicitly allow `F(x+iεη)` to blow up as `ε → 0+`;
- OS II confirms that the live reference estimate has the same shape:
  in the local offprint `references/reconstruction theorem II.pdf`,
  journal page 303 is local PDF page 24, and equation `(6.31)` still contains
  the inverse-power distance-to-boundary factor;
- after restricting to a fixed interior ray, that estimate remains of
  `ε^{-M}` type and therefore does not imply theorem 1.

Exact verdict:

- theorem 1 itself is the first honest missing analytic theorem on the current
  primitive FL source package;
- no smaller primitive theorem already suggested by current source/reference
  text honestly lands first;
- no genuine non-wrapper helper/stub is worth landing now;
- the current package gives only distance-to-boundary growth and distributional
  boundary values, not theorem-1’s `ε`-uniform bound in `x`.

Audit landing:

- no Lean artifact;
- docs-only.
Supervision correction (`2026-04-16`, theorem-1 downstream dependency audit tightened):
this pass is docs-only.

Exact consumers:

- the package constructor
  `exists_fourierLaplaceReprTempered` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:907](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:907)
  still requires the theorem-1 field as explicit input `hunif`;
- the first SCV DCT consumer after that package exists is
  `fourierLaplace_schwartz_integral_convergence_local` at
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:105](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:105),
  which immediately opens `hTempered.uniform_bound` at
  [120](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:120);
- the first local-open boundary recovery consumer is
  `fourierLaplace_boundary_recovery_on_open_of_tempered` at
  [156](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:156),
  using the local DCT theorem at
  [172](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:172);
- on the regular path,
  `fourierLaplace_schwartz_integral_convergence` at
  [OSReconstruction/SCV/LaplaceSchwartz.lean:305](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:305)
  and
  `fourierLaplace_boundary_recovery` at
  [386](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:386)
  already consume the same field via
  [316](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:316)
  and
  [395](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:395).

Audit decision on the sharp next theorem after theorem 1:

- not `B(1)` immediately;
- not `B(2)` immediately;
- yes `B(3)`:
  a smaller source-backed compact-slice polynomial-growth theorem for the
  primitive FL witness, extracted from
  `fourierLaplaceExtMultiDim_vladimirov_growth` by fixing compact `K ⊆ C`;
- that theorem genuinely sits between theorem 1 and the first honest
  `HasFourierLaplaceReprTempered` supplier, because the package theorem still
  needs both `hpoly` and `hunif`.

Exact chain now audited:

- theorem 1:
  primitive FL `uniform_bound`;
- smaller source-backed intermediate:
  primitive FL `poly_growth` on compact imaginary slices;
- first honest package theorem:
  primitive FL supplier of
  `HasFourierLaplaceReprTempered`;
- first honest boundary-continuity / regular-representative theorem after that:
  a theorem yielding some boundary-valued representative `G` with
  `HasFourierLaplaceReprRegular C G`,
  not the current zero-extension theorem surface
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`.

Exact file seams:

- compact-slice growth theorem:
  `PaleyWienerSchwartz.lean`, immediately after
  `fourierLaplaceExtMultiDim_vladimirov_growth`;
- tempered-package supplier:
  `LaplaceSchwartz.lean`, immediately after
  `exists_fourierLaplaceReprTempered`;
- first regular-representative theorem:
  `LaplaceSchwartz.lean`, at the existing placeholder seam beginning at
  [181](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:181).

Supervision correction (`2026-04-16`, theorem-1 uniform-bound seam re-audited live after the tempered-supplier blocker):
this pass is docs-only.

Exact theorem-1 statement in the required package shape:

- the proposed theorem remains
  `fourierLaplaceExtMultiDim_uniform_bound_near_boundary`;
- the exact statement it must prove is:
  for
  `F := fourierLaplaceExtMultiDim C hC_open hC_conv hC_cone hC_salient T`,
  `∀ η ∈ C, ∃ C_bd N δ, C_bd > 0 ∧ δ > 0 ∧
      ∀ x ε, 0 < ε → ε < δ →
        ‖F (fun i => ↑(x i) + ↑ε * ↑(η i) * I)‖ ≤ C_bd * (1 + ‖x‖) ^ N`;
- this is exactly the live `hunif` field shape required by
  [OSReconstruction/SCV/LaplaceSchwartz.lean:921](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:921)
  through
  [924](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:924),
  and matches
  `HasFourierLaplaceReprTempered.uniform_bound` at
  [151](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:151)
  through
  [154](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:154).

Exact FL-side inputs still honestly available:

- `fourierLaplaceExtMultiDim_boundaryValue` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:4398](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4398);
- `fourierLaplaceExtMultiDim_poly_growth_on_compact` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3342](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3342);
- `realPlusIEpsEta_tendsto_nhdsWithin_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3613](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3613);
- `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` at
  [3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241)
  is still `sorry`, so it is not available;
- `fourierLaplaceExtMultiDim_vladimirov_growth` at
  [3304](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3304)
  remains the strongest primitive pointwise estimate and still carries the
  inverse boundary-distance factor after substituting `x + i ε η`;
- the comment at
  [4405](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4405)
  through
  [4408](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:4408)
  still records that `F(x+iεη)` may blow up as `ε → 0+`.

Exact first missing ingredient and verdict:

- the first missing ingredient is still theorem 1 itself:
  an honest primitive derivation of the `ε`-uniform ray bound;
- Python extraction of
  `references/reconstruction theorem II.pdf`, PDF page `23` / journal page
  `303`, still presents equation `(6.31)` as a temperedness estimate with
  boundary-variable dependence rather than theorem-1's `ε`-uniform ray bound;
- no compile-clean theorem-sized Lean step at the checked seam survives the
  no-wrapper rule, because any new theorem now would only restate already-live
  growth or path facts;
- exact insertion seam remains
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241)
  through
  [3304](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3304).

Supervision correction (`2026-04-16`, exact primitive FL boundary-contract audit after the ray-limit mismatch):
this pass is docs-only.

Exact audit result:

- no single honest compile-clean theorem/helper lands now to replace
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed`;
- the only honest immediate action is docs-only contract repair.

Exact zero-extension source and target mismatch:

- `fourierLaplaceExtMultiDim` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3052](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3052)
  is definitionally `0` outside `TubeDomain C`;
- hence the current theorem target at
  `SCV.realEmbed x` in
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` at
  [3241](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3241)
  is the outside-tube value `0`;
- along the interior ray, the primitive kernel used by the BV proof is the
  cutoff oscillatory family
  `χ(ξ) * exp (-(ε : ℂ) * ⟪η, ξ⟫) * exp (I * ⟪x, ξ⟫)`,
  exposed in
  `regularizedKernel_pointwise` at
  [3712](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3712)
  through
  [3827](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3827);
- its `ε → 0+` limit is the cutoff oscillatory trace, not `0`.

Exact reason the candidate theorem landings fail:

- exporting an explicit cutoff-kernel boundary-ray theorem would only expose an
  internal BV ingredient and still would not match the live zero-extension
  target;
- exporting a theorem with a separate boundary representative `G` would require
  a new package/interface above the current primitive seam;
- therefore no theorem-sized Lean landing survives the no-wrapper rule at this
  stage.

Exact downstream dependency verdict:

- the tempered package/input
  `HasFourierLaplaceReprTempered` /
  `exists_fourierLaplaceReprTempered` in
  [OSReconstruction/SCV/LaplaceSchwartz.lean:139](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:139)
  through
  [154](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:154)
  and
  [907](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:907)
  through
  [929](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LaplaceSchwartz.lean:929)
  needs theorem 1, not this continuity target;
- the refined object is instead needed by the regular branch
  `HasFourierLaplaceReprRegular`, whose fields
  `boundary_continuous` and `tube_continuousWithinAt` currently assume one
  boundary function `x ↦ F (realEmbed x)` for the same ambient `F`;
- `LocalBoundaryRecovery.lean` already shows the honest shape of the needed
  downstream input: a separate local boundary trace hypothesis `hcontU`.

Supervision correction (`2026-04-16`, primitive FL boundary-interface audit after rereading the exact live source):
this pass is docs-only.

Exact audit correction:

- the current tree no longer has a live Lean theorem
  `SCV.fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` to prove or
  repair;
- `OSReconstruction/SCV/PaleyWienerSchwartz.lean` already records, in-source,
  that this theorem surface was removed because it targeted the wrong boundary
  object for the zero-extended `fourierLaplaceExtMultiDim`.

Exact consequence for the seam:

- the active primitive FL seam is no longer “prove the removed continuity
  theorem”;
- it is “do not reintroduce that false target; if pointwise boundary work is
  needed later, introduce a separate boundary-trace object/hypothesis”.

Exact bounded-pass verdict:

- no honest theorem-sized Lean landing remains available under the old target
  name;
- the only minimal repair still needed in this pass is synchronization of the
  audit/docs surfaces with the already-landed source correction.

Supervision correction (`2026-04-16`, post-contract-repair theorem-3 seam audit on the live consumer path):
this pass is docs-only.

Exact audit result:

- after the removal note for
  `fourierLaplaceExtMultiDim_continuousWithinAt_realEmbed` at
  [OSReconstruction/SCV/PaleyWienerSchwartz.lean:3236](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/PaleyWienerSchwartz.lean:3236),
  the theorem-3 frontier is not any primitive FL continuity theorem;
- the live boundary-trace object is
  `bvt_singleSplit_xiShiftHolomorphicValue` at
  [OSToWightmanBoundaryValueLimits.lean:4901](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:4901);
- the exposed theorem-3 input is the Wightman-side limit hypothesis
  `hHlimit` at
  [OSToWightmanBoundaryValueLimits.lean:5296](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:5296)
  through
  [5312](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanBoundaryValueLimits.lean:5312).

Exact blocker source-first:

- the first remaining theorem-sized gap is inside
  `section43_fixedPair_bvtW_timeShift_eq_osHolomorphicValue_fixedTime` at
  [OSToWightmanPositivity.lean:9923](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:9923);
- current source already discharges the source-support case and the
  positive-energy-region-outside-source-support case;
- current source explicitly records the first unresolved branch as the
  outside-region case
  `y ∉ section43PositiveEnergyRegion d (n + m)` at
  [OSToWightmanPositivity.lean:10145](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:10145)
  through
  [10154](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:10154);
- the exact next honest theorem surface is the limit-level ambient-vs-source
  shell comparison written there at
  [OSToWightmanPositivity.lean:10157](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:10157)
  through
  [10177](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:10177).

Verification status:

- no Lean edit was attempted or kept, because the source already identifies the
  new exact blocker and there is no compile-clean one-theorem landing before
  that outside-region shell-limit theorem.

Supervision correction (`2026-04-16`, audit refinement of the exact theorem category below the complement coefficient blocker):
this pass is docs-only.

Exact audit result:

- the earlier “explicit local trace supplier” candidate was too high relative
  to the checked live source;
- the exact minimal next theorem/interface below the coefficient theorem is the
  already-present branch-specific boundary-continuity theorem
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`,
  so the honest classification is:
  docs correction to **(B) boundary-continuity theorem**, not a new trace
  supplier.

Exact reason:

- the direct SCV consumer relevant at this layer is
  `SCV.fourierLaplace_boundary_recovery_on_open_of_tempered` at
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:232](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:232),
  and its local boundary hypothesis is only
  `hcontU : ∀ x ∈ U, ContinuousWithinAt F (TubeDomain C) (realEmbed x)`;
- the explicit-trace consumer
  `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace` at
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:179](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:179)
  exists, but it is not the minimal interface forced by the live positivity
  branch;
- the positivity file already contains the higher shell-specific continuity
  packaging:
  `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  at
  [OSToWightmanPositivity.lean:4686](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4686),
  explicitly documented there as the exact seam beneath the coefficient limit;
- that theorem already reduces the remaining content to the lower raw-`bvt_F`
  continuity theorem at the shifted real boundary point,
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  at
  [OSToWightmanPositivity.lean:4583](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4583);
- the immediate feeder
  `section43_fixedTimeShellRawCLM_boundaryValueData_fixedTime` remains a
  downstream package input for later local-open recovery, but it does not
  change the exact minimal theorem surface directly below the coefficient
  theorem.

Exact no-drift conclusion:

- keep the next theorem exactly in
  `OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean`;
- feed it only with the already-landed data
  `OS`, `lgc`, `hm`, `t`, `y`, `hy`;
- do not introduce an explicit trace object, a new `Uflat` wrapper theorem, or
  a separately passed tempered package at this seam.

Verification status:

- docs-only audit pass;
- no Lean verification command was run.

## 2026-04-17 supervision correction — supplier seam below the direct continuity bridge

Exact reread result:

- `section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime` gives only distributional boundary-value convergence of the fixed-time shifted continuation against Schwartz tests.
- `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace` still asks for pointwise local trace data `htraceU`.
- So the all-directions BV theorem is not yet the needed `htraceUflat` supplier after current checked transports.

Updated blocker classification:

- The direct continuity contract for `G_t` remains the first seam above the supplier layer.
- A separate local-trace supplier theorem remains missing strictly below it.
- No wrapper theorem, fake regular-package upgrade, or same-input shortcut is honest here.

## 2026-04-17 supervision correction — exact smaller theorem-sized object under the supplier seam

Exact audit result:

- the exact smaller real theorem-sized object under the missing positivity-side
  supplier is the already-landed SCV theorem
  `fourierLaplace_schwartz_integral_convergence_local_of_trace` at
  [OSReconstruction/SCV/LocalBoundaryRecovery.lean:179](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:179)
  through
  [219](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/SCV/LocalBoundaryRecovery.lean:219);
- its exact hypothesis surface is the honest supplier contract:
  separate boundary trace `G`, `ContinuousOn G U`, one interior direction
  `η ∈ C`, and pointwise
  `htraceU : ∀ x ∈ U, Tendsto ... (nhds (G x))`;
- the positivity-side fixed-time data still falls short of that contract:
  `section43_fixedTimeShellRaw_temperedRepr_fixedTime` supplies only
  `HasFourierLaplaceReprTempered` for `G_t`, while
  `section43_fixedTimeShellRawCLM_boundaryValueData_allDirections_fixedTime`
  supplies only convergence of test integrals to `T_t`.

Exact blocker classification:

- the supplier theorem itself is still not source-ready, because no checked
  theorem constructs a complement-side local trace function `B_t`, proves
  `ContinuousOn B_t Uflat`, and upgrades the existing distributional data to
  pointwise `htraceUflat`;
- the first live theorem above that still-missing supplier remains
  `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`.

## 2026-04-17 supervision correction — exact transport-layer reread below the supplier seam

Exact audit result:

- `section43_fixedTimeShellRaw_pointwiseContinuity_of_bvt_F_at_shifted_point`
  at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4999](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:4999)
  through
  [5061](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:5061)
  is a pure transport lemma; it consumes a shifted-point `ContinuousWithinAt`
  hypothesis and does not derive one.
- `section43_bvt_F_continuousWithinAt_shifted_section43PositiveEnergyRegion_compl`
  at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:5068](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:5068)
  remains the exact live `sorry`, so the missing source object under the flat
  continuity package is still the raw shifted-point continuity theorem.
- `section43_fixedTimeShellRaw_pointwiseFlatContinuity_on_section43PositiveEnergyRegion_compl`
  at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:5115](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:5115)
  through
  [5163](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:5115)
  is only flattening transport of the previous theorem.
- `section43_fixedTimeShellRaw_localFlatContinuity_on_section43PositiveEnergyRegion_compl`
  at
  [OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:5171](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:5171)
  through
  [5215](/home/claw/.openclaw/workspace/OSreconstruction/OSReconstruction/Wightman/Reconstruction/WickRotation/OSToWightmanPositivity.lean:5171)
  packages only `Uflat` plus `ContinuousWithinAt G_t` on `Uflat`;
  it is not a local-trace-package supplier in the stronger sense required by
  `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace`.

Exact source-first verdict:

- there is no hidden theorem-sized landing here from flattening, shifting, or
  local-open packaging alone;
- the exact smaller real theorem-sized object immediately beneath the missing
  complement-side package remains
  `SCV.fourierLaplace_schwartz_integral_convergence_local_of_trace`;
- the package supplier itself is still not source-ready because neither
  `B_t` nor `ContinuousOn B_t Uflat` nor pointwise `htraceUflat` is produced by
  the currently checked positivity-side transport layer, and the lower
  shifted-point continuity theorem is still missing outright.
